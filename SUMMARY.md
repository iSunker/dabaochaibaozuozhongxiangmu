# 大包拆包 · 单种保种工具链 v1 —— 方案与进度总结

> 最后更新：2026-09-12（§15 通知发信打通；§5.3 已修正：全量**没跑完**，只搜了 87/382，真实命中率 ~50%；
> §6.1 SiteB 已恢复；§6.5 429 真因是站点 502；§6.6 indexerId 会变；
> **§11 单片状态机已实现**，含 **§11.6 按站重搜周期（默认每站 14 天，2026-09-11 由 7 天改）** 与
> **§11.8 `drive` 控速 + 退避闭环**；§11.10 说明 `orchestrator/main.py`；
> **§10 多包支持已落地**，含 **§10.2 嵌套结构陷阱（DC 案例）**、
> **§10.6 cross-seed 的 searchee 枚举规则（读源码定论）**；
> **§11.7.1 `--limit` 分批**、**§10.5 A/B 方案对比**、**§11.11 全量能否排除已做种**、
> **§11.12 生产 `.env` 一键更新（含"怎么确认真的落地了"）**、
> **§10.6.4 `init --roots-from-env`（DC 47 组参数 → 一条命令）**、
> **§11.13 429 退避与 SKIPPED 的真相**、
> **§11.14 三个包 init 完成与 drive 分批策略**、
> **§12 收尾归档（现状快照 / 下一步手册 / 踩坑清单）—— 接手先读这里**、
> **§14 驱动层从 Windows 迁到 NAS（2026-09-11 深夜，含两个已查实的坑）**、
> **§15 通知发信打通 + DSM 的 ssmtp 配置路径坑（2026-09-12 凌晨）**、
> **§16 待做的四个自动化（设计预案，未实现 —— 两条前提与实测不符）**、
> **§17 首次真跑的探针发现（假 `exit=127` 已修 · **§17.5 两个疑点已结案**：农场路径 / 退避检查节奏 / 退避分级 / 闸门接线）**）
> 本文件是给「下一次接手的人（或下一个会话）」看的。读完这一篇应当能直接接着干，
> 不需要回翻聊天记录。

---

## 导航（先读这里 30 秒）

> **三份文档的分工**（按日志分级）：
> **README = INFO 层** —— 操作手册（命令、步骤、症状→解法）；
> **SUMMARY = DEBUG 层** —— 完整的**过程、踩坑、决策理由**（就是本文件）；
> **`ENVIRONMENT.md` = 检索层** —— 环境**前提**（A 部分）+ 每条坑一个 `ERR-*` 可检索 ID（B 部分）。
> 想知道「**怎么做**」看 README；想知道「**为什么这么做 / 当时踩了什么**」看这里；
> 想知道「**这台机器长什么样 / 这条症状对应哪个已知坑**」看 `ENVIRONMENT.md`。
> 同一事实**只在一处维护**，跨层用「详见 SUMMARY §N」单向指路。
> ★ `ENVIRONMENT.md` 是**索引**不是事实源 —— 与 README / 本文冲突时，以 README / 本文为准。
> ★ 原 `走过的弯路.md`（20 项技术弯路）已于 **2026-09-13 全部并入 `ENVIRONMENT.md` 并删除**；
> 文件名不再存在，要按历史编号找用 `grep -n '弯路 #' ENVIRONMENT.md`。
>
> ★★ **2026-09-16 起：本文件的正文已按章拆到 `summary/`。**
> 三份文档 **62% 的体积都堆在本文件里**（516 KB / 8305 行），整份读太贵。
> ⇒ **默认只读本文件**（导航 + 下面那张「§N → 文件」表），要读哪一章就只打开那一章
> （中位章 ~13 KB；最大的是 `18-目录布局调整.md`，114 KB）。
> **拆法是纯字节切分 —— 正文一字未改**：`cat summary/*.md` 拼回来与原文件**逐字节相同**
> （原始文本永久可从 git 历史取回）。因此 **§N 编号与全部跨文档引用继续有效**
> （实测约 **690+ 处 `§N` 类引用**，全仓库口径；精确数以 `python tools/doc-audit/02_refs.py` 现跑为准），
> 一处都不用改。★ 表里只放「§N → 文件」，**不放行号** —— 行号会随每次编辑静默作废。

**按目的查：**

| 我想… | 直接跳 |
|---|---|
| **接手这个项目**，先读什么 | §12 收尾归档 → **§13 接手会话**（含 7 条新坑）→ **§13.11 当前进度与唯一待办** → **§14 驱动迁到 NAS**；★ **最新两节是 §25**（2026-09-16~17，三轮收口 + **`#115` 等触发**）与 **§26**（2026-09-17，**跨会话任务盘面 —— 待办真源**），**首读先看它们**；★★ **2026-09-19 更新：最新是 §27~§30** —— **§27**（「装不出来的单种」真因 ≠ 校验不通过）· **§28**（`deploy.sh` 两个前提）· **§29**（等触发期巡检 + `phase` 误判）· **§30**（★ **qb 内存排查：一次重复推导** —— 内存真源在 `ERR-DOCKER-11`，**接手"症状类"问题前先 `grep` `ERR-*` 与最近几节标题**）；★ **内存问题不要从 §30 读结论，去读 `ERR-DOCKER-11`** |
| 现在系统什么状态 | **§13.7 状态快照**（唯一权威，别处不再重复） |
| 调度为什么从 Windows 搬到 NAS | **§14**（`<StopOnIdleEnd>` + 包装器行尾两个坑 · 排查表） |
| 出事了怎么让它通知我 | **§13.12 通知系统**（架构 · 探针推翻了什么 · 三个真缺陷）→ README「通知 / 告警」 |
| **邮件发不出去 / ssmtp 报 501** | **§15**（DSM 的 ssmtp 读 `synosmtp.conf`，不是 `/etc/ssmtp/ssmtp.conf`） |
| **下一步自动化做哪件** | **§16**（额度感知 / 农场巡检 / 趋势 / 日志轮转 —— 含**两条前提被探针推翻**） |
| **状态机说没做种、qB 里明明在做种** | **§17.5.1**（根因 = v3 农场路径认不出，**已修**）· 误判过程见 §17.3 |
| **`backoff_hits` 一直是 0 / 退避没被检测到** | **§17.5.2**（检查间隔 300s > 退避窗口 55s，**已修**）· 分级 §17.5.3 · 接线 §17.5.4 |
| **台账某一列整列是空的 / 恒空** | **§22.1**（`matched_indexers` 恒空：**输入源易失 + 写入端不合并**）· 修法 §22.2 |
| **某个站一直 429 /「被限流久不恢复」** | **§22.3**（那个 429 **是 Prowlarr 发的**，不是站点限流）· 三种机制见 `ENVIRONMENT.md` `ERR-SVC-17` |
| **判「能不能建硬链接」/ 某共享在哪个卷 / 某共享 SMB 里看不见** | **§23**（`docker_ssd ∈ /volume2` 的双来源复核 · ★★ **`stat -c %d` 只在 NAS 侧成立**，Windows/SMB 侧零分辨力 ⇒ 对照量用 `df` · **枚举 ≠ 全量**）· 判据本体在 `ENVIRONMENT.md` `A.3` |
| **新会话开局 / 环境前提 / 按症状找已知坑** | **`ENVIRONMENT.md`**（检索层：A 部分前提 · B 部分 `ERR-*` 条目 · 含全部 20 项技术弯路）|
| 遇到报错 / 症状 | **§13.6 本会话的坑** · §12.5 上次的坑 · §6 已了结的坑 |
| 加站 / 换站 | **§13.3 完整流程**（可复用，含"必须 force-recreate"） |
| 状态机怎么用 / 参数 | §11.7 用法 · §11.6 重搜周期 · §11.8 控速与退避 |
| 嵌套包（DC 那种）怎么接 | §10.2 陷阱 · §10.6 枚举规则（读源码定论） |
| 硬链接农场（v3） | §10.5 B/A 对比 · §10.5.7 实测 · §10.5.9 等价性证明 |
| 为什么不用 XX 方案 | §3 关键决策 · §10.5 A/B 对比 |
| **「包」是怎么被系统认出来的 / 搜索压力到底从哪来** | **§19 原理技术与风险须知**（包是**声明**的不是识别的 · 压力来自**分母每 14 天重生** · 风险清单） |

**章节总览（按"日志级别"分层）：**

| § | 标题 | 级别 | 文件（`summary/` 下） |
|---|---|---|---|
| 0 | 占位符约定 | 🟢 操作 | `00-占位符约定.md` |
| 1 | 目标 | ⚪ 背景 | `01-目标.md` |
| 2 | 拓扑 | ⚪ 背景 | `02-拓扑.md` |
| 3 | 关键决策与踩过的坑 | 🔵 决策 | `03-关键决策与踩过的坑.md` |
| 4 | 各文件最终状态 | 🟢 操作 | `04-各文件最终状态.md` |
| 5 | 进度（含 §5.3 命中率真相） | ⚪ 历史 | `05-进度.md` |
| 6 | 已了结的坑（保留记录，避免重走） | 🔴 症状→解法 | `06-已了结的坑.md` |
| 7 | 其它已知的非阻塞问题 | 🟢 操作 | `07-其它非阻塞问题.md` |
| 8 | 安全约定 | 🟢 操作 | `08-安全约定.md` |
| 9 | 环境备忘 | 🟢 操作 | `09-环境备忘.md` |
| 10 | v2 多包支持（§10.2 嵌套陷阱 · §10.5 A/B · §10.6 枚举规则） | 🔵 决策 | `10-v2多包支持.md` |
| 11 | 单片状态机（§11.6 周期 · §11.7 用法 · §11.8 控速） | 🟢 操作 + 🔵 决策 | `11-单片状态机.md` |
| 12 | 收尾归档（**接手先读**：快照 / 手册 / 踩坑） | 🔴 + 🟢 | `12-收尾归档.md` |
| 13 | **接手会话 —— 本会话全过程**（§13.6 坑单 · §13.7 快照 · §13.8 验证 · §13.11 最新进度 · §13.12 通知系统） | 🔴 + 🔵 | `13-接手会话.md` |
| 14 | **驱动层迁到 NAS**（两个坑：`<StopOnIdleEnd>` · `.cmd` 行尾 · 排查表 · 剩余人工步骤） | 🔴 + 🟢 | `14-驱动层迁到NAS.md` |
| 15 | **通知发信打通 + DSM ssmtp 配置路径坑**（501 真因 · BusyBox `ps` 假阴性 · 计划窗口存错） | 🔴 + 🟢 | `15-通知发信与ssmtp路径坑.md` |
| 16 | **待做的四个自动化（设计预案，未实现）**（额度感知 · 农场巡检前置修复 · 趋势数据已在库 · 日志轮转**前提被推翻**） | 🔵 决策 | `16-待做的四个自动化.md` |
| 17 | **首次真跑的探针发现 + 结案**（假 `exit=127` = 部署覆盖了在跑的脚本 · **§17.5 两个疑点已修：农场路径 / 退避检查节奏 / 退避分级 / 闸门接线**） | 🔴 + 🔵 | `17-首次真跑的探针发现.md` |
| 18 | **2026-09-12 当天全过程**（搬迁 · 加站 · 巡检 · 漂移哨兵 · 推前扫描 · 观测对账三条判据进生产） | 🔴 + 🔵 | `18-目录布局调整.md` |
| 19 | **原理技术与风险须知**（§19.1 包是**声明**的不是识别的 · §19.2 搜索压力来自**分母**不是频率 · §19.3 风险清单） | ⚪ 原理 + 🔴 风险 | `19-原理技术与风险须知.md` |
| 20 | **观测层「沉默 / 响铃」语义收敛**（2026-09-12 深夜 → 09-13；§20.1 `packs` 差集只报变化 + 基线 · §20.3「沉默 = 基线一致」· §20.7 三条首读兑现 · §20.9 `mbf` 排进 `--packs`） | 🔵 决策 + 🔴 症状→解法 | `20-观测层语义收敛.md` |
| 21 | **IYUU 辅种的保存目录从哪来 + 22 条旧根漏网**（2026-09-13；源码三跳：`savePath` = qB 里同 infohash 那条自己的 · 旧根残留实测 · qB「落进可写层」那枚雷） | 🔴 症状→解法 + 🔵 决策 | `21-IYUU保存目录与旧根漏网.md` |
| 22 | **`matched_indexers` 恒空 + HDtime 的 429 到底是哪种限流**（2026-09-14；§22.1 根因 · §22.2 修法 · §22.3 429 是 **Prowlarr 发的** · §22.4 `ERR-SVC-17` 改「有据」· ★★ §22.6 **`#60` 那个待验问错了问题**：`#115`/`#60` 结案，恢复时刻就一个数 `retry_after`） | 🔴 症状→解法 + 🔵 决策 | `22-matched_indexers恒空与HDtime的429.md` |
| 23 | **卷归属复核 + SMB 探针的分辨力 + 一份「日报加各包进度」方案的审订**（2026-09-14；§23.1 双来源复核 · ★★ §23.2 `stat -c %d` **只在 NAS 侧成立** · §23.3 **枚举 ≠ 全量** · §23.5 八条订正，**全未落笔**） | 🔴 症状→解法 + 🔵 决策 | `23-卷归属复核与SMB探针分辨力.md` |
| 24 | **SUMMARY 按章拆分 + 本轮收口**（2026-09-16；§24.1 为什么拆 · §24.3 **表里不放行号** · §24.4 引用按 §N 寻址，不按路径 · ★★ §24.5 三脚本 glob **静默**漏 24 章 · ★★ §24.6 层级跳跃判据恒报 24 条 · ★★ §24.8 **自检与 bug 自洽 ⇒ 永远绿** · §24.10 `#115` 现场读数） | 🔵 决策 + 🔴 症状→解法 | `24-SUMMARY分章与本轮收口.md` |
| 25 | **三轮收口：等触发 · 措辞 · 结案**（2026-09-16~17；§25.2 ★★ **`#115` 的 cron 是 session-only，下次先重建** · §25.3 `#116` 措辞「已过去的残值不许说成解禁」 · ★★ §25.3 `#60` **那个待验问错了问题** · ★★ §25.3 `#119` 活工具被同一类坑绕过（中文名文件全没扫） · §25.5 三条方法论：判据要能被证无用 / 因果必须从数据读出来 / 工具换了数据写法） | 🔵 决策 + 🔴 症状→解法 | `25-三轮收口与等触发.md` |
| **26** | **跨会话任务盘面：旧会话已清场 · 未完成任务已抽干**（2026-09-17 起，**12 次收口 / 33 节**）—— ★★ **本节是「待办真源」**；读法：`§26.0` 主题索引 → `§26.2` 主表 → `§26.2.3` 三张总表 | 🟢 操作 + 🔵 决策 | `26-跨会话任务盘面与旧会话清场.md` |

| **27** | **「装不出来的单种」—— 真因 ≠「校验不通过」**（2026-09-18 夜）—— ★★ 真机制：发布组把附件写进种子、农场没有 ⇒ 没 peer ⇒ 永远补不上 | 🔴 症状→解法 + 🔵 决策 | `27-装不出来的单种.md` |
| **28** | **`deploy.sh` 的两个前提：工作区干净 与 行尾一致**（2026-09-18 深夜）—— ★★ 发现一：`deploy.sh` **读磁盘、不读 git**；发现二：**CRLF 让 `cmp` 报假差异**；发现三：拿 `diff` 否证 `cmp`；发现四：时间窗说错 | 🔴 症状→解法 + 🔵 决策 | `28-推送与行尾.md` |
| **29** | **等触发期的巡检：cron 重建 · `#115` 仍 WAIT · 一个「在最该报的时候哑掉」的判据 + §29.8 `phase` 误判**（2026-09-19。本轮不推进功能，做的是 §25 交接里那两件「等条件」的活 + `#58` 剩余边界复核。★★ **`#115` 的 cron**：★ 判据只能是**当场 `CronList` 数一遍**（实测 = 空）—— 两个失效原因（**session-only** + ★★ **7 天自动过期**，后者与 `durable` 无关）**症状完全相同、到期前零提示** ⇒ 「我建过没有」永远不是判据；新 id **`7911180e`**、`recurring=true` / **`durable=false`**、最多管到 **09-24**。★★ **`#115` 新读数**：**552 批 / 411 PRE / 141 WAIT / 0 PASS / 0 FAIL** —— 最近各批**压根没出退避**⇒ **不算通过也不算失败**，三条判据仍一条未现（★ `PRE` 档是这条判据能成立的前提：`FAIL` 的定义是「一批 `ok=0` 且中止」，而**部署前有一堆这种批次**，没有 `PRE` 档就会把历史读成结论）。★★★ **本轮唯一真缺陷（本机实测复现）**：`scripts/diag/drive-loop-mount-selfcheck.sh` ② 段的 **python heredoc** 打 `✓/✗`（U+2713/U+2717），而 **Windows python 默认 stdout 是 GBK** ⇒ 抛。★★ **症状不是「整个脚本崩掉」**（那样反而显眼），是 **② 段半途而废**：`✓` 行都打完了（那些来自 shell `printf`），一到**第一条 `✗`**（= **真有问题、最该被看到**的那一行）就抛 ⇒ 该段**全部裁决** + `⇒ 路径不可达数` **一行都出不来**，而 `\|\| RC=1` 又兜住退出码 ⇒ **以 1 退出却不给解释**。✦ 即**判据恰好在最该报的时候哑掉** —— 它**不报错**，只在**真出问题时**才显形。★ 处置是 `export PYTHONIOENCODING=utf-8`，**照抄**（`chk58.sh:107` 早就有，且其头注第 ④ 条**记的正是同一个毛病**；`drive-loop-nas.sh` / `run-resident.sh` 同句）⇒ 属 **「同一个切换里漏改了一个」**（§19.1.3 同形）。★★ **反向判据比正面更值钱**：shell 的 `printf` **不需要**这条（实测 GBK 下照打 `✗`）⇒ **别把「给 shell 也加一条」当补强**，那是**钉一个不存在的失效模式**。★ 另两个 `sh` 工具**查过才敢说**没这缺陷（`notify-spool.sh` 的 ✓/✗ 出自 shell `say`；`rm-staging.sh` 压根没有 ✓/✗）⇒ **本次只改一处**。★ **阴性对照拿真脚本做**：删掉那行 ⇒ ② 段只剩 traceback、三行 ✗ 与汇总全丢；加回 ⇒ 三者都在、`rc=1` 正确。★ 回归 `test_drive_loop_service.py` ⑧ 段 **+5（78 → 83）**：两条「**有对象**」+ 两条「锁了 export」+ 一条反向（`chk58.sh` 的 ✓/✗ **两条路并存**，别只改一处）；变异 ⇒ **只**这一条红。★ 全套 **27 脚本 / 1088 断言 / 0 失败**（②b「表格 == 实跑」当场抓到 78 ≠ 83）。★★ **`#58` 边界②（属主/权限）复核**：`docker-compose.yml:203` 明确**不加 `user:`**（宿主 `run.sh` 由 DSM 以 root 跑 ⇒ 容器亦 root ⇒ 同一属主）；★ 本轮**独立复核**：全 compose 带 `user:` 的两处**只有** `cross-seed`(86) 与 `reseed-orchestrator`(123) ⇒ **设计自洽**—— 但 DSM ACL 真机**仍未验** ⇒ 从「未想清」变成「**想清了、旁证齐了、真机未验**」。★ 本轮**不需要** `deploy.sh --apply`（`mount-selfcheck.sh` 是 `LOCAL_ONLY`，不部署）。★ 同批带入上一会话未提交的 `ERR-SCHED-11`，其两条承重事实**本轮各自独立复验**（★ 提取时亲踩：`grep` **不排 `.deploy-backup/`** 会捞到几十条历史副本 ⇒ 结论读反成「有调用方」；同 `B.10` 第 1 条**反向**那一面：**「某处有」 ≠ 「活的那处在有」**））★★ **§29.8 第十次收口**（同日第二轮，**没有缺陷可修** —— 修的是**我自己的一个错结论**）：我在 NAS 上 `cat .drive-loop.state` 读到 `phase:"running"` 就断言「残留 `running` ⇒ 静默永久停工」，而 **7 分钟后同一条命令读到 `idle`**（`phase` **每 60 秒翻一次**）⇒ **现场从头到尾是对的**。⇒ ★★ **`cat` 一次状态文件不是有效的取证动作**（取的是**一帧**；与 §28.6「把单点观测当时刻表」同族）。⑨ 段 **+16（83 → 99）**钉的是**「别让将来的人把它改成 `running`」**（`_resident_state` 的 `phase` **默认值** + 三处收尾**必须**整体覆盖写 `phase`）；★★ 判据用 **`ast` 读 `defaults`**（字符串匹配分不出「默认值是 `idle`」和「注释里写了 `idle`」）；★★★ **写它时两次栽在「怎么算'收尾那个'」** —— `ast.walk` 是 **BFS 不是源码顺序** ⇒ 嵌套函数的 `write_state` 被误取 ⇒ **凡「第几个/最后一个」必须按 `lineno` 排**，且改用**结构特征**（认 `last_end_ts`）更稳。★ 全套 **27 脚本 / 1104 断言 / 0 失败**（本节把索引行里那个 1088 一并更到 1104）。 | 🔴 症状→解法 + 🔵 决策 | `29-等触发期的巡检与哑火判据.md` |
| **30** | **「NAS 上 qb 内存为什么那么高」—— 一个重复推导的会话**（2026-09-19。用户开局问「查一下日志，为什么 nas 上的 qb 内存占用那么高？」。★★ **本节的性质：不是新发现，是一次失败的重复劳动** —— 内存这件事**当天已经查完并处置过**（**`ERR-DOCKER-11`**：根因 = **`memory_working_set_limit` 512 太小**、已改 4096、RSS **8.82 → 4.21 GiB**；**§27.9**：DL 系 10 条三类），**而一个全新会话从头又推了一遍**。⇒ **本节不复述内存结论**（`A.11` 指路，见 30.1 的"不复述"表），价值**全在过程**：★★★ **30.2 连续 5 次归因、5 次都错** —— ① 容器重启/OOM（用户贴 `RestartCount=0` 否掉）② 7 次系统唤醒（那是 **NAS 休眠**、非容器事件）③ 那 3 个种子是原因（用户「**正在被限速下载的不是你说的那三个**」）④ inject 1240 次/天 = bug/双 driver（`README:1856` 写明是 **webhook 硬触发、设计如此**）⑤ 根因是 webhook 频率（**真根因是 `memory_working_set_limit`**，早已定位）⇒ **共同形状：每条都是"能解释"而非"已确认"**（`B.10` 第 1 条），且第 ③ 次**正好复现 §27.9 末尾自己记过的同一个病**（*先问"这条命令量的是什么"*）。★★ **本节新增的一条规矩**：**接手"症状类"问题先 `grep` 两处** —— ① `ENVIRONMENT.md` 的 `ERR-*` 症状索引 ② `summary/` 最近 3~5 节标题；**判据 = 能报出"这症状对应哪个 `ERR-*`"再开始推，报不出来 = 还没查够**（与 `B.10` 第 22 条「寄存物 ≠ 我的待办」**同族**，根因同为**动手前没先"数一遍"**）。★ 30.3 新读数：`BT_backup` 实测 **2070 种**（`.torrent`+`.fastresume` 各 2070、0 缺配 0 孤儿；★ SMB 侧 `ls \| wc -l`=**4141** 是两个后缀之和，开局差点读错）/ `No space left` **155 条 55 种、全在 09-13**、**两条路径混用**（旧 `reseed_singles/` vs `categories.json` 现用 `reseed/reseed_singles/`，与 §21 旧根同族）/ cross-seed **v6.13.7**、**`injectCadence=undefined`** / 09-18 **webhook 1241 条 ⇒ inject 阶段 1240 次** / `cross-seeds/` 残留 **3 条存档**（09-17 `MATCH_PARTIAL`/`MATCH_SIZE_ONLY` 注入）。★ 30.4 **一条推测级（未验，别当结论引用）**：3 条 incomplete 使每轮 inject 无法"空队列短路"⇒ **可能**是 WebAPI 全量风暴的放大因子 —— **但主因已被 `ERR-DOCKER-11` 的同口径对照实测占据**，本条留一个**可证伪的验法**。★ 30.5 如实记录**没做**的事：**全程只读、未改任何生产配置**；★ 并点名**一处越界倾向** —— 曾准备给"清 3 条种子"的 `rm`，而 **§27 用户已拍板"只识别+通知、一点不碰 qB"**，**幸而在给命令前先读了文档**。★ 30.6 交接：**A** `memory_working_set_limit=4096` 的**长期稳态**（`ERR-DOCKER-11` 读数只到 **+18 分钟**，"稳定"需 ≥1 小时后复读一次；★★【**2026-09-19 深夜**勘误**】原写的「本机低报 2.2 倍」**已撤回**：那个数**恰好是一次 `/1024` 归一化**，**两者量的是同一份 RSS、只是单位不同**）—— **最推荐先做**；**B** `injectCadence` 能否压住 webhook 硬触发（**需实测**，`config.js` 写上≠降频）；**C** `cross-seeds/` 3 条处置（**受 §27 约束，先问用户**；若动则「**先删 NAS 侧存档、后删 qB 种子**」，`README:1869`）；**D** `No space left` 那批路径混用现状**未复核**） | 🔵 决策 + ⚪ 过程（重复推导留痕） | `30-qb内存排查是一个重复推导.md` |

> 图例：🔴 症状→解法（卡住时搜这里）· 🟢 操作（照着敲）· 🔵 设计与决策（为什么）· ⚪ 历史快照。

---

