# tests/ —— 离线自测（不联网、不碰 NAS、不碰真库）

给仓库代码钉回归的断言集。**全部离线**：SQLite 都是在 `tempfile.mkdtemp()` 里现造的
合成数据，网络请求打桩，凭据一律用假 key。

## 怎么跑

```bash
python tests/test_backoff.py        # 任一 cwd 都行
```

路径按 `__file__` 解析（不是 cwd），所以从哪儿跑都一样。
每个脚本自己打印 `ok` / `FAIL`：**全过退出码 0，有失败退出码 1**，可以直接接 CI。

2026-09-13 实测：**18 个脚本、690 条断言、0 失败**（本轮新增 `test_linkguard.py` 66 条 + `test_hardlink_link_type.py` 17 条）。

2026-09-14 实测：**21 个脚本、757 条断言、0 失败**（本轮新增 `test_qb_census_props.py` 23 条 +
`test_notify_digest.py` 22 条 + `test_matched_indexers_union.py` 15 条）。

2026-09-17 实测：**22 个脚本、796 条断言、0 失败**。
★ **我能逐笔点清的只有这四笔（合计 +27）**：
`test_backoff.py` **51 → 54 → 60**（+9：09-16 加 ⑬「退避时刻必须带日期」，09-17 加 ⑭「过去 ≠ 将来」）；
`test_next_sleep.py` **17 → 21**（+4，⑧「分工钉死」：`next_sleep` 只管节奏、不管站点）；
`test_scan_secrets.py` **25 → 29**（+4，⑨「非 ASCII 路径必须真的被扫到」，#119）；
★★ **外加 1 个新脚本 `test_readme_counts.py`（10 条）**，它钉的是**这份 README 自己**。
★★ **剩下 12 条（796 − 757 − 27）来源未逐笔核对，如实记在这里** ——
09-14 的 757 与 09-17 的 796 之间还有零星加条，表没跟着改。**别把 +39 全归给本轮。**
（这与 09-16 那次是同一个形状：**别硬凑数字**。要把这 12 条查实，得去逐脚本比 HEAD 与 09-14 那版。）
★ 复核方式：下表 22 行的「断言」一列相加 = **796**，与上面那条命令重数的 TOTAL 一致
（两边都是数出来的，不是推的），22 个脚本 rc 全 0。
★ **这张表与这行数由 `test_readme_counts.py` 互相钉住** —— 以后改了一处、
忘了另一处，跑测试就会红（本次就是被它抓出来的）。

★ **顺带校正表里一处旧数**：`test_backoff.py` 那一格原写 **39**，实测（HEAD 版）是 **51** ——
和 09-14 那条 `test_reconcile.py 120 → 127` 是同一个毛病：**表没跟着断言一起改**。
本次实测：51（旧）→ 54（新）= **+3**，正好是加的那 3 条 ⇒ 其余断言未受影响。
★ 全套 757 → **772** 是 **+15**，比 +3 多出的 12 条**不是本次引入**的
（是 09-14 之后到本次之间累积的、表未同步的部分）—— **别把 +15 归给这次改动。**
★ 数法：本文档下面那条命令（**三种行形都要数**；只数 `  ok  ` 会漏 —— 实测会少 8 条）。
★ 同日顺手校正下面表里一处旧数：`test_reconcile.py` **120 → 127** —— 09-13 之后又加了 7 条
断言，表没跟着改。（数按上面那条命令重数，不手算。）

★★ **09-16 这次「补完」本身踩了同一个坑，记在这里**：那条（原来的「+3 / +15」那段）原先被我**抄成了两遍**
（`+3` 那段与 `+15` 那段各写一次，一行一字不差），而**检查它那一步是"读一遍看像不像"** ⇒
**绿**。是用户问起"两处数字看起来对不上"才翻出来的。教训与 `test_notify_digest` 的红过再绿同一族：
**文档自己也要能"红"** —— 不是断言，就没法红。所以这里的规矩是
「**同一句话只许出现一次；副本一律删掉**」，而不是"下次读仔细点"。
（真值复核：那段重复的话**已删掉一份**；当前真值见文件顶部的日期行。）

★ 这个总数**别手算**。测试脚本的"通过"有三种行形（`  ok  ` 前缀、
`[n] 标签 : PASS` 后缀、`scan-secrets` 自带的一套），任何**单一** grep 都会漏掉
其中一类 —— 实测：只数 `  ok  ` 会得到 374，把 `test_remove_indexer.py` 的 8 条
整个漏掉。要重数就跑这一行（判定"全过"看**退出码**，不看数出来的条数）：

```bash
tot=0; for f in tests/test_*.py; do out=$(python "$f" 2>&1); rc=$?; \
  n=$(printf '%s\n' "$out" | grep -cE '^  ok  |^ FAIL |: PASS|: FAIL'); \
  echo "$(basename $f) n=$n rc=$rc"; tot=$((tot+n)); done; echo "TOTAL=$tot"
```

★★ **还有第四种：新写的测试自己另起一套行形 —— 那就不是"漏一类"，是整份漏掉。**
2026-09-13 写 `test_notify_drain.py` 时打印的是 `  ✓ ` / `  ✗ `，上面这行数出来
`n=0`、TOTAL 原地停在 522，**28 条一条都没进账，而退出码仍是 0**
（"看着一切正常"）。所以宁可改测试去迁就既有行形，也**别往 grep 里再加一条** ——
每加一条就多一处"我猜的行形"，而漏掉的形态恰恰是**没报错的形态**。

## 各测什么

| 文件 | 断言 | 钉住什么 |
|---|---:|---|
| `test_backoff.py` | 60 | 退避检测 —— **55 秒的短窗口发生在批次中途也必须记一笔**（复刻 2026-09-12 现场）；残值不误记、禁用的索引器不挡路。★ 另钉 **`aborted_kind` 的两个计数各走各的**（2026-09-13）：「站点退避超时」是**良性**、**一点失败都不涨**（复刻 09-13 那条 `failed=0` 与「连续 3 批失败」同批的 TSV）、真故障的 key 是 `consec-abort` / 良性是 `consec-backoff`、**两者互不清零**（写成"互相清零"会让交替出现的退避把真故障永远压在 3 以下 ⇒ 告警等于没有）、只有**正常跑完**才一起清零、认不出种类时**按真失败算**（宁可吵不可静默）、混着出现时标题补「共 N 批没跑成」。<br>★★ **退避时刻必须带日期**（#114，新增 ⑬，2026-09-16）：`解禁` / `等到` 一律 `%m-%d %H:%M`（或带秒）—— 复刻 2026-09-15 13:39 那条「解禁 **01:31:11**」（看着像**当天**凌晨，**其实是次日**；与 Prowlarr 侧 `disabledTill 09-16 13:32` 差 12 小时，审阅时被当成"其中一个抄错了"）。★ 判据**成对**：既要断言带日期，也要断言**旧写法判不出来**（否则那条判据恒真）；★ 还要先断言「**真的走到了那条 warn**」—— 第一版三条全踩空（20 小时的退避走的是 abort 那条路，它本来**就**带日期 ⇒ 断言落在空集上，"任何正则都成立"），**这是本文件第二次栽在"假测试"上**。<br>★★ **「已过去的残值」不许说成「解禁」**（#116，新增 ⑭，2026-09-17）：现场是 09-16 10:41:06 那两条 `…被限流（RATE_LIMITED），解禁 10:21:19` / `解禁 09:22:06` —— 两个「解禁」**都早于**触发时刻，与 `cross-seed.db` 的 `retry_after` **逐秒相同** ⇒ **数没印错，是「解禁」这个词印错了**：它不是「将要解禁」，而是「上次那个窗口**已经在 … 结束了**」。★ 代价（真实发生）：按「将要解禁」读 ⇒ 得到"时间倒流" ⇒ 把一条**陈旧残值**读成「今天上午又被限了一次」，而那天**根本没有**新限流（09-16 全天 `要等到` = 0）。★ 与 ⑬ 的关系：**⑬ 管格式（带日期），⑭ 管语义（过去 ≠ 将来）**，两条不重叠 —— 旧写法（带日期、但说"解禁"）在 ⑬ 是**绿**的，在 ⑭ 必须**红**（已变异验过） |
| `test_check_indexers.py` | 45 | 索引器自检 `check_indexers()` —— **两份分开维护的名单对不上时会静默错记**（2026-09-12 真栽过：HDtime 早就在 Prowlarr 和 `.env` 里，`--indexers` 却没有它，224 部 UNMATCHED 一部都没往新站重搜）。钉：`_norm_indexer_name` 的括号归一（不归一就**每批误报**）、三分支（`missing`/`extra`/`unnamed`）各自的 key 与 metrics、**修法文案不许再出现「两处都要改」**（电脑端已退役，`--indexers` 只有 `run.sh` 一处）、以及自检自己**绝不能拖垮跑批**（没 `--db-path` 静默跳过、读库炸了要吞）。★ 另钉 **unnamed 分支的查证命令必须自带脱敏**（2026-09-13）：命令里要有 `sed -E` 与 `<redacted>`、sed 咬的是 `apikey` 的**值**（`(apikey=)[^,&]+`，只换值、留键名与 `/N/api`）、且正文要明说**别把原样输出粘进聊天/命令行** |
| `test_drift_junk.py` | 12 | 漂移哨兵 `KNOWN_NAS` 的 `.env*` 分类（`classify()` **先匹配先赢**）：本体 `.env` **不许**被放宽后的杂物规则吃掉、模板 `.env.example` **不许**归进杂物，而 `.env.bak`（**裸名，无时间戳**）/ `.env.bak.<时间戳>` / `.env_bak` / `.env.backup` **都**得归进杂物。★ 钉的是 2026-09-13 那条窄口：旧规则 `^\.env\.bak\..*` 漏掉裸名 ⇒ 它掉进 `unknown` ⇒ 哨兵**误报**「NAS 上有没登记过的文件」。★★ **同一类坑当天咬了 `rm-staging.sh` / `.gitignore` / 本哨兵三处**（教训：「别让保证取决于我们猜没猜对文件名」），所以这里钉的是**命名变体整个集合**，不是当时那一例 |
| `test_farm_root.py` | 18 | v3 农场路径归包：单根 / 多根嵌套 + 季层 / 老包**不**认领农场路径 |
| `test_next_sleep.py` | 21 | `next_sleep()` 三档退避分级（45 / 90 / 120 分钟）+ 单调性，外加老分支没被碰坏。★★ **2026-09-16 补了 ⑧「分工钉死」这一节（17 → 21 条）** —— 上面 ①~⑦ 全都在喂 `backoff_hits`/`waited_sec` 这两个**数**，读起来很容易变成「退避这件事被处理了」。事实上两条腿是分开的：**判断谁在退避、还剩几个健康站、要不要不等照发 → `wait_out_backoff`**；**判断下一批隔多久再来 → `next_sleep`**。少一条就静默 —— **#115 那次事故正是「站点被限 24 小时，而 `next_sleep` 只是把间隔拉到 2 小时」：节奏对了，条目一条没发。** 判据：同样「等了 700 秒」，**命中 1 次与 9 次必须同档**（档位只看等掉的时间），且 `aborted` 一进就吃满 3 小时、**与退避档位无关**（「超过上限先收工」这个决定不在这里做）。★ 已做变异验证：把实现换成「命中次数决定档位」⇒ 这条**红** |
| `test_notify_digest.py` | 22 | 每日摘要新加的**汇总行**（#67）—— 钉的不是「加了一行」，是**它数的对不对**：★★ **「批次」不能按 kind 数** —— `每日台账` 与 `全部包已无待搜项` **也是 kind=batch**（走 batch 是为了「记账但不发信」，见 `drive-loop.py`）；按 kind 数，生产上 09-13+09-14 那两个窗口会得到 **17**，而真批次只有 **12**。判据取「metrics 里有没有 `pack=`」。★★ **`packs=` 不是 `pack=`**（「无待搜」那行的键，整键相同才算命中）—— 前缀匹配会把它算成一批，同时污染批次数与 `ok=0` 的批数。★★ **「键在不在」≠「值等不等于 0」**：一条缺 `ok=` 的流水**仍是一批**，但不许被算成 `ok=0` —— 与 #63/#64（`content_path` 缺键被印成「(空)」）**同一形状**。另钉：台账/无待搜**不进退避与做种合计**、明细行一字不动、只有一天的 TSV 不炸、一条批次都没有时汇总出 0 而明细仍打「（无）」。★ 全程 `--dry-run`，不发信。★★ **红过再绿**：把脚本回退到 #67 之前 ⇒ **红 12 条**（全是汇总那一族）。<br>★ **写这个文件时先红了一次，红的是测试自己**：`batch(pack, **kv)` 的形参名把 `pack` 从 `**kv` 里吃掉了（`if k in kv` 连它一起滤），于是生成的流水**一个 `pack=` 都没有**，被测脚本忠实地每行都判成「非本批」—— 又一次「测试自己也是判据，报红时先怀疑断言本身」 |
| `test_notify_drain.py` | 28 | **2026-09-13 邮件风暴**（同一封告警被重发约 6 次）—— 钉住三个环节，每个环节一条对照：① **重试必须有上界**：发信一直失败时，必须发满 `MAX_SEND_TRIES`（默认 3）就停 —— 不是 5 次、更不是无限次 —— 然后**归档 + 记一条 `[未确认]` 告警**（于是"有条告警你没收到"会进每日摘要，不被静默吞掉），且**封顶之后一趟都不再发**；② ★ **`say` 的 stdout 坏掉（那天 `/volume1` 剩 0 字节）不许带走整趟**：老代码在这里 `set -e` 退出，而 `say "  [已发] $ti"` 恰好夹在 `send_mail` 成功与 `log_event`/`mv` **之间** ⇒ **信已发出、日志没记、文件还在 spool** ⇒ 下一趟再发一遍 —— 这正是风暴的机械结构（复现手段是 `sh -c 'exec 1>&-; exec sh …'`）；③ 正常路径与 alert+batch 混装的对照组（batch 事件**不发信**但必须归档记账，一行失败不许带走整趟）。★★ 用老代码跑它**红 8 条**，恰好就是"信发了、没记账、下轮再发"那几格 |
| `test_once_gate.py` | 32 | `--once` 闸门接线：算出的间隔**真的落盘、真的挡住下一批**；跳过文案说的是不是真原因；没待搜时农场巡检照样跑。★ 另钉 **轮转存的是「下标不是名字」**（2026-09-13）：三包下 `last_pack_idx=0/1/2` 各自轮到谁、落盘的下标对不对，以及**改常量顺序 = 偷偷改「下一批跑谁」**—— 同一个 `last_pack_idx=0`，只把 `mbf` 从中间挪到末尾，下一批就从 `mbf` 变成 `frds`。这是「为什么 `mbf` 必须排中间」的**证据**，而它此前**一条断言都没有**（本节所有用例都传 `last_pack_idx: -1`） |
| `test_orchestrator_state.py` | 29 | `state` 子命令：**库不存在时绝不许把文件建出来**；且必须在读 `config.yml` **之前**分派（配置坏了它还得能用） |
| `test_linkguard.py` | 66 | 链接守护 —— 既有那批**硬链接**有没有被写穿（判据 `state.linkguard_*` + 接线 `drive-loop.linkguard_watch`）。钉的不是"算得对"，而是四个**看着像噪声、其实是要命处**的分界：① ★★ **扫了个空绝不许落盘** —— 扫盘失败/根目录写错 ⇒ `cur` 为空，照常落盘的话下一轮拿空基线比对、**每个文件都被报成「新增」**，一条假警报赔掉整条观测的可信度；② ★ **首读不许响**（#47 的规矩：现存差异是"已接受的现状"）；③ ★★ **路径前缀必须带斜杠** —— `/x/甲` 吞掉 `/x/甲乙/…` 会让"哪条种子被写穿"**指错人**，而指错人比不指更坏（会照着错的对象去重建），加「互相是前缀时取最长匹配」；④ ★★ **正文只报数量**（日报是发出去的：不出现路径、不出现 `volume1`、不出现 hash）。另钉：`.!qB`/`.parts`/`.tmp` 不计入基线（重下本来就动它们）、size 变了也抓得到（不只看 mtime）、坏掉的基线**静默重新起锚**而不是全员误报、<br>★★ **`uploading` 必须显式算「静止」** —— 它**不以 `UP` 结尾**，只按 `endswith("UP")` 判会被当成"正在下载"⇒ **误暂停健康做种**。这个坑是**本文件当场抓到**的：生产上跑着的那把看门狗用的是同一个写错判据，只是那 628 条恰好全是 `stalledUP`（看门狗日志 `巡检 40 次：628 条，无下载态`）才没误伤 —— 测试的价值就在这种"它今天没炸、但它就是错的"上。★ 另钉 `checkingUP` **故意算「在动」**（那是校验失败→就地重下的前奏，正是要盯的）。<br>★★ **首读时 `detail.changed` 必须留空**（2026-09-13 生产上真栽了，**只**栽在这一格）：首读 `base={}` ⇒ `linkguard_diff` 把**整库**算进 `added`，而 `linkguard_watch` 把 `changed + added` 合并写进同一个 `detail.changed` 键 ⇒ 诊断端 `crossseed-linkguard.py` 吐出「★ 被改写 / 新增（4 条种子受影响，共 **2816** 个文件）」——**而那天什么写穿都没发生**（18:00 那次起锚的真实输出）。正文与 metrics 本来是对的（`first` ⇒ 三个数全 0），所以**只看日报是发现不了的**：错的恰恰是"给人去查证时读的那份清单"。与 ① 同一族 —— 假警报的形状是**逐字看着像真的** |
| `test_hardlink_link_type.py` | 17 | `orchestrator/hardlink.py` 的 link_type 分派（#77 把 `matcher.link_type` 从**死键**接活）。钉三处：① ★ **认不出就抛、绝不 fallback 回硬链接** —— 静默退回**正是这个 bug 本身的形状**；② ★★ **`FICLONE` 失败必须清掉半成品** —— 内核可能已经把 dst 建出来了，不清掉下次会命中"已存在则跳过"，留下一个 **0 字节的假链接**，而"跳过"看起来完全正常；③ ★ **断链的 symlink 要当作"已存在"**（`Path.exists()` 对它为假 ⇒ 不特判就每跑一次多建一层）。另钉：硬链接真的同 inode、symlink 指向**绝对**路径（相对路径会在别的工作目录下断掉）、dry-run 只报数不落盘、源目录不存在抛 `FileNotFoundError`。<br>★ 本机（Windows，无 symlink 特权）会**跳过** symlink 那几条并打印 "--  symlink 跳过"；`reflink` 的真创建需要 BTRFS/XFS + Linux，这里**不假装测了** —— 只测失败路径与"Windows 上给的是清楚报错而不是怪异异常" |
| `test_qb_999_band.py` | 52 | 「qB 卡 999」判据 `state.qb_999_band()`（`progress>=0.99` 且 `amount_left>0` 且 `last_activity` 停滞 > 24h）—— 钉的不是"算得对"（那太简单），而是两个**看着像噪声、其实是判据本身**的分界：① ★ **健康下载中不许被记进去**（现场就挂着一条 `progress=0.9975` 的真·下载中的种子，`last_activity` 0.0h 前；没有停滞闸它会天天被记 —— 而"重新下载"要治，"正常下载中"不要）；② ★ **阈值的位置**（同一颗种子 23.3h 前不记、25h 前记，差的 1.7 小时就是"24h"这个约定本身 —— 随手把 `>` 写成 `>=` 或改成 12h，表现都是"测试全绿、行为不同"）。另钉：**上界故意没写**（`progress` 恰好 == 1.0 但没下完必须**命中**，浮点舍入会误杀它）、`error` 不重复记、`total` 是**全量分母**而不是命中数、结果里**只出 hash 不出 name**、字段缺失/None 一律不抛（+ 缺 `last_activity` 按"停了很久"算 —— 宁可吵不可静默）。<br>★ 后 8 条钉的是**基线 + 接线**（`drive-loop.qb_999_watch`）：首读只记基线**不响**、与基线一致则静默、**新增才点名"新增 N 条"**、★ **缩回时不许说「与基线一致」**（基线刚被改写成现状，那句是**假话**，且会让"清掉了"和"判据没读到"长得一样）、清空要说出来、`n/a` 那一轮**不抹掉**旧基线、以及 ★★ **跑完一整份 `report_daily` 后基线仍在 `.reconcile.state` 里**（`reconcile_watch` 是"开头读快照、末尾整文件覆盖写回"，夹在它读→写之间的写入会被静默抹掉 —— 这条钉的就是"没被夹进去"） |
| `test_qb_census_props.py` | 23 | `qb-census-savepath.py` 属性 dump 的三形态**分得开**（#63 的修 ⇒ #64 的回归）：**缺键**「(字段不存在)」/ **真空串**「(空)」/ **有值**前 4 段。钉的不是"印得对"，而是那条**假读数本身** —— qB v4.6.5 的 `/api/v2/torrents/properties` **压根不返回 `content_path`**，旧代码 `pr.get()` 给 None ⇒ `depth(None, 4)` ⇒ 印成「(空)」，注释又把它解释成「qB 没能建立起内容路径」⇒ 一个**取错接口**的问题被印成了 qB 的病症。★ 用**沙箱 + 假 `.env`**（真文件逐字节拷进 `tempfile.mkdtemp()`，`call()` 全程打桩）：被测脚本在**模块顶层**读 `<仓库>/.env`、读不到就 `sys.exit(1)`，而 importlib 载入时 `SystemExit` 被吞掉 ⇒ 不拷就根本载不进 `main`，测试会变成空转。另钉：**没有偷偷退回 `info`**（本现场的 info 行里故意**带着** `content_path`，缺键仍须印「(字段不存在)」）、脱敏（第 5 段站点名与资源名都不出现在整份输出里）、键全时不许出现「(字段不存在)」、★ **`null` 仍印「(空)」**（**这里钉的是现状不是期望** —— 修法只判 `k not in pr`，没覆盖"键在、值为 null"这一格）、属性接口炸了不许带走整趟普查（rc 仍是 0、前半段照常打完）。★★ **红过再绿手工验过**：回退到 `012bcbb^` ⇒ 恰好红 4 条（全是缺键那一族），恢复则 23/23 绿 |
| `test_quota_trend.py` | 96 | 额度台账（A 来源滚动 24h + B 来源 Prowlarr + 两者互校）+ 趋势（按周按站）+ 农场巡检判断（**argv 里绝不出现 `--prune`**） |
| `test_reconcile.py` | 127 | 观测对账三条判据：**合取 vs 单字面量**（`] Found ` 会吃到 `Found 0 torrents for {`，实测 2231 vs 靶心 1011）、**两口径不是同一个数**（三包共用一个 `farm_root` → 生产口径 other_pack 865/146/1011 而非 0）、**全场无人认领**（1888 条里恰好 1 条，且必须点名路径）。外加接线层：`reconcile_watch` **绝不抛**、读不到给 `n/a` 不给 0、正文里带口径名与留证行，★ **三个出口各钉一条 alert**（`reconcile-controls` 判据没走通 / `reconcile-empty` 空转 / `log-parse-miss` 形状对正则没吃下），★ **状态库不在时不许把空库建出来**（连父目录都不能建），以及 ★ **第四类「声明点〔--packs〕」**：两个方向的差集、各自点名、对得上时两个 0 且不发 alert、没给 `--packs` 与库读不到都给 `n/a`。★ 差集改成**只报变化**：首读记基线不告警、与基线一致不许再喊（核心那条是**同一个差集连读两次，第二次必须静默**）、缩回基线内静默采纳且**再长回来必须报**、★★ 缩回**必须在正文里写出来**（少了谁要点名；**不许**打印「与基线一致」—— 那句在当时是**假话**，且会让「`mbf` 被排进 `--packs` 了（预期）」与「判据今天没读到（故障）」长得一模一样；2026-09-13 #40 的缩回正好撞上这个洞）、`n/a` 轮**不许抹掉旧基线**。★★ **无人认领也走同一套**（2026-09-13）：首读只记基线**不响**（那 1 条是已接受的现状，配上 12h 冷却就是每天响两次、永远）、**同一个集合连读两次第二次必须静默**、出现**新的**一条才发且正文只点**新增**那条（metrics 把总数与新增数分开）、缩回静默但**正文写出来**（否则"清掉了"和"判据没读到"分不开）、**缩回后再长回来必须报** |
| `test_roots_from_env.py` | 25 | `init --roots-from-env` 的**取值来源**：首选 `FARM_SOURCES`、缺席时退回 `DATA_DIRS`（★ 与 `build-farm.sh:132-155` 同一种优先顺序）。核心那格是**同一个 `.env`、同一句 `--match`**：`FARM_SOURCES` 在 → 挑得到，只有 `DATA_DIRS` → 挑 0 —— 差异只可能来自取值来源，所以它量的就是"键换对了"本身。另钉：退回时**必须说出来**（不静默）、两个键都没有时报错要点名两个键、`--match` 挑不中时文案点名**实际用的那个键** |
| `test_remove_indexer.py` | 8 | `add-torznab-indexer.py --remove` 的**字节保真**与安全闸（合成样本 + 假 key，绝不碰生产 `.env`） |
| `test_readme_counts.py` | 10 | **这份 README 自己的三处「数」必须互相对得上**（2026-09-16 新增）。起因是**本文件自己漂过**：那段「+3 / +15」的话被**抄成了两遍**，两份说的还是**同一个对**的数，而检查它的方式是「读一遍看像不像」⇒ 绿；是用户问了一句才翻出来。★ 教训与 `test_notify_digest` 的红过再绿同族：**文档自己也要能「红」** —— 不是断言，就没法红。钉三处（各自会**独立**漂）：① 日期行的「N 个脚本 / M 条断言」；② 表格 21 行的「断言」列**逐行相加** == ①；③ 表格里 `test_backoff.py` 那一格 == 日期行里「51 → 54」那句的**新值**；④ 外加一条守门：**没有「连续两行完全相同」的抄写**（≥30 字符）—— 正是本次的病根。★★ 反面：**别把它写成「正则搜一下 772 在不在文件里」** —— 那种检查在「有两个 772」时**同样通过**
| `test_scan_secrets.py` | 29 | `scripts/scan-secrets.py`：阳性拦得住（值指纹 + 形状两条路）、阴性不误报；**形状层必须参与退出码**、**只拦「本次新引入」**、三类假阳性（读变量 / 英文短语 / 同字符重复）必须被规则认出来。<br>★★ **2026-09-17 加 ⑨（25 → 29）：非 ASCII 路径必须真的被扫到**（`#119`/`ERR-GIT-04`）—— `git ls-files` 默认 `core.quotepath=true` 把中文路径输出成**带引号 + 八进制转义**，下游 `(REPO / rel).is_file()` 为假 ⇒ **静默跳过**。实测本仓 **111 个文件里 25 个中文名全被跳过**（全是 `summary/*.md`）—— **而屏幕上照旧打「零命中」**。★ 修法：`-c core.quotepath=false` + `ls-files -z` 配 `split("\0")`；并加**自证那一行**「扫描范围：N/N 个文件」+「N 个路径读不到文件」告警。★ 判据是**在沙箱里放一个中文名文件**再断言它被点到名 —— 已做变异验证：换回旧写法 ⇒ **红 3 条**（含「读不到文件」与「点名」两条）。★★ **写这条时测试自己先红了一次**：我第一版断言 `rc=1`，但那个文件已被 commit ⇒ 闸门按设计报「HEAD 里本就有」而**正确地**返回 0 —— **是我把 ⑧ 的语义套到了 ⑨ 上**，断言改成量「有没有点名」，不量 rc。 |
| `test_matched_indexers_union.py` | 15 | `matched_indexers` 是**单调事实**（#73）—— 与 `indexer_seen` **语义相同、写法却相反**（一个合并、一个整行覆盖）是**遗漏**，不是设计：`facts.found` 的**唯一**输入是**当天**的 `info.current.log`，日志跨天一滚动（`info.current.log` → `info.YYYY-MM-DD.log`），下一次 sync 就拿不到那批 Found 行 ⇒ 整列被抹成 `[]`；而 **SEEDING 的行不会再被搜** ⇒ **永不恢复**。生产见证：同一张 605 行的表 **09-12 非空 215 部 → 09-14 变 0 部**（215 那个数见 `SUMMARY` 的「`on (\S+) by` → `on (.+?) by`」一节）。钉四格：★★ **跨天滚动后旧值不许丢**、新站来了要**并集**（不是替换）、源头不再造 `"A|B"` **合体标签**、老库残留的合体标签要**拆开**再并（读侧 `_sites()` 按 JSON 数组**逐项**取，不认里面的 `\|`）。★ 另两组对照：**单调的只有这一列**（`matched_hashes` 仍被整行覆盖、`stage` 照旧会塌、searchee 落到别的包时三者表现完全不同），以及 per-站展开**没有**污染计数（`matched` 从 `\|hashes\|` 个元组变成 `\|hashes\|×\|站\|` 个，`matched_hashes` / `seeding_count` 仍须原样）。★★ **红过再绿**：回退 `state.py` 那两个 hunk ⇒ **红 4 条**，恰好就是「跨天不丢 / 并集 / 拆合体 / 别的包仍保留」那一族 |
| `test_sync_empty_sets.py` | 31 | `sync_movie` 整行覆盖写 + 四个空集的后果：**SEEDING 塌成 PENDING 是降级不是删行**（连 `searched_indexers` 都被抹掉），而 `indexer_seen` 是**合并**保留的 → `next_retry_at` 被推到未来一整个周期 = **额度悄悄烧掉一轮**；端到端钉「一条计数器 / 两条静默通道」——searchee 落到**别的包**（`other_pack`）与**认不出的名字**（`unresolved`）后果一模一样，**只有后者会喊** |
| `test_iyuu_watch.py` | 30 | IYUU 辅种条数（§18.8 的唯一生产级证伪点）：**重构没改行为**（`qbit_torrents` 的 URL 仍是老那个字面量 —— 它在每 15 分钟的热路径上）、中文 tag 必须被百分号编码（URL 要纯 ASCII）、`iyuu_watch` **三种情形都不许抛**、以及 **数字真的进了 `metrics`**（TSV 只记 metrics 不记正文，"看着发了其实没留痕"肉眼看不出来） |

## 写测试时踩过的坑（别再踩）

- ★ **控制台/管道是 GBK 时 `⑪`(U+246A) 编不出去**，脚本会在打印到那一节时中断 ——
  而**已经过的断言看着全是"ok"**，极易误判成"代码坏了"。
  所以每个脚本开头都强制 `sys.stdout.reconfigure(encoding="utf-8")`。
  **加新的节编号时不要越过 `⑩`**（`①`..`⑩` 在 GBK 里，`⑪` 不在）。
- ★ **测试自己也是判据 —— 报红时先怀疑断言本身。** 真栽过两次：
  `test_remove_indexer.py` 第 4 项拿 `api\n\r\n` 当针（那行实际以 `apikey=FAKE` 结尾，
  针在哪儿都匹配不到），以及 `check-indexer-timestamps.py` 用严格 `==` 比
  `NanyangPT` 与 `NanyangPT (南洋)`。**两次的表现都和真 bug 一模一样。**
  第三次是同一天：`test_sync_empty_sets.py` §② 只期望一条流水，实际
  `PENDING → SEEDING` 那次**也是变档**、也留了一条 —— 代码是对的，断言写窄了。
- ★★ **测试会把「误判」固化成契约 —— 于是修它反而像在破坏约定。** 2026-09-14 实例：
  `test_reconcile.py` 里有一条断言**逐字**要求告警正文出现「匹配逻辑」—— 那是**为一条写错的
  告警文案写的**（它把「本日还没搜出去」断言成了「匹配逻辑坏了」）。有这条断言在，
  **改正文就要先改测试**，看起来像在「绕过测试」，其实是在**拆掉一个假契约**。
  ⇒ 写断言时先问一句：**我钉住的是「应有的行为」，还是「此刻的行为」？**
  后者要显式标出来（"这里钉的是现状，不是期望"），否则它会在下一次修 bug 时**冒充判据**。
  ★ 与上一条的区别：上一条是「断言写窄/写错针」，这条是**断言把已知的错误行为供了起来** ——
  前者报红，后者**一直绿**，所以更难发现。
- ★ **"生产会写文件"的路径，测试里必须改向到临时目录** —— `STATE_FILE` /
  `FARM_CHECK_FILE` / `DAILY_FILE` / spool。否则跑一次测试就往**仓库**里留产物：
  `test_once_gate.py` §⑤ 就是这么把 `.farm-check.state` 写进 `scripts/` 过的（已清掉）。
- ★ **闸门自己的对照集，必须先过闸门自己。** `test_scan_secrets.py` 里那些"凭据样本"
  一律由**变量拼出来**（`"-".join([...])`、`"y" * 16`），源码里不留字面量 ——
  否则推前扫描会扫到这个测试文件本身，"防凭据入库"的工具先给自己报红。
  同理，沙箱是 `tempfile.mkdtemp()`（**仓库之外**）：曾经把探针写成
  `<repo>/.ctl-secret-probe.txt`（随即删掉），那是错的 —— 往待推目录里塞一个
  凭据形状的文件，本身就是这件事要防的。
- ★ **别用"按某个标记切源码再 exec 前半段"的取巧办法**（`src.split("hits = []")[0]`）
  去拿被测文件里的正则/函数：切点一改就**静默失效**，切出来的前半段照样能跑，
  只是少了后半个文件的定义。用 `importlib` 把**真的那份**载进来（吞掉 `SystemExit`、
  重定向 `stdout`）。
- ★ **`importlib` 载进来的模块，必须先 `sys.modules[name] = mod` 再 `exec_module`。**
  否则被测文件里只要有 **`@dataclass`** 就炸，而且报的是一个看着毫不相干的
  `AttributeError: 'NoneType' object has no attribute '__dict__'` ——
  因为 `dataclasses` 要 `sys.modules.get(cls.__module__).__dict__` 去找注解的命名空间，
  而 `spec_from_file_location` **不会**替你登记。
  **同一段 `_load` 代码对两个文件行为不同，原因却不在它们身上**：
  `drive-loop.py` 没有 dataclass 所以先跑通了，一度让人以为只有 `state.py` 有问题。

## 约定

- 这些脚本**不在 `deploy.sh` 的白名单里**，也**不进容器** —— 是 Windows 侧手动诊断用的，
  跟 `check-indexer-timestamps.py` / `migrate-reseed-dirs.py` 同一档。
- 涉及凭据的工具只用**合成样本**（`test_remove_indexer.py` 里全是 `FAKEKEY…`）。
- 别把测试产物写进仓库；需要落盘就 `tempfile.mkdtemp()`。
- ★ **「`--db` 路径在真库上跑得通吗」用拷副本验，不碰生产库。** 方法：
  把生产 `state.db` 拷到 `D:/tmp/<随便>`（**拷前先确认旁边没有 `-wal`/`-shm`** ——
  有就说明库正被打开着，此时拷出来是残的），然后本地跑
  `python scripts/drive-loop.py --once --dry-run --db D:/tmp/<...>/state.db ...`。
  阳性对照：给一个**不存在**的路径，必须报 `FileNotFoundError` 且**不建文件**。
  **产物用完即删** —— 它是**那次**验证的证据，源库会变，留着就有被当成
  「上次验过」的风险。（NAS 的 ssh 没开，本机不能在 NAS 上跑，所以只能这么验。）
