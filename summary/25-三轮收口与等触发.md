## §25 三轮收口：`#115` 等触发 · `#116` 措辞 · `#60` 结案 · `#119` 活工具的静默漏扫 · `#66/#68/#69`（2026-09-16 ~ 09-17）

> ★ **给下一个会话**：这一节是**当前状态**的入口。做完就往下补，别新建文件。
> 上一节是 §24（分章与本轮收口）。

### 25.1 一句话状态

**代码全绿、已部署、已推送。剩下的活主要是「等条件」和「拍板」。**
- 全套自测：**809 条断言 / 22 脚本 / 0 失败**（`tests/README.md` 是唯一出处，三处读数互相钉住）。
- 工作区：只有 `?? .claudine/`（按惯例不动）；`HEAD == origin/main`。
- 棋盘：**94 completed / 19 pending**（`~/.claude/tasks/<会话id>/NNN.json`，**必须重读**，别凭记忆）。

### 25.2 ★★ 下次先做这一件：重建 `#115` 的 cron（**它是 session-only，已经没了**）

`#115` 要「等触发」验收，而那个 cron 提醒**随会话结束就消失了**。
⇒ 新会话第一件事：照 `#115.json` 描述里那段「2026-09-17 交接」**重建 cron**（prompt 原文在那里）。
- 周期：**每 2 小时 + 分钟 18**。理由写在 `#115` 里：`next_sleep()` 四档
  （45/90/120/**180**）⇒ 撞退避时批间隔会拉到 3 小时，只盯 45 分钟会**正好错过**关键那几批。
- 巡检命令：`python tools/check-115.py --all` —— **只读**，随时可手跑。
- ★ **静默是常态，静默不等于通过**（`WAIT` 就是「没触发」，不是「验过了」）。

### 25.3 本轮修掉的四件（都有实测 + 判据 + 变异验证）

| # | 症状 | 根因 | 落地 |
|---|---|---|---|
| **`#116`** | 09-16 10:41 打出「解禁 10:21:19」而**它早于触发时刻** | ★ **数没印错，是「解禁」这个词印错了**：库里 `until` 已过时它**不是解禁时刻**，而是「上次窗口已结束」的**陈旧残值** | 按 `until <= now` 分两句；`test_backoff.py` ⑭（**⑬管格式、⑭管语义**，两条不重叠）|
| **`#60`** | 挂着「Prowlarr 放行后 cross-seed 动不动」的待验**三天** | ★★ **那个待验问错了问题** —— 它**按自己的 `retry_after` 到点就走**（实测 11:32:28→11:32:31，**差 3 秒**），**压根不看 `disabledTill`** ⇒ 那一格**永远不会发生** | `ERR-SVC-17` 按实测更正；`summary/22` §22.6 |
| **`#119`** | `scan-secrets.py` 报「零命中」 | ★★ **活工具被同一类坑绕过**：`core.quotepath` 默认 true ⇒ 非 ASCII 路径被转义 ⇒ `is_file()` 为假 ⇒ **111 个文件里 25 个中文名全跳过**（全是 `summary/*.md`）| 加 `-c core.quotepath=false` + `-z`；加**自证那行**「扫描范围：N/N」；`test_scan_secrets.py` ⑨ |
| **`#66`** | cross-seed 日志里 `Unknown endpoint: /api/v2/app/version` | 本仓自带的踩法：`qbit_client.py:83` 请求的是 **qB 的**路径，而 README 把 `URL=…:2468` 与 `Q=…:3060` **挨着写**，`--qbit-url` 无校验 ⇒ 抄错一格**报错出现在别人家日志里** | `reseed-state.py` 加 `_guard_qbit_url`；`test_orchestrator_state.py` ⑨ |

### 25.4 `#68` / `#69`（顺手清掉的两条）

- **`#69`** 09-13 那 4 条「每日台账」⇒ **是 `--daily-now` 手工催了 3 次，行为正确**，
  **代码一行没改**（条目要求「查清了就别去重」—— 去重会把已解释的现象抹平）。
- **`#68`** TSV 加**第 5 列 `msg`** = 正文首行（摘要的告警明细原来只打时间+标题，
  标题里没数的告警**一个字都判不了**）。★ **向后兼容**：`NF>=5 && $5!=""`，
  旧行行为一字不变、`$1/$3` 位置没动。

### 25.5 ★★ 本轮真正该带走的三条方法论

1. **判据要能被证伪，也要能被「证无用」。** `#60` 卡在「到点动不动」上三天，
   而那一格**永远不会发生** —— 读到「它按自己的时钟走」之后，那条待验应当**当场作废**。
2. **「A 与 B 是同一事件」必须能从数据本身读出来。** 我为解释 `#116` 那两行，
   **编**了一个机制（「04:48 丢站名 → 10:41 补显」）—— **三处全错**，因为我把
   **两条相邻的日志行**当成了因果。正解是去读 `cross-seed.db` 的 `retry_after`。
   （已登记进 `#70`：「看到两个就编出一个关系」，比「漏一类」**更阴** —— 它**看起来也有结构**。）
3. **工具换了数据的写法，而过滤条件是按另一种写法写的** ⇒ **一条都不匹配且不报错**。
   记进 `ENVIRONMENT.md` `B.10` **第 16 条** + `ERR-GIT-04`（含现场复现：同一条 grep，**0 vs 1**，
   两次 git/grep **都退出 0**）。★ 唯一可靠的判据是**数数量**，不是「再仔细看一眼」。

### 25.6 下一步的候选（按建议顺序）

> ★★ **2026-09-17 晚：序 0 / 1 / 2 三条已做完**（本轮），下面保留原表并就地标注。

| 序 | 做什么 | 为什么 |
|---|---|---|
| **0** | ✅ **已完成（2026-09-17 晚）** —— **重建了 `#115` 的 cron** | 它确实已经没了（session-only）。★★ **本轮补一条交接说明里没写的，后来发现「两件」要分开算**：<br>· ① **session-only**（建时 `durable=false`、没落盘）⇒ 会话一结束就没了；<br>· ② **7 天自动过期** —— 调度器自己的规矩，**与 ① 无关**：即便会话一直开着，**满 7 天也会自删**（★ 到期会**先触发一次再删** ⇒ 那最后一次仍可能是等到的 PASS，别提前放弃）。<br>⇒ 两者**症状完全相同**（`CronList` 里没了）、**到期前零提示** ⇒ 判据只能是**当场 `CronList` 数一遍**，不能是"我建过没有"（`B.10`「枚举 ≠ 全量」同族）。<br>**本次仍是** `recurring=true` + **`durable=false`** ⇒ 两个原因都在，**最多只管到 2026-09-24**（或本会话结束，取先到者）。<br>★ 两处（本节 + `#115.json` 交接段）已同步一致；重建命令/prompt 原文以 `#115.json` 为**唯一出处**。（2026-09-17 晚补注） |
| **1** | ✅ **已完成（2026-09-17 晚）** —— **`#87`+`#88`+`#89` 一组** | 同一根因（**枚举 ≠ 全量共享表**），纯文档、无阻塞。★ `ERR-HW-03` 的结论**建立在 `net view` 上**，而同一条 ★（**看不见 ≠ 不存在**）**在 8 行之外被自己违反**。★ 本轮**把它们改成用 `share_list.csv`**（**逐共享 `Volume` 列 + 精确字节、一次读全 12 个、不依赖枚举**）—— 判据更强，且 `net view` 拆成了独立的 `ERR-HW-03b` |
| 2 | ✅ **已完成（2026-09-17 晚）** —— **`#90`–`#97` 一组（8 条）** | 原状是「**全部未落笔**」。★ 本轮**落成代码**（不是只写文档）：`state.py` 的 `pack_progress` + `notify.py` 的键规范化 + `drive-loop.py` 接进日报同一个 `with` + 新测试 15 条。★★ **`#95` 实测比原记录重**：含空格的包名不只拆坏字段，碎片能**伪造 `pack=` 键** ⇒ 误计一批 |
| 3 | **`#100`** | zhuanzhong 侧两处旧话，小而具体 |
| — | `#109`（key 轮换）· `#72`（缺伴生文件）· `#77`（存储互校） | **等你拍板**：一个要你先动、两个是写操作。★ `#77` 本轮**顺手实测到一条更强的岔路**：`share_list.csv` 的 `Size (…)(Byte)` 是**逐共享精确字节**（`docker` = 2271792183 B）⇒ 「换对照量」那条岔路**已确认可行**（但仍未选） |
| — | `#70` | **登记处，不需要"做完"** |
| — | `#58` | **架构探讨，不动手** |

### 25.7 部署台账（本轮）

| 时刻（NAS 本地）| 内容 | 备份 |
|---|---|---|
| 09-17 00:30 | `#116` 措辞（`state.py` ×2 处）| `.deploy-backup/20260917-003049` |
| 09-17 01:01 | `#66` 护栏 + `#68` 第 5 列 | `.deploy-backup/20260917-010124` |
| 09-17 02:45 | `#90`–`#97` 落地（4 文件 / **4 条映射**，含 `state.py` **两个副本**）| `.deploy-backup/20260917-024555` |

★ **09-17 02:45 那次的三步回读证据**（按台账规矩逐条做过）：
① **哈希**：NAS 4 份 vs 本地源**逐条相等**（`state.py` 两副本同为 `b1050d5e603a`）
—— 这一条正对 `ERR-DOCKER-04`「改一个副本、漏另一个，只在某条路径上炸」；
② **标识符计数**：NAS 侧 `pack_seeding_total` ×5、`pack_progress` ×2（两份 `state.py` 各一份）
+ `pp_metrics` ×3（`drive-loop.py`），与本地同数；
③ **复跑 dry-run = 「✓ 生产与本地一致，无需同步」**。
★ 前置：开工前 `running_pid=None`、`last_sleep_sec=2700`（45 分钟档）⇒ 窗口 **31 分钟**，未撞退避延长。

回滚一律 `bash deploy.sh --rollback`。★ 两次都先确认 `running_pid=None`、读回验证过、
复跑 dry-run =「生产与本地一致」。

★ 本轮提交：`7da1b62`（#114 + ctx）· `b6ad8af`（分工 + README 自检）· `0fc6fb4`（check-115 PRE 档）
· `4c14ff8`（#116）· `a02707d`（#60）· `35675ac`（#119）· `8d41467`（#66/#68/#69）。
