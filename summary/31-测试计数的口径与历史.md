# §31 测试计数的口径与历史

> ★ **本文件是 2026-09-20 从 `tests/README.md` 整段搬来的**（原 L15–L107，92 行）。
> 搬的理由：那是**追加式读数史**（每跑一轮往里加一段）+ **计数口径的说明**。
> 数字已降级成**派生数据**（源 = 真跑；`tests/COUNTS.json`；`tests/gen-counts.py`），
> ⇒ 历史挪到这里留档，`tests/README.md` 只留**当前那一条**（机器渲染）。
> ★ **正文逐字搬，未改一字** —— 下面的读数都是**当时**的，别当现值。

---

2026-09-13 实测：**18 个脚本、690 条断言、0 失败**（本轮新增 `test_linkguard.py` 66 条 + `test_hardlink_link_type.py` 17 条）。

2026-09-14 实测：**21 个脚本、757 条断言、0 失败**（本轮新增 `test_qb_census_props.py` 23 条 +
`test_notify_digest.py` 22 条 + `test_matched_indexers_union.py` 15 条）。

2026-09-17 实测：**25 个脚本、868 条断言、0 失败**。

2026-09-17 实测：**26 个脚本、911 条断言、0 失败**（本轮新增 `test_drive_loop_service.py`
43 条 —— `#58` drive-loop 迁容器：compose 服务形状 + `profiles` 安全锁 + 常驻入口参数区段 +
`drive-loop.py` 常驻分支写心跳的 AST 回归）。★ 那两个数按上面那条命令**实测**填的，不是手估。
★ **本日晚间 +31 条**，逐笔两笔：**新增 `test_pack_progress.py`（15 条）**
+ **新增 `test_prowlarr_indexers.py`（16 条，`#83`）**

2026-09-18 实测：**26 个脚本、986 条断言、0 失败**（**第五读**，`#90–#97` 的续篇：日报「各包进度」加 **② 口径完成度**与**总计行**）。<br>★ **`test_pack_progress.py` 15 → 71（+56）**，加的是 **④ 段**：`pack_stage_census()` 的分子/分母/百分比 —— 夹具 `beta` 特意用**六个目录六个阶段**（`1/5/6` 三个数互不相等），这样「把 UNMATCHED 算进分母」和「按 `total` 算」这两条错法**都不可能靠巧合过掉**。<br>★★ **本节最重要的一条是阴性对照**：`100×1/6 = 17 ≠ 20` —— 少了它，一个把分母写成 `total` 的错实现能过掉其余**所有**断言。另有 `0` 与 `"n/a"` 的**成对**断言（`mbf` 的 `0/4` 分母是 4 ⇒ 它的 `0%` 是**合法读数**，与除零产生的 `n/a` 是两个不同输出，**合并两者 = 毁掉 `ERR-AI-03`**）。<br>★ 渲染层那几条走的是**真 `notify.py` 的键规范化 + 真 `drive-loop.py` 的渲染**（只在 `S.pack_progress` 这个**唯一入口**上打桩），并钉住「总计用**和**（2/6 ⇒ 33%）不是平均（⇒ 38%）」与「正文**不含**下降/退化/变差/做种率」两个方向。<br>★ 那两个数按上面那条命令**实测**填的，不是手估。

2026-09-18 实测：**26 个脚本、995 条断言、0 失败**（**第六读**：常驻容器**每 41 分钟被 autoheal 误杀**那次的回归）。<br>★★ **现象**：群晖容器管理每 41 分钟一封「drive-loop **意外停止**」邮件。**根因不是崩溃** —— `Heartbeat` 原本只包住**跑批**，睡眠期（日志「等待 45.0 分钟后跑下一批」）心跳**冻结** ⇒ 45 分钟 ≫ healthcheck 阈值 **900s** ⇒ **每一轮**判 unhealthy ⇒ autoheal 杀容器 ⇒ `restart: unless-stopped` 拉起 ⇒ 群晖按**退出码**报「意外」。<br>★★★ **真正的根是「一职两用」**：`heartbeat_ts` 同时要满足两个**要求相反**的消费者 —— healthcheck 要它「**睡觉也刷**」（否则误杀），而 `batch_alive()` 要它「**睡觉别刷**」（否则永远判「在跑」⇒ **静默永久停工**）。⇒ 加**正交字段 `phase`**（`idle`|`running`）把两职拆开：healthcheck 仍看心跳（现在**全时刷**），`batch_alive()` 改看 `phase`。<br>★ **`test_drive_loop_service.py` 60 → 71（+11）**：`batch_alive()` 的**四输入**（`running`+新鲜 ⇒ True；★ `idle`+**新鲜** ⇒ **False**；`running`+**陈旧** ⇒ False ⇒ 卡死仍可检测；**无 `phase`** ⇒ 原逻辑逐字保留）+ compose 阈值 ≥1800 + 睡眠点真被 `Heartbeat(phase=PHASE_IDLE)` 包住。<br>★★ **最重要的仍是阴性对照**：`phase=idle` 而**心跳新鲜**必须返回 **False** —— 若返回 True，说明 `phase` 压根没被读，那么「睡眠期心跳全时刷」一上线就会**永远判在跑** ⇒ **静默永久停工**（正是 `Heartbeat` docstring 里最怕的失败模式）。<br>★ **①p 的判据也改了**：原先写死 `"900" in test`，判的是**一个具体的数**；改成**读出来再比下界**（≥1800）—— 因为该钉的是「阈值 > 一轮睡眠」这个**性质**，而写死数字会让这条断言在 900→1800 的**修复**里变红，方向正好相反。<br>★ **日期行的格式是有约定的**：本文件的判定器只认 `<日期> 实测：**N 个脚本、M 条断言、0 失败`，我第一版写成「实测（第二次）：」⇒ 正则不匹配 ⇒ 它**取到了上一条 986** ⇒ ② 红。★ 教训：**加日期行时先读正则**（`test_readme_counts.py:48`），别自造格式。<br>★ 那两个数按上面那条命令**实测**填的。
2026-09-18 实测：**26 个脚本、1002 条断言、0 失败**（**第七读**：`write_state()` 的**原子写**——`#27` 留下的既有债，本轮补上）。<br>★ **为什么它值得单独一轮**：`write_state()` 原先是最裸的写法（`STATE_FILE.write_text`），而它有一个「读走半截 JSON」的窗口。★★ 那个窗口**本来就在**，但 `#27` 把心跳改成「**睡眠期也刷**」之后，写频率从「跑批期」扩到「**全时**」⇒ **暴露面被放大**，所以现在补。<br>★★ **后果链条**（为什么它比"少一个字节"严重）：读者是**容器外**的 `compose.yaml` healthcheck —— `json.load(open(...))`；读到半截 ⇒ 解析抛 ⇒ 判 **unhealthy** ⇒ autoheal 杀容器 ⇒ **又是一封「意外停止」邮件**，也就是 `#27` 刚修好的那个形状**换个入口复发**。<br>★ **改动**：`write_state` 走 `.tmp` + `os.replace`（同目录 rename ⇒ 原子），与 `.daily-report` / `.reconcile` / `.farm-check` / `notify.py` 的 spool **四处既有先例同形**。<br>★★★ **本轮最值钱的一条不是代码，是「临时名」那个约束** —— `test_drive_loop_service.py` **71 → 78（+7）**，其中 **⑤o 与 ⑤q** 钉的就是它：<br>· **哨兵 `check-deploy-drift`** 的 `KNOWN_NAS` 有一条「`drive-loop/scripts/` 下 `.state` 结尾的隐藏文件」—— 临时名以 `.tmp` 结尾 ⇒ **不匹配** ⇒ 扫在 `os.replace` 之前会报**未知文件**。<br>· **`chk58.sh` `[3]` 的反向判据**要求「`.state` 结尾」且不在六件白名单里 ⇒ 若把临时名写成 `.drive-loop.tmp.state`，**会从那边**被报「清单该更新了」。<br>⇒ ★★ **两个守卫的判据方向相反，只有 `<原名>.tmp` 同时满足**（两边都实测跑过）。⇒ 哨兵里**显式加一条 `.tmp` 豁免**（**不**写成宽通配 —— 宽规则等于在哨兵上开静默口）。<br>★ **⑤o 自己栽过一次，也记在下面**：第一版只比对字符串常量 ⇒ 把实现改成 `with_suffix(".tmp.state")` 它**照样过**（恒真假断言）；改成「**真写一次、用 spy 抓 `os.replace` 的源名**」之后才真能红（已做阴性对照）。★ 教训：**断言要落在被测对象的**行为**上，不落在它自己声明的常量上。**<br>★ 另三条阴性对照（都恢复了）：改回裸 `write_text` ⇒ ⑤k + ⑤n 红；`with_suffix(".tmp.state")` ⇒ ⑤o 红（`实际源名 ['.drive-loop.tmp.state']`）；撤掉哨兵豁免 ⇒ ⑤q 红。<br>★ 那两个数按上面那条命令**实测**填的。<br>★★ **现象**：群晖容器管理每 41 分钟一封「drive-loop **意外停止**」邮件。**根因不是崩溃** —— `Heartbeat` 原本只包住**跑批**，睡眠期（日志「等待 45.0 分钟后跑下一批」）心跳**冻结** ⇒ 45 分钟 ≫ healthcheck 阈值 **900s** ⇒ **每一轮**判 unhealthy ⇒ autoheal 杀容器 ⇒ `restart: unless-stopped` 拉起 ⇒ 群晖按**退出码**报「意外」。<br>★★★ **真正的根是「一职两用」**：`heartbeat_ts` 同时要满足两个**要求相反**的消费者 —— healthcheck 要它「**睡觉也刷**」（否则误杀），而 `batch_alive()` 要它「**睡觉别刷**」（否则永远判「在跑」⇒ **静默永久停工**）。⇒ 加**正交字段 `phase`**（`idle`|`running`）把两职拆开：healthcheck 仍看心跳（现在**全时刷**），`batch_alive()` 改看 `phase`。<br>★ **`test_drive_loop_service.py` 60 → 71（+11）**：`batch_alive()` 的**四输入**（`running`+新鲜 ⇒ True；★ `idle`+**新鲜** ⇒ **False**；`running`+**陈旧** ⇒ False ⇒ 卡死仍可检测；**无 `phase`** ⇒ 原逻辑逐字保留）+ compose 阈值 ≥1800 + 睡眠点真被 `Heartbeat(phase=PHASE_IDLE)` 包住。<br>★★ **最重要的仍是阴性对照**：`phase=idle` 而**心跳新鲜**必须返回 **False** —— 若返回 True，说明 `phase` 压根没被读，那么「睡眠期心跳全时刷」一上线就会**永远判在跑** ⇒ **静默永久停工**（正是 `Heartbeat` docstring 里最怕的失败模式）。<br>★ **①p 的判据也改了**：原先写死 `"900" in test`，判的是**一个具体的数**；改成**读出来再比下界**（≥1800）—— 因为该钉的是「阈值 > 一轮睡眠」这个**性质**，而写死数字会让这条断言在 900→1800 的**修复**里变红，方向正好相反。<br>★ **日期行的格式是有约定的**：本文件的判定器只认 `<日期> 实测：**N 个脚本、M 条断言、0 失败`，我第一版写成「实测（第二次）：」⇒ 正则不匹配 ⇒ 它**取到了上一条 986** ⇒ ② 红。★ 教训：**加日期行时先读正则**（`test_readme_counts.py:48`），别自造格式。<br>★ 那两个数按上面那条命令**实测**填的。
2026-09-18 实测：**27 个脚本、1083 条断言、0 失败**（**第八读**：新增 `test_reseed_freeze.py` 81 条 —— 「装不出来 / 没 peer」判据 + 它的 watcher）。<br>★★ **先说这一轮为什么**：用户报「连校验过后、一点都不相符的文件都开始直接下载了」，而且**真因不是他以为的那个**。实测（全量 1999 条）是：cross-seed 按「名称 + 大小」匹配上了**发布组把附件（`.nfo`/`.jpg`）也写进种子的那一版**，而农场里没有这些附件 ⇒ qB 去补那几个缺文件 ⇒ **没有 peer ⇒ 永远补不上**（`stalledDL`）。★ 证据是**四个位置的读数**：逐文件 progress 里 `.mkv ≈ 99.98%` 而 `.jpg`/`.nfo` **恰好 0.00000%**；那个 0% 的文件在 `reseed_singles/` 里**不存在**、在 `reseed_farm/` 里**存在**；载荷（`.mkv`）两侧**大小逐字节相同**；`amount_left`（2097152）**≠**（农场 size − 物理 size）（254167）—— 最后这条证明 qB 的进度是**按 piece 全局推导**的，**不能跨文件对账**（`ENVIRONMENT.md` `A.15.3`「原理 C」）。<br>★★ **阈值 `1e-3` 的依据是实测的断层，不是拍的**：全量 1999 条按 `amount_left/size` 分档 ⇒ `==0` 1990、`(0,1e-3]` **3**、`(1e-3,1e-2]` **0**、`(1e-2,0.5]` **0**、`(0.5,1]` 6 ⇒ **1e-3 与 0.5 之间一条都没有**。★ 取 `1e-3` 是取**靠缺口那一侧**：将来出现「比 0.1% 稍大但仍够不着」的会落进那个**今天还是空的**区间里 —— 那正是需要人看一眼的地方，不该被静默放过。★★ **本文件 §③ 钉的就是这个位置**：`int(MKV*1e-3)` 命中、`+1` 不命中 —— 少了这条，把 `>` 写成 `>=`、或把 1e-3 改成 1e-2，表现都是「测试全绿、行为不同」。<br>★★ **两条判据是正交的，§② 专门钉「不许混」**：「装不出来」是**缺口小**（差附件、已定型、互锁已生效不再新增），「没 peer」是**全缺**（一个字节都没下到、`availability == 0`、可能自己活过来）—— 两者**处置方向相反**，并成一个数就再也分不开。§④ 反向钉住**健康下载中不许被记**：缺口 40% 的不命中、有 1 个 peer 的不进「没 peer」、`availability≠0` 的也不进。<br>★ **§⑫ 钉的是用户那句原话**：「有的是 iyuu 推过来的种子，这先不管，但**也要添加到邮件通知里**」⇒ 断言正文里**显式**有 IYUU 那一格、且条数**把它算进去**（不然它就从通知里消失了）。<br>★★★ **§⑬ 是本轮最值钱的一条，而且它是我自己踩出来的**：`_reseed_freeze_flat()` 第一版写的是 `for grp, by_src in groups.items()` —— 而它的入参有**两个来源**，`_reseed_freeze_split()` 那个**多一个标量 `total`**，于是 `total=1999` 被当成分组字典去 `.items()` ⇒ 实测 `AttributeError: 'int' object has no attribute 'items'`（**当场炸，没静默**，算运气好）。改成只遍历 `RESEED_FREEZE_GROUPS` 这两个**规范组名**之后才对。§⑬ 现在钉的是它的**正面价值**：同一个 hash 从「装不出来」变成「没 peer」**必须算「新增」** —— 拍平时若丢掉组名前缀，这种迁移就**看不见**了。<br>★ **§⑮ 钉「坏掉的基线 ≠ 空基线」**：基线文件写坏 ⇒ 当**首次读数**处理、静默起重锚、**不**把所有条目报成新增（同 `_linkguard_read` 那条：假警报比漏报更消耗信任）。<br>★ **§⑯ 键名卫生单独立一节**：`pack` **不许**恰好是键名、键/值**都不许含空格**（`notify.py:_render` 会把空格换成 `_`，`fz x=1` 能裂出一个 key 恰好是 `pack` 的段 ⇒ **静默污染** `notify-spool.sh` 的批次计数）；并用 `fz_` 前缀兜住。★ 这条在日报那一关（§⑰）又测了一遍。<br>★ **§⑰ 接线**：`report_daily` 里 `fz_` 进了 metrics、**卡 999 与链接守护两节都没被挤掉**（三条 watch 互不影响）、且基线落进**自己的**文件（`.reseed-freeze.state`，**不**塞 `.reconcile.state`）。<br>★ **另有一条 pin 的是「当前行为」而不是「期望行为」，已在文件头写明**：`size` 缺失或 `== 0` ⇒ **不命中**（不是当 0 算）—— 算不出比值就**不声称**。★ 为什么不跟 `qb_999_band` 的 `last_activity` 缺失「宁可吵不可静默」一个方向：那边缺的是**时间**（按最坏算只是提前报，代价小），这边缺的是**分母**（按 0 算会 `ZeroDivisionError`、按最大算会把正常种子说成坏的）。<br>★ 那两个数按上面那条命令**实测**填的（`tot=1083`、脚本数 `27`），不是手估。
2026-09-18 实测：**27 个脚本、1088 条断言、0 失败**（**第九读**：`#58` 那两个 shell 工具里 **python heredoc 的 UTF-8 锁** —— `mount-selfcheck.sh` **重建时漏抄了那句** `export PYTHONIOENCODING=utf-8`，而 `chk58.sh` 一直带着它）。<br>★★ **症状不是「整个脚本崩掉」**（那样反而显眼），是 **② 段半途而废**：`print("  ✓/✗ …")` 打的是 U+2713/U+2717，Windows 的 python 默认 stdout 是 **GBK** ⇒ 编不出 ⇒ 抛 `UnicodeEncodeError`；而**前面的 `✓` 行都打完了**，一到**第一条 `✗`** —— 也就是**真有问题、最该被看到**的那一行 —— 就抛 ⇒ 那一段的**全部裁决** + `⇒ 路径不可达数` **一行都出不来**，只剩一段 traceback；而 heredoc 那支 `|| RC=1` 又兜住了退出码 ⇒ **脚本以 1 退出却不给解释**。✦ 即**判据恰好在最该报的时候哑掉**。<br>★★★ **本轮的阴性对照是拿真脚本做的，不是只改测试**：把 `export` 那行删掉、用同一棵假树重跑 ⇒ ② 段**只剩 traceback**，三行 `✗` 与 `⇒ 路径不可达数：3` 全丢；加回那行 ⇒ 三者都在、`rc=1` 正确。⇒ 证明那句 `export` **是承重的**，不是装饰。<br>★ **处置是照抄、不是新设计**：`chk58.sh:107` 早就有这句，而它头注第 ④ 条**记的正是同一个毛病**（python heredoc 打了 U+21D2，GBK 控制台编不出 ⇒ 整段崩掉）⇒ `mount-selfcheck.sh` 属于**重建时漏抄了一句**，即本项目反复出现的「**同一个切换里漏改了一个**」（§19.1.3）。<br>★★ **反向判据比正面更值钱**：shell 那侧的 `bad()`/`ok()`（`printf`）**不需要**这条 —— 实测 GBK 下 `printf` 照打 `✗`（它不过 python 的编码器）。⇒ **别把「给 shell 也加一条」当补强**，那是把判据钉在一个**不存在**的失效模式上。<br>★ **另外两个 `sh` 工具也查了，结论是它们没有这个缺陷（查过才敢说）**：`notify-spool.sh` 的 ✓/✗ 出自 shell `say`（不是 python 打印）；`rm-staging.sh` 压根没有 ✓/✗。⇒ **本次只改一处**。<br>★ **⑧ 段 +5 条**（78 → 83）：两条「有对象」（确认那些文件真有 python heredoc 且真打 ✓/✗ —— 否则断言是恒真的）+ 两条「锁了 export」+ 一条反向（`chk58.sh` 的 ✓/✗ **两条路并存**，别只改一处）。★ 阴性对照：删掉 `export` ⇒ **只**这一条红（83 条 82/1），恢复即全绿。<br>★ 那两个数按上面那条命令**实测**填的（`tot=1088`、脚本数 `27`），不是手估。
2026-09-19 实测：**27 个脚本、1104 条断言、0 失败**（**第十读**：`phase` 的**默认值**与**三处收尾** —— ★★ 这一轮**没有缺陷可修**，修的是**我自己的一个错结论**）。<br>★★★ **来历（我上一轮把结论搞反过）**：我在 NAS 上 `cat .drive-loop.state` 读到 `phase:"running"`，就断言「收工后残留 `running` ⇒ 下一个容器永远判在跑 ⇒ **静默永久停工**」。**那是错的** —— **7 分钟后**同一条命令读到 `phase:"idle"`。★ 根因：`phase` **每 60 秒翻一次**（`Heartbeat.__init__(period=60.0)`）；`running` 是**批次正在跑**时的正常瞬时值，睡眠期被 `Heartbeat(phase=PHASE_IDLE)` 覆盖成 `idle` ⇒ **现场从头到尾是对的**。★★ **教训**：**`cat` 一次状态文件不是有效的取证动作** —— 它取的是**一帧**，而这个字段每拍就翻；判这类问题必须**两个时刻各读一次**（详见 SUMMARY §29.8）。★ 与 **§28.6** 同族：那条是「把**单点观测**当时刻表」，这条是「把**单帧读数**当稳定状态」。<br>★★ **所以 ⑨ 段不修任何东西，钉的是「别让将来的人把它改成 `running`」** —— 那个我**以为**存在的缺陷形状是：`_resident_state()` 的 `phase` 形参有**一处默认值**（`drive-loop.py:2944` `phase: str = PHASE_IDLE`），而它**四处调用里三处不传参**（`:2969` 启动 / `:2992` dry-run / `:3035` 本批无动作）⇒ **默认值写错 = 三处同时写错** ⇒ 「永远判在跑 ⇒ 静默永久停工」（正是 `Heartbeat` docstring 最怕的那个）。<br>★ **⑨a / ⑨a-2**：默认值是 `PHASE_IDLE`，且**有**默认值（没有 ⇒ 三处不传参当场 `TypeError`）。★★ **判据用 `ast` 读 `defaults`，不用字符串匹配** —— 字符串匹配**分不出**「默认值是 `idle`」和「注释里写了 `idle`」（而 `drive-loop.py` 里解释 `phase` 的那段注释恰好就写着 `PHASE_IDLE`）。<br>★ **⑨b ×6**：`once_round` / `main` 的**收尾那次**、`_resident_state` 的**每轮快照**，`write_state({...})` 字面量里**都含 `"phase"`**。★★ 理由是**「整体覆盖」**这条性质：三处都是**字典字面量、不是 `{**st, ...}` 合并** ⇒ **不写 `phase` 就等于删掉它** ⇒ 下一个读者走「旧格式」分支（只信心跳）⇒ 「说在跑的那位早收工了」**检测不到** —— **不是**「值写错了」，而是**键整个没了**（不看代码看不出来）。<br>★★ **⑨c-1 / ⑨c-2 是本段唯一钉行为的，而且正反都要**：`running`+心跳**新鲜**(60s) ⇒ **True**（正常在跑的**绝不许**判成可接管 —— 两批并发发 webhook ⇒ 一次 429 废几百条，README 坑 4）；`running`+心跳**陈旧**(900s) ⇒ **False**（卡死**必须**能被接管，否则 autoheal 那套**白装**）。★ 我上一轮提的「甲案」（把 600 抬到 1800）就是**牺牲后一边去修前一边**，而前一边**本来就没坏**。<br>★★ **⑨c-3 / ⑨c-4 如实写明它证明的比看上去少**：`HEARTBEAT_STALE_SEC` 仍是 `600`；599s ⇒ True、601s ⇒ False **只证方向、不证开闭** —— `time.time()` 不可能正好落在 `stale == 600.000000`，`<=` → `<` 这种改动**行为完全一样**（实测 97 过 / 0 失败）；想钉开闭只能注入可控时钟，**本文件的 `batch_alive` 用的是模块级 `time.time`** ⇒ 那种改动**本机未验**，不许当成已覆盖。<br>★★★ **写这段时栽的两次，两次栽在同一个地方**（「怎么算'收尾那个'」）：第二版用 `ast.walk` 的**出现顺序**取「最后一个」 ⇒ `main()` 里取到的是**嵌套函数 `_resident_state` 的 `write_state`（L2957）**，而不是常驻分支真正的收尾（**L3073**）⇒ 削掉 L3073 的 `phase` 之后**断言照样过**。★ 根因：`ast.walk` 是 **BFS**（先父后子），**不是源码顺序**；`_resident_state` 是 `main()` 的嵌套函数 ⇒ 调用点**行号更小**却在 walk 里**排得更晚**。⇒ **凡「第几个/最后一个」都必须按 `lineno` 排**。★★ 第三版改用**结构特征**指名收尾 —— 认 `"last_end_ts"`（开跑那次写的是 `running_pid_pidns`/`heartbeat_ts`，**没有**这个键）：**特征比位置稳**，位置法已栽过两次。<br>★★ **⑨d 是一条「未验」，不是通过**：`phase` 的**翻转频率**（60s/拍）是「`cat` 一次不算取证」这条方法学的**依据本身**，而它**离线判不了**（要真跑常驻进程 + 两个时刻各读一次）⇒ 显式打一行 `--`，**不计入断言数**：**「拿不到 ≠ 通过」**（`B.10` 第 14 条）。★ 那两个数按上面那条命令**实测**填的（`tot=1104`、脚本数 `27`），不是手估。
2026-09-19 实测：**28 个脚本、1124 条断言、0 失败**（**第十二读**：补上 `test_orchestrator_clients.py` 的登记 —— ★ 它本是**上一个会话**留下的产物：`orchestrator/crossseed_client.py` **用了 `HttpClient`/`HttpError`/`Response` 却从诞生起就没 import**（`e675e9e` 引入，`git log -S` 全历史查过：**不是「漏改」，是从一开始就缺**）⇒ 模块能 import（`from __future__ import annotations` 让注解不求值），但**一构造就 `NameError`**（本机实测：`NameError: name 'HttpClient' is not defined`）。★★ **而它在生产路径上**：`orchestrator/main.py:33` 与 `matcher.py:16` 都 import 它。★ 修法是加一行 `from .http import HttpClient, HttpError, Response`（与同构件的 `qbit_client.py` **同形**，实测两边都是 `level=1, module='http'`）。<br>★★ **判据用 AST 而不是 grep，且这条是必须的**：本改动的**本体就是一行 import**，而 `qbit_client.py` 里有**同名符串** ⇒ grep 式判据**恒真**。★ 判据分两层：(a) **自由变量**检查 —— 收集**作用域绑定**（形参/赋值/`except as`/import/函数类名）与**只算 Load 上下文**的 `Name`，两者之差必须为空；(b) **反向**要求 `from .http import` 这条**相对导入存在** —— 否则将来改成 `import orchestrator.http as _h` 再全写 `_h.HttpClient`，(a) 仍绿，但那是**另一种**写法，本判据此刻不该替它背书。<br>★★ **两处都是「判据写错的样子和代码写错长得一样」**：(a) 第一版把 **Store** 也算进 Load ⇒ 把 `base_url`/`api_key`/`e`/`logging` 全报成「缺 import」⇒ **假红**；(b) 第一版写 `.http in module` ⇒ `ast.ImportFrom.module` 报的是 **`http`（不带点）**、层级在 `level` 上 ⇒ **假红**；故判据写成「`level >= 1` **且** `module == 'http'`」。★ 另有**前提断言**（`len(_used) > 5`）防 (a) 因空集而恒真；★ **阴性对照**：删掉那行 import ⇒ (a) 当场红（`NameError` 在构造处显形）。<br>★ **本文件不联网、不碰任何状态文件**：两个客户端各构造一次（`CrossSeedClient` + `QbitClient` 作对照），外加一次 AST 解析。★★ **它挡的是一个「不报错、只在真跑时炸」的失效模式** —— `preflight` / `run` 里一构造就 `NameError`，而**静态检查与 import 都看不出来**。<br>★★ **第十二读（2026-09-19 深夜，1124 = 1114 + 10）**：给 `test_orchestrator_clients.py` 加了 **③ 段 `ping()` 判据**（10 → **20**）。★ 起因是修一处**恒假观测**：`preflight` 长期把 cross-seed 报成 `[??] 暂不可达`，而日志同刻写着 `cross-seed 返回 HTTP 404 @ http://cross-seed:2468` —— **404 与「连不上」不是一回事**。★★ 根因（读 cross-seed v6.13.7 源码定论）：它的 **HTTP 面只有 `/api/webhook` 一条路由**，根路径 `/` **本来就 404**，且「路由在、方法不对」回 **405** ⇒ 而 `CrossSeedClient.ping()` 按 `response.ok`（= 2xx）判活 ⇒ **恒判 False**，与 daemon 死活无关。<br>★ 修法：`ping(strict=False)`（默认）把判据改成「**拿到任何 HTTP 应答 = 活着**」，只有 `HttpError`（连接被拒/超时/DNS 失败）才算不可达；`strict=True` 保留旧语义供对照。★★★ **③ 段自带阴性对照**：复刻**旧实现**（按 2xx 判活）⇒ 在 404 上判 False、在 200 上判 True，**证明差异只在 404 这一档、改动前后结论确实相反**；否则这条判据等于没钉住任何东西。★ 另加**反向**断言：`preflight` 里那次 `ping()` 调用**不得**带 `strict=True`（带上的话修复等于绕过），且带一句**前提断言**（确实只找到 一次 `ping()` 调用，否则上面那条恒真）。★★ **判据的教训：一个永远返回 False 的探活，和「服务真的挂了」长得一模一样** —— 与 `test_readme_counts.py` ②b 同族：判据要**能被证无用**。<br>★ 那两个数按上面那条命令**实测**填的（`tot=1114`、脚本数 `28`），不是手估。


（`pack_progress` / `#90–#97`）—— 钉三件事，各对应一条订正：① **#95 空格键**：包名含空格会让 metrics 字段裂开，且碎片能**伪造出 `pack=` 键**
（摘要数批次靠它整键相等）⇒ 写入端规范化；★ 判据**成对**，另有一条阴性对照
断言「旧写法**确实**会伪造」（撤掉规范化 ⇒ 红 3 条，已变异验过）。
② **#96 接线**：`pack_progress` 返回结构化数据、空集**只有**「一个包都没登记」一种含义，
与「库读不到 ⇒ `n/a`」分开（`ERR-AI-03`）。
③ **#97 计数只有一份**：断言 `sync_pack` 改调共用函数、且 `r["stage"] == STAGE_SEEDING`
全文件**只剩一处**（复制成两份会静默分叉）。
★ 809 → 840 = **+31**，正好是这两个新脚本 ⇒ 下面那笔**未逐笔核对的 12 条**不受影响、
仍然挂着（**别把它一起算进本次**）。
★★ **本次自己先错了一次**：我先把这 15 条写成了 **13**（拿"我心里有几组断言"去填，
没去数）—— 是 `test_readme_counts.py` 的 ② 当场红的（表格相加 ≠ 日期行）。
**"看着对"的数字一律要去数**，这条与本项目 `B.10` 第 15/16 条同族。
★ **我能逐笔点清的只有这六笔（合计 +40，来自 09-14 → 09-17 那一段）**：
`test_backoff.py` **51 → 54 → 60**（+9：09-16 加 ⑬「退避时刻必须带日期」，09-17 加 ⑭「过去 ≠ 将来」）；
`test_next_sleep.py` **17 → 21**（+4，⑧「分工钉死」：`next_sleep` 只管节奏、不管站点）；
`test_scan_secrets.py` **25 → 29**（+4，⑨「非 ASCII 路径必须真的被扫到」，#119）；
`test_orchestrator_state.py` **29 → 36**（+7，⑨「`--qbit-url` 指向 cross-seed 必须出声」，#66）；
`test_notify_drain.py` **28 → 34**（+6，⑥「TSV 第 5 列 msg + 旧行向后兼容」，#68）；
★★ **外加 1 个新脚本 `test_readme_counts.py`（10 条）**，它钉的是**这份 README 自己**。
★★ **剩下 12 条（809 − 757 − 40）来源未逐笔核对，如实记在这里** ——
09-14 的 757 与 09-17 的 809 之间还有零星加条，表没跟着改。**别把差额全归给本轮。**
（这与 09-16 那次是同一个形状：**别硬凑数字**。要把这 12 条查实，得去逐脚本比 HEAD 与 09-14 那版。）
★ 复核方式：下表 **24** 行的「断言」一列相加 = **840**，与上面那条命令重数的 TOTAL 一致
（两边都是数出来的，不是推的），24 个脚本 rc 全 0。
★ **这张表与这行数由 `test_readme_counts.py` 互相钉住** —— 以后改了一处、
忘了另一处，跑测试就会红（本次就是被它抓出来的）。

★ **顺带校正表里一处旧数**：`test_backoff.py` 那一格原写 **39**，实测（HEAD 版）是 **51** ——
和 09-14 那条 `test_reconcile.py 120 → 127` 是同一个毛病：**表没跟着断言一起改**。
本次实测：51（旧）→ 54（新）= **+3**，正好是加的那 3 条 ⇒ 其余断言未受影响。
★ 全套 757 → **772** 是 **+15**，比 +3 多出的 12 条**不是本次引入**的
（是 09-14 之后到本次之间累积的、表未同步的部分）—— **别把 +15 归给这次改动。**
★ 数法：本文档下面那条命令（**三种行形都要数**；只数 `  ok  ` 会漏 —— 实测会少 8 条）。
★ 同日顺手校正下面表里一处旧数：`test_reconcile.py` **120 → 127** —— 09-13 之后又加了 7 条
断言，表没跟着改。（数按上面那条命令重数，不手算。）

★ **顺带校正表里一处旧数**：`test_backoff.py` 那一格原写 **39**，实测（HEAD 版）是 **51** ——
和 09-14 那条 `test_reconcile.py 120 → 127` 是同一个毛病：**表没跟着断言一起改**。
本次实测：51（旧）→ 54（新）= **+3**，正好是加的那 3 条 ⇒ 其余断言未受影响。
★ 全套 757 → **772** 是 **+15**，比 +3 多出的 12 条**不是本次引入**的
（是 09-14 之后到本次之间累积的、表未同步的部分）—— **别把 +15 归给这次改动。**
★ 数法：本文档下面那条命令（**三种行形都要数**；只数 `  ok  ` 会漏 —— 实测会少 8 条）。
★ 同日顺手校正下面表里一处旧数：`test_reconcile.py` **120 → 127** —— 09-13 之后又加了 7 条
断言，表没跟着改。（数按上面那条命令重数，不手算。）

★★ **09-16 这次「补完」本身踩了同一个坑，记在这里**：那条（原来的「+3 / +15」那段）原先被我**抄成了两遍**
（`+3` 那段与 `+15` 那段各写一次，一行一字不差），而**检查它那一步是"读一遍看像不像"** ⇒
**绿**。是用户问起"两处数字看起来对不上"才翻出来的。教训与 `test_notify_digest` 的红过再绿同一族：
**文档自己也要能"红"** —— 不是断言，就没法红。所以这里的规矩是
「**同一句话只许出现一次；副本一律删掉**」，而不是"下次读仔细点"。
（真值复核：那段重复的话**已删掉一份**；当前真值见文件顶部的日期行。）

★ 这个总数**别手算**。测试脚本的"通过"有三种行形（`  ok  ` 前缀、
`[n] 标签 : PASS` 后缀、`scan-secrets` 自带的一套），任何**单一** grep 都会漏掉
其中一类 —— 实测：只数 `  ok  ` 会得到 374，把 `test_remove_indexer.py` 的 8 条
整个漏掉。要重数就跑这一行（判定"全过"看**退出码**，不看数出来的条数）：

```bash
tot=0; for f in tests/test_*.py; do out=$(python "$f" 2>&1); rc=$?; \
  n=$(printf '%s\n' "$out" | grep -cE '^  ok  |^ FAIL |: PASS|: FAIL'); \
  echo "$(basename $f) n=$n rc=$rc"; tot=$((tot+n)); done; echo "TOTAL=$tot"
```

★★ **还有第四种：新写的测试自己另起一套行形 —— 那就不是"漏一类"，是整份漏掉。**
2026-09-13 写 `test_notify_drain.py` 时打印的是 `  ✓ ` / `  ✗ `，上面这行数出来
`n=0`、TOTAL 原地停在 522，**28 条一条都没进账，而退出码仍是 0**
（"看着一切正常"）。所以宁可改测试去迁就既有行形，也**别往 grep 里再加一条** ——
每加一条就多一处"我猜的行形"，而漏掉的形态恰恰是**没报错的形态**。

---

## 附：**当前那一条**读数的正文（2026-09-20 第十三读）

> ★ 下面这段原本紧跟在 README 那条日期行之后。日期行本身已归生成器（机器写数），
> 但**这段解释是人的**，所以**整段搬到本节留档** —— 数字是派生的，**理由不是**。

2026-09-20 实测：**28 个脚本、1143 条断言、0 失败**（**第十三读**：每日摘要**明细段按包聚合** —— 用户报「内容排版很难看」）。<br>★★ **一轮两个真东西，都值得记**：<br>① **排版**（生产代码 `scripts/notify-spool.sh` 的 `do_digest()`）：「批次明细」从「每批摊 2 行、`metrics` 原样打」改成**一行一个包**（批次数 + ok 合计 + **非零项**），台账从 **655 字符单行**拆成按 `prefix:包名` 分组的多行。★ 起因是**量出来的**：真邮件里 23 批 + 2 台账 ⇒ 该段约 **50 行**，而批次高度重复、`metrics` 长 83 字符却**几乎全是常数**，真信号（`newly_seeding=443` 全天 1 次、`failed=1` 2 次）**埋在 46 行里**。<br>★★★ **`test_notify_digest.py` 22 → 41（+19）**，而**最值钱的是那对「成对」判据**：`failed` 平时恒 0、一旦非 0 就是最该看的 ⇒ 既要钉「全零时**不**出现」（④）、也要钉「非零时**必须**出现」（④b）。**少了任一半，「省略常数项」就退化成「漏掉异常」= 把告警砍掉** ——「让邮件好看点」这类改动最容易犯的正是这个。★ 阴性对照**拿真脚本做**：`git show HEAD:scripts/notify-spool.sh` 回退 ⇒ **41 条里红 15**（26/41）；恢复 ⇒ 41/41。<br>★★ **本轮唯一真缺陷是测试抓出来的，不是我想到的**：聚合第一版对非本批（台账 / 无待搜）**只 `other++`**，把它们的**标题**丢了 ⇒ ④ 段当场红（「全部包已无待搜项」不见了）。★ 那个标题是**结论**（「3 个包都没待搜了」），不是你从别处能推出来的 ⇒ 改成单列标题。**又一次「测试自己也是判据，报红时先怀疑断言本身」**。<br>★ 另钉两条 `ERR-AI-03` 同族的：`n/a` 不许印成 `n/a%`、缺键印 `-` 不伪造成 `0`（原型阶段就抓到 `n/a%` 这个假读数）。<br>② **根 `README.md:1225` 的过期读数**：DSM 任务表写「每天 **21:00**」，实测应为 **21:20**（真源 `ENVIRONMENT.md:185` / `summary/15` L99）。★ 这个 `21:00` 是**危险的**：照它把任务建回，会得到一个「**看起来修好了**」的任务（`B.10` 第 22 条：错的读数读起来完全合理）。<br>★ 那两个数按上面那条命令**实测**填的（`tot=1143`、脚本数 `28`），不是手估。

（`pack_progress` / `#90–#97`）—— 钉三件事，各对应一条订正：① **#95 空格键**：包名含空格会让 metrics 字段裂开，且碎片能**伪造出 `pack=` 键**
（摘要数批次靠它整键相等）⇒ 写入端规范化；★ 判据**成对**，另有一条阴性对照
断言「旧写法**确实**会伪造」（撤掉规范化 ⇒ 红 3 条，已变异验过）。
② **#96 接线**：`pack_progress` 返回结构化数据、空集**只有**「一个包都没登记」一种含义，
与「库读不到 ⇒ `n/a`」分开（`ERR-AI-03`）。
③ **#97 计数只有一份**：断言 `sync_pack` 改调共用函数、且 `r["stage"] == STAGE_SEEDING`
全文件**只剩一处**（复制成两份会静默分叉）。
★ 809 → 840 = **+31**，正好是这两个新脚本 ⇒ 下面那笔**未逐笔核对的 12 条**不受影响、
仍然挂着（**别把它一起算进本次**）。
★★ **本次自己先错了一次**：我先把这 15 条写成了 **13**（拿"我心里有几组断言"去填，
没去数）—— 是 `test_readme_counts.py` 的 ② 当场红的（表格相加 ≠ 日期行）。
**"看着对"的数字一律要去数**，这条与本项目 `B.10` 第 15/16 条同族。
★ **我能逐笔点清的只有这六笔（合计 +40，来自 09-14 → 09-17 那一段）**：
`test_backoff.py` **51 → 54 → 60**（+9：09-16 加 ⑬「退避时刻必须带日期」，09-17 加 ⑭「过去 ≠ 将来」）；
`test_next_sleep.py` **17 → 21**（+4，⑧「分工钉死」：`next_sleep` 只管节奏、不管站点）；
`test_scan_secrets.py` **25 → 29**（+4，⑨「非 ASCII 路径必须真的被扫到」，#119）；
`test_orchestrator_state.py` **29 → 36**（+7，⑨「`--qbit-url` 指向 cross-seed 必须出声」，#66）；
`test_notify_drain.py` **28 → 34**（+6，⑥「TSV 第 5 列 msg + 旧行向后兼容」，#68）；
★★ **外加 1 个新脚本 `test_readme_counts.py`（10 条）**，它钉的是**这份 README 自己**。
★★ **剩下 12 条（809 − 757 − 40）来源未逐笔核对，如实记在这里** ——
09-14 的 757 与 09-17 的 809 之间还有零星加条，表没跟着改。**别把差额全归给本轮。**
（这与 09-16 那次是同一个形状：**别硬凑数字**。要把这 12 条查实，得去逐脚本比 HEAD 与 09-14 那版。）
★ 复核方式：下表 **24** 行的「断言」一列相加 = **840**，与上面那条命令重数的 TOTAL 一致
（两边都是数出来的，不是推的），24 个脚本 rc 全 0。
★ **这张表与这行数由 `test_readme_counts.py` 互相钉住** —— 以后改了一处、
忘了另一处，跑测试就会红（本次就是被它抓出来的）。

★ **顺带校正表里一处旧数**：`test_backoff.py` 那一格原写 **39**，实测（HEAD 版）是 **51** ——
和 09-14 那条 `test_reconcile.py 120 → 127` 是同一个毛病：**表没跟着断言一起改**。
本次实测：51（旧）→ 54（新）= **+3**，正好是加的那 3 条 ⇒ 其余断言未受影响。
★ 全套 757 → **772** 是 **+15**，比 +3 多出的 12 条**不是本次引入**的
（是 09-14 之后到本次之间累积的、表未同步的部分）—— **别把 +15 归给这次改动。**
★ 数法：本文档下面那条命令（**三种行形都要数**；只数 `  ok  ` 会漏 —— 实测会少 8 条）。
★ 同日顺手校正下面表里一处旧数：`test_reconcile.py` **120 → 127** —— 09-13 之后又加了 7 条
断言，表没跟着改。（数按上面那条命令重数，不手算。）

★ **顺带校正表里一处旧数**：`test_backoff.py` 那一格原写 **39**，实测（HEAD 版）是 **51** ——
和 09-14 那条 `test_reconcile.py 120 → 127` 是同一个毛病：**表没跟着断言一起改**。
本次实测：51（旧）→ 54（新）= **+3**，正好是加的那 3 条 ⇒ 其余断言未受影响。
★ 全套 757 → **772** 是 **+15**，比 +3 多出的 12 条**不是本次引入**的
（是 09-14 之后到本次之间累积的、表未同步的部分）—— **别把 +15 归给这次改动。**
★ 数法：本文档下面那条命令（**三种行形都要数**；只数 `  ok  ` 会漏 —— 实测会少 8 条）。
★ 同日顺手校正下面表里一处旧数：`test_reconcile.py` **120 → 127** —— 09-13 之后又加了 7 条
断言，表没跟着改。（数按上面那条命令重数，不手算。）

★★ **09-16 这次「补完」本身踩了同一个坑，记在这里**：那条（原来的「+3 / +15」那段）原先被我**抄成了两遍**
（`+3` 那段与 `+15` 那段各写一次，一行一字不差），而**检查它那一步是"读一遍看像不像"** ⇒
**绿**。是用户问起"两处数字看起来对不上"才翻出来的。教训与 `test_notify_digest` 的红过再绿同一族：
**文档自己也要能"红"** —— 不是断言，就没法红。所以这里的规矩是
「**同一句话只许出现一次；副本一律删掉**」，而不是"下次读仔细点"。
（真值复核：那段重复的话**已删掉一份**；当前真值见文件顶部的日期行。）

★ 这个总数**别手算**。测试脚本的"通过"有三种行形（`  ok  ` 前缀、
`[n] 标签 : PASS` 后缀、`scan-secrets` 自带的一套），任何**单一** grep 都会漏掉
其中一类 —— 实测：只数 `  ok  ` 会得到 374，把 `test_remove_indexer.py` 的 8 条
整个漏掉。要重数就跑这一行（判定"全过"看**退出码**，不看数出来的条数）：

```bash
tot=0; for f in tests/test_*.py; do out=$(python "$f" 2>&1); rc=$?; \
  n=$(printf '%s\n' "$out" | grep -cE '^  ok  |^ FAIL |: PASS|: FAIL'); \
  echo "$(basename $f) n=$n rc=$rc"; tot=$((tot+n)); done; echo "TOTAL=$tot"
```

★★ **还有第四种：新写的测试自己另起一套行形 —— 那就不是"漏一类"，是整份漏掉。**
2026-09-13 写 `test_notify_drain.py` 时打印的是 `  ✓ ` / `  ✗ `，上面这行数出来
`n=0`、TOTAL 原地停在 522，**28 条一条都没进账，而退出码仍是 0**
（"看着一切正常"）。所以宁可改测试去迁就既有行形，也**别往 grep 里再加一条** ——
每加一条就多一处"我猜的行形"，而漏掉的形态恰恰是**没报错的形态**。
