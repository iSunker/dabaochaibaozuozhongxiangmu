# ===== 切勿提交：密钥 / 运行时数据 =====
# 注意：.gitignore 不支持行尾注释，注释必须独占一行（否则会被当成路径规则）。

# .env 含 NAS IP / cross-seed apikey / (经 TORZNAB 携带的) PT passkey
# ★ 用 `.env*` 兜底，**不是** `.env`：`.env` 是**精确匹配**，任何备份/变体都拦不住，
#   而它们**同样是完整副本、同样含全部密钥**。
#   2026-09-13 实测（`git check-ignore -v` 逐个验）——旧规则写的是 `.env.bak.*`，
#   于是这些**全都漏**：`.env.bak`（无时间戳后缀）、`.env_bak`、`.env.backup`
#   ⇒ `git add -A` 能把一份带密钥的 .env 副本**提交进仓库**。
#   ★ 教训与 rm-staging.sh 那条窄口同源：**别让「会不会泄漏」取决于我们猜没猜对
#     文件名**。所以这里按前缀全兜，再显式放行模板 —— 漏检变成"多忽略一个"，
#     而不是"少忽略一个"。**两个方向的错不对称：多忽略看得见，少忽略看不见。**
.env*
# 唯一的例外：`.env.example` 是**纯占位符模板**（本地 .env 的存根），本来就该提交。
# 注意 `!` 规则必须在 `.env*` **之后**，且父级目录不能被排除。
!.env.example

# Prowlarr 配置目录含各站 cookie/passkey/apikey —— 忽略全部内容，只保留占位 .gitkeep
prowlarr/*
!prowlarr/.gitkeep

# cross-seed 运行时数据（config.js 需提交，故只忽略下列 db/日志/缓存）
cross-seed/*.db
cross-seed/*.db-*
cross-seed/logs/
cross-seed/torrent_cache/

# 我们自己的单片状态库（运行时数据，含包内片名清单）+ 由它导出的待办清单
hlink/state.db
hlink/state.db-*
# ★ 2026-09-12 由 todo.txt 改名：它是一份**路径清单**（reseed-state.py todo 的
#   输出，334 行全是绝对路径），不是给人和工具当"待办事项"读的文件。
scripts/todo-paths.txt

# 由状态机导出的派生数据（含真实片名/站点名，属运行时产物）
# ★ 2026-09-18：`run-batch.sh` 搬进 `scripts/diag/` 后，`batch-report.tsv`/
#   `unmatched.tsv` 也跟着产在那儿 —— 所以这条要跟着走，否则那两个文件
#   会**从忽略列表里掉出来**（不再是 silence，而是显示成未跟踪）。
scripts/*.tsv
scripts/diag/*.tsv

# drive-loop.py 的运行日志与状态文件（含真实片名/时间，属运行时产物）
scripts/drive-loop.log
scripts/.drive-loop.state
# ★ 上面那份的**原子写临时文件**（write_state 先写它再 os.replace；微秒级存活，
#   但崩在中间就会留下）。★ 必须单独列 —— `scripts/*.state` 那类通配**不覆盖**它。
scripts/.drive-loop.state.tmp
# 每日台账「今天发过没有」的标记（运行时产物；内容只是一个日期）
scripts/.daily-report.state
# 农场巡检「上次什么时候查的、结论是什么」（运行时产物）
scripts/.farm-check.state
# 对账读数台账：每一格**最后一次真的读到数**是什么时候（运行时产物；#34）
scripts/.reconcile.state
# 计划任务那一层的起跑/退出码留痕 + stderr 捕获（诊断「批次凭空消失」用）
scripts/drive-loop.attempts.log
scripts/drive-loop.task.err

# notify.py 的告警冷却状态（运行时产物：记住「哪个 key 最近什么时候发过」）
scripts/.notify.state
# notify-spool.sh 的**真实**配置（含收件人邮箱 —— 属个人信息，不入库）。
# 模板 notify.conf.example 要提交，供别人照着填。
scripts/notify.conf

# 由 gen-nas-env-update.py 生成的 NAS 更新脚本（内容 = 本机 .env 的 DATA_DIRS，
# 属派生数据；.env 一变就过期，别提交，用生成器现生成）
scripts/nas-update-env.sh

# 本地 NAS 地址（真实主机名 / 内网 IP）—— 各人自建，不入库
scripts/.nasrc

# ★ 2026-09-17 加：本机 AI 工具（claudine）的状态目录 —— 内容只有
#   `{"conversations": [], "lastUpdated": "<时间戳>"}`，是**本机运行时状态**，
#   不是项目事实。与上面 `.drive-loop.state` / `.notify.state` / `.reconcile.state`
#   **同一族**：跟着"哪台机器、什么时候跑的"变，入库只会制造噪音 diff。
#   ★ 它此前**不在任何 ignore 规则里**（所以一直显示为未跟踪）—— 不是"故意留着"，
#     是**漏了**。与 `.env*` 那条同源：靠"我记得加了"不如靠一条规则兜住。
.claudine/

# 本地分析用的临时目录（复制 cross-seed.db 出来读）
.tmp-db/

# `A.2.1` 的**一次性件**约定：本机上给 NAS 中转用的临时目录（镜像 tar 包之类）。
# ★ 为什么必须在这里有一条**通配**规则，而不是靠"我记得删"：
#   `docker save reseed-drive-loop:0.1.0 | gzip > _tmp-*/_drive-loop-img.tar.gz`
#   产出 **46 MB 二进制**，名字又人畜无害 ⇒ `git add -A` 直接吞进去，
#   而**镜像层是删不掉的**（删了也还在历史里，同 `.dockerignore` 那条 `.env` 的教训）。
#   ★ 与上面 `.env*` 那条同一族：**别让"会不会进仓库"取决于我们猜没猜对文件名。**
#   ⇒ 按前缀 `_tmp-` 全兜；目录名本身就是约定（`A.2.1` 要求"一眼看出是临时的"）。
_tmp-*/

# 文档审计工具的产物：标题树 / 引用表 / 关键词倒排 / 探针读数。
# 全是**可反复生成**的派生数据（01/02/03/04 各跑一次就回来），没有版本价值；
# 而探针读数还是**某个时刻的**快照，冻进仓库只会变成一条会漂的"事实"。
tools/doc-audit/out/

# 文档审计的**基线快照**（`out/` 的某一份副本，供两周观察逐行 diff）。
# ★ 与上面那条**理由不同**：`out/` 是可反复生成的派生数据；
#   这一份是**一次测量的快照** —— 进 git 就会从"某天的读数"升级成"事实源"。
#   09-20 若决定长期保留，再单独讨论要不要破例。
tools/doc-audit/out.baseline-*/

# Python
**/__pycache__/
**/*.pyc

# deploy.sh 覆盖生产前留下的备份
.deploy-backup/
  