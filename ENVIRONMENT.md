---
doc: ENVIRONMENT.md
title: 环境前提与环境坑手册
project: reseed-toolkit（大包拆包 · 单种保种工具链）
generated: 2026-09-13
purpose: >
  给「新会话的 AI」和「接手的人」一份可直接读的环境层事实与归因。
  事前交代（前提）防掉坑，事后归因（错误）能按症状检索。
derived_from:
  - README.md          # INFO 层：怎么做
  - SUMMARY.md         # DEBUG 层：为什么（7979 行，只在需要时按 §N 查）
  - 走过的弯路.md       # 20 项技术弯路全表（★ 2026-09-13 已**并入本文并删除**，见附录 2）
  - tests/README.md    # 测试层踩过的坑 + 行形陷阱
  - .env.example       # 配置键清单（纯占位符，无凭据值）
  - docker-compose.yml / deploy.sh / patch 文件
precedence: |
  ★ 本文是「检索层」，不是第三个事实源。
  与 README / SUMMARY 冲突时，一律以那两份为准 —— 本项目有「同一事实只在一处维护」的成文约定。
  本文的价值在于：把散在 1850 行 README + 7979 行 SUMMARY 里的环境事实收成一张可 grep 的索引。
contains_secrets: false   # 只指出凭据"住在哪"，不含任何凭据值
entry_id_scheme: "ERR-<层>-<两位序号>"
---

# 环境前提与环境坑手册

> **为什么要有这一份**：README 是操作手册、SUMMARY 是过程记录，两份都**假设读者已经知道环境长什么样**。
> 新会话的 AI 恰恰不知道 —— 它会把「这台 NAS 没有 `sendmail`」读成「没配邮件」，把「容器名」和「宿主机 IP」当成一条路。
> 本文做的事只有一件：**把「环境前提」显式写出来**，并给每条踩过的坑一个可检索的 ID。

## 怎么用这份文档

| 场景 | 读哪 |
|---|---|
| 新会话开局（做任何事之前） | **A 部分**通读一遍；至少读 `A.1 可达性`、`A.3 卷布局`、`A.12 三处路径` |
| 报错了 / 卡住了 | 按症状关键词 grep **B 部分**：`grep -n "症状" ENVIRONMENT.md`，再按 `ERR-` 编号定位 |
| 要给用户发一条命令 | 先看 `B.10` 元层清单第 1、2 条（成本纪律 + 只放可直粘的命令） |
| 想知道某个值（IP / 端口 / 路径 / 版本） | 下面这张「关键取值速查」 |
| 想知道某处历史过程、决策理由 | **不在这里** → README（怎么做）/ SUMMARY（为什么）|
| 想按「第 N 项弯路」找 | 每条的 边界/怎么发现的 里带 `弯路 #N` 标记 → `grep -n '弯路 #' ENVIRONMENT.md`（20 项全在本文，已无外部出处）|

```bash
# 列出全部错误条目（每条的 H3 就是一个 ID）
grep -n '^### ERR-' ENVIRONMENT.md
# 按症状找
grep -n -A2 '^\- \*\*症状\*\*' ENVIRONMENT.md | grep -i '关键词'
# 按「第几项弯路」反查（1..20）
grep -n '弯路 #' ENVIRONMENT.md
```

---

## 关键取值速查（2026-09-13）

> ★ 这里的数字**会变**。凡能一条命令读出来的，就别信写死的 —— 每次用之前回读一次。

### 机器

| 项 | 值 |
|---|---|
| NAS | Synology **DS423+**，DSM **7.2**，主机名 **`iSunker-DS423`** |
| NAS 局域网 IP | **`192.168.0.7`**（`.env.example` 里是 `192.168.1.10` 占位符，别照抄）|
| NAS 侧 shell | ★ **没有** —— SSH(22) 关闭。只读走 SMB，写走 `deploy.sh` 或用户在 DSM 里执行 |
| 开发机 | Windows 11 Pro，MINGW64 / Git Bash，控制台代码页 **GBK** |
| 工作目录 | `D:\Projects\dabaochaibaozuozhongxiangmu\reseed-toolkit` |
| 临时目录 | `C:\Users\艾森克\.claude\jobs\<job>\tmp`（后台任务）／`D:\tmp`（人工诊断）|

### 端口与容器

| 端口 | 服务 | 容器名 | 栈 |
|---:|---|---|---|
| 9696 | Prowlarr | `reseed-prowlarr` | 本栈 |
| 2468 | cross-seed (daemon) | `reseed-cross-seed` | 本栈 |
| 8191 | FlareSolverr | `reseed-flaresolverr` | 本栈 |
| 3060 | **qBittorrent-reseed** | `qbittorrent-reseed` | **独立栈**（`qbittorrent-reseed_default`）|
| 8787 | IYUU Plus | `iyuuplus_ssd` | ★ **别人的**，不在本项目管辖范围 |
| 8085 | 另一套 qB | — | 别人的 |
| 3020 | opencd | — | 别人的 |

- Docker 网络：本栈 `reseed-net`（bridge）+ **external** `qbit-net` = `qbittorrent-reseed_default`。
- `PUID=1026` / `PGID=100` / `TZ=Asia/Shanghai`。
- 生产栈目录：`/volume2/docker_ssd/prowlarr_cross-seed_autohardlink`，SMB 视图 `//iSunker-DS423/docker_ssd/prowlarr_cross-seed_autohardlink`。

### 版本

| 组件 | 版本 | 备注 |
|---|---|---|
| qBittorrent (:3060) | **4.6.5**，WebAPI **2.9.3** | `free_space_on_disk` 在 `sync/maindata → server_state`，**不在** `app/preferences` |
| cross-seed | **v6.13.7** | 官方镜像，**完全不吃我们的文件**（改配置只能改 `config.js` 挂载）|
| NAS 上的 python3 | **3.8.15**（DSM 自带，Package Center 无更新）| ★ 整条链路的单点依赖，见 `ERR-PY-01` |
| `/volume1` 文件系统 | **BTRFS**（`/dev/mapper/cachedev_1`）| ⇒ reflink / COW 可用 |

### 路径

| 用途 | 路径 |
|---|---|
| 大包源数据 | `/volume1/video/download/movies/…`、`/volume1/video/download/TV/…` |
| 硬链接农场（当前 `DATA_DIRS`）| `/volume1/video/download/reseed/reseed_farm` |
| 单片做种数据 | `/volume1/video/download/reseed/reseed_singles/<站点>/<发布名>` |
| 未查清的第三方目录 | `/volume1/video/link`（7.47 TB 表观 / 150 个媒体目录）—— 任务 #81 |
| 状态机库 | `<compose>/drive-loop/hlink/state.db` |
| cross-seed 库 | `<compose>/cross-seed/cross-seed.db` |
| 链接守护基线 | `<compose>/drive-loop/scripts/.linkguard.state` |
| 对账/基线状态 | `<compose>/drive-loop/scripts/.reconcile.state` |
| 批次心跳状态 | `<compose>/drive-loop/scripts/.drive-loop.state` |
| 通知 spool | `<compose>/notify/spool/*.txt` |

### 卷与容量

| 卷 | 设备 | 容量 | 可用 |
|---|---|---:|---:|
| `/volume1` | `/dev/mapper/cachedev_1` | 56 TB | ★ **已用 100%** |
| `/volume2` | `/dev/mapper/cachedev_0` | 448 GB | **304 GB**（33%）|

共享文件夹归属 —— **两个卷都长成 `/volumeN/...`，光看路径看不出它在哪块盘上**：

| 卷 | 共享文件夹 |
|---|---|
| `cachedev_1`（56 T，**满**）| `video`、`Download`、`homes`、`docker`、`drive`、`web`、`web_packages`、`ActiveBackupforBusiness` |
| `cachedev_0`（448 G，**304 G 可用**）| `docker_ssd`、`qb_temp`、`技术文档`、`000 临时文件夹nas（内容可删除）` |

> ⚠ **`/volume1` 的"可用"读数不是常量**。不同时刻的记录：SUMMARY §9 记 **0**、README 多处记 **~19 / ~20 GB**、2026-09-13 实测 **31.91 → 31.76 GiB**（连量 4 次还在动）。
> ⇒ 结论是「**接近 0、且在数 GiB 级浮动**」，**别把任何一个单次读数当成恒定值**，也别拿它去算减法。
>
> ⚠ **另一处文档自相矛盾**（同一份 README 里）：每日摘要的时间，任务表写 **21:00**、排错段写 **21:20**。
> 实测以 NAS 上 `sudo /usr/syno/bin/synoschedtask --get` 为准。**这正是本项目"同一事实只在一处维护"那条规矩被破的例子**，见到别惊讶。

### 当前配置开关（`.env`）

| 键 | 值 | 含义 |
|---|---|---|
| `LINK_TYPE` | **`reflink`** | 2026-09-13 起；★ 与 `MATCH_MODE` **互锁**（见 `ERR-FS-04`）|
| `MATCH_MODE` | `partial` | 只在 `LINK_TYPE=reflink` 时才作数 |
| `SKIP_RECHECK` | `false` | ★ 它**不是**安全网 —— 见 A.9 |
| `DATA_DIRS` | **1 条**（农场那条）| 从 49 条收成 1 条，`FARM_SOURCES` 保留原始源清单 |
| `TORZNAB_URLS` | **4 条**：`/1` HDtime、`/2` HDFans、`/3` BTSCHOOL、`/4` NanyangPT | ★ 值里每条各带一个 `apikey=` |
| `QBIT_CATEGORY` | `reseed-singles` | |

### 调度时刻表

| 任务 | 频率 | 脚本 |
|---|---|---|
| 排空 spool | 每 **5 分钟** | `sh <compose>/notify/notify-spool.sh` |
| 每日摘要 | 每天 **21:00–21:20**（文档不一致，实测为准）| `… notify-spool.sh --digest` |
| 驱动跑批 | 每 **15 分钟** | `sh <compose>/drive-loop/run.sh` |
| IYUU 辅种 | 每天 **01:45** | ★ 别人的系统 |
| IYUU 另一条 | 每天 02:34 | 同上 |

- DSM 三个任务**用户都选 `root`**、**都不要勾「发送运行详情」**（勾了 = 一天 288 封骚扰邮件）。
- drive-loop 批间间隔：**45 min ~ 180 min**，按站点反馈动态调（`next_sleep()` 三档分级）。

---

---

# A 部分 · 前提信息（事前交代）

> 这一部分是**先告诉 AI 这些事实**，一整类错误就不会发生。
> 每节末尾的 ★ 是「**AI 最常忘的那一条**」。

## A.1 物理与网络拓扑

**两台机器**：一台 Synology DS423+（DSM 7.2）+ 一台 Windows 开发机。就这两台。
NAS 上还跑着**别人的**容器（IYUU Plus、另一套 qB、opencd），它们不在本项目管辖范围，但**会影响本项目**。

**可达性是有方向的、逐服务的** —— 不是「网络通不通」这种全局属性：

| 从 | 到 | 走什么 | 备注 |
|---|---|---|---|
| Windows | NAS 文件 | **只有 SMB / UNC**（`//iSunker-DS423/...`）| ★ 22 端口拒绝，**没有 SSH** |
| Windows | NAS 上的服务 | TCP 直连 `192.168.0.7:<端口>` | 9696 / 2468 / 3060 / 8191 / 8787 都已发布到宿主机 |
| NAS 宿主机（`drive-loop.py` 就跑在这）| 容器 | **用 LAN IP + 端口映射** | 宿主机**不解析容器名** |
| 容器 | 另一个容器 | **服务名**（`http://qbittorrent-reseed:3060`）| 走 `reseed-net` / `qbit-net` |
| 容器 | 宿主机 LAN IP | ✗ **timeout**（hairpin NAT）| 见 `ERR-NET-01` |

> ★ **AI 最常忘的一条**：**「可达性」是逐服务、有方向的** —— 同一个目标（qB :3060），
> 从 Windows 用 `192.168.0.7:3060`、从容器用 `qbittorrent-reseed:3060`、从 NAS 宿主机用 `192.168.0.7:3060`。
> **这三个写法在代码里同时存在，两边是故意不同的**，别互相"纠正"（`docker-compose.yml` 顶部有原话）。

## A.2 权限与操作边界

**谁能做什么**：Windows 侧只能**读**、只能写 SMB 可达的路径；**NAS 上的写操作必须人来做**（没有 shell）。

**明令禁止的动作**（违反会造成不可逆损失）：

- ❌ 对 NAS 的 **UNC 路径跑 `rm`**（SMB 上没有回收站，glob 打错一次不可逆）
- ❌ `docker system prune -a`
- ❌ 对链接文件 `chown -R`（硬链接与源是**同一个 inode**，会改掉**源文件**属主）
- ❌ 走 **DSM File Station 删除**（只是挪进 `#recycle`，文件仍在盘上）
- ❌ 别在**批次运行中** `deploy.sh --apply`
- ❌ 别用 **DSM File Station** 做删除；别往 `/volume1` 写真实文件
- ❌ 别对大包根打 webhook（`ERR-HTTP-04`）
- ❌ 别 `cp` 生产 SQLite 库来做诊断（丢 WAL，`ERR-SQL-01`）

**要在"批次间隙"才能做的事**：`deploy.sh --apply`（覆盖正被 `sh` 读的脚本）、`--force-recreate`。

**一次只能一条的动作**：qB 的 `setLocation`（触发重校验，而 `max_active_checking_torrents=1`）。

> ★ **AI 的边界**：**只读随便做；写操作一律走「给命令、人执行」。**
> 这不是保守 —— 是环境**没有 SSH**，本机根本不能在 NAS 上执行命令。
> 判活、判存在、判容量，全部要能在 SMB 或 HTTP 端口上做完。

## A.3 存储布局与容量水位

**各卷分工**：`/volume1`（机械盘 56 T，媒体 + 源 + 农场 + linkDir，**已满**）、
`/volume2/docker_ssd`（SSD，compose + 配置 + 库）。★ 归属表见上面「关键取值速查」，别靠路径猜。

**硬链接约束**：源与 `LINK_DIR` **必须同一物理卷** ⇒ 这决定了几乎所有路径决策
（`build-farm.sh` 用 `stat -c %d` 比设备号，比的正是这件事）。
`/volume1/video` 与 `/volume1/docker_ssd` 是**两个卷**，所以 `LINK_DIR` 不能挪到 SSD。

**水位与行为**：`/volume1` 接近满 ⇒ **ENOSPC 会级联** —— 波及 cross-seed 注入、IYUU 辅种、
甚至**日志**（实测：卷剩 0 字节时 `say` 的 stdout 坏掉，`set -e` 带走整趟，见 `ERR-SH-06`）。

**「看着在但不在共享文件夹里」**：`/volume1/docker` **不是**共享文件夹，SMB 里看不到。

**农场为什么能在满卷上跑**：农场与单片全是**硬链接，不占数据块**。
★ 反过来说：**机械盘被大量占用 = 一定有人在"拷贝"而不是"链接"** —— 2026-09-13 那批
48.20 GiB 旧根残留就是这么暴露的。

> ★ **AI 最常忘的一条**：**「同一物理卷」不等于「同一路径前缀」。**
> 判断硬链接可行性要看**设备号**，不是看字符串开头像不像。

## A.4 服务清单与资源共享

| 服务 | 谁写的 | 跑在哪 | 读什么 / 写什么 |
|---|---|---|---|
| Prowlarr | 别人 | 容器 `reseed-prowlarr` | 存**所有 PT 站的 cookie/passkey** ⇒ 它的 API key 等于总钥匙 |
| cross-seed | 别人 | 容器 `reseed-cross-seed` | 按内容匹配 → 建链接 → 注入 :3060 |
| FlareSolverr | 别人 | 容器 | ★ 装了、在跑，但**一个 tag 都没挂 ⇒ 实质空转** |
| reseed-orchestrator | **我们** | **一次性 CLI**（无 daemon）| 预检/驱动/汇报 |
| qBittorrent-reseed | 别人 | **独立栈** | 单种做种（分类 `reseed-singles`）|
| IYUU Plus | 别人 | 容器 `iyuuplus_ssd` | ★ **也在往我们同一个 qB 辅种** |
| drive-loop / notify | **我们** | **NAS 宿主机**（不是容器）| DSM 计划任务拉起 |

> ★ **AI 最常忘的一条**：**「这个资源只有我们在用」是一个需要验证的假设 —— 本项目栽过。**
> **IYUU 和 cross-seed 共用同一个 qB（:3060）和同一个 `reseed_singles/` 目录**，
> 这是本项目最容易被忽略的共享。qB 里的种子**不全是我们的**（有 `IYUU自动辅种` 这个 tag）。
> ★ 但已查清：IYUU 抓的是**网页**（`details.php`/`download.php`），**不是 Torznab**
> ⇒ 两条通道不同，额度台账的分母**没有被污染**（SUMMARY §16.6）。
> ★ **差点搞错的就是这一步** —— 当初**先入为主**地断定"IYUU 的 16 站查询会挤占 Torznab 额度"，
> 并据此准备改配置；溯源（看它实际请求的 URL）才发现是 HTML 通道。
> **弯路 #19**，形状见 `B.10` 第 12 条。★ 这条同样适用 `A.8` 的半衰期：
> 前提是**站点把「API 查询额度」和「网页浏览」分开计** —— 站点规则页一变，结论要重验。

## A.5 数据资产与敏感度分级

**哪些文件含凭据**（只列位置，**不含值**）：

| 位置 | 里面是什么 |
|---|---|
| `.env`（生产）| Prowlarr 应用级 key、qB 密码、`TORZNAB_URLS` 里每条的 `apikey=` |
| `.env.bak.*` / `.env.bak` / `.env_bak` / `.env.backup` | ★ **与 `.env` 同等敏感** —— 是完整明文副本 |
| `prowlarr/` | 所有 PT 站的 cookie / passkey + db |
| `cross-seed/`（含自建 db）| db 的 `indexer` 表 `url` 列**含 apikey** |
| `notify/notify.conf` | 收件人邮箱（不含密码 —— 密码只在 DSM 里）|
| IYUU 的 `cn_client` / `cn_sites` | 别的系统的凭据 |

**为什么值钱**：Prowlarr 应用级 key 能**完全控制 Prowlarr**，而 Prowlarr 存着所有 PT 站的 cookie。

**哪些目录是"垃圾场"**：`#recycle`（File Station 删的进这里，**不是真删**）、`_cleanup-*`。

> ★ **AI 最常忘的一条**：**「配置文件表」要在读之前就假定含密钥 —— 按「值长什么样」脱敏，不按「键名叫什么」。**
> 按键名匹配会漏掉嵌在结构里的凭据：`TORZNAB_URLS` 里逗号分隔的多条 URL 各带一个 `apikey=`、
> Prowlarr 的 `indexer.fields` 里塞着 cookie/passkey、`options`/`fields` 里还嵌一层。

**本项目自我施加的额外纪律**（比"别泄密"更严，是为了**不制造新的泄露面**）：

- 绝不打印 qB 种子的 `tracker` 或 `content_path`
- 绝不原样导出 IYUU 配置表
- 绝不打印 Prowlarr 的 `indexer.fields`
- `cross-seed.db` 的 `indexer` 表 `url` 列含 apikey —— **只选需要的列**
- `--prowlarr-api-key` **不许出现在命令行上**（会留在 shell 历史与进程列表里），只能从 `.env` 读
- 需要回显 `TORZNAB_URLS` 核对时先脱敏：
  `grep '^TORZNAB_URLS=' .env | sed -E 's/(apikey=)[^,&]+/\1<redacted>/g'`
- 推前必跑 `python scripts/scan-secrets.py`（0 才能推）

## A.6 人的操作界面与响应节律

- **命令怎么执行**：用户从 Windows 粘贴到 **DSM 脚本框** 或 **Git Bash**。
  ★ Git Bash 是 **MINGW64**，**括号粘贴模式（`^[[200~`）会破坏多行粘贴** ⇒ **给 NAS 的命令一律写单行、一次一条**。
  ★ **多行 `\` 续行会被 DSM 脚本框折叠**。
- **能响应多快**：单人操作、**无常驻在线**；**批次 25 分钟起**（50 部 × 30s + 收尾 90s）。
  设计**不能要求"5 分钟内响应"** —— 需要人盯 5 分钟的事，实际不会被执行。
- **输出往哪放**：DSM 脚本框的输出要**重定向才留痕**，默认不保留。
  ⚠ 但**别重定向到 `/volume1`**（满卷）。要看输出就看 `drive-loop.log` / `notify/log/` / `attempts.log`。
- **喜欢什么形式**：**可复制粘贴的单行命令**，不要"你编辑一下这个文件"。

> ★ **AI 最常忘的一条**：**设计方案时要考虑"人的响应延迟"。**
> 需要人盯着 5 分钟的事，实际不会被执行 —— 所以本项目所有自动化都挂在 NAS 的 cron 上，而不是"提醒人去点一下"。

## A.7 时间节律与调度窗口

| 什么 | 什么时候 |
|---|---|
| cross-seed 重扫 | ★ **没有自动重扫** —— 全靠 drive-loop 驱动 |
| drive-loop 唤醒 | 每 **15 分钟**；批间 45~180 分钟 |
| IYUU 辅种 | 每天 **01:45**（另一条任务 02:34）|
| 每日摘要 | 当天首批（约 `00:0x`）出日报；发信 21:00–21:20 |
| 通知排空 | 每 5 分钟 |

**哪些动作有"下次最早"**：HDtime 退避到 **2026-09-14 11:32**；qB 校验队列单线程。

**时区**：所有 cron 是**本地时间**；库里的 epoch 是 **UTC**，读要 **+8h**。

**写操作窗口**：批次间隙 —— 看 `<compose>/drive-loop/scripts/.drive-loop.state` 里的 `running_pid`。

> ★ **AI 最常忘的一条**：**"现在能做"依赖时间窗** —— 问题不是"这条命令对不对"，而是"**现在跑它会不会撞上什么**"。

## A.8 外部依赖及其脾气

**有哪些**：4 个 PT 站（**HDtime / HDFans / BTSCHOOL / NanyangPT**）+ Prowlarr 代理。

| 站 | 脾气 |
|---|---|
| HDtime | 老 502 / 退避；★ 2026-09-12 才进 `TORZNAB_URLS` |
| HDFans | 稳，但命中率一般 |
| BTSCHOOL | 曾卡 CF，**现在好了**（命中率 23/46 = 50%，四站最高）|
| NanyangPT | ★ 片库不合口味：**4/452 = 0.9%** |
| Prowlarr | ★ 会**伪造 429**；它的 **Query Limit 是本地保险丝**，不是站点真实余额 |

**哪些读数是"代理"**（不能互相替代）：`Found` 行数、`matched_indexers`、`timestamp` 行数。

> ★ **AI 最常忘的一条**：**外部服务的"状态"有半衰期。**
> 两天前的故障描述，今天可能已经恢复 —— BTSCHOOL 那次就白白"待替换"了很久，
> 最后发现是**「当初的判据失效了」，不是「换好了」**。引用旧状态前先回读一次。

## A.9 历史失效模式与已知未归因

**坏过什么、怎么坏的**（括号里是**归因条目**，不是又一次描述）：

- 农场路径认不出 → `SEEDING` 塌方（13 → 21 → **201**，真根因是 `pack.roots` 没跟上农场切换）
  → `ERR-SVC-10` 边界
- 正则吃不下带空格的站名（`NanyangPT (南洋)`）→ `matched_indexers` 整列瞎掉 → `ERR-ENC-06`
- 检查间隔 > 退避窗口 → 退避检测**从来没生效过** → `ERR-SVC-12`（弯路 #10）
- 部署覆盖正在跑的脚本 → 假的 `exit=127` → `ERR-SH-01`（弯路 #12）
- partial 匹配 × 硬链接 → **qB 就地重下写穿源文件**（本项目最贵的一课）→ `ERR-FS-04`
- 邮件风暴：同一封告警重发约 6 次 → `ERR-SH-06` + `ERR-SCHED-02`

**已知但未归因的**：**无**（那个 `120` 已归因给 CPython `Py_FinalizeEx()`）。

**已知但已接受的**（设计，不是债）：杂物计数不为 0（`__pycache__` 会自己长回来）、
`mbf` 0 匹配、`scan-secrets.py` 的已知盲区。

> ★ **AI 最常忘的一条**：**「已知已接受」和「已知未修」要分开** —— 前者是设计，后者是债。
> 混在一起会让"设计如此"被当成"还没做"，或者反过来，让真债永远躺在"这是接受的"那一栏里。

## A.10 AI 自身的协作模式

| 能做 | 不能做 |
|---|---|
| 读文件、读 NAS（只读）、写本地代码、写文档、跑本地测试、**生成命令** | 直接操作 NAS、直接发 webhook、碰生产库、做 qB 写操作 |

- **怎么确认 AI 真的做了**：**回读实际状态**，不信调用的返回值。
  回读失败要区分「**没跑到**」和「**跑了没事**」（对照 `ERR-AI` 那一族）。
- **AI 的产出怎么验**：查日志、查数据库、查 NAS 上的文件 —— **不看终端输出**。

> ★ **AI 最常忘的一条**：**「报告了」≠「做了」。**
> 本项目反复栽在"看着像做了"上 —— 典型形状有四种（见 B.10 元层清单）：
> ① `n/a` 被念成"没事"　② 首读基线被念成"新增了"　③ 只看日报发现不了（错在给人查证的那份清单）
> 　④ 退出码 0 但断言数根本没进账。

## A.11 已有的约定（成文与不成文）

- **文档分工**：README = **INFO 层**（怎么做）、SUMMARY = **DEBUG 层**（为什么）—— **同一事实只在一处维护**
- **唯一出处原则**：跨层用「详见 SUMMARY §N」单向指路，不重复登记
- **判据要带"另一侧"**：任何计数/判据的期望值要**能指回一条判据之外的真实记录**
- **不静默跳过**：跳过要连理由一起列出来
- **测试自己也是判据**：报红时先怀疑断言本身
- **只报变化，不报现状**：首读只记基线不响；与基线一致不许再喊；缩回要写出来（但别写"与基线一致"——那句是假话）
- **文档里的编号引用会用符号**（`§19.1.2`、函数名），不用行号 —— 行号会漂

> ★ **AI 最常忘的一条**：**这些约定本身就是"前提信息"** —— 新会话不告诉 AI，它会**另立一套**。

## A.12 项目元信息

- **规模**：单人项目、**无 CI、无 PR、不打分支**；git **直推 main**。
- **会话任务列表是临时的**：换会话就没了 —— **它不是项目的待办真源**。
  ★ 但对**当前这一局**而言它是最新的：**列当前棋盘必须从 TaskList 重读，别凭上一轮的记忆抄**（已因此错过三次）。
- **文档的"活"与"死"**：README / SUMMARY / ENVIRONMENT 是**活的**；
  会话任务列表、`D:\tmp` 下的脚本、`*.tsv` / `todo-paths.txt` 是**死的**（已多次被清）。
- **哪些脚本在哪跑** —— ★ **三条路径，别混**：

  | 路径 | 谁 | 改了怎么生效 |
  |---|---|---|
  | **构建上下文** `<compose>/orchestrator/` | 进 `reseed-orchestrator` **镜像** | 要 `docker compose build reseed-orchestrator`（★ `--force-recreate` 不带 `--build` 只拿旧镜像重启，等于什么也没做）|
  | **NAS 宿主机** `<compose>/drive-loop/**` | DSM 计划任务直接跑 | **拷完即生效**，下次唤醒就用新的，**不用动容器** |
  | **容器内** | `cross-seed` 用官方镜像，**完全不吃我们的文件** | 只能改 `config.js`（挂载进去的）|

- `deploy.sh` 白名单（**唯一**从 Windows 发起的写操作）当前 **28 个文件**，见 `deploy.sh` 的 `FILES` 数组 ——
  ★ **那是唯一一份拷贝**，漂移哨兵从它解析受管集合，别复制第二份。
- **只在 Windows 跑、不进 NAS 也不进容器**（LOCAL_ONLY）：`scan-secrets.py`、`check-deploy-drift.py`、
  `audit-found-*.py`、`check-indexer-timestamps.py`、`migrate-reseed-dirs.py`、`tests/`、`run-batch.sh`、`gen-*.py`。
- 本地 `.env` 是**开发存根**（`TORZNAB_URLS` 就是 `.env.example` 里那两个占位符），
  **真实凭据只存在于 NAS 的生产 `.env` 与 Prowlarr UI 里**。

> ★ **AI 最常忘的一条**：**"这个文件在哪跑"决定了"改了要不要 deploy"** —— 拷错一层，改了半天下次唤醒还是旧的。

---

---

# B 部分 · 环境错误条目（事后归因）

> **统一的七格 schema**：**症状** / **环境前提** / **根因** / **触发条件** / **规避做法** / **怎么发现的（证据）** / **边界**。
> 「**环境前提**是灵魂」—— 漏了它，条目的适用范围会被误读成"所有环境"。
> 「**边界**与症状同等重要」—— 知道什么情况下**不**适用，比知道什么情况下适用更值钱。
> ★ 同一条错误可能在多层出现（如 CRLF 既是 OS 层也是 shell 层），**两层都登记、互相指路，不合并** ——
> 合并会丢掉"从哪一层看"的信息。
> ★ 节末的 **◆ 未实例** 列出的，是大纲里有、但本项目**没有实例或未验证**的条目 —— **不编**。

---

## B.1 操作系统层

### B.1.1 Windows 侧

### ERR-OS-01 `.cmd` 的 LF 行尾让 cmd.exe 从行中间开始执行
- **症状**：`attempts.log` 里出现 `exit=9009` + `'""' 不是内部或外部命令`，且**有 `exit=` 没有 `start=`**；批次静默消失。
- **环境前提**：Windows 计划任务 → 一个 `.cmd` 包装器 → 里面再调 python。**路径里有中文用户名**。
- **根因**：`cmd.exe` **按字节块**读批处理文件。LF-only 会让它从**某行中间**开始执行 ——
  `REM` 注释里的英文单词被当成命令跑，前面的 `set` 全部丢失。
- **触发条件**：任何 `.cmd`/`.bat` 出现 LF 行尾（典型来源：跨平台编辑器保存、Git 的 `core.autocrlf`）。
- **规避做法**：包装器**必须 CRLF**；`.sh` / `.py` **必须 LF**。
  改完务必确认：`python -c "b=open('x.cmd','rb').read(); print('CRLF',b.count(b'\r\n'),'LF',b.count(b'\n')-b.count(b'\r\n'))"`。
  同时**别把包装器换成直接指向 `python.exe`**（路径里的中文用户名经 `schtasks` / MINGW 传递会被搞坏），
  要用 `%USERPROFILE%` 让 `cmd.exe` 在**运行时**展开，任务定义本身保持纯 ASCII。
- **怎么发现的**：2026-09-11 深夜改包装器时引入的，故障现象与"没跑"一模一样；见 README 1327–1351、SUMMARY §14。
- **边界**：**只对 Windows 侧的 `.cmd`/`.bat` 成立**。`.sh` 在 Git Bash 与 DSM BusyBox 下**必须 LF** —— 同一条"行尾"知识，两个方向相反，别套错。
  弯路 #2（当初的形态：`.cmd` 包装器 LF ⇒ 丢 `set` 变量 + `exit=9009`）。

### ERR-OS-02 `StopOnIdleEnd` 一动键鼠就把整批 `TerminateProcess`
- **症状**：批次跑一半静默消失，**没有 traceback、没有 finally 收尾**，状态文件里 `running_pid` 永远挂着，之后每轮都空转。
- **环境前提**：Windows 计划任务 + 一台**被人在用**的桌面机。
- **根因**：任务定义里 `<StopOnIdleEnd>true` —— 系统一旦检测到"用户不再空闲"，就直接杀整个任务实例。
- **触发条件**：任务运行期间碰鼠标/键盘。
- **规避做法**：**已全量迁到 NAS**（DSM 计划任务）。若将来要回退到 Windows，**必须显式关掉这个字段**。
- **怎么发现的**：当场读任务定义确认；见 README 1324–1326、SUMMARY §14。
- **边界**：桌面机特有；服务器上无此问题。这条是"**环境选型错误**"，不是代码 bug —— 弯路 #1。

### ERR-OS-03 `os.kill(pid, 0)` 在 Windows 上是"探测即击杀"
- **症状**：只想判进程活着，结果把它杀了。
- **环境前提**：同一段 Python 代码要同时在 POSIX（NAS）和 Windows 上跑。
- **根因**：`os.kill(pid, 0)` 在 POSIX 语义是"发 0 号信号 = 只探测"，
  在 Windows 上 **`os.kill` 直接映射到 `TerminateProcess`**。
- **触发条件**：跨平台复用同一段判活代码。
- **规避做法**：Windows 分支改走 `tasklist`。★ 回退时要**连 `import subprocess` 一起打开**，
  否则分支会 NameError（那段被注释而非删除，见 README 1352–1354）。
- **怎么发现的**：迁移时踩到；弯路 #3。
- **边界**：**只在"同一段代码跨平台"时成立**。纯 POSIX 环境这条永远不会咬人 —— 这正是它危险的地方。

### ERR-OS-04 ★ 控制台代码页 GBK 编不出 `⑪`，脚本会**在打印到那一节时中断**
- **症状**：脚本跑到某一行**突然中断**，而**已经过的断言看着全是 `ok`** —— 极易误判成"代码坏了"。
- **环境前提**：Windows 控制台 / 管道的默认编码是 **GBK（cp936）**，`sys.stdout` 用的是它。
- **根因**：`①`..`⑩`（U+2460–U+2469）在 GBK 里有，**`⑪`（U+246A）不在**。
  `print` 到它时 `UnicodeEncodeError`，脚本当场停。
- **触发条件**：源码里出现 `⑪` 及以后的圈码；或任何 GBK 表外的字符。
- **规避做法**：每个脚本开头**强制** `sys.stdout.reconfigure(encoding="utf-8")`；
  **加新的节编号时不要越过 `⑩`**。
- **怎么发现的**：写测试时踩到，`tests/README.md` 有原话（59–62 行）。
- **边界**：只在 **stdout 是非 UTF-8**（GBK 控制台 / 管道 / 重定向到文件）时触发；
  在 UTF-8 环境里完全正常 —— 所以它**在本机报、在别人那儿不报**。
  ★ 同一现象也在 **编码层**登记（`ERR-ENC-01`），**两处都留**。

### ERR-OS-05 反斜杠 UNC 在这个 Git Bash → Python 环境里失败
- **症状**：`\\iSunker-DS423\...` 传给 Python 后路径不认 / 报错。
- **环境前提**：Windows + Git Bash（MINGW64）+ Python。**多层各有自己的转义规则**。
- **根因**：反斜杠要经过 bash 引号 → Python 字面量 → Win32 API 三层转义，任何一层没对上就废。
- **规避做法**：**一律用正斜杠** `//iSunker-DS423/...`。Python 与 Win32 都接受正斜杠 UNC。
- **怎么发现的**：多次踩到后固化成规矩。
- **边界**：**只在这个 shell → Python 的栈上成立**。在 `cmd.exe` / PowerShell 里反斜杠是正常的。
  ★ 与 `ERR-CMD-01`（MINGW 路径改写）是**两个相反方向**的坑，别混。

### ERR-OS-06 MINGW 会把命令行里的 POSIX 路径参数改写成 Windows 路径
- **症状**：`--notify-spool /tmp/x` 实际落到 `D:\tmp\x`；`curl` 带路径参数**全部 400**。
- **环境前提**：Git Bash（MSYS/MINGW）调用**原生 Windows 程序**（python.exe、curl.exe）。
- **根因**：MSYS 认为你在传 Unix 路径，**自动做前缀翻译**；但接收方是原生 Windows 程序，它拿到的是被改写过的串。
- **触发条件**：参数值以 `/` 开头、且看起来像路径。
- **规避做法**：用 `D:/tmp/x` 或 UNC；curl 的路径参数**走 stdin** 绕开；
  必要时 `MSYS_NO_PATHCONV=1`（★ 有副作用：**所有**参数都不再翻译）。
- **怎么发现的**：`run-batch.sh` 首次跑出「全 400」；SUMMARY §6.3。
- **边界**：只在 **Git Bash → 原生 Windows 程序**这一跳上成立。**在 Linux / NAS 上不存在这个问题** ——
  所以本地测过的命令直接抄到 NAS 上通常没事，反过来会。

### ERR-OS-07 判活别用 `ps | grep`（见 `ERR-DSM-01`）
> 交叉引用：这条本体在 DSM 侧（BusyBox 的 `ps` 只显示 `comm`），登记在 `ERR-DSM-01`，**不在此重复**。

**◆ 本小节未实例 / 未验证**（大纲里有、本项目没踩到，**不编**）：
文件名里的其他特殊字符（除中文用户名与空格已实例）、
防病毒/安全钩子对 `rm`/`del` 的干预（本机未见）。

### B.1.2 Synology DSM 侧

### ERR-DSM-01 BusyBox 的 `ps` **只显示 comm**，看不到命令行参数
- **症状**：`ps | grep drive-loop.py` **怎么 grep 都是空的**，于是误判"任务没在跑"。
- **环境前提**：DSM 用的是 **BusyBox** 的 `ps`（精简版），不是 procps 那个。
- **根因**：BusyBox `ps` 默认只打 `comm`（进程名），对 python 就是 `python3` —— **命令行参数根本不在输出里**。
- **触发条件**：任何"用 ps 找带参数的进程"的写法。
- **规避做法**：用 **`pgrep -f drive-loop.py`**；或看
  `<compose>/drive-loop/scripts/.drive-loop.state` 里的 `heartbeat_ts` 在不在涨（批次运行期间每 60s 刷一次）。
- **怎么发现的**：排查"批次到底在不在跑"时踩到；README 1031–1034。
- **边界**：**DSM 专有**。在普通 Linux（procps）上 `ps aux | grep` 是好的 —— 别把这条当成通用规则。

### ERR-DSM-02 DSM 7 **没有 `nc`**、**没有 `jq`**
- **症状**：抄来的"测端口 `nc -vz`"和"解析 JSON `jq`"两条命令**当场 command not found**。
- **环境前提**：DSM 7 的最小化用户态。
- **根因**：这些工具不在 DSM 自带集合里。
- **规避做法**：测端口用 `netstat`；解析 JSON 用 `grep -o` + `cut`（或在 Windows 侧用 Python 解）。
- **怎么发现的**：SUMMARY §9 原话。
- **边界**：DSM 7 特指。装 Entware 后会变，但**本项目没装**。

### ERR-DSM-03 共享文件夹**看不出**在哪块物理盘上
- **症状**：以为 `/volume1/docker` 和 `/volume1/video` 是一回事（都 `/volume1/`），实际归属要靠 `ContainerManager/all_shares` 读。
- **环境前提**：DSM 上 `video` 与 `docker_ssd` 这类共享文件夹**分属两个不同的卷**，路径前缀却都是 `/volumeN/...`。
- **根因**：共享文件夹 → 卷的映射是 DSM 的元数据，**不体现在路径字符串里**。
- **触发条件**：任何"按路径前缀判断是不是同一块盘"的推理。
- **规避做法**：读 `ContainerManager/all_shares`；硬链接判据一律用 `stat -c %d` 比设备号。
- **怎么发现的**：2026-09-13 复测补上"卷归属表"那一格（SUMMARY §9 原话：当时没记，只写了两个挂载点）。
- **边界**：DSM 专有。★ 与 `A.3` 的 ★ 是同一条知识的两种表述（一个在前提层、一个在错误层），**都留**。

### ERR-DSM-04 `/etc/ssmtp/ssmtp.conf` 是 **0 字节空壳**，真路径是 `/usr/syno/etc/synosmtp.conf`
- **症状**：探测到 `ssmtp.conf` 为 0 字节 ⇒ 误判"邮件没配" ⇒ 白干半天。
- **环境前提**：DSM **改过** ssmtp 的配置路径。
- **根因**：DSM 实际读 `/usr/syno/etc/synosmtp.conf`；`/etc/ssmtp/ssmtp.conf` 是个空壳，
  而且**邮件天天正常送达时它照样是 0 字节**。
- **触发条件**：拿"配置文件为空"当"功能没配"。
- **规避做法**：判邮件通不通**只有一条路：真发一封**（`sh notify-spool.sh --test-mail`）。
  另外别想"读 DSM 配置自己发信" —— 那个文件里的密码是**加密**存的（`eventpasscrypted`）。
- **怎么发现的**：先误判、后实测；SUMMARY §15。
- **边界**：**DSM 专有**（群晖移植 ssmtp 时改的路径）。弯路 #13。

### ERR-DSM-05 ★ `synoschedtask` 的**小时位存错** ⇒ "手动能跑、计划永远不跑"
- **症状**：在 DSM 里点「运行」正常跑完，**计划时间到了却从不触发**。
- **环境前提**：DSM 任务计划的「最后运行时间」字段，GUI 与 CLI 的**存储语义不一致**。
- **根因**：实测存成了 `Run time: [2]:[0] Repeat every [15] min until [2]:[45]`
  ⇒ 这条任务**只在 02:00–02:45 之间**才触发。
- **触发条件**：在 GUI 里选"重复"时没注意小时位。
- **规避做法**：**小时位明确设成 `23`**。改完**务必复核**：
  ```bash
  sudo /usr/syno/bin/synoschedtask --get | grep -A 12 'reseed-drive-loop'
  # 期望看到： Repeat every [15] min (s) until [23]:[45]
  ```
- **怎么发现的**：`synoschedtask --get` 读出来的实测值；README 1368–1387。
- **边界**：**DSM 专有**。★ 它属于「配置层错误」，但形状和代码 bug 一样（"看着正常"），所以必须列进来。
  **弯路 #14** —— 当初的形态正是"小时位填成 `[2]:[0]`，任务全天大部分时段不触发"。
  ★ 教训不在"填错了"，而在**没有自检机制**：GUI 看着是对的、手动能跑，
  唯一的判据是回读 `synoschedtask --get`。
- **判据**：`attempts.log` 里**压根没有 `start` 行** = 任务没被触发（没启用 / **计划窗口不对** / 路径错）。
  见 `ERR-SCHED-01` / `ERR-SCHED-05`。

### ERR-DSM-06 DSM 任务计划脚本框：多行 `\` 会被折叠；Git Bash 的**括号粘贴模式**破坏多行粘贴
- **症状**：粘一串多行命令进 DSM，只有第一行生效 / 报奇怪的语法错；或粘进 Git Bash 后前面多出 `^[[200~`。
- **环境前提**：给 NAS 的命令**只能靠人粘贴**（没有 SSH）。
- **根因**：① DSM 脚本框会**折叠 `\` 续行**；② MINGW 的 bracket paste mode 会在粘贴内容外面包一层控制序列。
- **触发条件**：任何多行粘贴。
- **规避做法**：**一律写成单行、一次一条**。逻辑超过一屏就**写成脚本文件走 SMB 拷过去**，不要靠粘贴。
- **怎么发现的**：SUMMARY §9 原话。
- **边界**：只在这套"人肉粘贴"的链路上成立。★ 相关：`B.10` 元层第 2 条（代码块里只放能直接粘的东西）。

### ERR-DSM-07 `#recycle` 的语义：**删除 ≠ 真删**；File Station 删除只是挪进回收站
- **症状**：以为删干净了，**空间一分没回来**，凭据副本还在盘上。
- **环境前提**：DSM 的共享文件夹默认启用**回收站** `#recycle`。
- **根因**：`rm` 之外的删除途径（File Station、DSM GUI）是**移动**到 `#recycle`。
- **触发条件**：走 File Station / GUI 删除。
- **规避做法**：**别走 File Station**。用 `rm-staging.sh`（`sh <compose>/rm-staging.sh <目标> --apply`），
  或用户在 NAS 原生路径上 `rm -rf`。★ 删完**回读读数**确认空间真的回来了。
- **怎么发现的**：2026-09-13 实测：`docker_ssd/#recycle/env-bak-20260912/` 里躺着 **8 个 `.env.bak*`**
  （其中 4 个与生产 `.env` 同档大小 7.0–7.3 KB），**全是之前几次"File Station 删除"留下的**。
- **边界**：DSM 专有。★ **但"删了没释放空间"还有第二个原因**（BTRFS COW），见 `ERR-FS-04` ——
  **两条成因不同、表现一样**，都要能想到。

### ERR-DSM-08 DSM 自带的 python3 是 **3.8.15**，且是整条链路的**单点依赖**
- **症状**：见 `ERR-PY-01`（静默崩溃）。以及：DS 升级后可能换版本，症状同样会是"静默"。
- **环境前提**：`drive-loop.py` / `notify.py` 跑在 **NAS 宿主机**上，用 DSM 自带的 `/usr/bin/python3`。
- **根因**：DSM 自带版本老（3.8.15），Package Center 里**没有更新的**。
- **触发条件**：用了 3.8 不支持的语法 / 依赖更高版本的库。
- **规避做法**：文件顶部**必须有** `from __future__ import annotations`；
  `run.sh` 在 shell 这一层另记一份 `attempts.log`（用来区分"python 没起来"与"python 崩了"）。
  后备方案：把 `run.sh` 的 `PY=` 指到一次性容器（`docker run --rm … python:3.12-slim`），或装 Entware 的 python3。
- **怎么发现的**：`attempts.log` 记到 `start py=/usr/bin/python3` + `exit=0`，
  旁边生出 `__pycache__/*.cpython-38.pyc` ⇒ 版本由此**坐实**（不是猜的）。
- **边界**：**这台 NAS 上成立**。别把它当成"python 3.8 通用问题" —— 关键前提是"跑在 NAS 宿主机、用 DSM 的解释器"。

### ERR-DSM-09 DSM 的 SSH 默认关闭 ⇒ **没有远程 shell**
- **症状**：任何"SSH 上去跑一下"的方案直接作废。
- **环境前提**：`22` 端口拒绝连接。
- **根因**：DSM 默认不开 SSH（安全设置）。
- **触发条件**：任何"你 ssh 上 NAS 执行 X"的思路。
- **规避做法**：**只读走 SMB / HTTP 端口；写操作给命令让人在 DSM 里执行**（或用 `deploy.sh`）。
- **怎么发现的**：SUMMARY §9、README `docker-compose.yml` 注释。
- **边界**：**本机设置**。★ 这条是整个协作模式的**前提** —— 见 `A.2` 的 ★。

**◆ 本小节未实例 / 未验证**：`/usr/syno/bin` 下哪些是 setuid、哪些依赖 i18n 资源（本项目未碰）。

### B.1.3 Linux 通用（与 DSM 的差异点）

### ERR-OS-08 交互式 shell 与非交互式 / `sh` 与 `bash` 的差异
- **症状**：登录 shell 里好好的脚本，被计划任务拉起后行为不同；`pipe` 里的 `while` 计数器传不出来。
- **环境前提**：DSM 计划任务用 `/bin/sh`（BusyBox 的 ash）拉脚本，**不是交互式 bash**。
- **根因**：`sh` 与 `bash` 语法/内建不完全一致；管道里的每一段跑在**子 shell** 里，变量改了也传不回父进程。
- **触发条件**：用 bash 特性写 `run.sh`；或在管道里累加计数。
- **规避做法**：面向 NAS 的脚本**按 `sh` 写**（POSIX 子集）；需要跨管道传值就用临时文件或进程替换。
- **怎么发现的**：`attempts.log` 与 `run.sh` 的设计（`run.sh` 是 `sh` 脚本）；README 顶部几处 `sh <路径>/…`。
- **边界**：★ **本项目只在"NAS 侧 `sh`"这条上实例化**。"管道子 shell"那条**本项目无实例**，此处只作提醒。

---

## B.2 Shell / 解释器层

### B.2.1 shell 脚本解析

### ERR-SH-01 ★ `sh` 是**边读边执行**的 —— 脚本运行中被覆盖会执行到错位的代码
- **症状**：`attempts.log` 里出现 `exit=127 (no python: /usr/bin/python3)`，**但同一批的 python 明明跑完了**。
- **环境前提**：`deploy.sh` 会覆盖 NAS 上**正在被 `sh` 读取**的 `run.sh`。
- **根因**：`sh` **不把整个脚本读进内存**，而是从文件描述符**按偏移继续读**。
  文件被覆盖（同 inode，内容变了）后，它会**从旧偏移读到新文件的字节** ⇒ 执行到一半的、错位的代码。
- **触发条件**：批次运行期间 `deploy.sh --apply`。
- **规避做法**：`deploy.sh` 已改成**原子替换**（写 `.new` 再 `mv`，**换 inode**，老进程继续读旧内容），
  并在检测到心跳新鲜时给出告警。**但它不会拦你** —— 那批会继续用旧脚本跑完。
  ⇒ 纪律：**别在批次运行中 `--apply`**；真要动就挑批次间隙（看 `.drive-loop.state` 的 `running_pid`）。
- **怎么发现的**：踩到后定位；README 215–218、SUMMARY §17.2。
- **边界**：**只对"边读边执行"的解释器 + 覆盖式更新**成立；`mv`（换 inode）就不会有这个问题 ——
  **这就是修法的依据**。弯路 #12（当初的形态：直接覆盖 `run.sh` ⇒ `exit=127`）。

### ERR-SH-02 `.env` 是 **CRLF**，锚定 `$` 的 `sed` 会**静默什么都不做**
- **症状**：`sed -i 's|^LINK_TYPE=hardlink$|LINK_TYPE=reflink|' .env` 跑完**毫无反应**，
  而且**不报错、退出码 0** —— 你以为改了，其实没改。
- **环境前提**：本项目的 `.env` **是 CRLF 行尾**（在 Windows 侧维护）。
- **根因**：`$` 锚定的是**行尾**，而 CRLF 的行尾在 `$` 之前还有一个 `\r` ⇒ 模式匹配不上。
- **触发条件**：任何 `sed 's|^KEY=old$|…|'` 的写法。
- **规避做法**：**去掉 `$` 锚定**：`sed -i 's|^LINK_TYPE=hardlink|LINK_TYPE=reflink|' .env`。
  或整体按 `awk` 就地替换（`nas-update-env.sh` 就是**故意不走 `sed`** 的，为的是避开分隔符与 `&` 转义坑）。
- **怎么发现的**：切换 reflink 时踩到；见 `local-env-is-a-stub` 一族的记录。
- **边界**：**只在 CRLF 文件上成立**。同一段 sed 在 LF 的 `.sh` 上完全正常 ——
  ★ 这正是"**同一条命令在两个文件上行为不同**"的典型，也是 `B.10` 元层要防的形状。

### ERR-SH-03 `$()` 只剥尾部的 `\n`，**不剥 `\r`**
- **症状**：`x=$(cat file)` 之后 `"$x"` 里带着不可见的 `\r`，比较/拼接时莫名不等。
- **环境前提**：CRLF 文件 + 命令替换。
- **根因**：命令替换的尾部去除规则是 POSIX 定义的 —— **只去 `\n`**。
- **触发条件**：把 CRLF 文本读进变量再精确比较。
- **规避做法**：读进来先 `tr -d '\r'`，或用 `awk`/参数展开剥掉。
- **怎么发现的**：与 `ERR-SH-02` 同族，`.env` 处理时暴露。
- **边界**：只在"源是 CRLF"时成立。

### ERR-SH-04 ★ `source` 会**覆盖环境变量** ⇒ `DST=/tmp/x bash deploy.sh` 打到了**生产**
- **症状**：想拿临时目录演练，**实际写进了生产 NAS**。
- **环境前提**：`deploy.sh` 会 `source scripts/.nasrc` 来提供默认值。
- **根因**：`.nasrc` **在**命令行赋值**之后**被 source ⇒ 它把 `DST` 覆盖回去了。
- **触发条件**：任何"用环境变量临时覆盖某个默认值"的写法（`X=… cmd`）。
- **规避做法**：**先把外部给的值存下来，`source` 完再恢复**：
  `_DST_FROM_ENV="${DST:-}"; source …; DST="${_DST_FROM_ENV:-${DST:-}}"`。
  ⇒ 规则：**命令行 > 环境变量 > `.nasrc` 默认值**。
- **怎么发现的**：当时正想做沙箱演练，差点打到生产；README 28–34、SUMMARY §17.4.1。
- **边界**：**这是 shell 通用规律**，任何"配置默认值靠 source"的脚本都有。
  ★ 危险点在于：**想做安全的事（演练）反而造成了危险的事（打生产）**。

### ERR-SH-05 环境变量必须写在命令**前面**
- **症状**：`sh build-farm.sh --verify VAR=val` —— `VAR` 被当成**脚本的参数**，环境变量没生效。
- **环境前提**：Git Bash / `sh` 的调用形式。
- **根因**：`VAR=val cmd` 是"给这条命令临时加环境变量"；写在后面就是普通参数。
- **触发条件**：抄命令时顺序放错。
- **规避做法**：**一律写在前面**：`COMPOSE_DIR="$D" FARM="…" sh build-farm.sh --verify`。
- **怎么发现的**：README 1200 行专门标了这句。
- **边界**：通用 shell 规律。

### ERR-SH-06 ★ `set -e` + **满卷把 stdout 弄坏** ⇒ 整趟被带走（邮件风暴的机械结构）
- **症状**：告警**发了**、**日志没记**、文件**还在 spool** ⇒ 下一趟**再发一遍**。
  实测：同一封「站点退避中：HDtime」重发了约 **6 次**。
- **环境前提**：`/volume1` 剩 0 字节；`notify-spool.sh` 用 `set -e`，且在
  `send_mail` **成功之后**、`log_event` / `mv` **之前**，有一句 `say "  [已发] $ti"` —— 它写 stdout。
- **根因**：stdout 写失败 ⇒ `say` 非零退出 ⇒ `set -e` **带走整趟** ⇒
  恰好卡在"信已发出"与"记账/归档"之间 ⇒ **状态机永久不一致**。
- **触发条件**：满卷导致 stdout 不可写（`sh -c 'exec 1>&-; exec sh …'` 可复现）。
- **规避做法**：**`say` 的失败不许带走整趟**（单独吞掉）；
  并且**重试必须有上界**：连败到 `MAX_SEND_TRIES`（默认 3）就 **归档 + 记一条 `[未确认]` 告警**，
  于是"有条告警你没收到"会进每日摘要 —— **既没被丢弃、也没被重发，而是变成了一条更响的告警**。
- **怎么发现的**：`tests/test_notify_drain.py`（28 条）**用老代码跑会精确红 8 条**，
  红的恰好就是"信发了、没记账、下轮再发"那几格；SUMMARY §21.7。
- **边界**：★ **触发条件至今没 100% 判明**（风暴窗口 12:45–13:10 那条任务每趟的退出码没取到）。
  但**两种候选触发条件的可观测行为与修法完全相同**，所以不阻塞。
  ★ 另：**"归档 = 丢弃"这个绝对律是错的** —— 错在漏掉了"重试没有上界"那一半。

### ERR-SH-07 `cp` vs `mv` 对运行中进程的影响（inode 换不换）
- **症状**：覆盖式更新正在被读取的东西 → 见 `ERR-SH-01`。
- **环境前提**：要更新一个**可能正在被读**的文件。
- **根因**：`cp` 保持 inode（原地改内容）；`mv` 换 inode（改名/换目录项）。
- **触发条件**：更新长跑进程正在读的脚本 / 库 / 证书。
- **规避做法**：**一律原子替换**（写 `.new` + `mv`），这是本项目 `deploy.sh` 的既定做法。
- **怎么发现的**：由 `ERR-SH-01` 反推出来的规矩。
- **边界**：通用。★ **`mv 文件 /dev/null`** 那条（不是丢弃）**本项目没有实例**，此处不展开。

**◆ 本小节未实例 / 未验证**：`mv 文件 /dev/null` 的真实语义。

### B.2.2 命令行为差异

### ERR-CMD-01 MSYS 版 `sed`/`awk` 会剥 `\r`，Linux 版**不会**
- **症状**：同一条 `sed` 在 Git Bash 里"能用"，搬到 NAS 上**不работа**（或反过来）。
- **环境前提**：两套工具链（MSYS vs BusyBox/GNU）处理文本的默认行为不同。
- **根因**：MSYS 的文本模式会做 CRLF 归一化；Linux 侧按字节原样处理。
- **触发条件**：跨平台复用同一条文本处理命令。
- **规避做法**：**别假设**。涉及 `\r` 的地方显式处理（`tr -d '\r'` / 显式匹配 `\r?$`），
  并在**目标平台上**验证一次。
- **怎么发现的**：由 `ERR-SH-02`（`.env` CRLF）与跨平台脚本的经历归纳。
- **边界**：只在**跨平台**时成立。★ 与 `ERR-OS-05`/`ERR-OS-06` 是同族的"MSYS 悄悄改字节"。

### ERR-CMD-02 ★ 在 56 TB 满盘上跑全树 `du` / `btrfs filesystem du` 是**分钟~小时级**
- **症状**：用户敲下去之后**当场卡住**，两条命令都卡。
- **环境前提**：`/volume1` = **56 TB、100% 满、碎片化的 BTRFS**，而且当时 cross-seed 与 qB **还在写**。
- **根因**：`btrfs filesystem du` 是**逐文件查 extent 表**（元数据随机读）；
  `du -shx /volume1/*` 是**全树遍历**。在这种盘上都是分钟到小时级，不是秒级。
- **触发条件**：对**整棵目录 / 通配整卷**跑量空间的命令。
- **规避做法**：**先估代价、并写在命令旁边**。优先顺序：
  ① **走 SMB 从本机递归量**（实测 173,080 个文件 / **128 秒**跑完 72 TiB 表观大小）；
  ② **抽样** —— `stat -c 'nlink=%h'`、**单个文件**的 `btrfs filesystem du` 都是**秒级**；
  ③ 只在**小目录**上跑全量。
  ⇒ **抽样永远优于全量** —— 判据要的是"能不能定性"，不是"精确到 GiB"。
- **怎么发现的**：2026-09-13 把两条直接发给用户，两条都当场卡住；
  用户原话：「卡住了！！！…你能不能靠谱一点！56tb文件啊！」。
- **边界**：**针对这块盘**。同一条命令在小卷 / 空盘上是快的 ——
  ★ 所以这条**不能**记成"du 很慢"这种通用结论，它的前提是"**56 TB 满盘 + 碎片化 + 有写负载**"。

### ERR-CMD-03 把中文序号 `①` `②` 放进代码块里 ⇒ 用户当成命令敲
- **症状**：`-sh: ①: command not found`。
- **环境前提**：命令是**给人粘贴**的；人会**整块选中复制**。
- **根因**：代码块里混进了说明性的编号，粘贴时一起进去了。
- **触发条件**：代码块里既有命令又有中文序号/说明。
- **规避做法**：**代码块里只放能直接粘的命令**；中文编号与说明放到**代码块外面**。
- **怎么发现的**：2026-09-13 与 `ERR-CMD-02` 同一轮踩到；已写进记忆。
- **边界**：只在"命令要靠人粘贴"的协作模式里成立 —— ★ 但**本项目恰恰全是这个模式**。

---

## B.3 文件系统 / 存储层

### B.3.1 卷与挂载

### ERR-FS-01 **跨卷硬链接禁止** —— 源与 `LINK_DIR` 必须同卷
- **症状**：链接建不出来，直接失败。
- **环境前提**：`video`（`/volume1`，56 T）与 `docker_ssd`（`/volume2`，448 G）**是两个物理卷**。
- **根因**：硬链接**只能在同一个文件系统内**建立（同一个 inode 的两个名字）。
- **触发条件**：把 `LINK_DIR` 指到 SSD 上"图省事"。
- **规避做法**：`LINK_DIR` 必须在 `/volume1/video` 下。
  `build-farm.sh` 用 **`stat -c %d` 比设备号**做闸；compose 里对相关服务都是 `/volume1/video:/volume1/video` **1:1 挂载**。
- **怎么发现的**：设计期就确认（SUMMARY §2、§10.5.4）。
- **边界**：通用文件系统规律。★ 但**判可行性要看设备号**，不是看路径前缀像不像（`A.3` 的 ★）。

### ERR-FS-02 同一物理卷的不同挂载点 / 路径前缀 ≠ 同一块盘
- **症状**：同样的字符串前缀，判断同卷时判断错。
- **环境前提**：DSM 上多个共享文件夹映射到同一个卷；容器里又是 1:1 挂载。
- **根因**：路径是**视图**，设备号才是**本体**。
- **触发条件**：用路径字符串做同卷判断。
- **规避做法**：`stat -c %d`。
- **怎么发现的**：与 `ERR-DSM-03` 同源。
- **边界**：★ 与 `A.3` 的 ★ 是同一条知识的两种表述，**都留**（一个在前提层、一个在错误层）。

### ERR-FS-03 满卷的 ENOSPC 会**级联**，波及面远超"写不进文件"
- **症状**：不只是注入失败 —— **告警脚本自己也会坏**（见 `ERR-SH-06`），
  连"记日志"都可能失败。
- **环境前提**：`/volume1` 已用 100%。
- **根因**：很多看起来无关的操作（写 stdout 到文件、SQLite 建 WAL、加锁文件）都要写盘。
- **触发条件**：卷可用空间为 0（或接近 0）。
- **规避做法**：**别往 `/volume1` 写真实文件**；临时落脚放 `cachedev_0` 那几个共享；
  容器日志加 `max-size`/`max-file`（`compose.yaml` 的 `x-logging` 锚点，10m × 3）。
- **怎么发现的**：09-13 邮件风暴的触发条件之一就是满卷；README 1003–1014。
- **边界**：★ **满卷"不阻塞本项目运行"** —— 农场与单片全是硬链接，不占数据块。
  这条只在**要写真实数据**时成立。

### B.3.2 文件系统特性

### ERR-FS-04 ★★ BTRFS **CoW / reflink**：`%h == 1` 判不出共享，**删了空间不回来**
- **症状**：删掉 9 个文件（`%h` 全 = 1、看起来都不是硬链接），
  `--apply` 报 `OK：已删`、目录确实消失，**但可用空间 31.91 → 31.76 GiB，48.20 GiB 一分没回来**（连量 4 次，值在动 ⇒ 不是 SMB 缓存）。
- **环境前提**：`/volume1` 是 **BTRFS**（`/dev/mapper/cachedev_1 /volume1 btrfs`），**reflink（COW）可用**。
- **根因**：**reflink 副本有自己的 inode、`%h` 也是 1**，行为却与硬链接一样（**共享 extent**）。
  ⇒ **inode 计数判不出 COW**。
- **触发条件**：任何"用 `nlink` 或 inode 判断"的判据，在 BTRFS + reflink 上都会给出**看起来正确**的答案。
- **规避做法**：判据换成能看见 extent 的：**`sudo btrfs filesystem du -s <目录>`，看 `Exclusive` 列**
  （≈ 0 ⇒ 与别处共享、删了不释放）。
  ★ 单文件也适用且是**秒级**：实测某个农场文件 `13.84 GiB / 13.84 GiB / 0.00B` ⇒ 它是自身那 13.84 GiB 的**唯一引用者**。
  ★ `Set shared` 那个数是**集合范围**的（对参数去重），而 `Exclusive` 用的是**整个文件系统**的 extent backref。
- **怎么发现的**：删旧根残留时实测；README 726–730、1155、SUMMARY §21.5。
- **边界**：**BTRFS/XFS 的 reflink 专有**。NTFS / ext4 上没有 COW，这条不成立。
  ★ **"不是硬链接"与"删了能释放"是两件事** —— 这条的意义就在这。
  ★ 已知警告：intra-file reflink 的情况下 `Exclusive` 可能被**高报**。

### ERR-FS-05 NTRS 与 POSIX 的语义差异（权限、mtime）
- **症状**：经 SMB 看到的 mtime / 权限与 POSIX 侧不一致。
- **环境前提**：NAS 是 Linux（BTRFS），从 Windows 经 SMB 访问。
- **根因**：SMB 协议有自己的属性模型，与 POSIX 不是一一对应。
- **触发条件**：拿 SMB 侧读取的属性去做**精确判据**。
- **规避做法**：mtime 判据要留余量；真要在意 ±1 秒就回 NAS 侧读。
- **怎么发现的**：`linkguard` 的 `(size, mtime_ns)` 指纹设计（同时看 size，不只看 mtime）。
- **边界**：**只在这条 SMB 链路上成立**。

### ERR-FS-06 「SMB 上的硬链接是假的」——★ **本项目实测**：**假的这个假设才是假的**
- **症状（当初的假设）**：以为 Windows 经 SMB 建的"硬链接"只是伪装的，`stat -c %h` 会谎报。
- **环境前提**：Windows 走 SMB 对 NAS 上的文件调 `os.link()` / `cp -al`。
- **根因（实测结论）**：**这个 NAS 上经 SMB 过来是服务端真硬链接** ——
  同 inode、**`nlink=2`**、**删掉原文件后另一个还在**。
  原先"Windows 建不了硬链接"的说法**是错的**。
- **触发条件**：把"Windows 不支持硬链接"这条常识直接套上去。
- **规避做法**：`build-farm.sh` **每次都用探针自证**硬链接真的建成了（inode 自检，不成立就 `die`）。
  Windows 侧要跑就加 `--map` 做前缀翻译，结果与 NAS 本机跑**完全一样**（代价是逐文件 RPC，475 条数分钟）。
- **怎么发现的**：2026-09-11 实测（SUMMARY §10.5.7），推翻了一个写在文档里的假设 —— 弯路 #「推翻假设」。
- **边界**：★ **这张结论只对这一台 NAS + 这个 SMB 实现成立**。
  换一台 NAS / 换一个 SMB 版本，可能就真的建不了 —— **所以判据必须是"每次自证"，不是"记住结论"**。

### B.3.3 SMB / UNC 访问

### ERR-SMB-01 对 UNC 路径跑 `rm` 的后果（**无回收站**）
- **症状**：glob 打错一次**不可逆**。
- **环境前提**：从 Windows 经 SMB 看到的 NAS 路径**没有回收站**这一层。
- **根因**：回收站是 DSM 在共享文件夹层提供的，SMB 直删**绕过**它。
- **触发条件**：任何在 `//iSunker-DS423/...` 上跑的 `rm`。
- **规避做法**：**禁止**。要删就在 **NAS 原生路径**上用 `rm-staging.sh`（有闸），或用户手动 `rm -rf`。
  `rm-staging.sh` **只认两类路径**：`_cleanup-*` 与 `#recycle/` 下的 `env-bak-*`（**都按 basename**），其余一律拒。
- **怎么发现的**：作为硬规矩写进 README 794、1740。
- **边界**：★ 注意"按 basename 当通行证"会把**任何叫这名字的目录**一起放行 ——
  一次性口子用**完整路径字面量**，且**删完立刻撤**（本项目就是这么处理旧根残留那第三类的）。

### ERR-SMB-02 UNC 上的 mtime / inode 可信度 —— ★ **判不出 reflink**
- **症状**：见 `ERR-FS-04`。
- **环境前提**：从 SMB 侧只能读到 `nlink` / inode 这类**POSIX 元数据的一个投影**。
- **根因**：COW 共享 extent 这件事**不在 inode 层体现**。
- **触发条件**：用 SMB 侧读数下"删了能释放空间"的结论。
- **规避做法**：**下结论前说明这是 SMB 侧读数**；真要坐实就回 NAS 侧看 `Exclusive`。
- **怎么发现的**：旧根残留那次，"可释放 48.20 GiB"就是 SMB 侧读数得出的**错误结论**。
- **边界**：★ 这条不是说"SMB 读数不可信"，而是**"它证明不了这件事"** —— 两者差别很大。

### ERR-SMB-03 UNC 上 `mv` 与本地 `mv` 的语义差异
- **症状**：以为在做 rename，实际在做跨机拷贝。
- **环境前提**：UNC 的 `mv` 要跨网络。
- **根因**：同文件系统内的 `mv` 是改目录项（原子、瞬时）；跨主机/跨协议的 `mv` 往往是 **copy + delete**。
- **触发条件**：对 UNC 做大目录 `mv`。
- **规避做法**：要原子的目录级搬移，**在 NAS 侧做**（`deploy.sh` 的 `.new` + `mv` 就是在 NAS 侧）。
- **怎么发现的**：搬家（`migrate-reseed-dirs.py`）时的设计与踩坑。
- **边界**：只在跨协议时成立。

### ERR-SMB-04 反斜杠 UNC 会失败 —— 用正斜杠（见 `ERR-OS-05`）
> 交叉引用：登记在 OS 层（`ERR-OS-05`），**不在此重复**。

### B.3.x 一条不属于以上任何格、但必须记的

### ERR-FS-07 ★ 机械盘被大量占用 = **一定有人在拷贝，而不是在链接**
- **症状**：以为"全是硬链接 ⇒ 零开销"，结果盘在满。
- **环境前提**：本项目所有设计都建立在"硬链接不占数据块"之上。
- **根因**：只有**真拷贝**才会吃数据块。
- **触发条件**：有人用 `cp` / 下载 / 还原代替了链接。
- **规避做法**：**把"盘在被吃"当成一条判据**，而不是背景噪声。
- **怎么发现的**：48.20 GiB 旧根残留就是这么**暴露**出来的（SUMMARY §9 原话）。
- **边界**：`/volume1` 上成立。★ 与 `ERR-FS-04` 配套：**一个说"该释放的没释放"，一个说"不该占的占了"**。

---

## B.4 容器 / 网络层

### B.4.1 容器网络

### ERR-NET-01 ★ 群晖上的 Docker **hairpin NAT 不通**：容器 → 宿主机 LAN IP → 另一个容器 = timeout
- **症状**：容器里 `curl http://192.168.0.7:3060` **超时**。
- **环境前提**：群晖 DSM 上的 Docker（bridge 网络）+ 端口映射。
- **根因**：发往宿主机 LAN IP 的包要经 DNAT 回到同一台机器上的另一个容器 —— 这条回环在群晖上走不通。
- **触发条件**：容器内的代码用**宿主机 IP**去访问**同机另一个容器**。
- **规避做法**：容器之间**用服务名**（`http://qbittorrent-reseed:3060`），
  并把对方栈的网络以 `external` 接进来（`qbit-net` = `qbittorrent-reseed_default`）。
- **怎么发现的**：2026-09-11 实测；`docker-compose.yml` 顶部**专门写了"不要改回 `${NAS_IP}`"**。
- **边界**：★ **只对"容器 → 同机另一容器"成立**。
  **宿主机上跑的 `drive-loop.py` 走的是另一条路**（LAN IP + 端口映射），**两边是故意不同的**。
  —— 这就是 `A.1` 那个 ★ 最具体的实例。

### ERR-NET-02 容器名 vs 宿主机 IP：**哪个地址从哪个位置解析得到**
- **症状**：见 `ERR-NET-01` 与 `A.1`。
- **环境前提**：同一个服务在三个位置有**三个不同的地址写法**。
- **根因**：DNS / 服务发现的范围不同：容器名只在容器网络内可解析；宿主机不解析容器名。
- **触发条件**：把在一个位置验证过的地址抄到另一个位置。
- **规避做法**：按 `A.1` 的表选。
- **怎么发现的**：与本项目多处配置注释对照得出。
- **边界**：通用容器知识，但**本项目三个位置同时存在**，所以必须显式。

### B.4.2 容器配置

### ERR-DOCKER-01 ★★ `docker compose restart` **不重新注入环境变量** ⇒ 改了 `.env` 像没改
- **症状**：磁盘上的 `.env` 明明是对的，**跑着的容器用的是旧值**；
  cross-seed 持续对已移除的站发请求、吃 `HTTP 410`。
- **环境前提**：`env_file` / `environment` 是在**创建**容器时注入的。
- **根因**：`restart` 只是停+起**同一个容器**（沿用旧 spec）；`up -d --force-recreate` 才**重建**容器并重新注入。
- **触发条件**：改了 `.env` 里任何键后只 `restart`。
- **规避做法**：**必须** `sudo docker compose up -d --no-deps --force-recreate cross-seed`
  （`--no-deps` 防连带重建 Prowlarr）。★ 重建会打断正在跑的 drive，**等批次跑完**。
  **弯路 #15** —— 当初的形态正是"以为 `restart` 能刷新环境变量，旧配置一直驻留"。
  ★ **代价不对称才是关键**：多重建一次只是中断一批；少重建一次是**磁盘上写着对的、跑着的是错的**，
  之后所有排查都被这个假象带偏（`ERR-AI-05`：绿，但绿是因为规则压根没参与匹配）。
- **怎么发现的**：`/3/api` 的 410 排查 —— 磁盘上 `.env` 早就是对的，**纯粹是容器没重建**（SUMMARY §10.5.10）。
- **边界**：★ 反过来说：**不是每个键都需要重建**。
  `FARM_SOURCES` 只被 `build-farm.sh` 读（compose 只透传 `DATA_DIRS`）⇒ **改它不用重建**。
  ★ **弯路 #4 是这条的近亲**：`DATA_DIRS` 早期是**49 条子目录硬编码**，
  每加一个大包都要重算整张表 + 重建容器 —— 改一个键的成本高到能劝退人，
  于是"该改的没改"变成常态。v3 把它收成**单根硬链接农场**（1 条 `dataDirs`）之后，
  这个键才回到"一条一改"的量级。**配置项的成本也是设计的一部分。**
- **判据**（★ 别看 `ls`，别看 `.env.new` 存不存在）：
  ① `docker inspect reseed-cross-seed` 回读容器内的键；② `cross-seed.db` 的 `data` 表里有没有新路径。

### ERR-DOCKER-02 `--no-deps`：避免连带重建
- **症状**：只想重建 cross-seed，结果 Prowlarr 也被掀了。
- **环境前提**：compose 里 `depends_on: [prowlarr]`。
- **根因**：默认 `up -d` 会连带处理依赖。
- **触发条件**：重建有依赖关系的服务。
- **规避做法**：加 `--no-deps`。
- **怎么发现的**：README 248 行原话。
- **边界**：通用。

### ERR-DOCKER-03 ★ Docker 默认的 `json-file` 日志驱动**没有上限**
- **症状**：容器一直往 `/volume2/@docker/containers/*/*-json.log` 写、**永不清理**。
- **环境前提**：四个服务原先**都没有 `logging` 段**；而 `/volume1` 只剩约 20 GB。
- **根因**：除非全局 `daemon.json` 配过，`json-file` 默认**无上限**。
- **触发条件**：长期运行的容器。
- **规避做法**：用 YAML 锚点 `x-logging` 统一给四个服务加 `max-size: 10m` / `max-file: 3`
  （10m × 3 = 每容器 30 MB，四个合计 ~120 MB）。★ **改这里必须 `--force-recreate` 才生效**。
- **怎么发现的**：探"日志轮转"时**推翻了两条前提** ——
  `drive-loop.log`（`RotatingFileHandler(5MB × 5)`）与 `cross-seed/logs/*`（按天轮转、留 14 天）
  **其实都已在自我轮转**；**真正没上限的只有容器日志**。
  ⇒ 照"给日志加轮转"的直觉去动那两处，等于做一个**已经存在的东西**。
- **边界**：★ 判据：`docker inspect --format '{{.HostConfig.LogConfig}}' <容器>` 回读 `{"Type":"json-file","Config":{"max-file":"3","max-size":"10m"}}`。

### ERR-DOCKER-04 ★★ **构建上下文 vs 运行时副本**：同一个文件被 deploy 两次，漏一个只在某条路径上炸
- **症状**：`docker compose run reseed-orchestrator state` 报**模块级 `ImportError`**，
  **连默认的 `preflight` 一起打挂**。
- **环境前提**：`orchestrator/state.py` 在 `<compose>/` 下**有两个不同用途的副本**：
  `<compose>/orchestrator/`（**构建上下文**，进镜像）与 `<compose>/drive-loop/orchestrator/`（**运行时**，宿主机直接 import）。
- **根因**：`deploy.sh` 白名单原先**只同步了后者**；而 Dockerfile 是 `COPY orchestrator/ /app/orchestrator/`
  ⇒ 镜像里**没有** `state.py`。平时看不出来（`main.py` 以前不导入它），加 `state` 子命令就成了硬故障。
- **触发条件**：新增一个被 import 的模块，而白名单只更新了一处。
- **规避做法**：**"这个文件在哪几个地方需要"要按「谁拷它」分别数**。
- **怎么发现的**：加 `state` 子命令时炸出来；`deploy.sh` 里那段注释是原话。
- **边界**：★ 与 `A.12` 的**三处路径**是同一条知识。**三条路径**：构建上下文 / 宿主机 / 容器内。

### ERR-DOCKER-05 `up -d --force-recreate` 对**一次性 CLI** 等于什么也没做
- **症状**：改完 `orchestrator/state.py`，`--force-recreate` 之后**行为没变**。
- **环境前提**：`reseed-orchestrator` 是**一次性 CLI**（`ENTRYPOINT python -m orchestrator.main`，**无 daemon**）。
- **根因**：不带 `--build` 的 `--force-recreate` 只会拿**旧镜像**重启 ⇒ 白掀一次容器。
- **触发条件**：照 `deploy.sh` 末尾那句"下一步 `--force-recreate`"无脑跑。
- **规避做法**：**要 `docker compose build reseed-orchestrator`**。
- **怎么发现的**：README 958–966 专门标了「**`deploy.sh` 末尾那句不是每次都适用**」。
- **边界**：★ **只对一次性 CLI 成立**。
  同一轮同步的 4 个目的地里，`<compose>/drive-loop/**` 那 3 个是**热路径**（跑在宿主机上，**拷完即生效，不需要动容器**），
  第 4 个才是构建上下文副本。**别照着那句话无脑跑。**

### ERR-DOCKER-06 容器默认保存路径落在**容器可写层**（宿主机看不见、重建即丢）
- **症状**：任何不带 savepath 的添加会落进可写层。
- **环境前提**：`qbittorrent-reseed` 只挂了 `/downloads/incomplete`，而默认保存路径是 `/downloads/`。
- **根因**：**没挂载的那部分路径落在容器可写层**。
- **触发条件**：有代码/工具往 `:3060` 添加种子而不指定 savepath。
- **规避做法**：走 qB API 改 `save_path`（`app/setPreferences`，**零停机、可逆**，不动 conf 也不动 compose）。
  ★ 验收判据是 qB 自己报的 **`free_space_on_disk`**：改前 **302.14 GiB**（= volume2，可写层所在卷）
  → 改后 **0**（= volume1）⇒ 落点**真的**换了卷。
- **怎么发现的**：2026-09-13 潜伏雷排查；SUMMARY §21.6.1。
- **边界**：★ 落点**只能选在 `/volume1/video` 下**，因为**硬链接不能跨卷**（`ERR-FS-01`）。

### ERR-DOCKER-07 只读语义的子命令，挂载**必须 `ro`**
- **症状**：一个"只读"的子命令**有写坏生产库的能力**。
- **环境前提**：`StateStore` 打开库时会跑**一遍 schema 迁移**（`ALTER TABLE …`）。
- **根因**：`rw` 挂载 + 打开即迁移 ⇒ 语义上的"只读"被实现细节击穿。
- **触发条件**：给只读子命令挂 `rw`。
- **规避做法**：`- ./drive-loop/hlink:/state:ro`。这一行**不用重建容器**，下次跑编排器就生效。
- **怎么发现的**：SUMMARY §11.10 的四个决定之一。
- **边界**：通用。★ 配套：`StateStore(create=…)` 那条根因闸 —— **库不存在时绝不许把文件建出来**
  （连父目录都不能建），否则会留下 0 字节 `state.db`（本项目的 `ERR-SQL-04`）。

### B.4.3 凭据边界

### ERR-SEC-01 哪些端口 / 目录对外可见
- **症状**：以为"内部服务"，其实**已发布到宿主机局域网**。
- **环境前提**：9696 / 2468 / 3060 / 8191 / 8787 都是 `ports:` 发布的。
- **根因**：发布 = 局域网任何机器都能连。
- **触发条件**：把"只在容器网络里"当成"对外不可见"。
- **规避做法**：`:3060` 走**局域网免密白名单**（`172.16.0.0/12` + 局域网段）**或** `.env` 里的密码，**二选一**。
- **怎么发现的**：Phase 0 的配置项；SUMMARY §3.2。
- **边界**：★ 白名单**必须包含 `172.16.0.0/12`**，否则容器过来的请求会被 403。

### ERR-SEC-02 容器内的 env 与磁盘上的 `.env` **不是一回事**
- **症状**：见 `ERR-DOCKER-01`。
- **环境前提**：`.env` 是**创建时**注入的快照。
- **规避做法**：判"生效没生效"一律**回读容器内**的键，不读磁盘文件。
- **怎么发现的**：同 `ERR-DOCKER-01`。
- **边界**：通用。

### ERR-SEC-03 ★★ **告警正文在教人泄露凭据** —— 而且专挑人最着急的时候
- **症状**：告警里的查证命令 `docker inspect reseed-cross-seed | grep TORZNAB_URLS`
  **原样会把凭据打出来**（那行里每条 URL 各带一个 `apikey=`，
  值就是 **Prowlarr 的应用级 key**，而 Prowlarr 里存着**所有 PT 站的 cookie**）。
- **环境前提**：告警是**给人照着敲的**；人照着敲的时候**正是最急的时候**。
- **根因**：**让程序替我们决定了"这时候会把什么打到终端上"**。
- **触发条件**：告警正文里嵌了未脱敏的诊断命令。
- **规避做法**：改成
  `… | sed -E 's/(apikey=)[^,&]+/\1<redacted>/g'`，并**在正文里写明**
  「★ 先脱敏再看，**别把原样输出粘进聊天/命令行**」。
  ★ 拿掉那个值**不影响判读**：要判的是"容器里还剩哪个站"，靠 `apikey=` 这个键名和 `/N/api` 就够了。
- **怎么发现的**：2026-09-13 审计告警正文时发现；README 893–903、§20.8。
- **边界**：★ **规矩要写在命令里，不是写在"记得别看"里** ——
  靠人记得的那种防护，这条链上**已经漏过一次**（本地 `.env` 是存根 ⇒ 值指纹那道网覆盖很薄）。
  **覆盖薄的那道网，绿了也不代表安全。**

---

## B.5 运行时 / 版本层

### B.5.1 Python

### ERR-PY-01 ★★ `str | None` 在 3.8 上**定义期求值** ⇒ 崩在 logging 配置之前，**连一行日志都留不下**
- **症状**：任务跑了，**什么都没发生**（静默）。
- **环境前提**：NAS 宿主机的 python3 = **3.8.15**（`ERR-DSM-08`）。
- **根因**：`str | None` / `list[str]` 这类注解在 **< 3.10** 上是**函数定义时**当场求值的，
  直接抛 `TypeError` —— **不是等到调用**。而且它崩在 `logging` 配置**之前**。
- **触发条件**：文件里通篇用了 3.10+ 的注解语法，且**没有** `from __future__ import annotations`。
- **规避做法**：文件顶部**必须有** `from __future__ import annotations` —— **不能删**。
- **怎么发现的**：`attempts.log` 记到 `start py=/usr/bin/python3` + `exit=0`、
  旁边生出 `__pycache__/*.cpython-38.pyc` ⇒ 版本坐实；README 1047–1056。
- **边界**：★ 只在 **< 3.10** 成立。但**症状（静默）在任何版本不匹配时都一样** ——
  所以 `run.sh` 在 shell 层另记一份 `attempts.log`，用来区分"python 没起来"与"python 崩了"。

### ERR-PY-02 `locale` / `encoding` 默认值：`isatty()` 对 NUL 返回 True
- **症状**：脚本以为自己在跟终端说话，其实输出被重定向 / 接到了 NUL。
- **环境前提**：Windows + GBK 控制台（与 `ERR-OS-04` 同族）。
- **根因**：`isatty()` 的判定不总是符合直觉。
- **触发条件**：靠 `isatty()` 决定输出格式 / 是否加颜色 / 是否重定向。
- **规避做法**：别依赖它做**承载语义**的判断；显式参数开关（如 `--no-color`）。
- **怎么发现的**：`scan-secrets.py` 的输出接到 `NUL` 也要一样（`tests/test_scan_secrets.py` 有一格专门钉这个）。
- **边界**：★ 本条**本项目实例较薄**（主要落在"输出接到 NUL 也一样"那条断言上），**别过度引用**。

### ERR-PY-03 ★ `spec_from_file_location` + `@dataclass`：必须先 `sys.modules[name] = mod`
- **症状**：报一个看着毫不相干的 `AttributeError: 'NoneType' object has no attribute '__dict__'`。
- **环境前提**：测试里用 `importlib` **把被测文件真的那份载进来**（不是切源码再 exec）。
- **根因**：`dataclasses` 要 `sys.modules.get(cls.__module__).__dict__` 去找注解的命名空间，
  而 `spec_from_file_location` **不替你登记**。
- **触发条件**：被测文件里有 `@dataclass`。
- **规避做法**：**先 `sys.modules[name] = mod` 再 `exec_module`**；吞掉 `SystemExit`、重定向 `stdout`。
- **怎么发现的**：`tests/README.md` 89–95 有原话 ——
  ★ **同一段 `_load` 代码对两个文件行为不同，原因却不在它们身上**：
  `drive-loop.py` 没有 dataclass 所以先跑通了，一度让人以为只有 `state.py` 有问题。
- **边界**：只在"动态加载 + dataclass"这一组合上成立。

### ERR-PY-04 `read_text` / `write_text` 的默认 `newline` 行为会**改行尾**
- **症状**：读进来再写出去，行尾被悄悄改了（CRLF ↔ LF）。
- **环境前提**：Windows 侧维护 `.env`（CRLF）与 `.sh`/`.py`（**必须 LF**）。
- **根因**：文本模式默认做换行转换。
- **触发条件**：用 `Path.read_text()` / `write_text()` 处理**行尾敏感**的文件。
- **规避做法**：**显式传 `newline=""`** 或按二进制读写（`bytes`）保真。
- **怎么发现的**：本项目同时存在"必须 CRLF"和"必须 LF"的文件（`ERR-OS-01`），所以这条是**结构性的**。
- **边界**：★ 只在 **Windows** 上会咬人；在 Linux 上默认不转换。

### B.5.2 各服务版本

### ERR-SVC-01 ★ qB 的 `setLocation` 是**异步**的，且**校验队列是单槽**
- **症状**：`max_active_checking_torrents = 1` ⇒ **145 条 IYUU 种子**挤在 `checkingDL`，**8 小时一步没挪**。
- **环境前提**：qB v4.6.5 / WebAPI 2.9.3。
- **根因**：`setLocation` 会触发**重新校验**，而校验队列**一次只跑一条**。
- **触发条件**：批量 `setLocation`（本项目一次 509 条）。
- **规避做法**：**一次只能一条**地规划；搬完等它校完（`wait-for-checks.py`）。
- **怎么发现的**：搬迁后 145 条卡住；README 1155 那条待办。
- **边界**：★ **"卡住"的判据要小心**：已排除三种（497 个文件逐个 `stat` 全部大小相符、`save_path` 无映射失败、
  不是缺文件）⇒ 剩下的"**校验队列/校验器卡住**"**仍是"最符合证据的解释"，不是已坐实**。
  坐实要一条写操作：对任意一条卡住的种子手动「强制重新校验」，几秒内跳 100% 即坐实。

### ERR-SVC-02 Prowlarr 的 **`indexerId` 会变**
- **症状**：`TORZNAB_URLS` 里的 `/N/api` 指错了站。
- **环境前提**：索引器详情页 URL 里的数字（如 `/indexer/3` → id=3）。
- **根因**：**删站再重加，ID 往往就换了**。
- **触发条件**：增删索引器之后没重新核对。
- **规避做法**：**每次增删索引器后都要重新核对**：
  ```bash
  curl -s -H "X-Api-Key: <Prowlarr API key>" http://<NAS_IP>:9696/api/v1/indexer \
    | python -c "import sys,json;[print(i['id'], i['name']) for i in json.load(sys.stdin)]"
  ```
- **怎么发现的**：SUMMARY §6.6。
- **边界**：★ 还要区分：**「只在 Prowlarr 里禁用索引器」≠「cross-seed 不搜它」** ——
  cross-seed 只认自己的 `TORZNAB_URLS`，会继续请求已禁用的站，每搜一次吃一个 `HTTP 410` 并 snooze。
  **彻底停用某站必须把它从 `TORZNAB_URLS` 移除**再重建。
  **弯路 #17** —— 当初的形态：只在 Prowlarr 界面禁用，cross-seed 持续请求，大量 `HTTP 410`。
  ★ 一般化：**"禁用"是个界面动作，"不请求"是个配置事实**，中间没有任何连线。

### ERR-SVC-03 cross-seed 的**静默过滤**与**周期粒度**
- **症状**：以为"搜过了"，实际被静默筛掉 / 跳过。
- **环境前提**：cross-seed v6.13.7。
- **根因**：① `includeSingleEpisodes` 会**静默过滤**；② **索引器退避时待搜项直接跳过**（cross-seed **不排队、不重试**）。
- **触发条件**：一次 429 之后继续跑。
- **规避做法**：判据落在外围（状态机的 `SKIPPED` 阶段）；重搜周期粒度是 **(片 × 站)**，默认 **14 天**，
  来自 cross-seed 自己的 `timestamp(searchee_id, indexer_id, last_searched)` 表。
- **怎么发现的**：2026-09-11 有 **295 条**就这么消失了（SUMMARY §5.3）。
- **边界**：★ `searchCadence` / `excludeRecentSearch` 的粒度**不能互相替代**。

### ERR-SVC-04 ★ IYUU 辅种的目录从哪来：**qB 里同 infohash 那条自己的 `save_path`**，原样回灌
- **症状**：纠结"要不要手动配监控文件夹/资源文件夹/种子文件夹"。
- **环境前提**：IYUU Plus 往 `:3060` 辅种。
- **根因（溯源结论）**：源码三跳 ——
  `Client.php:726` → `ReseedServices.php:379` → `ReseedDownloadServices.php:149`，
  `savePath` = **qB 里同 infohash 那条已有种子自己的 `save_path`**。
  ⇒ 那三个路径框**在这条路上一次都没被读**，`cn_folder` 目录映射表也**零行**。
- **触发条件**：（不触发 —— 这是一条**澄清**）
- **规避做法**：**不需要手动配任何目录**。唯一参与路径决策的是「创建多文件子目录」→ qB 的 `root_folder`
  （已勾是对的）；`autoTMM='false'` 硬关 ⇒ 分类的 savePath 不会覆盖。
- **怎么发现的**：源码三跳溯源，README 1154 / §21.2–§21.3。
- **边界**：★「容器路径映射不一致」（IYUU 看 `/video`、qB 看 `/volume1/video`）**碰不到辅种目录** ——
  它只对那两个**空转的字段**成立。

### ERR-SVC-05 FlareSolverr：Prowlarr **按索引器挂 tag** 启用，**没有全局开关**
- **症状**：装了、容器在跑、健康 —— **一次都没被用上**。
- **环境前提**：Prowlarr + FlareSolverr v3.5.2。
- **根因**：启用方式是**给那个索引器挂一个名为 `flaresolverr` 的 tag**；
  「Settings → Indexers 里填个 URL」这个说法是**错的**。实测 Prowlarr 的 tag 列表是**空的**，
  四个索引器**一个 tag 都没挂**。
- **触发条件**：以为配了 URL 就等于启用了。
- **规避做法**：**怎么判断某个站要不要它**：在 Prowlarr 里手动搜一下 ——
  看到 `403` + 挑战页 HTML / 日志里出现 `Cloudflare` → 需要；
  `500/502/520/522/timeout`（站点后端挂了）或 `429`（限流）或能正常返回 → **不需要**。
  ★ **现在四个站都不需要它**。
- **怎么发现的**：SUMMARY §18.12；弯路 #7。
- **边界**：★ 「从 GHCR 拉镜像失败」那条**已过时**（2026-09-12 已成功拉过 12 层），
  compose 里的镜像名**不用**换。**别引用旧状态**（`A.8` 的半衰期）。

### ERR-SVC-06 cross-seed 启动报 `unknown option` / 键名错误
- **症状**：容器起不来或配置被忽略。
- **环境前提**：cross-seed 版本升级会**改配置键名**。
- **规避做法**：按 `cross-seed/config.js` 顶部说明与官方文档改键名。
- **怎么发现的**：Phase 2 的验证步骤就是这么设计的（`docker compose logs -f cross-seed`，看有无 `unknown option`）。
- **边界**：★ 本项目用的是 `ghcr.io/cross-seed/cross-seed:latest` —— **版本会自己飘**，这条随时可能复发。

### ERR-SVC-07 各系统自己的 cron 时刻
- **症状**：拿"搬迁当天"的读数去判"搬迁有没有把 IYUU 搞停"。
- **环境前提**：IYUU 的 cron 是 **`45 1 * * *`（01:45）**；日报在**当天第一批（约 `00:0x`）**采样。
- **根因**：采样时刻**早于**被观测事件。
- **触发条件**：日报读得比事件早。
- **规避做法**：**第一个有意义的读数是 09-14 凌晨，不是 09-13**。
- **怎么发现的**：README 1759–1766 专门标了这句。
- **边界**：★ 这是**时间窗判据**（`A.7` 的 ★）最典型的一例：**"判据跑了"和"判据能看见"是两件事**。

### ERR-SVC-08 Prowlarr 会**伪造 429**；它的 Query Limit 是**本地保险丝**，不是站点真实余额
- **症状**：把 Prowlarr 的 Query Limit 当节流阀用，结果**没有节流作用**。
- **环境前提**：Prowlarr 作为代理层。
- **根因**：Query Limit 的设计目的是**全局保险丝**（设成明显高于实际用量的值，如 1000/天）；
  真实控速要靠 cross-seed 的 `delay`。
- **触发条件**：拿它当节流阀。
- **规避做法**：`delay` 保持 **30~45**；对站点友好的节流在 cross-seed 侧。
- **怎么发现的**：弯路 #16、README 255–256。
- **边界**：★ **站点真实余额 Prowlarr 根本不知道** —— 它能提供的是"代理视角"，见 `A.8`。

### ERR-SVC-09 外部站点的状态**有半衰期**
- **症状**：拿着两天前的故障描述做今天的决策。
- **环境前提**：4 个外部 PT 站。
- **根因**：站点侧故障会自己恢复，而**文档不会自己更新**。
- **触发条件**：引用旧记录。
- **规避做法**：引用前**回读一次**。
- **怎么发现的**：BTSCHOOL —— 当初因为它出 CF 挑战页而要换站，
  后来它**自己恢复了**且是四站里命中率最高的（23/46 = 50%），**只是没人回来销账**。
  ⇒ 结论是「**当初的判据失效了**」，不是「换好了」。
- **边界**：★ 反例也要记：**真要换，按数据该换的是 `NanyangPT`（4/452 = 0.9%）**，
  不过南洋可能只是**片库对不上**这批包。**先看目录，再下结论。**

### ERR-SVC-10 ★ 状态机的 `stage` 是**推导出来的**：少喂一个输入 ⇒ **必然**误降级
- **症状**：回灌打出 `已在 qB 里: 0` / `本次新增做种: -51`（**负数**）；已做种的片子被从 `SEEDING` 打回 `MATCHED`。
- **环境前提**：`stage` 由「状态机库 + cross-seed 库 + **qB 实况**」三者联合推导，qB 那一路靠 `--qbit-url` 进来。
- **根因**：忘传 `--qbit-url` ⇒ 哈希交集恒为空 ⇒ 推不出"在做种" ⇒ **降级**。
  它**不是**"数据暂时缺"的容错问题，是**函数少了入参，结果必然错**。
- **触发条件**：手敲 `reseed-state.py drive` / `sync` 时漏参数（脚本里写死的路径不会漏，手敲会）。
- **规避做法**：★ 补救是**幂等**的 —— **补跑一次带 `--qbit-url` 的 `sync` 即完全恢复**，不发任何站点请求。
  ⇒ 看到负数**别慌、别重建库**，补跑即可。
- **怎么发现的**：README 204–208、1463–1466；SUMMARY §13.6 坑 1。**弯路 #8**。
- **边界**：★ 这里有个更深的教训：**"降级"和"真的没做种"在输出上长得一样**。
  ★ **同一节课本项目上了两次**：后来 v3 农场切换又出过一次同形状的
  （cross-seed 报农场路径、`pack.roots` 还是原路径 ⇒ searchee 全被静默跳过 ⇒ 状态机再次误降级）。
  第一次（缺 `--qbit-url`）**补跑 `sync` 就够了**；第二次**补跑治不了** ——
  实测只从 13 回到 21，修掉真根因（`pack.farm_root` 列）才到 **201**。
  ⇒ 判据：**"补跑一下就好了"只对"输入缺失"成立；对"判定依据过时"必须改数据。**
  详见 SUMMARY §17.5.1、§17.3（当时的误判过程）。

### ERR-SVC-11 ★ 两个独立系统各有一个"深度"参数，对不上 ⇒ 幽灵条目 / 漏搜
- **症状**：状态机里有这条、cross-seed 里没有（幽灵条目）；或反过来，真在搜的片子状态机漏掉。
- **环境前提**：`reseed-state.py` 的 `--depth` 与 cross-seed 的 `maxDataDepth` 是**两处独立配置**。
- **根因**：两边枚举出来的 searchee 集合**不同**。含义就是"从 dataDir 往下数几层"，
  第 1..N 层的**目录和视频文件**都算 searchee（不是"几层目录"）。
- **触发条件**：任一侧改了深度，另一侧没跟。
- **规避做法**：**`--depth` 必须与 `maxDataDepth` 完全一致**（本项目未显式配置 ⇒ 默认 **2**）。
  ★ **别用"调大 `maxDataDepth`"去解决某个包的局部问题** —— 它是**全局**开关，
  会连带改变 FRDS / MBF 的枚举行为、产生更多 searchee 和更多 indexer 请求。
- **怎么发现的**：README 309–311、1478–1479；SUMMARY §10.6.1。**弯路 #9**。
- **边界**：★ 两套系统的边界校验**在运行时没有任何断言** —— 它们只是各扫各的，
  差异只以"某些条目说不出话"的形式浮现。**跨系统对齐的配置项，必须有一处写死"必须相等"。**

### ERR-SVC-12 ★ 轮询间隔 **长于**被观测的窗口 ⇒ 判据**从来没生效过**
- **症状**：`backoff_hits` 一直是 **0**，**但**索引器状态明明是 `RATE_LIMITED`。
- **环境前提**：退避是"按秒"的短事件；检查是"按批"的长周期。
- **根因**：检查间隔 `--check-every × --interval = 10 × 30s = 300 秒`，
  **比实测的退避窗口（30~60 秒）还长** ⇒ 整段窗口落在两次检查之间 ⇒ 既不等待、也不计数。
- **触发条件**：任何"采样周期 > 现象周期"的观测。
- **规避做法**：已修 —— 加 `--backoff-check-secs`（默认 **60**）**按秒**触发检查，
  并按 `retry_after` **变化**认定"新发生了一次退避"（而不是按"当前是否在退避"）。
  现在是**双触发**：每 `--check-every 10` 条 **+** 每 `--backoff-check-secs 60` 秒。
- **怎么发现的**：README 209–213、1674；SUMMARY §17.5.2。**弯路 #10**。
- **边界**：★★ **这类失效最坏的地方是它长得像"没事"**（0 就是"没发生过"）。
  ⇒ 判据：**任何恒为 0 的计数器，先算一遍"采样周期 vs 现象周期"**，再决定信不信它。

### ERR-SVC-13 ★★ `--once` 模式下的 `next_sleep()`：**降速策略压根没接线**
- **症状**：日志里**写着**"站点在退避，缓一缓"，实际上**没有任何等待**（`--once` 模式）。
- **环境前提**：同一份 `drive-loop.py` 有两种跑法：常驻循环 / 一次性（DSM 计划任务每 15 分钟拉一次）。
- **根因**：`next_sleep()` 的返回值**只进了日志**，阻塞等待只接在常驻分支上。
  ⇒ 本项目实际用的是 `--once` ⇒ **这条策略从上线起就没生效过**。
- **触发条件**：任何"分支模式"的代码 —— 只测/只接线了其中一条。
- **规避做法**：已修（SUMMARY §17.5.4）；`--once` 模式下的接管机制见 `ERR-SCHED-04`。
- **怎么发现的**：查 `backoff_hits=0` 时**连带**发现 —— 不是主动查出来的（README 213、1675）。**弯路 #11**。
- **边界**：★★ 它与 `ERR-SVC-12` 是**同一次排查里挖出的两根钉子**，形状不同：
  - `ERR-SVC-12`：**判据看不见**（采样太快，事件在两次采样之间）。
  - `ERR-SVC-13`：**判据看见了、结论也记了，只是没接动作**（日志有、行为无）。
  ⇒ 「日志说它做了」**从来不等于**「它做了」。判据要落在**可观测的行为**上（心跳、下一批的起始时刻），
  不是落在日志措辞上。（同类：`ERR-AI-05`。）

### ERR-SVC-14 ★ 把**目录**当成搜索名 ⇒ 白烧站点查询额度
- **症状**：搜索请求的名目是 `合集` / `分类标签` 这种**根本不是发布名**的串；额度被无声吃掉。
- **环境前提**：大包（DC 类）解压后是「包根 → 中文分类标签 → 真发布名」的多层结构。
- **根因**：`DATA_DIRS` 直接指向包根 ⇒ 枚举出来的 searchee 包含了**中间层的分类目录**，
  它们被拿去当搜索名。★ 这也是**本项目最高优先的资源是站点额度**的具体体现。
- **触发条件**：接入新的大包时，没先看它的目录形状。
- **规避做法**：**先看目录再定根**。两条正解（按包形择优，都已在用）：
  ① 根指到"标签目录的直接子目录就是真发布名"的那一层，`maxDataDepth` 保持默认 2；
  ② 确需包根作 dataDir 时，才动深度**并**配 `blockList`（如 `nameRegex:^\d{2}\.`）兜底 ——
     属启发式，代价大，见 `ERR-SVC-11` 的"别动全局开关"。
- **怎么发现的**：README 360–443（多包支持）、390–395；SUMMARY §10.4 / §10.6。**弯路 #18**。
- **边界**：★ 代价**不在**搜索本身，而在**额度**（本项目最稀缺的资源）；
  而且它**不报错** —— 只是命中率悄悄变低。**先看目录，再下结论**（同 `A.8` / `ERR-SVC-09`）。

### ERR-SVC-15 ★ cross-seed 的 `ENOENT` 是**它自己的并发**，不是丢种
- **症状**：`error.<日期>.log` 里 `[inject] Failed to parse ... ENOENT` —— 看着像注入失败、种子丢了。
- **环境前提**：cross-seed 注入完成后会做**善后清理**（删掉已没用的存档）；清理与其它操作并发。
- **根因**：清理拿到的是一份**文件清单**，然后**逐个**打开 —— 清单里的某个文件若在这期间
  被**另一路**删掉 ⇒ `open()` ⇒ `ENOENT`。**竞态，不是损坏。**
- **触发条件**：自身高并发时（`Failed to parse` 与 `ENOENT` 各 7 条，同一批）。
- **规避做法**：★ 判据：**看时间差与后果** —— ENOENT 落在 `09:40:07.336`，与删档动作**相差 320 ms**，
  且同一文件被删了两次 ⇒ **结论：种早已注入且完整，丢的只是一份已经没用的存档**。
- **怎么发现的**：README 1568–1582；SUMMARY §16.6.5（那一节的标题就是
  「**两个「看着像事实、其实是观测工具坏了」的发现**」——"先撞上 ENOENT，再撞上真正的根因"）。**弯路 #20**。
- **边界**：★★ **"先撞上 ENOENT，再撞上真根因"** —— 这条的教训是**顺序**：
  噪声先出现，会**带走**排查的方向。⇒ 见到 `ENOENT` 先问"是谁在删、删的是不是废料"，
  **别**直接跳到"我的数据丢了"。（同一形状见 `B.10` 元层清单。）

**◆ 本小节未实例 / 未验证**：`searchCadence` / `excludeRecentSearch` 的具体键名与取值（本项目走的是自建状态机的周期，未直接用它们）。

---

## B.6 编码 / 字符层

> ★ 这一节与 OS 层**故意重复登记**几条（CRLF、GBK）—— 按 1.txt 的规矩：
> **同一错误可能在多层出现，两层都登记、互相指路，不要合并**（合并会丢掉"从哪一层看"的信息）。

### ERR-ENC-01 控制台代码页（GBK / cp936）与 UTF-8 的差异
> 交叉引用：本体登记在 `ERR-OS-04`。**从编码层的看法**：这不是"编码错了"，
> 而是"**同一个字符在两种编码里的存在性不同**"。

### ERR-ENC-02 ★ 哪些字符在哪些编码里存在 —— **`⑪` 不在 GBK**
- **症状**：见 `ERR-OS-04`。
- **环境前提**：`①`..`⑩`（U+2460–U+2469）在 GBK 里，**`⑪`（U+246A）不在**。
- **规避做法**：**加新的节编号时不要越过 `⑩`**。
- **边界**：★ 这是**具体的**字符集事实，别推广成"中文编码不好用"。

### ERR-ENC-03 `stdout` / `stderr` 的 `encoding` 默认值
- **症状**：同一个脚本，**重定向到文件**与**打屏**行为不同。
- **环境前提**：Python 的 `sys.stdout` 编码来自 locale / 控制台代码页。
- **规避做法**：脚本开头显式 `sys.stdout.reconfigure(encoding="utf-8")`。
- **怎么发现的**：`tests/README.md` 的约定。
- **边界**：★ 只在 stdout 不是 UTF-8 时成立。

### ERR-ENC-04 ★ 中文串里嵌 **ASCII 双引号**会破坏 Python 源码
- **症状**：语法错误 / 字符串提前结束。
- **环境前提**：在中文语境里顺手写 `"`。
- **规避做法**：中文串里的引号用 **`「」`**。
- **怎么发现的**：写中文文案时踩到，固化成规矩。
- **边界**：只在"中文串 + 半角引号"时成立。

### ERR-ENC-05 正则里的 `\b` 与 `_` 的关系（`PROWLARR_API_KEY` 词边界不成立）
- **症状**：`\bPROWLARR_API_KEY\b` 匹配不上。
- **环境前提**：正则的"词字符"是 `[A-Za-z0-9_]`。
- **根因**：`_` **本身是词字符** ⇒ 它出现在两边时，`\b` **不成立**。
- **触发条件**：拿 `\b` 去框一个带下划线的标识符。
- **规避做法**：改用 `(?<![A-Za-z0-9_])` / `(?![A-Za-z0-9_])` 这类显式环顾。
- **怎么发现的**：写凭据扫描的形状正则时踩到。
- **边界**：通用正则知识。★ 这条危险在于**它是静默的**：正则"没匹配上"不会报错。

### ERR-ENC-06 ★★ 站点显示名**可以带空格和括号** ⇒ `(\S+)` **静默漏**
- **症状**：台账里 **`matched_indexers` 永远只有 HDFans**，南洋**0 部** ——
  而 `indexer_seen`（452）与 `attempt.indexers` 里**南洋都在**（那两处不经过这个正则）。
  ⇒ "**一个字段瞎了**"，不是"南洋不行"。
- **环境前提**：Prowlarr 里的站点显示名**可以带空格和括号** —— 本项目那个就叫 **`NanyangPT (南洋)`**。
- **根因**：`_RE_FOUND` 用 `on (\S+) by (\w+)` 取站名。`(\S+)` 只吃到 `NanyangPT`，
  紧跟着要求 ` by `、实际却是 ` (南洋) by ` ⇒ **整行静默不匹配**。
- **触发条件**：站名里有空格 / 括号 / 任何非 `\S` 字符。
- **规避做法**：`on (\S+) by` → **`on (.+?) by`**（后面 ` by (\w+) from dataDir \(` 是**硬锚点**，非贪婪不会越界）。
- **怎么发现的**：`info.2026-09-11.log` 399 行里**漏 91、全部是南洋**；
  09-12 的 770 行里**漏 278、全部是南洋**。HDFans / HDtime 名字没空格，所以**毫发无伤**。
  ★ 它还会**反向污染**：一部在南洋匹配到的片，因为 HDFans 那一行解析成功，
  整条 `matched_indexers` 会被写成 `["HDFans"]` —— **把南洋的功劳记到红豆饭头上**。
- **边界**：★ 影响面**只到 `matched_indexers` 这一列**（`report` 的站点归属、`trend` 的换站决策表）。
  **不影响 `stage`**（`compute_stage` 只看计数）、**也不影响重搜**（`next_retry_at` 走 `indexer_seen`）。
  ⇒ **做种一直在做，只是台账把它记成了别人的。**

### ERR-ENC-07 ★★ **一个名字盖了两种模型** —— 本项目最高频的形状
- **症状**：同一份统计，**两个脚本报出两个数**，而两边都"看着对"。
- **环境前提**：同一个词（一个站名 / 一个包名 / 一个口径名）**被两套东西用成了不同含义**。
- **本项目实例（至少四处）**：
  | 名字 | 两种模型 | 后果 |
  |---|---|---|
  | `NanyangPT` vs `NanyangPT (南洋)` | 日志里的**截断名** vs Prowlarr 的**显示名** | 见 `ERR-ENC-06` |
  | `other_pack` | **全量口径**（所有包一起试、命中即停）的 0 vs **生产口径**（三包共用一个 `farm_root`）的 865 | 两个数不可比，却长得像一回事 |
  | 杂物计数的 **18 / 23 / 26** | 含不含 `__pycache__`、什么时候读的 | 谁看谁重数一遍 |
  | `mbf` vs `my-brilliant-friend-s01-s04` | `pack` 表里的包名 vs `hlink/config.yml` 的 job 名 | 照 job 名去 `init` 会**多建一个包行** |
- **触发条件**：两个来源各自命名，没有一个"权威拼写"。
- **规避做法**：**把口径名印在数旁边**；跨来源比较前先确认两边说的是不是同一个东西。
- **怎么发现的**：四处各自踩过一次，都是当天才发现。
- **边界**：★ 这是**元形状**，不是某一处的 bug —— 见到"两个数对不上"时**先怀疑这个**，
  再怀疑算术。

### ERR-ENC-08 非 ASCII 路径 / `git core.quotepath`
- **症状**：`git status` / `git diff` 里中文文件名显示成 `\344\270\255\346\226\207` 转义。
- **环境前提**：git 默认 `core.quotepath=true`。
- **规避做法**：`git -c core.quotepath=false …`。
- **怎么发现的**：处理中文路径时遇到。
- **边界**：只影响**显示**，不影响内容与匹配。

---

## B.7 工具链层

### B.7.1 git

### ERR-GIT-01 ★ `.gitignore` 的**精确匹配拦不住命名变体**
- **症状**：`.gitignore` 里写了 `.env`，**一份带全部密钥的 `.env.bak.<时间戳>` 照样能被 `git add -A` 提交进去**。
- **环境前提**：本项目会在仓库根目录留下 `.env` 的各种备份副本。
- **根因**：`.env` 是**精确匹配**；`git check-ignore -v` 逐个实测发现
  **`.env.bak`（裸名）/ `.env_bak` / `.env.backup` 全都不被忽略**。
- **触发条件**：命名变体 + `git add -A`。
- **规避做法**：改**前缀全兜** `.env*` + **显式放行** `!.env.example`（纯占位符模板，本来就该提交）。
  `.dockerignore` 同理（原先也是 `.env` 精确匹配）—— ★ **镜像层是删不掉的**，删了也还在历史层里。
- **怎么发现的**：README 1563–1567、1840–1846。
- **边界**：★ **教训（当天连犯三次，故单列）：别让「凭据会不会泄漏」取决于我们猜没猜对文件名。**
  三处同源，都改了：`rm-staging.sh`（**删除**口径）、`.gitignore`（**忽略**口径）、
  `check-deploy-drift.py` 的 `KNOWN_NAS`（**分类**口径）。分类那处配了回归 `tests/test_drift_junk.py`。
  ★ **弯路 #5 是同一个病根的另一面**：`build-farm.sh` 等运维脚本早期**只在 NAS 上手工存在**，
  版本库里没有 ⇒ 两端漂移，谁也说不清跑的是哪一版。修法就是本文件所在的这套东西：
  **白名单（`deploy.sh` 的 28 条）+ 漂移哨兵（`check-deploy-drift.py`）**。
  ⇒ 一般化的判据：**凡是"只在某一端存在"的东西，都迟早会漂移** —— 除非有个能自动回读的对照点。

### ERR-GIT-02 `git ls-files` 与 `git log --name-only --full-history` 的覆盖面差异
- **症状**：以为"仓库里没有这个文件"，其实它**改名过 / 在历史里**。
- **环境前提**：查"某凭据有没有进过库"。
- **根因**：`ls-files` 只看**当前索引**；历史要 `log --name-only --full-history`。
- **触发条件**：拿"路径匹配"当"内容匹配"。
- **规避做法**：两个都查；**并且查不可达对象**。
- **怎么发现的**：凭据审计那一轮。
- **边界**：★ 这正是 `B.10` 元层那条「**把路径匹配当成内容匹配**」。

### ERR-GIT-03 `amend` + force-push 之后**残留不可达对象**
- **症状**：以为"改好了、推新的就干净了"，其实**旧对象还在本地库里**。
- **环境前提**：曾经 amend 过 / force-push 过。
- **根因**：git 对象进库后不自动回收；可达性变了而已。
- **触发条件**：以为"不可达 = 不存在"。
- **规避做法**：扫描要覆盖**不可达对象**（本项目做过这一项）。
- **怎么发现的**：凭据审计时补的一步（任务 #64）。
- **边界**：★ **推上去之后，"删掉"只是多一个 commit，历史里还在** —— 这是本项目"推前扫描"存在的全部理由。

### B.7.2 SQLite

### ERR-SQL-01 ★★ `cp` 生产 SQLite 库**会丢 WAL 里的数据**
- **症状**：诊断脚本拷了一份库来读，读出来的是**脏数据 / 少的行**。
- **环境前提**：`cross-seed.db` 是 **WAL 模式**，随库还有 `-wal` / `-shm`。
- **根因**：未 checkpoint 的数据在 `-wal` 里；只拷 `.db` ⇒ 丢。
- **触发条件**：`cp` 完再读。
- **规避做法**：**直读原库**（走 UNC），配 **`PRAGMA query_only=1`**（实测 0.17s，且能看到 WAL 数据）。
  真要拷副本兜底，**拷前先确认旁边没有 `-wal` / `-shm`** —— 有就说明库正被打开着，此时拷出来是**残的**。
- **怎么发现的**：README 354–355、SUMMARY §13.6 坑 2。
- **边界**：★ 只在**库正被打开（有 WAL）**时成立。★ 一次性口子用完即删 ——
  留着就有被当成「上次验过」的风险（源库会变）。
  **弯路 #6** —— 当初的形态正是"诊断脚本 `cp` 了一份库，漏了 `-wal`，读出脏数据"。
  ★ 这条的坏法很特别：**它不报错，它给你一份看起来完整、其实是旧的库**。
  （与 `ERR-AI-01` 同源：错的读数比没有读数更危险。）

### ERR-SQL-02 `PRAGMA query_only=1` 的作用：防建 WAL / 防加锁
- **症状**：一个只读诊断**改变了**被观测对象（建了 `-wal`、加了锁）。
- **环境前提**：诊断脚本直读生产库。
- **根因**：默认打开方式会写 journal / 加锁。
- **规避做法**：`PRAGMA query_only=1`。
- **怎么发现的**：本项目所有直读生产库的地方都带这道硬闸。
- **边界**：通用。

### ERR-SQL-03 在 UNC / SMB 上打开库的后果
- **症状**：性能差 / 锁行为异常。
- **环境前提**：从 Windows 经 SMB 打开 NAS 上的 SQLite 库。
- **根因**：SQLite 的加锁语义建立在**本地文件系统**的锁原语上，SMB 不完全等价。
- **触发条件**：远程直读 + 有并发写。
- **规避做法**：**只读 + `query_only`**（实测 0.17s 可用）；**绝不远程写**。
- **怎么发现的**：本项目一贯的只读模式就是这么定的。
- **边界**：★ 本项目**只做只读**，所以这条没有造成过事故 —— **别因此把它当成"没问题"**。

### ERR-SQL-04 `StateStore(create=…)`：**库不存在时绝不许把文件建出来**
- **症状**：一次误操作在盘上留下了一个 **0 字节的 `state.db`**。
- **环境前提**：某条只读路径走到了 `StateStore` 的打开逻辑。
- **根因**：`sqlite3.connect()` **会顺手创建**空库文件。
- **触发条件**：路径写错 / 库还没迁移过来。
- **规避做法**：加**根因闸**（`create=` 参数 + 13 个调用点核对），
  库不存在时**连父目录都不能建**。
- **怎么发现的**：留下过物证（`stray-state-db-20260912/state.db`，0 字节，后已删）。
- **边界**：★ 配套：**状态库不在时不许把空库建出来**这条有多条回归钉着（见 `tests/README.md`）。

### B.7.3 curl / HTTP

### ERR-HTTP-01 命令行参数里的**路径会被 MINGW 改写** ⇒ 走 stdin 绕开
> 交叉引用：本体登记在 `ERR-OS-06`。**从 HTTP 工具层的看法**：
> 这是"curl 的路径类参数在 Windows 上不可靠"的一个具体表现。

### ERR-HTTP-02 429 的来源：是**站点**发的，还是**中间件伪造**的
- **症状**：看到 429 就以为被限流。
- **环境前提**：请求经过 Prowlarr。
- **根因**：**Prowlarr 会伪造 429**。
- **触发条件**：不区分来源就读状态码。
- **规避做法**：去 Prowlarr 日志确认站点**真实状态码**：
  `500/502/520/522` = 站点侧故障（等它好，重打一次**根目录** webhook 续跑）；
  只有真 **`429` + `Retry-After`** 才需要把 `delay` 往上调。
- **怎么发现的**：README 176–178、SUMMARY §6.5。
- **边界**：★ 这条与 `A.8` 的"代理读数不能互替"是同一条知识。

### ERR-HTTP-03 `:3060` API 403
- **症状**：所有 API 调用 403。
- **环境前提**：`:3060` 用白名单免密模式。
- **根因**：白名单网段**没覆盖来源 IP**。
- **触发条件**：从新的网段 / 新容器访问。
- **规避做法**：改 `WebUI\AuthSubnetWhitelist`（★ 必须**包含 `172.16.0.0/12`**），
  或改用 `QBIT_AUTH_MODE=password`。
- **怎么发现的**：SUMMARY §3.2。
- **边界**：二选一，别两套同时上。

### ERR-HTTP-04 ★★ 对**大包根**打 webhook 会**废掉整批**
- **症状**：一次 429 之后，后面几百条全部
  `Skipped searching (filtered by temporarily disabled indexers)` —— **实测一次废掉 295 条**。
- **环境前提**：cross-seed 的 webhook 是**单线程顺序处理**。
- **根因**：中途撞一次 429，后续全部被"临时禁用的索引器"过滤掉。
- **触发条件**：对大包根打 webhook（而不是分批）。
- **规避做法**：**_ 日常重搜别打大包根的 webhook** —— 它**不会排除已做种的片子**。
  走 `reseed-state.py drive --limit N`（已排除 `SEEDING`/`MATCHED`）。
- **怎么发现的**：SUMMARY §6.4 / §11.11。
- **边界**：★ 加完站要"重跑一轮全量"时**可以**打根 —— 那是**一次性的、有意的**，
  且**得等**；日常则不行。两者别混。

---

## B.8 定时 / 任务调度层

### ERR-SCHED-01 DSM 任务计划**小时位存错** ⇒ "手动能跑、计划不跑"
> 交叉引用：本体登记在 `ERR-DSM-05`（DSM 侧）。**从调度层的看法**：
> 这是"任务的**定义**与任务的**行为**不一致"，且**没有任何东西会提醒你**。

### ERR-SCHED-02 DSM 任务计划的"**发送运行详情**"：频率高时 = 骚扰
- **症状**：一天 288 封（排空任务每 5 分钟）+ 96 封（驱动任务）的邮件。
- **环境前提**：DSM 任务计划有个"发送运行详情"的勾选。
- **根因**：勾了 = 每趟都发一封。
- **触发条件**：建任务时顺手勾上。
- **规避做法**：**三个都不要勾**。★ 勾了的唯一好处是"能收到失败邮件"，
  但**失败信号在日志文件里更全**（还带退出码和时间线）。
- **怎么发现的**：README 995–1001。
- **边界**：★ 结果一定是"你去建一条过滤规则，**连真正的告警一起过滤掉**" —— 这就是"噪音的代价"。

### ERR-SCHED-03 Windows 计划任务的 `StopOnIdleEnd`
> 交叉引用：本体登记在 `ERR-OS-02`。

### ERR-SCHED-04 ★ 心跳与跨进程节流：`--once` 模式下的**接管机制**
- **症状**：常驻模式卡死**没人接管**。
- **环境前提**：`--once` 模式**每次都是新进程**，所以有"上一批还在跑就跳过"和"卡死 10 分钟后接管"两套机制；
  常驻模式**没有**。
- **根因**：`drive-loop.py` 的 `write_state()` **全文件只有两处调用，都在 `once_round()` 里**
  （批前写 `running_pid` + `heartbeat_ts`；收尾写 `{"running_pid": None, …}`）。
  常驻分支调 `run_round()` 时**没有 `with Heartbeat()` 包着**。
- **触发条件**：走常驻路线（如"迁到容器"）。
- **规避做法**：**要这么走得先改代码**。★ 另：`restart: unless-stopped` **只对容器退出生效**，
  **healthcheck 不健康并不会触发重启** ⇒ 接管机制得再加一个 `autoheal` 侧车（或 DSM 轮询 `docker inspect`）。
- **怎么发现的**：2026-09-13 静态对账（任务 #58）；README 1774–1788。
- **边界**：★ 这条**同时否掉了一份外部分析里"常驻模式代码已存在、不用改代码"的结论** ——
  **跑起来**确实不用改；**要"卡死能接管"** 就得改。

### ERR-SCHED-05 ★★ `attempts.log` 的**四信号** —— 判"到底跑没跑"的唯一可靠方法
- **症状**：分不清"任务没被触发"和"跑了但崩了"。
- **环境前提**：DSM 任务输出默认不保留；`ps` 又不可靠（`ERR-DSM-01`）。
- **四种形状**：
  | 形状 | 含义 |
  |---|---|
  | **没有 `start` 行** | 任务**压根没被触发**（没启用 / 计划窗口不对 / 路径错）|
  | **有 `start` 无 `exit`** | ★ **先别下结论** —— 最常见的是**批次还在跑**（50 部 × 30s ≈ 25 分钟起步，再加回灌前的 90s 收尾），其次才是进程树被强杀 |
  | `exit≠0` | 它自己出错了（`exit=127` = `run.sh` 找不到 python3 或脚本；有 traceback = python 层，多半是版本）|
  | 有 `start` 有 `exit=0` | 正常跑完 |
- **规避做法**：判活**别用** `ps | grep …`（`ERR-DSM-01`）—— 用 `pgrep -f drive-loop.py`，
  或看 `.drive-loop.state` 的 `heartbeat_ts` 在不在涨（批次期间每 60s 刷一次）。
- **怎么发现的**：README 1027–1034、1389–1392；SUMMARY §14.5。
- **边界**：★ **`exit=127` 可能是假的**（部署覆盖了正在跑的脚本，`ERR-SH-01`）——
  判据是「**同一次 `start` 没有配对的 `exit`** 时，先怀疑"脚本被换过"，别急着信那个退出码」。

### ERR-SCHED-06 ★ 部署必须等**批次间隙**
> 交叉引用：本体（为什么）登记在 `ERR-SH-01`。
> **从调度层的看法**：这是"写操作的**时间窗**约束"——见 `A.7` 的 ★。

### ERR-SCHED-07 ★ **"这个文件在哪跑"决定"改了要不要 deploy"**
> 交叉引用：三处路径的完整表在 `A.12`。
> **从调度层的看法**：`<compose>/drive-loop/**` 是**热路径**（拷完即生效），
> 构建上下文副本要 `--build`，`cross-seed` 完全不吃我们的文件。**别拷错一层。**

---

## B.9 硬件 / 物理层

### ERR-HW-01 磁盘水位对写入的影响：满卷时的 ENOSPC 波及哪些操作
> 交叉引用：本体登记在 `ERR-FS-03`。**从硬件层的看法**：这是**容量**问题，
> 不是"代码在某个分支上写错了"。

### ERR-HW-02 NAS 的 SSH 端口默认关闭 ⇒ **没有远程 shell 时的替代通道**
> 交叉引用：本体登记在 `ERR-DSM-09`。**替代通道清单**：
> 只读 → SMB（UNC）/ HTTP 端口；写 → `deploy.sh`（白名单）/ 给用户一条命令在 DSM 里执行。

### ERR-HW-03 共享文件夹与 SMB 视图的差异（`/volume1/docker` 不是共享文件夹）
> 交叉引用：本体登记在 `ERR-DSM-03` / `A.3`。
> ★ 另一条同族、且**更隐蔽**的：**SMB 侧看不见 `@` 前缀的系统目录** ——
> 所以"**没有快照**"这件事**不能靠 `ls` 排除**（`@snapshots` 之类根本不出现在 SMB 视图里）。
> **怎么发现的**：查"有没有备份可恢复"时，`net view` 没有任何备份共享、4 处 `#snapshot` 均不存在 ⇒
> 结论只能写成「**SMB 侧看不见**」，不能写成「**不存在**」。

### ERR-HW-04 File Station 的删除行为：**挪进 `#recycle` 而非真删**
> 交叉引用：本体登记在 `ERR-DSM-07`。

### ERR-HW-05 ★ 机械盘被大量占用 = **一定有人在拷贝**（`ERR-FS-07`）
> 交叉引用：本体登记在 `ERR-FS-07`。**从物理层的看法**：这是**容量异常的暴露面**。

---

## B.10 元层 · AI 常见的"不了解环境"错误模式（**给人看的**）

> ★ 这一节不是给 AI 填充的，是**给人用来识别 AI 何时正在犯这类错**。
> 每条都配一个本项目的真实实例 —— 有实例的才是真的会发生。

| # | 形状 | 本项目实例 |
|---:|---|---|
| 1 | **把"某台机器上没有"当成"环境里都没有"** | 探测到 `/etc/ssmtp/ssmtp.conf` 是 0 字节 ⇒ 推断"没配邮件"（错：DSM 读的是 `synosmtp.conf`，见 `ERR-DSM-04`）|
| 2 | **把两个不同入口当成一条路** | 容器名 `prowlarr` vs 宿主机 `NAS_IP` —— 同一个服务三个地址（见 `ERR-NET-01`）|
| 3 | **把配置文件为空当成功能没配** | 同第 1 条 |
| 4 | **把过滤后的输出当成全量** | `grep '  ok  '` 只数到 374，**整个漏掉 `test_remove_indexer.py` 的 8 条**（见 `tests/README.md`）|
| 5 | **把总数当成已完成数** | 同一段文字里两个数各报各的（如杂物的 18 / 23 / 26，见 `ERR-ENC-07`）|
| 6 | **把代理变量当成目标量** | 用 `Found` 行数当"搜过几部"、把 Prowlarr 的 Query Limit 当站点余额（见 `ERR-SVC-08`）|
| 7 | **把"看起来正常"当成"真的正常"** | 目录不可读时 cross-seed **静默当空**；`uploading` 判据写错时"622 条健康种子一条条被 pause 掉"看起来也完全正常（见 `ERR-AI-02`）|
| 8 | **把路径匹配当成内容匹配** | `git ls-files` 看不到改名后的副本（见 `ERR-GIT-02`）|
| 9 | **把静态读数当成动态事实** | `status=RATE_LIMITED` **不代表现在正在被限流**；`FlareSolverr 装了` ≠ `在用`（见 `ERR-SVC-05`）|
| 10 | ★★ **不估代价就把命令发给人**（本项目新增）| 把 `du -shx /volume1/*` 与整目录 `btrfs filesystem du` 直接发给用户 ⇒ **两条都当场卡住**（见 `ERR-CMD-02`）|
| 11 | ★★ **把说明性文字混进代码块**（本项目新增）| 中文序号 `①` `②` 被用户当成命令敲 ⇒ `-sh: ①: command not found`（见 `ERR-CMD-03`）|
| 12 | ★ **把"我觉得"当成"已证实"**（本项目新增）| 断定 IYUU 的 16 站查询挤占了 Torznab 额度 ⇒ 实际抓的是 HTML，通道互不干扰（见 `A.4` 弯路 #19）|
| 13 | ★ **把工具自身的并发现象读成故障**（本项目新增）| cross-seed 善后清理撞上它自己的并发 ⇒ `ENOENT` 被读成"丢种"，其实相差 320 ms、种早就注完了（见 `ERR-SVC-15`）|

> ★ 第 10~13 条是**本项目自己加的**（1.txt 只列到第 9 条）—— 它们全是"AI 在真实会话里
> **实际犯过**、且**当场没意识到**"的那几种。**判据：能配一个真实实例的才收进来。**

### ERR-AI-01 ★★ **首读基线被念成"新增了"** —— 假警报逐字看着像真的
- **症状**：诊断端吐出「★ 被改写 / 新增（4 条种子受影响，共 **2816** 个文件）」—— **而那天什么写穿都没发生**。
- **环境前提**：所有"只报变化"的判据都有**首读**这一步；基线存在 `.reconcile.state` / `.linkguard.state` 里。
- **根因**：首读 `base={}` ⇒ 差分把**整库**算进 `added`，而接线把 `changed + added` **合并写进同一个键**。
- **触发条件**：第一次建立基线的那个时刻，恰好有人去看诊断端的输出。
- **规避做法**：**首读时 `detail.changed` 必须留空**；正文与 metrics 本来是对的（`first` ⇒ 三个数全 0），
  错的恰恰是"**出事后去查证时读的那份清单**"。
- **怎么发现的**：2026-09-13 18:00 起锚时真的这样（README 706–711）。
- **边界**：★★ **只看日报是发现不了的** —— 这一族的意义就在这。
  ★ 更狠的是：`unclaimed=1` 那条"看起来像修复失效"的告警，**真相是它到那时为止一次都还没在生产上执行过**。
  **物证要从 `.deploy-backup/` 里取** —— 备份存的是**被覆盖前**的内容，所以每份备份恰好是
  **那一次部署之前、磁盘上正在跑的那份代码**。

### ERR-AI-02 ★★ **它会静默地做正确的事之外的一切** —— 判据写错但"看起来完全正常"
- **症状**：一把看门狗按 `endswith("UP")` 判"正在下载"，会把 628 条健康种子**一条条 pause 掉** ——
  而"暂停"**看起来完全正常**。
- **环境前提**：qB 的状态字符串里，**`uploading` 不以 `UP` 结尾**。
- **根因**：判据写错，**不会报错**，只会做错事。
- **触发条件**：任何"用字符串后缀/关键词分类状态"的判据。
- **规避做法**：`uploading` **必须显式算「静止」**；`checkingUP` **故意算「在动」**
  （那是校验失败→就地重下的前奏，正是要盯的）。
- **怎么发现的**：**是测试文件当场抓到的**（`tests/test_linkguard.py`）——
  生产上跑着的那把看门狗用的是**同一个写错判据**，只是那 628 条恰好全是 `stalledUP` 才没误伤。
  ⇒ ★ **测试的价值就在这种"它今天没炸、但它就是错的"上。**
- **边界**：★ 这一族**四个出口**都是"沉默"：① 与基线一致 ② 缩回基线之内 ③ 状态库读不到 ④ 调用方没给参数。
  **后两种同样不发 alert** ⇒ 「**沉默 = 没事**」要多读一步：看正文那一行**有没有值 / 是不是 `n/a`**。
  反过来：**响铃不需要附加判断** —— 响 ⟺ 真的算了、且真有新增。

### ERR-AI-03 ★ `n/a` ≠ `0` ≠ "没事"
- **症状**：把"判据没跑到"念成"今天没事"。
- **环境前提**：日报有八格对账 metrics（TSV 流水**只记数字不记正文**）。
- **三种"读不到"必须分开**：
  | 读法 | 含义 |
  |---|---|
  | **`n/a`** | **这一路没输入**（没跑到），**不是**"没事"。括号里跟 `上次成功 X 前`；从未读到过则写 `★ 从未成功读到过` |
  | **`0`** | 跑了、**确实没有**（判据是好的）|
  | **报 0 且低于基线** | 跑了、且**比基线少** ⇒ 静默采纳新基线，**但正文会把"比基线少 N 条"写出来**（不是无痕）；将来**再长回来会被抓住** |
- **规避做法**：连续几天 `n/a` 是**判据长期失效**，昨天还好今天抖一下是**偶发抖动** ——
  **两句话指向完全不同的处置**，所以必须指得回时间。
- **怎么发现的**：README 1215–1261（对账八格）与 `tests/test_reconcile.py`（120 条）。
- **边界**：★ **`n/a` 那一轮不许抹掉旧基线**（否则会冒充"缩回基线"）。

### ERR-AI-04 ★★ **退出码 0，但断言数根本没进账**
- **症状**：新写的测试打印的是 `  ✓ ` / `  ✗ `，而统计用的 grep 只认既有行形 ⇒
  数出来 `n=0`、**TOTAL 原地不动**，**28 条一条都没进账，而退出码仍是 0**。
- **环境前提**：测试脚本各有各的通过行形（本项目实测**至少四种**）。
- **根因**：**"看着一切正常"**。
- **规避做法**：**宁可改测试去迁就既有行形，也别往 grep 里再加一条** ——
  每加一条就多一处"我猜的行形"，而**漏掉的形态恰恰是没报错的形态**。
- **怎么发现的**：`tests/README.md` 28–32 有原话。
- **边界**：★ 判"全过"看**退出码**，不看数出来的条数。

### ERR-AI-05 ★ "编辑过但从来没人走过"的那条路 —— **绿，但绿可能是规则压根没参与匹配**
- **症状**：`fb_c_farm = 0`（农场那条防线的对账）**恒为 0**，看着是"干净"。
- **环境前提**：`a−b = 0 / b−c = 0` 只覆盖**已经发生过的**那条制度。
- **根因**：实测 1011 条 `Found` 行**全部**是 `[webhook]`、
  组 5 **全部**是原路径 ⇒ `_RE_FOUND` 的 `[inject]` 分支、以及 `_resolve_searchee_to_pack()` 的
  **农场分支流量为 0**。
- **触发条件**：**v3 农场那条防线至今没在生产里被走到过**。
- **规避做法**：**给这条判据配一个自检格**（`组5 落在农场根下` 恒为 0 ⇒ 上面那个 0 **覆盖不到农场**）。
- **怎么发现的**：README 1141。
- **边界**：★ 这是本项目「**判据要带另一侧**」那条约定最锋利的实例：
  **绿是真的绿，但它管不到那条路。**

### ERR-AI-06 `exclusive` 覆盖薄的那道网，**绿了也不代表安全**
- **症状**：推前凭据扫描的"① 值指纹"层 0 命中 ⇒ 误以为"没有凭据泄露"。
- **环境前提**：**本地 `.env` 是开发存根** —— 它的 `TORZNAB_URLS` 用的就是 `.env.example` 里那两个
  `xxxx…/yyyy…` 占位符（实测 sha256 相同）；真实凭据**只存在于 NAS 的生产 `.env` 与 Prowlarr UI 里**。
- **根因**：**拿一份占位符去查泄露**。
- **规避做法**：**真防线是「② 形状正则」**（完全不依赖 `.env`）。
  ★ 且退出码判的是「**这次推新引入了什么**」，不是"仓库里有没有" ——
  否则闸门会次次都红，而**次次都红的闸门等于没有闸门**。
- **怎么发现的**：README 826–841、SUMMARY §18.15。
- **边界**：★ **已知盲区（写在代码注释里，不装作没有）**：一个**真的**由小写字母和连字符组成、
  每段都是纯字母的长密钥，这道闸门**抓不到**。
  ⇒ 建议**不修**：要收口就得换成"熵 / 字符集"判据，而那会**同时**把文档里的英文短语重新变成假阳性 ——
  换来的是一个"次次都红"的闸门。

---

## 附录 1 · 文档自身的边界（**别把这份读成"全知"**）

- 本文是**检索层**，事实的权威版本在 README（怎么做）与 SUMMARY（为什么）。
  **发现冲突时改 README/SUMMARY，再回来更新这里。**
- 本文**不含任何凭据值**。它只说明凭据"住在哪"（`A.5`）。
- 本文里的**地址、端口、路径、版本**都是**2026-09-13 的读数**。
  凡能一条命令读出来的（`docker inspect` / `synoschedtask --get` / `df -h` / `stat`），**用之前回读一次**。
- 已知的**读数冲突**（不是本文的错，是上游文档内部的不一致，已如实标出）：
  每日摘要时间（21:00 vs 21:20）、`/volume1` 可用空间（0 / ~19 / ~30 GiB）。
- 本文**故意不重复登记**的：测试层行形陷阱的完整版（`tests/README.md`）、逐条的调试过程（SUMMARY 各节）。
  —— 依据是本项目的「**同一事实只在一处维护**」。
- ★ **20 项技术弯路**（原 `走过的弯路.md`）**已并入本文**（2026-09-13）：
  它们**唯一**的落点就在 B 部分的 `ERR-*` 条目里，用 `弯路 #N` 标出。原文件已删除，
  **不再有外部出处** —— 上面这条「不重复登记」现在不适用于它们。

## 附录 2 · 这份文档是从哪长出来的

| 来源 | 取了什么 |
|---|---|
| `D:\Projects\1.txt` | 错误大纲的**层与条目骨架**（十节 → B 部分）|
| `D:\Projects\2.txt` | 前提信息大纲的**十二节骨架** + 「硬前提 / 易忽略前提 / 元前提」三档划分（→ A 部分）|
| `README.md`（1850 行）| 环境事实、命令、症状→解法、六条原理与风险清单 |
| `SUMMARY.md`（7979 行）| 卷归属表（§9）、占位符约定（§0）、拓扑（§2）、各处根因（按 §N 交叉引用）|
| `走过的弯路.md` | 20 项技术弯路的归因 —— ★ **已全部并入本文**（2026-09-13，原文件已删）。见下方说明 |
| `tests/README.md` | 测试层踩过的坑 + 行形陷阱（`ERR-AI-04`）|
| `.env.example` / `docker-compose.yml` / `deploy.sh` | 配置键、服务与网络、白名单与三处路径 |
| 会话记忆（`nas-commands-cost-first` 等）| `ERR-CMD-02` / `ERR-CMD-03` 两条最新教训 |

> ★ **维护方式**：新踩一个坑 → 在 B 部分对应层加一条 `ERR-XX-nn`（填满七格）；
> 前提变了 → 改 A 部分对应节。
> **别在这里记"过程"** —— 过程归 SUMMARY。

### 附：20 项技术弯路的落点对照（原 `走过的弯路.md`）

> 2026-09-13 拆解并入。**「弯路 #N」是历史编号**（来自那份已删的文档），
> 只用来跟旧记录对账；**日常检索请用 `ERR-*` 编号**。
> ★ 有 5 条**不落在 ERR 条目上**：它们要么本来就写在 A 部分（前提），要么只进 `B.10` 表（人看的元层）。

| 弯路 | 落点 | 并入方式 |
|---:|---|---|
| #1 Windows 休眠强杀 | `ERR-OS-02` | 该条**本就是为它写的**，边界里已标 |
| #2 `.cmd` LF 截断 | `ERR-OS-01` | 边界补记当初形态 |
| #3 `os.kill` 跨平台 | `ERR-OS-03` | 已有标记 |
| #4 49 条 `DATA_DIRS` | `ERR-DOCKER-01` 边界 | 作为"改配置成本"实例并入 |
| #5 脚本脱离版本控制 | `ERR-GIT-01` 边界 | 作为"只在单端存在⇒必漂移"实例并入 |
| #6 `cp` SQLite 丢 WAL | `ERR-SQL-01` 边界 | 补记"不报错，只给你旧数据" |
| #7 FlareSolverr tag | `ERR-SVC-05` | 已有标记 |
| #8 忘 `--qbit-url` 降级 | **`ERR-SVC-10`**（新）| 新建条目 |
| #9 `--depth` vs `maxDataDepth` | **`ERR-SVC-11`**（新）| 新建条目 |
| #10 退避窗口 300s 失效 | **`ERR-SVC-12`**（新）| 新建条目 |
| #11 `--once` 下 `next_sleep` 没接线 | **`ERR-SVC-13`**（新）| 新建条目 |
| #12 覆盖运行中脚本 | `ERR-SH-01` 边界 | 补记当初形态（`exit=127`）|
| #13 DSM 邮件路径误判 | `ERR-DSM-04` | 已有标记 |
| #14 计划任务小时位填错 | `ERR-DSM-05` | 补记"教训在缺自检" |
| #15 `restart` 刷不掉环境变量 | `ERR-DOCKER-01` 规避做法 | 补记"代价不对称" |
| #16 Query Limit 当节流阀 | `ERR-SVC-08` | 已有标记 |
| #17 禁用站未剥离 env | `ERR-SVC-02` 边界 | 补记"禁用≠不请求" |
| #18 嵌套目录当搜索名 | **`ERR-SVC-14`**（新）| 新建条目 |
| #19 IYUU 流量定性错误 | **`A.4`** ★ 注 + `B.10` 表第 12 条 | **不建 ERR 条目**（它是前提层的一个假设订正 + 一个元层形状）|
| #20 `ENOENT` 误判为丢种 | **`ERR-SVC-15`**（新）+ `B.10` 表第 13 条 | 新建条目 + 元层登记（两层都留）|

> ★ **为什么有的条目进两张表**：1.txt 的成文规则 ——「同一条错误可能在多层出现，
> **两层都登记、互相指路，不要合并**」。#20 就是实例：`B.5.2` 记**事实**（那是竞态不是损坏），
> `B.10` 记**形状**（把噪声读成故障）。合并会丢掉"从哪一层看"的信息。
