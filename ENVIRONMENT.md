---
doc: ENVIRONMENT.md
title: 环境前提与环境坑手册
project: reseed-toolkit（大包拆包 · 单种保种工具链）
generated: 2026-09-13
purpose: >
  给「新会话的 AI」和「接手的人」一份可直接读的环境层事实与归因。
  事前交代（前提）防掉坑，事后归因（错误）能按症状检索。
derived_from:
  - README.md          # ★ 2026-09-20 起是**门面**（怎么用），不再是事实源
  - SUMMARY.md         # DEBUG 层：为什么（体量很大，只在需要时按 §N 查）
  - 走过的弯路.md       # 20 项技术弯路全表（★ 2026-09-13 已**并入本文并删除**，见附录 2）
  - tests/README.md    # 测试层踩过的坑 + 行形陷阱
  - .env.example       # 配置键清单（纯占位符，无凭据值）
  - docker-compose.yml / deploy.sh / patch 文件
precedence: |
  运维事实（主机名 / 路径 / 调度 / 读数基线）**以本文为准**（`A.13`–`A.19`）。
  ★★ **文档分工与优先级见 `CLAUDE.md` 的真源表**（唯一完整版，别在本文复述 —— `A.11`）。
  ★ 历史沿革（本文曾自称"不是事实源"、由 2026-09-20 README 拆分翻转）见 `summary/26`。
contains_secrets: false   # 只指出凭据"住在哪"，不含任何凭据值
contains_internal_topology: true   # ★ 2026-09-20 新增：见下方 ★★ 警告
entry_id_scheme: "ERR-<层>-<两位序号>"
---

# 环境前提与环境坑手册

> ★★ **本文件含内网拓扑 —— 别把它的正文公开贴出去（聊天 / issue / 截图）。**
>
> 它明写了**真实主机名**（`iSunker-DS423`）、**内网 IP**（`192.168.0.7`）、
> 以及**完整的 NAS 路径与共享布局**（`/volume1` 与 `/volume2` 各挂了哪些共享）。
>
> ★ **这几处内容不是「可以脱敏的历史残留」，是「判据本体」。** 本文件里的那些读数和命令
> （`df` 的输出、`synoschedtask --get` 的回读、`stat -c %d` 的设备号对照）
> **只有在这台机器上才成立** —— 同 `A.3` 已经写过的形状：
> `stat -c %d` 只在 NAS 侧有分辨力，在 Windows/SMB 侧恒假。
> 把它们换成 `YOUR-NAS` 占位符，读数就**失去参照物**、命令也变成"照着抄跑不通"。
> ⇒ 这是**取舍**，不是遗漏：**宁可留着真实值并在这里立警告，也不要把判据本体改瞎**。
>
> ★★ **与 README 的分工是互补的**（2026-09-20 起）：
> README 已改成**门面**（通篇 `YOUR-NAS` / `NAS_IP` / `SiteA` 占位符，可安全公开）；
> 本文件是**你这台机器的现状**（含真实值，不可公开）。
> ⇒ **要贴公开内容的场合，贴 README；要判这台机器的现状，读本文件。**

> **为什么要有这一份**：`CLAUDE.md` 是**入口**（去哪找）、`README` 是**门面**（怎么用）、
> `summary/` 是**过程记录**（为什么）—— 三者都**假设读者已经知道环境长什么样**。
> 新会话的 AI 恰恰不知道 —— 它会把「这台 NAS 没有 `sendmail`」读成「没配邮件」，把「容器名」和「宿主机 IP」当成一条路。
> 本文做的事只有一件：**把「环境前提」显式写出来**，并给每条踩过的坑一个可检索的 ID。

## 怎么用这份文档

| 场景 | 读哪 |
|---|---|
| 新会话开局（做任何事之前） | ★ **先读 `CLAUDE.md`（唯一入口：开局三步 + 真源表）**；本文要读 **A 部分**，至少 `A.1 可达性`、`A.3 卷布局`、`A.12 三处路径` |
| 报错了 / 卡住了 | 按症状关键词 grep **B 部分**：`grep -n "症状" ENVIRONMENT.md`，再按 `ERR-` 编号定位 |
| 要给用户发一条命令 | 先看 `B.10` 元层清单第 1、2 条（成本纪律 + 只放可直粘的命令） |
| 想知道某个值（IP / 端口 / 路径 / 版本） | 下面这张「关键取值速查」 |
| 想知道某处历史过程、决策理由 | **不在这里** → README（怎么做）/ SUMMARY（为什么）|
| 想按「第 N 项弯路」找 | 每条的 边界/怎么发现的 里带 `弯路 #N` 标记 → `grep -n '弯路 #' ENVIRONMENT.md`（20 项全在本文，已无外部出处）|

```bash
# 列出全部错误条目（每条的 H3 就是一个 ID）
grep -n '^### ERR-' ENVIRONMENT.md
# 按症状找
grep -n -A2 '^\- \*\*症状\*\*' ENVIRONMENT.md | grep -i '关键词'
# 按「第几项弯路」反查（1..20）
grep -n '弯路 #' ENVIRONMENT.md
```

---

## 关键取值速查（2026-09-13）

> ★ 这里的数字**会变**。凡能一条命令读出来的，就别信写死的 —— 每次用之前回读一次。

### 机器

| 项 | 值 |
|---|---|
| NAS | Synology **DS423+**，DSM **7.2**，主机名 **`iSunker-DS423`** |
| NAS 局域网 IP | **`192.168.0.7`**（`.env.example` 里是 `192.168.1.10` 占位符，别照抄）|
| NAS 侧 shell | ★ **没有** —— SSH(22) 关闭。只读走 SMB，写走 `deploy.sh` 或用户在 DSM 里执行 |
| 开发机 | Windows 11 Pro，MINGW64 / Git Bash，控制台代码页 **GBK** |
| 工作目录 | `D:\Projects\dabaochaibaozuozhongxiangmu\reseed-toolkit` |
| 临时目录 | `C:\Users\艾森克\.claude\jobs\<job>\tmp`（后台任务）／`D:\tmp`（人工诊断）|

### 端口与容器

| 端口 | 服务 | 容器名 | 栈 |
|---:|---|---|---|
| 9696 | Prowlarr | `reseed-prowlarr` | 本栈 |
| 2468 | cross-seed (daemon) | `reseed-cross-seed` | 本栈 |
| 8191 | FlareSolverr | `reseed-flaresolverr` | 本栈 |
| 3060 | **qBittorrent-reseed** | `qbittorrent-reseed` | **独立栈**（`qbittorrent-reseed_default`）|
| 8787 | IYUU Plus | `iyuuplus_ssd` | ★ **别人的**，不在本项目管辖范围 |
| 8085 | 另一套 qB | — | 别人的 |
| 3020 | opencd | — | 别人的 |

- Docker 网络：本栈 `reseed-net`（bridge）+ **external** `qbit-net` = `qbittorrent-reseed_default`。
- `PUID=1026` / `PGID=100` / `TZ=Asia/Shanghai`。
- 生产栈目录：`/volume2/docker_ssd/prowlarr_cross-seed_autohardlink`，SMB 视图 `//iSunker-DS423/docker_ssd/prowlarr_cross-seed_autohardlink`。

### 版本

| 组件 | 版本 | 备注 |
|---|---|---|
| qBittorrent (:3060) | **4.6.5**，WebAPI **2.9.3** | `free_space_on_disk` 在 `sync/maindata → server_state`，**不在** `app/preferences` |
| cross-seed | **v6.13.7** | 官方镜像，**完全不吃我们的文件**（改配置只能改 `config.js` 挂载）|
| NAS 上的 python3 | **3.8.15**（DSM 自带，Package Center 无更新）| ★ 整条链路的单点依赖，见 `ERR-PY-01` |
| `/volume1` 文件系统 | **BTRFS**（`/dev/mapper/cachedev_1`）| ⇒ reflink / COW 可用 |

### 路径

| 用途 | 路径 |
|---|---|
| 大包源数据 | `/volume1/video/download/movies/…`、`/volume1/video/download/TV/…` |
| 硬链接农场（当前 `DATA_DIRS`）| `/volume1/video/download/reseed/reseed_farm` |
| 单片做种数据 | `/volume1/video/download/reseed/reseed_singles/<站点>/<发布名>` |
| 第三方媒体库目录（**已查清**，2026-09-17） | `/volume1/video/link` —— ★ **不是本项目的东西，也不是软链**（它本身是个**真目录**）。形状 = **moviepilot 风格的媒体库**：149 个媒体目录按**分类**摆放（`电影`/`电视剧`/`欧美剧`/`华语电影`/`未分类`/`综艺`/`纪录片`/`日韩剧`/`国产剧`/`国漫`/`儿童`…），每个片子目录里是 **`.nfo` + 封面图（`poster.jpg`/`backdrop.jpg`/`fanart.jpg`…）+ 媒体文件**；含 **TMDB 命名**（如 `白莲花度假村-2021-[tmdb=111803]`）。★ **7.47 TiB 是「表观」** —— 抽样见 `蝙蝠侠：影之谜 (2005)` 一个 **74 GB `.iso`**（全库至少 3 个 iso：62/74/91 GB），表观体积由这些大文件撑起。**是否硬链接 / 是否占真实空间，SMB 侧判不了**（正是 §23.2 那条：`stat -c %d` 只在 NAS 侧成立；Windows 侧 `[ -e /volume1/… ]` 恒假 ⇒ 必须换 `//YOUR-NAS/video/…` 才看得见）—— 要定论须在 NAS 上跑。★ 149 目录里 **23 个是空的**（分类占位）。 |
| 状态机库 | `<compose>/drive-loop/hlink/state.db` |
| cross-seed 库 | `<compose>/cross-seed/cross-seed.db` |
| 链接守护基线 | `<compose>/drive-loop/scripts/.linkguard.state` |
| 对账/基线状态 | `<compose>/drive-loop/scripts/.reconcile.state` |
| 批次心跳状态 | `<compose>/drive-loop/scripts/.drive-loop.state` |
| 通知 spool | `<compose>/notify/spool/*.txt` |

### 卷与容量

| 卷 | 设备 | 容量 | 可用 |
|---|---|---:|---:|
| `/volume1` | `/dev/mapper/cachedev_1` | 56 TB | ★ **已用 91% / 可用 5.33 TiB**（2026-09-17 实测，见下 ★）|
| `/volume2` | `/dev/mapper/cachedev_0` | 448 GB | **304 GB**（33%）|

> ★★ **2026-09-17 更正：`/volume1` 不是"已用 100%"**（本节原写"已用 100%"，与实测不符）。
> **NAS 侧原生 `df -h` 与 `df -B1` 两次实读**（21:32 / 21:34，同一会话）：
> ```
> /dev/mapper/cachedev_1   56T   51T  5.4T  91% /volume1
>   /volume1 可用 = 5860247740416 B = 5457.78 GiB = 5.3299 TiB
> ```
> 与**本机 SMB 探针**（`tools/doc-audit/04_probes.py`，走 `//iSunker-DS423/video`）13:10 报的
> **5457.79 GiB** 一致（**差 0.01 GiB = 取整**）⇒ **探针读数可信、可复现**。
> ★ 判据落点：`scripts/diag/chk-volume1-free.sh`（NAS 侧只读，一次性诊断件）。
>
> ★★★ **一条必须记住的口径差**：群晖自己的 **Storage Analyzer 周报**
> （`scripts/diag/sa-volume-usage.py` 可读）在同一时刻报 **Volume 1 Used = 99.9%**，
> 反推可用只有 **~57 GiB** —— **与 `df` 差 5.27 TiB**。**两者都没错，但答的不是同一个问题**：
>
> | 口径 | 说的什么 | `/volume1` 的读数 |
> |---|---|---|
> | `df` / `statvfs` | **实际写入的数据**能用的余量 | 可用 **5.33 TiB**（91%）|
> | 群晖 SA 报告 | **_chunk 已分配_** 的比例 | **99.9%** |
> | `btrfs filesystem usage` | 含未分配 chunk 的估算余量 | `Free (estimated)` **7.44 TiB** |
>
> 依据（NAS 侧实读）：`btrfs filesystem df` 的 `Data,total=55.85TiB,used=50.51TiB`、
> `usage` 的 `Device allocated: 56.06TiB / Device size: 58.17TiB` = **96.4%**、`Device unallocated: 2.11TiB`。
> ⇒ **SA 看的是 chunk 分配率**，BTRFS 把块分给 chunk 后即使没写满，SA 也按"分掉了"算。
> ★ **别把 SA 的百分数与 `df` 的可用空间混用**，更**别拿 SA 的 `Used%` 去反推可用 GiB**。
> ★ 该表 `volume_usage.csv` 只有 `Size` 与 `Used(%)`（**没有可用空间这一列**），
> 且 0.1% 一格 = **57 GiB**、舍入 **±29 GiB** ⇒ 它**本来就不适合**当可用空间的判据。
>
> ★ **对上游的影响**：`SUMMARY §26.4` 写的「`df -h /volume1`  # 5.98 TiB 可回收」**从未兑现过**
> —— 那只是一句估算。`#129` 的删除**与这 5.33 TiB 无关**（SA 记到 09-09 仍是 100.0%，
> 即删除之后 SA 仍报满 ⇒ 空间不是那次删除带来的）。

共享文件夹归属 —— **两个卷都长成 `/volumeN/...`，光看路径看不出它在哪块盘上**：

| 卷 | 共享文件夹 |
|---|---|
| `cachedev_1`（56 T，**满**）| `video`、`Download`、`homes`、`docker`、`drive`、`web`、`web_packages`、`ActiveBackupforBusiness` |
| `cachedev_0`（448 G，**304 G 可用**）| `docker_ssd`、`qb_temp`、`技术文档`、`000 临时文件夹nas（内容可删除）` |

> ⚠ **`/volume1` 的"可用"读数不是常量**。不同时刻的记录：SUMMARY §9 记 **0**、README 多处记 **~19 / ~20 GB**、2026-09-13 实测 **31.91 → 31.76 GiB**（连量 4 次还在动）、2026-09-14 实测 **17.83 GiB**、**2026-09-17 实测 5457.78 GiB（5.33 TiB）**。
> ⇒ 结论是「**读数会动，量级从 0 到 5.3 TiB 都出现过**」，**别把任何一个单次读数当成恒定值**，也别拿它去算减法。
> ★★ **2026-09-17 更正：量级已不在"接近 0"那一档**。上面那句「接近 0、且在数 GiB 级浮动」
> 描述的是 09-13/09-14 的窗口；**09-17 实测是 5.33 TiB**。⇒ **别拿"接近满"当当前前提**
> （`ERR-FS-03` 的 ENOSPC 级联虽然仍值得防，但它**不再是"现在就差几十 GB"的状态**）。
> ★ **为什么从 0 变到 5.3 TiB，仍未定论** —— 只能记成「读数在动、且未必是本项目动的」。
>
> ⚠★ **"是谁动了它"没有定论 —— 别读成"水位回升"。** 09-13 → 09-14 之间 NAS 自测 `df -h` 从
> **0** 动到 **17.83 GiB**；但**同一天**还另有一处读数是 31.91 → 31.76 GiB，**两者并未对齐**。
> 且 09-13 那批 `reseed_singles` 残骸虽然已经不在了，**"它到底释放了多少"仍未确认**
> （见 `ERR-SVC-16` 边界）。⇒ 只能记成「**读数在动、且未必是本项目动的**」。
> 同 `ERR-SVC-09` 那条：`status` 是残留标签得看时刻，这里「可用空间」是**残留读数**，
> 得看**是什么动过它**。
>
> ⚠ **另一处文档自相矛盾**（同一份 README 里）：每日摘要的时间，任务表写 **21:00**、排错段写 **21:20**。
> 实测以 NAS 上 `sudo /usr/syno/bin/synoschedtask --get` 为准。**这正是本项目"同一事实只在一处维护"那条规矩被破的例子**，见到别惊讶。

### 当前配置开关（`.env`）

| 键 | 值 | 含义 |
|---|---|---|
| `LINK_TYPE` | **`reflink`** | 2026-09-13 起；★ 与 `MATCH_MODE` **互锁**（见 `ERR-FS-04`）|
| `MATCH_MODE` | `partial` | 只在 `LINK_TYPE=reflink` 时才作数 |
| `SKIP_RECHECK` | `false` | ★ 它**不是**安全网 —— 见 A.9 |
| `DATA_DIRS` | **1 条**（农场那条）| 从 49 条收成 1 条，`FARM_SOURCES` 保留原始源清单 |
| `TORZNAB_URLS` | **4 条**：`/1` HDtime、`/2` HDFans、`/3` BTSCHOOL、`/4` NanyangPT | ★ 值里每条各带一个 `apikey=` |
| `QBIT_CATEGORY` | `reseed-singles` | |

### 调度时刻表

| 任务 | 频率 | 脚本 |
|---|---|---|
| 排空 spool | 每 **5 分钟** | `sh <compose>/notify/notify-spool.sh` |
| 每日摘要 | 每天 **21:20**（★ 实测值；文档曾写 21:00）| `… notify-spool.sh --digest` |
| 驱动跑批 | 每 **15 分钟** | `sh <compose>/drive-loop/run.sh` |
| IYUU 辅种 | 每天 **01:45** | ★ 别人的系统 |
| IYUU 另一条 | 每天 02:34 | 同上 |

- ★★ **2026-09-19 勘误 —— 上表「每日摘要」那一行，任务当前*不存在*。**
  `reseed-notify-digest` 已不在 DSM 任务清单里（`summary/14` 2026-09-12 全量枚举只剩
  `reseed-notify-drain` + `reseed-drive-loop`）。⇒ 它的 **`--digest` 无人调用**，
  所以**一封摘要邮件都不会发出去**，而 `.tsv` 里天天照有「每日台账」那一行
  （`batch` **不发信**，只归档）。★ 判据与完整因果见 **`ERR-SCHED-11`**。
- ★ 那三个数**都以"实测 `synoschedtask --get` 回读"为准**，不许照抄本表 ——
  本表 2026-09-12 抄了一次 `21:00`，**四处同时过期**（同 `B.10` 第 23 条）。
- DSM 三个任务**用户都选 `root`**、**都不要勾「发送运行详情」**（勾了 = 一天 288 封骚扰邮件）。
- drive-loop 批间间隔：**45 min ~ 180 min**，按站点反馈动态调（`next_sleep()` 三档分级）。

---

---

# A 部分 · 前提信息（事前交代）

> 这一部分是**先告诉 AI 这些事实**，一整类错误就不会发生。
> 每节末尾的 ★ 是「**AI 最常忘的那一条**」。

## A.1 物理与网络拓扑

**两台机器**：一台 Synology DS423+（DSM 7.2）+ 一台 Windows 开发机。就这两台。
NAS 上还跑着**别人的**容器（IYUU Plus、另一套 qB、opencd），它们不在本项目管辖范围，但**会影响本项目**。

**可达性是有方向的、逐服务的** —— 不是「网络通不通」这种全局属性：

| 从 | 到 | 走什么 | 备注 |
|---|---|---|---|
| Windows | NAS 文件 | **只有 SMB / UNC**（`//iSunker-DS423/...`）| ★ 22 端口拒绝，**没有 SSH** |
| Windows | NAS 上的服务 | TCP 直连 `192.168.0.7:<端口>` | 9696 / 2468 / 3060 / 8191 / 8787 都已发布到宿主机 |
| NAS 宿主机（`drive-loop.py` 就跑在这）| 容器 | **用 LAN IP + 端口映射** | 宿主机**不解析容器名** |
| 容器 | 另一个容器 | **服务名**（`http://qbittorrent-reseed:3060`）| 走 `reseed-net` / `qbit-net` |
| 容器 | 宿主机 LAN IP | ✗ **timeout**（hairpin NAT）| 见 `ERR-NET-01` |

> ★ **AI 最常忘的一条**：**「可达性」是逐服务、有方向的** —— 同一个目标（qB :3060），
> 从 Windows 用 `192.168.0.7:3060`、从容器用 `qbittorrent-reseed:3060`、从 NAS 宿主机用 `192.168.0.7:3060`。
> **这三个写法在代码里同时存在，两边是故意不同的**，别互相"纠正"（`docker-compose.yml` 顶部有原话）。

## A.2 权限与操作边界

**谁能做什么**：Windows 侧只能**读**、只能写 SMB 可达的路径；**NAS 上的写操作必须人来做**（没有 shell）。

**明令禁止的动作**（违反会造成不可逆损失）：

- ❌ 对 NAS 的 **UNC 路径跑 `rm`**（SMB 上没有回收站，glob 打错一次不可逆）
- ❌ `docker system prune -a`
- ❌ 对链接文件 `chown -R`（硬链接与源是**同一个 inode**，会改掉**源文件**属主）
- ❌ 走 **DSM File Station 删除**（只是挪进 `#recycle`，文件仍在盘上）
- ❌ 别在**批次运行中** `deploy.sh --apply`
- ❌ 别用 **DSM File Station** 做删除；别往 `/volume1` 写真实文件
- ❌ 别对大包根打 webhook（`ERR-HTTP-04`）
- ❌ 别 `cp` 生产 SQLite 库来做诊断（丢 WAL，`ERR-SQL-01`）
- ❌ ★★ **别在 NAS 上"顺手放文件"** —— 见下条（2026-09-17 用户点出）

### ★★ A.2.1 NAS 上**不许乱放文件**（2026-09-17 立，用户原话）

> "本项目能不能不要再 NAS 里面乱放文件啊，各种小文件，奇奇怪怪的冒出来，
>  一次性的用完就自动删掉，经常用的，就放在生产地址，**NAS 其他位置不要乱放文件**！"

**规则**（按文件寿命二分，**没有第三类**）：

| 文件寿命 | 放哪 | 例 |
|---|---|---|
| **一次性的**（中转包、验证脚本、导入的 tar） | ★ **专用的临时目录 + 用完立刻删** —— 目录名要**一眼看出是临时的**（如 `<compose>/_tmp-<用途>/`），**不许放共享根**、**不许放 `/volume1`** | `_drive-loop-img.tar.gz`、`_tmp129/` 下的 `_del.sh` |
| **长期用的**（脚本、配置、日志、状态） | ★ **只在生产地址**：`/volume2/docker_ssd/prowlarr_cross-seed_autohardlink/` 下**它该在的那一层**（见 `deploy.sh` 的 FILES 映射）；★ 或**入库**（在 NAS 上手工跑、只进 `scripts/diag/check-deploy-drift.py` 的 `LOCAL_ONLY`） | `drive-loop/scripts/*`、`notify/*`；`scripts/diag/chk58.sh`、`scripts/diag/chk-volume1-free.sh` |

> ★★ **一条代价（2026-09-17 实测，它把这条规则的边界划出来了）**：
> **「用完就删」是对的，但"判据"不能跟着脚本一起删。**
> 实例：`#58` 的 NAS 侧验证脚本当初叫 `<compose>/_tmp58/_chk58.sh`，按本表当**一次性件**
> 放临时目录、用完删掉。**结果「五段只读验证」要验哪五段，一处都没留下** ——
> `git log --all -p -S chk58` **搜不到任何代码**，全仓只有两处提到它的**名字**。
> ⇒ 后来只能**从零重建**（`scripts/diag/chk58.sh`）。
> ⇒ 判据：**删一次性件之前，先问"它验的是什么"有没有落到文档或仓库里**；
>   没有 ⇒ 要么把它**升级为长期件**（入库 + `LOCAL_ONLY`），要么把**该验什么**写进
>   `SUMMARY`。★ 与 `B.10` 第 14 条同族：**删掉脚本 ≠ 那件事不用验了。**

**为什么这条要立**（两条实测的代价，不是洁癖）：
- ★ **`/volume1` 是 100% 满、且正被删除占着 I/O** —— 往里放 46 MB 是**添乱**（本次实际发生过：
  把镜像包放进了 `video` 共享，用户当场指出**位置不对**）；
- ★★ **散落的文件会被哨兵当成"未知"**，而**"常红"等于"没有哨兵"**（同 `#70` 第 18 条那族：
  真漂移会被淹在"已知的乱"里）。本次就是靠**哨兵**才发现 `.env.bak-*` 与那一堆未登记文件。

**判据（可执行，不是口号）**：
1. **新增文件前先问**："它三个月后还在吗？" —— 不在 ⇒ 临时目录 + **写进清理步骤**；
2. **临时目录必须有主人**：谁建的谁删，**并在同一个流程里安排**（别指望"回头再清"）；
3. ★ **哨兵会替你查**：`python scripts/diag/check-deploy-drift.py` 的 A 方向会报"未登记/未知" ——
   **它红了就要么登记、要么删**，不许让它常红。

**要在"批次间隙"才能做的事**：`deploy.sh --apply`（覆盖正被 `sh` 读的脚本）、`--force-recreate`。
（★ 判「批次在不在跑」看 `.drive-loop.state` 的 `running_pid`；**逐个动作的允许条件表 + 改动前自检清单在 README「开工前的两道门」**。）

**一站一批的动作**：qB 的 `setLocation` —— 按 `save_path` 分组、一批一次调用（实测 500 条只有 3 个
`save_path` ⇒ **3 次调用，不是 500 次**）；它触发重校验，而 `max_active_checking_torrents=1`。
**唯一要避开的是种子的在途状态**：`checkingDL` / `checkingUP` / `moving` / `allocating`。

> ★ **AI 的边界**：**只读随便做；写操作一律走「给命令、人执行」。**
> 这不是保守 —— 是环境**没有 SSH**，本机根本不能在 NAS 上执行命令。
> 判活、判存在、判容量，全部要能在 SMB 或 HTTP 端口上做完。

## A.3 存储布局与容量水位

**各卷分工**：`/volume1`（机械盘 56 T，媒体 + 源 + 农场 + linkDir，**已满**）、
`/volume2/docker_ssd`（SSD，compose + 配置 + 库）。★ 归属表见上面「关键取值速查」，别靠路径猜。

**硬链接约束**：源与 `LINK_DIR` **必须同一物理卷** ⇒ 这决定了几乎所有路径决策
（`build-farm.sh` 用 `stat -c %d` 比设备号，比的正是这件事 —— ★ 该判据**只在 NAS 侧成立**，见本节末 ★）。
`/volume1/video` 与 `/volume2/docker_ssd` 是**两个卷**，所以 `LINK_DIR` 不能挪到 SSD。
（★ 2026-09-14 更正：这里原写 `/volume1/docker_ssd` —— 那**正是本段上一行那条错法**的实例。）

**卷归属怎么核**（★ 2026-09-14 复核：下面两条**独立来源逐格吻合**）：
- **从 Windows** 跑 `df -h //iSunker-DS423/<共享>` —— 它按**底层文件系统**分组，实测 8 个共享
  **8/8 正确**（`video`/`Download`/`homes`/`drive`/`web`/`docker` → 56 T / 100%；
  `docker_ssd`/`qb_temp` → 448 G / 33%）。
- **群晖自己的报告**：Storage Analyzer 的 `share_list.csv` **带 `Volume` 列**
  （`…/home/存储空间分析器/synoreport/StorageAnalysisReport/<时刻>/csv/`，**每周三 04:07** 自动生成、
  无凭据；`scripts/diag/sa-volume-usage.py` 已在读它）。
- 原来的 `ContainerManager/all_shares` **只在 NAS 上**读得到 —— 上面两条是
  **Windows 侧第一次能独立复核卷归属**的通路（本机没有 SSH，见 `A.2` 的 ★）。

**水位与行为**：`/volume1` 接近满 ⇒ **ENOSPC 会级联** —— 波及 cross-seed 注入、IYUU 辅种、
甚至**日志**（实测：卷剩 0 字节时 `say` 的 stdout 坏掉，`set -e` 带走整趟，见 `ERR-SH-06`）。

**「看着在但不在共享文件夹里」** —— ★ **2026-09-17 实测更正：这句话原来是错的。**
`/volume1/docker` **是**共享文件夹（DSM 自己的 `share_list.csv` 列为 `Shared Folder / Volume 1`），
`//iSunker-DS423/docker` **UNC 可达**（`ls` / `stat` / `df` 都能跑，`df` 报 56 T = `cachedev_1`）。
真正成立的是**较弱的那句**：**它不在"枚举"里** —— `net view iSunker-DS423` 与本机 `ls //iSunker-DS423/`
**都只列出 5 个**共享，漏掉 8 个**能访问的**（`Download` / `docker` / `qb_temp` / `web` /
`drive` / `homes` / `web_packages` / `ActiveBackupforBusiness`）。
⇒ ★★ **「枚举里看不见」≠「不是共享文件夹」**（`net view` / `ls` **≠ 全量共享表**，见 `ERR-HW-03`）。
★ 可用的**负对照**：`ls //iSunker-DS423/nosuchshare_zzz123` → `No such file or directory`
⇒ MSYS **会**区分真假共享名，所以"可达"不是幻觉。

**农场为什么能在满卷上跑**：农场与单片全是**硬链接，不占数据块**。
★ 反过来说：**机械盘被大量占用 = 一定有人在"拷贝"而不是"链接"** —— 2026-09-13 那批
48.20 GiB 旧根残留就是这么暴露的。

> ★ **AI 最常忘的一条**：**「同一物理卷」不等于「同一路径前缀」。**
> 判断硬链接可行性要看**设备号**，不是看字符串开头像不像。
>
> ★★ **但那枚探针只在 NAS 侧成立 —— 搬到 Windows/SMB 侧，它对「任何一对」都说「不同」。**
> 2026-09-14 实测 `stat -c %d //iSunker-DS423/<共享>`：**十个共享十个互不相同的号**，
> 含**同属一个卷**的 `video`(403405124) / `Download`(864492843) / `homes`(2727544675) /
> `drive`(12209342) / `web`(2153088345) / `docker`(4258917541) ⇒ **分辨力为零**
> （它对 `docker_ssd` ≠ `video` 恰好说对，**是巧合**）。
> **Windows 侧的对照量是 `df`**（见上面「卷归属怎么核」）。
> `build-farm.sh` 的 `stat -c %d` 闸**跑在 NAS 上，是对的**；但该脚本的注释自承
> **也可以从 Windows/SMB 跑** —— 真那么跑，它会把**每一个源目录**都判成
> 「与农场不在同一物理卷，跳过」。

## A.4 服务清单与资源共享

| 服务 | 谁写的 | 跑在哪 | 读什么 / 写什么 |
|---|---|---|---|
| Prowlarr | 别人 | 容器 `reseed-prowlarr` | 存**所有 PT 站的 cookie/passkey** ⇒ 它的 API key 等于总钥匙 |
| cross-seed | 别人 | 容器 `reseed-cross-seed` | 按内容匹配 → 建链接 → 注入 :3060 |
| FlareSolverr | 别人 | 容器 | ★ 装了、在跑，但**一个 tag 都没挂 ⇒ 实质空转** |
| reseed-orchestrator | **我们** | **一次性 CLI**（无 daemon）| 预检/驱动/汇报 |
| qBittorrent-reseed | 别人 | **独立栈** | 单种做种（分类 `reseed-singles`）|
| IYUU Plus | 别人 | 容器 `iyuuplus_ssd` | ★ **也在往我们同一个 qB 辅种** |
| drive-loop / notify | **我们** | **NAS 宿主机**（不是容器）| DSM 计划任务拉起 |

> ★ **AI 最常忘的一条**：**「这个资源只有我们在用」是一个需要验证的假设 —— 本项目栽过。**
> **IYUU 和 cross-seed 共用同一个 qB（:3060）和同一个 `reseed_singles/` 目录**，
> 这是本项目最容易被忽略的共享。qB 里的种子**不全是我们的**（有 `IYUU自动辅种` 这个 tag）。
> ★ 但已查清：IYUU 抓的是**网页**（`details.php`/`download.php`），**不是 Torznab**
> ⇒ 两条通道不同，额度台账的分母**没有被污染**（SUMMARY §16.6）。
> ★ **差点搞错的就是这一步** —— 当初**先入为主**地断定"IYUU 的 16 站查询会挤占 Torznab 额度"，
> 并据此准备改配置；溯源（看它实际请求的 URL）才发现是 HTML 通道。
> **弯路 #19**，形状见 `B.10` 第 12 条。★ 这条同样适用 `A.8` 的半衰期：
> 前提是**站点把「API 查询额度」和「网页浏览」分开计** —— 站点规则页一变，结论要重验。

## A.5 数据资产与敏感度分级

**哪些文件含凭据**（只列位置，**不含值**）：

| 位置 | 里面是什么 |
|---|---|
| `.env`（生产）| Prowlarr 应用级 key、qB 密码、`TORZNAB_URLS` 里每条的 `apikey=` |
| `.env.bak.*` / `.env.bak` / `.env_bak` / `.env.backup` | ★ **与 `.env` 同等敏感** —— 是完整明文副本 |
| `prowlarr/` | 所有 PT 站的 cookie / passkey + db |
| `cross-seed/`（含自建 db）| db 的 `indexer` 表 `url` 列**含 apikey** |
| `notify/notify.conf` | 收件人邮箱（不含密码 —— 密码只在 DSM 里）|
| IYUU 的 `cn_client` / `cn_sites` | 别的系统的凭据 |

**为什么值钱**：Prowlarr 应用级 key 能**完全控制 Prowlarr**，而 Prowlarr 存着所有 PT 站的 cookie。

**哪些目录是"垃圾场"**：`#recycle`（File Station 删的进这里，**不是真删**）、`_cleanup-*`。

> ★ **AI 最常忘的一条**：**「配置文件表」要在读之前就假定含密钥 —— 按「值长什么样」脱敏，不按「键名叫什么」。**
> 按键名匹配会漏掉嵌在结构里的凭据：`TORZNAB_URLS` 里逗号分隔的多条 URL 各带一个 `apikey=`、
> Prowlarr 的 `indexer.fields` 里塞着 cookie/passkey、`options`/`fields` 里还嵌一层。

**本项目自我施加的额外纪律**（比"别泄密"更严，是为了**不制造新的泄露面**）：

- 绝不打印 qB 种子的 `tracker` 或 `content_path`
- 绝不原样导出 IYUU 配置表
- 绝不打印 Prowlarr 的 `indexer.fields`
- `cross-seed.db` 的 `indexer` 表 `url` 列含 apikey —— **只选需要的列**
- `--prowlarr-api-key` **不许出现在命令行上**（会留在 shell 历史与进程列表里），只能从 `.env` 读
- 需要回显 `TORZNAB_URLS` 核对时先脱敏：
  `grep '^TORZNAB_URLS=' .env | sed -E 's/(apikey=)[^,&]+/\1<redacted>/g'`
- 推前必跑 `python scripts/diag/scan-secrets.py`（0 才能推）

## A.6 人的操作界面与响应节律

- **命令怎么执行**：用户从 Windows 粘贴到 **DSM 脚本框** 或 **Git Bash**。
  ★ Git Bash 是 **MINGW64**，**括号粘贴模式（`^[[200~`）会破坏多行粘贴** ⇒ **给 NAS 的命令一律写单行、一次一条**。
  ★ **多行 `\` 续行会被 DSM 脚本框折叠**。
- **能响应多快**：单人操作、**无常驻在线**；**批次 25 分钟起**（50 部 × 30s + 收尾 90s）。
  设计**不能要求"5 分钟内响应"** —— 需要人盯 5 分钟的事，实际不会被执行。
- **输出往哪放**：DSM 脚本框的输出要**重定向才留痕**，默认不保留。
  ⚠ 但**别重定向到 `/volume1`**（满卷）。要看输出就看 `drive-loop.log` / `notify/log/` / `attempts.log`。
- **喜欢什么形式**：**可复制粘贴的单行命令**，不要"你编辑一下这个文件"。

> ★ **AI 最常忘的一条**：**设计方案时要考虑"人的响应延迟"。**
> 需要人盯着 5 分钟的事，实际不会被执行 —— 所以本项目所有自动化都挂在 NAS 的 cron 上，而不是"提醒人去点一下"。

## A.7 时间节律与调度窗口

| 什么 | 什么时候 |
|---|---|
| cross-seed 重扫 | ★ **没有自动重扫** —— 全靠 drive-loop 驱动 |
| drive-loop 唤醒 | 每 **15 分钟**；批间 45~180 分钟 |
| IYUU 辅种 | 每天 **01:45**（另一条任务 02:34）|
| 每日摘要 | 当天首批（约 `00:0x`）出日报；发信 21:00–21:20 |
| 通知排空 | 每 5 分钟 |

**★★ 每轮满载 / 待搜池一直不回落 ⇒ 查「有没有站长期退避」（**这是 `due_indexers` 的一个死循环形状，2026-09-19 实测**）**：

| 项 | 说明 |
|---|---|
| **机制** | `due_indexers()` 的判据是 `indexer_seen[站]` 的**最后搜索时间**；**`last is None`（从没搜过）⇒ 判「该搜」**（`state.py` 原话：*从没在这个站搜过 → 该搜*） |
| ★★ **死循环怎么形成的** | 某站**长期退避** ⇒ cross-seed 每次都跳过它 ⇒ **`indexer_seen` 里那个站的时间戳永远写不进去** ⇒ **永远 `last is None`** ⇒ **每一轮都把该片重新入队** ⇒ **待搜池永不回落、批批满载** |
| **本项目的现场** | 2026-09-19 实测：`HDtime` 退避到 `09-20 03:50`（`RATE_LIMITED`），而 `run-resident.sh` 的 `--indexers` **含 HDtime** ⇒ 每轮 `[第 N 轮] 计划 40/43/4 条`，`回灌：搜过 595 / 匹配 443` **连续两轮逐字相同**、`新增做种 0` ⇒ **同一批片子被反复搜** |
| **判据（怎么认出来）** | ① `drive-loop.log` 里**相邻两轮的 `回灌：搜过 N` 完全相同**且 `新增做种 0`；② 每轮条数**恒等于**该包规模（40/43/4）；③ `state` 报的"待搜"**远小于**日志的"计划 N 条"（见 `ERR-SVC-19`）|
| ★ **和 `--cadence-days` 无关** | 拉长周期**治不了它** —— 因为病根是 `last is None`，**不是**"周期太短"（那条规则在 `last` 有值时才算）|
| ★ **真正的处置** | **让那个站不再是"从没搜过"**：① **修上游**（`A.8`：HDtime 对 Prowlarr 返 5xx ⇒ 唯一抓手）② 或**把它从 `--indexers` 里暂时摘掉**（代价：它的历史时间戳缺失，摘了之后那些片子不再为它入队）③ **别用"清退避"**（`ERR-SVC-17`：代价 +24 小时）|
| ★ **降频杠杆（另一件事）** | `--cadence-days 30`（`run-resident.sh` 一行，只影响 `last` 有值的站）；按站设 `--cadence "HDtime=30"` |

**★ 加新站也是同一个形状**（**一次性**）：`--indexers` 每加一个站，对每部片都是"这个站我还没搜过"
⇒ 全量 `UNMATCHED` 重入队一轮（2026-09-12 加 `HDtime` ⇒ 83 部）。**区别在"会不会收敛"**：
- **加站** ⇒ 一旦该站**真被搜过一次**，时间戳写入 ⇒ **收敛**（一轮性）；
- **站长期退避** ⇒ 时间戳**永远写不进去** ⇒ **不收敛**（这就是 09-19 现场）。

**哪些动作有"下次最早"**：★ **HDtime 的退避时刻不是常量 —— 别抄本节的历史值**（本节原先写死
`2026-09-14 11:32`，09-19 实测已变成 `09-20 03:50` 且**还在退避**）。**现读判据只有一条**：
`python tools/doc-audit/04_probes.py` 的第 4 列（或 `scripts/diag/check-indexer-timestamps.py`）；
恢复时刻 = cross-seed 自己的 `retry_after`（**`ERR-SVC-17` 已结案：不必去对 Prowlarr 的时刻**）。
qB 校验队列单线程。

**时区**：所有 cron 是**本地时间**；库里的 epoch 是 **UTC**，读要 **+8h**。

**写操作窗口**：批次间隙 —— 看 `<compose>/drive-loop/scripts/.drive-loop.state` 里的 `running_pid`。

> ★ **AI 最常忘的一条**：**"现在能做"依赖时间窗** —— 问题不是"这条命令对不对"，而是"**现在跑它会不会撞上什么**"。
> ★★ **本节的时间类读数**（退避时刻、批间间隔）**一律是"当时值"** —— 抄之前先现读，见 `A.8` 的半衰期那条。
> ★★★ **本节 09-19 新增的"死循环"那条，与"AI 最常忘"同族但更贵**：
> 看到一个"批批满载"的现象，**先问"哪个站没搜成"**，别先问"周期是不是太短"—— 见 `ERR-SVC-19` 的代价栏。

## A.8 外部依赖及其脾气

**有哪些**：4 个 PT 站（**HDtime / HDFans / BTSCHOOL / NanyangPT**）+ Prowlarr 代理。

| 站 | 脾气 |
|---|---|
| HDtime | 老 502 / 退避；★ 2026-09-12 才进 `TORZNAB_URLS` |
| HDFans | 稳，但命中率一般 |
| BTSCHOOL | 曾卡 CF，**现在好了**（命中率 23/46 = 50%，四站最高）|
| NanyangPT | ★ 片库不合口味：**4/452 = 0.9%** |
| Prowlarr | ★ 会**伪造 429**；它的 **Query Limit 是本地保险丝**，不是站点真实余额 |

**哪些读数是"代理"**（不能互相替代）：`Found` 行数、`matched_indexers`、`timestamp` 行数。

> ★ **AI 最常忘的一条**：**外部服务的"状态"有半衰期。**
> 两天前的故障描述，今天可能已经恢复 —— BTSCHOOL 那次就白白"待替换"了很久，
> 最后发现是**「当初的判据失效了」，不是「换好了」**。引用旧状态前先回读一次。

## A.9 历史失效模式与已知未归因

**坏过什么、怎么坏的**（括号里是**归因条目**，不是又一次描述）：

- 农场路径认不出 → `SEEDING` 塌方（13 → 21 → **201**，真根因是 `pack.roots` 没跟上农场切换）
  → `ERR-SVC-10` 边界
- 正则吃不下带空格的站名（`NanyangPT (南洋)`）→ `matched_indexers` 整列瞎掉 → `ERR-ENC-06`
- 检查间隔 > 退避窗口 → 退避检测**从来没生效过** → `ERR-SVC-12`（弯路 #10）
- 部署覆盖正在跑的脚本 → 假的 `exit=127` → `ERR-SH-01`（弯路 #12）
- partial 匹配 × 硬链接 → **qB 就地重下写穿源文件**（本项目最贵的一课）→ `ERR-FS-04`
- 邮件风暴：同一封告警重发约 6 次 → `ERR-SH-06` + `ERR-SCHED-02`

**已知但未归因的**：**无**（那个 `120` 已归因给 CPython `Py_FinalizeEx()`）。

**已知但已接受的**（设计，不是债）：杂物计数不为 0（`__pycache__` 会自己长回来）、
`mbf` 0 匹配、`scan-secrets.py` 的已知盲区。

> ★ **AI 最常忘的一条**：**「已知已接受」和「已知未修」要分开** —— 前者是设计，后者是债。
> 混在一起会让"设计如此"被当成"还没做"，或者反过来，让真债永远躺在"这是接受的"那一栏里。

## A.10 AI 自身的协作模式

| 能做 | 不能做 |
|---|---|
| 读文件、读 NAS（只读）、写本地代码、写文档、跑本地测试、**生成命令** | 直接操作 NAS、直接发 webhook、碰生产库、做 qB 写操作 |

- **怎么确认 AI 真的做了**：**回读实际状态**，不信调用的返回值。
  回读失败要区分「**没跑到**」和「**跑了没事**」（对照 `ERR-AI` 那一族）。
- **AI 的产出怎么验**：查日志、查数据库、查 NAS 上的文件 —— **不看终端输出**。

> ★ **AI 最常忘的一条**：**「报告了」≠「做了」。**
> 本项目反复栽在"看着像做了"上 —— 典型形状有四种（见 B.10 元层清单）：
> ① `n/a` 被念成"没事"　② 首读基线被念成"新增了"　③ 只看日报发现不了（错在给人查证的那份清单）
> 　④ 退出码 0 但断言数根本没进账。

## A.11 已有的约定（成文与不成文）

- **文档分工**：★★ **见 `CLAUDE.md` 的真源表**（唯一完整版）—— 本文不复述，免得又长成第二份真源。
  ★ 只说本文这一侧：**`ENVIRONMENT.md` = 运维事实（怎么做 + 本机真实值，`A.13`–`A.19`）**；
  跨层用**单向指路**，不重复登记。
  > ★ 分工描述曾指着旧结构（README 当事实源）⇒ 下一轮真有人去 README 里找已经搬走的判据。
  > ⇒ **改分工就是改「去哪找事实」**：改 `CLAUDE.md` **一处**即可（其余各处只留指针）。
  > ★★ 这条是 2026-09-20 "不重复登记"那一刀的结果：原先分工在 **5 处**各写一遍、两两打架。
- **唯一出处原则**：跨层用「详见 SUMMARY §N」单向指路，不重复登记
- **判据要带"另一侧"**：任何计数/判据的期望值要**能指回一条判据之外的真实记录**
- **不静默跳过**：跳过要连理由一起列出来
- **测试自己也是判据**：报红时先怀疑断言本身
- **只报变化，不报现状**：首读只记基线不响；与基线一致不许再喊；缩回要写出来（但别写"与基线一致"——那句是假话）
- **文档里的编号引用会用符号**（`§19.1.2`、函数名），不用行号 —— 行号会漂
- ★ **就地更正用三段式**（2026-09-19 立，见下）—— **别直接改掉原句**

> ★★ **就地更正的三段式**（`A.11` 第 7 条的展开；2026-09-19 立）：
> 改一份**已发布**的文档时，**不许**把原句直接改成对的 —— 用三段：
>
> | 段 | 写什么 | 为什么 |
> |---|---|---|
> | **① 原句 `~~划掉~~`** | 原文照抄，用 `~~…~~` 包住 | 让人看见"**原来写的是什么**" —— 否则读者无从判断这是新写的还是改过的 |
> | **② `> ★ 更正（日期，来源）：…`** | 新的事实 + **为什么改** | 日期是**唯一的时序依据**；来源指回现场 |
> | **③ 若影响下游，加一句「以 X 为准」** | 明确现在该信谁 | 例：`§26` 多处复述过同一数 ⇒ 立一张勘误表说"以 §26.2 为准" |
>
> ★ **为什么**：本节（`§26`）是**按轮次叠加**写的，`§26.16` 自己说「**过程记录里的话不追改**」——
> 那就**必须让人一眼看出哪句已废**。只加一段"勘误"而不划原句 ⇒ 读者要读完全文才知道哪句是真的。
> ★★ **本项目已在用它**：全仓 `~~划掉~~` **61 处**、"更正" **61 处**、"勘误" **33 处** ⇒
> **格式事实上已经收敛，这条只是把它写下来**（不是"新发明一种格式"）。
> ★ **反例（别这么做）**：把原句直接改成新值 ⇒ 过程记录被改坏，且**下一个读者看不出这里改过**
> （`§28.4` 那次"拿 `diff` 否证 `cmp`"的栽跟头，就是改坏了现场）。
> ★ **AI 最常忘的一条**：**这些约定本身就是"前提信息"** —— 新会话不告诉 AI，它会**另立一套**。

## A.12 项目元信息

- **规模**：单人项目、**无 CI、无 PR、不打分支**；git **直推 main**。
- **会话任务列表是临时的**：换会话就没了 —— **它不是项目的待办真源**。
  ★ 但对**当前这一局**而言它是最新的：**列当前棋盘必须从 TaskList 重读，别凭上一轮的记忆抄**（已因此错过三次）。
- **文档的"活"与"死"**：README / SUMMARY / ENVIRONMENT 是**活的**；
  会话任务列表、`D:\tmp` 下的脚本、`*.tsv` / `todo-paths.txt` 是**死的**（已多次被清）。

### ★★ A.12.1 跨会话任务盘面 —— **`#N` 不能单独当判据**（2026-09-17 立）

> 起因：**同一条任务在 `~/.claude/tasks/` 下的多个会话目录里各有一份**，
> 于是「`#N` 是什么」这个引用方式**本身就是错的**。详见 SUMMARY **§26**。

**三条硬规矩（引用 / 报数 / 动手前各一条）**：

| # | 规矩 | 为什么 |
|---|---|---|
| ① | 引用任务**必带会话前缀**：`79536301#78`，不写光秃秃的 `#78` | 实测：120 个 id 里 **88 个跨目录重复**，其中**真撞号**的有 `#4`/`#60`/`#72`（主语完全不同）。同一个 `#72` 一个讲"缺伴生文件"、一个讲"Win32 FileId 判硬链接" |
| ② | 报「pending N 条」**必须说清是哪张棋盘**，别只报光秃秃的数 | 说"pending 12 条"时，`79536301` 的 12 条**不在那个数里** —— 而它们是真待办 |
| ③ | 动手前 `glob` **全部** `*/*.json` | ★ 只读"自己那个目录"会**系统性漏掉别的会话的任务，且不会有任何提示**。★ 而且 `glob *.json` **枚举的是文件、不是目录** ⇒ 扫不到空目录（实测 `tasks/` 下 18 个目录里 10 个是空的）—— 又一次「**枚举 ≠ 全量**」 |

**★ 一个 id 可能有三种情形，不能一刀切**：
① **真撞号**（两条不同任务）② **同一条的副本**（同主语、同描述）③ **同一件事的两个编号**（如 `79536301#78` 与 `97ecd174#1` 都是"腾磁盘"）。
⇒ **判据是逐个比对主语与描述**，不是"看 id 一样就当成同一条"。

**★★ 会话任务是"死的"，所以**跨会话的真待办**必须落盘**：
> 任务卡是会话级的（`~/.claude/tasks/<会话 UUID>/`），**项目没有一个能容纳"跨会话待办"的落点**。
> ⇒ **做法：把「跨会话仍成立」的条目落进 SUMMARY 的一节**（现在在 **§26**），
>   卡里只留指针；**别把真待办只留在会话卡里** —— 那是 `B.10` 那句"**报告了 ≠ 做了**"在**任务管理**上的同族形状。

> ★ **AI 最常忘的一条**：**"列当前棋盘"必须先 glob 全部会话目录** —— 只看自己那个目录，会得出一个**看着完整、其实漏了**的清单。
- **哪些脚本在哪跑** —— ★ **三条路径，别混**：

  | 路径 | 谁 | 改了怎么生效 |
  |---|---|---|
  | **构建上下文** `<compose>/orchestrator/` | 进 `reseed-orchestrator` **镜像** | 要 `docker compose build reseed-orchestrator`（★ `--force-recreate` 不带 `--build` 只拿旧镜像重启，等于什么也没做）|
  | **NAS 宿主机** `<compose>/drive-loop/**` | DSM 计划任务直接跑 | **拷完即生效**，下次唤醒就用新的，**不用动容器** |
  | **容器内** | `cross-seed` 用官方镜像，**完全不吃我们的文件** | 只能改 `config.js`（挂载进去的）|

- `deploy.sh` 白名单（**唯一**从 Windows 发起的写操作）当前 **28 个文件**，见 `deploy.sh` 的 `FILES` 数组 ——
  ★ **那是唯一一份拷贝**，漂移哨兵从它解析受管集合，别复制第二份。
- **只在 Windows 跑、不进 NAS 也不进容器**（LOCAL_ONLY）：`scan-secrets.py`、`check-deploy-drift.py`、
  `audit-found-*.py`、`check-indexer-timestamps.py`、`migrate-reseed-dirs.py`、`tests/`、`run-batch.sh`、`gen-*.py`。
- 本地 `.env` 是**开发存根**（`TORZNAB_URLS` 就是 `.env.example` 里那两个占位符），
  **真实凭据只存在于 NAS 的生产 `.env` 与 Prowlarr UI 里**。

> ★ **AI 最常忘的一条**：**"这个文件在哪跑"决定了"改了要不要 deploy"** —— 拷错一层，改了半天下次唤醒还是旧的。

---

## A.13 运行语义（真实值）—— 状态机 / 包 / 预留位

> ★★ **本节是 2026-09-20 从 `README.md` **逐字节搬来**的**（原因：README 瘦身成 GitHub 门面）。
> 搬迁时**正文一字未改** —— 只把标题层级从 `###/####` 提到 `##/###/####`，并接在本节之下。
> ★ 本节带**真实值**（真实包名、真实路径、本机实测读数）⇒ **别公开贴出**，见本文顶部 ★★。

### A.13.1 单片状态机（已实现）—— 记录每部片子走到哪一步了

cross-seed **不会排队、不会重试**：索引器被退避时，待搜索项直接跳过
（2026-09-11 有 295 条就这么消失了，见 SUMMARY §5.3）。
所以补了一个 sidecar 状态库，把每部片子的阶段记下来，**已完成的阶段不重复，被跳过的阶段必须补上**。

```bash
PACK=frds-top250-2024
N=//YOUR-NAS/docker_ssd/prowlarr_cross-seed_autohardlink

# ★推荐：根清单从 .env 派生（首选 FARM_SOURCES；缺席时退回 DATA_DIRS）
python scripts/reseed-state.py init --pack $PACK --depth 2 \
  --roots-from-env .env --match "DouBan_IMDB" --exclude "0观影清单*"
python scripts/reseed-state.py sync --pack $PACK \
  --db-path "$N/cross-seed/cross-seed.db" \
  --log "$N/cross-seed/logs/info.current.log" --log "$N/cross-seed/logs/verbose.current.log" \
  --qbit-url http://NAS_IP:3060 --indexer-alias "http://prowlarr:9696/1/api=SiteB"
python scripts/reseed-state.py report --pack $PACK
python scripts/reseed-state.py todo  --pack $PACK --indexers SiteA,SiteB --out scripts/todo-paths.txt
python scripts/reseed-state.py drive --pack $PACK --indexers SiteA,SiteB --limit 50 --apply \
  --url http://NAS_IP:2468 --api-key <CROSSSEED_API_KEY> --db-path "$N/cross-seed/cross-seed.db" \
  --qbit-url http://NAS_IP:3060
```

**`init` 的根怎么给** —— 推荐**从 `.env` 派生**（`--roots-from-env .env --match "<路径关键词>"`）：
它读的是 `.env` 的 **`FARM_SOURCES`**（v3 之后的**源清单**；切换前那份源目录写在 `DATA_DIRS` 里，
所以 `FARM_SOURCES` 缺席时会退回 `DATA_DIRS` —— 与 `build-farm.sh` 同一种顺序）。DC 那种
47 个根的包也一行搞定。另有显式 `--root/--local-root`（可重复、**必须按序一一对应**）用于单根包。
`--match` 可重复（OR），`--unc-host //YOUR-NAS` 把 NAS 路径翻成 UNC（不给则试着从
`scripts/.nasrc` 的 `NAS_NAME` 推断），`--nas-prefix` 默认 `/volume1`。
完整参数与多根用法见 SUMMARY §10.6。

> ⚠ **`--depth` 必须与 cross-seed 的 `maxDataDepth` 完全一致**（本项目未显式配置 ⇒ 默认 2）。
> 它是"从 dataDir 往下数几层"，第 1..N 层的**目录和视频文件**都算 searchee。
> 对不上就会出现"状态机有、cross-seed 没有"的幽灵条目，或漏掉真在搜的片子。详见 SUMMARY §10.6.1。

**分批**：`--limit N` 每批 N 条，`--batch K` 指定第几批，`--plan` 只打印计划不发请求。
因为待办清单**已排除已做种的片**、且按 `SKIPPED → ERROR → PENDING → UNMATCHED` 排序，
所以 **一批做完重跑同一条命令就自动推进**（不用记批次号）。
`todo` 的路径走 stdout、分批说明走 stderr，管道不会被打扰。

阶段（**由事实推导，不是手工填的**）：

| 阶段 | 含义 | 会重搜吗 |
|---|---|---|
| `SEEDING` | qB 里真有这个 info_hash | ❌ 永不 |
| `MATCHED` | 匹配到、已注入，等 qB 确认 | ❌ 不 |
| `UNMATCHED` | 真搜过、没匹配到 | ⏳ 仅当**出现没搜过的索引器**或过了重搜周期（默认 **14 天**） |
| `SKIPPED` | ★被退避秒跳 | ✅ **立刻** |
| `PENDING` / `ERROR` | 没搜过 / 异常 | ✅ 立刻 |

> **加一个站，所有 `UNMATCHED` 自动变成待搜** —— 不需要人工挑片子。
> 实测：只配 SiteA 时待搜 286 条；加上 SiteB 后那 48 个 `UNMATCHED` 全部解锁。

**重搜周期（每站按周期重搜，默认 14 天）**：粒度是 **(片 × 站)**，来自 cross-seed 自己的
`timestamp(searchee_id, indexer_id, last_searched)` 表。默认每站 14 天，可按站覆盖：
`--cadence "SiteA=14,NanyangPT=30"`；`todo --include-cooldown` 忽略周期强制全量重扫。
`report` 会打印按站周期表（搜过几部 / 到周期几部 / 下次最早可重搜是哪天）。详见 SUMMARY §11.6。

> **为什么是 14 天而不是 7 天**：无人值守下这个周期直接决定长期查询量 ——
> 约 1000 部单片，7 天 = 每天 ~150 次查询/站、一年 5 万+ 次，且是**永不停止**的机器人流量。
> `delay=30` 解决的是"快不快"，解决不了"像不像人"。翻倍到 14 天查询量减半，
> 代价只是未命中的片子多等一周。**账号比命中率重要** —— 站点查限额见 SUMMARY §11.6。

**`drive` 已经会控速、会等退避、会回灌**（SUMMARY §11.8）：
`--interval 30` 对齐 cross-seed 的 `delay`；读 cross-seed.db 的 `indexer` 表检查退避 ——
**双触发**：每 `--check-every 10` 条、以及每 `--backoff-check-secs 60` 秒
（★按秒那一路是 2026-09-12 加的：实测退避窗口只有 30~60 秒，只按条数 300 秒才看一眼会**整段错过**）。
撞上 `RATE_LIMITED` 就**睡到解禁**；若要等到**超过 `--max-wait 1800` 秒**，
★**先看是不是"全都在退避"**（2026-09-16 改）—— 还剩下健康站就**照发**
（cross-seed 会跳过被禁的站、用其余的搜，实测见 SUMMARY §11.13），
**全都落在退避里**才中止；没给 `--indexers` 时退回旧行为（照旧中止，宁可吵）；
打完自动 `sync` 回灌，直接告诉你 `newly_seeding`（这轮真赚到几部）和 `still_skipped`（又被退了几部）。
**默认 dry-run**，不加 `--apply` 不会发任何请求。

> 为什么退避只能读库、不能用 API：cross-seed v6.13 的 HTTP 接口只暴露
> `/api/ping` 与 `/api/status`，`/api/indexerstatus` 是 **404**（见 SUMMARY §11.9）。

- 状态库是 `hlink/state.db`（**运行时数据，已 gitignore**）。它**只读** cross-seed 的库/日志与 qB，
  唯一被写的就是自己。为什么不直接给 cross-seed 的库加字段？见 SUMMARY §11.1。
- 读 cross-seed.db 走**直读 UNC**（`PRAGMA query_only=1`，实测 0.17s 且能看到 WAL 数据），
  拷 `.db/-wal/-shm` 只是兜底。⚠ **诊断脚本一律直读，别 `cp`** —— `cp` 丢 WAL，会误判（SUMMARY §13.6 坑 2）。
- ⚠ `--root` 是 **cross-seed 视角的 NAS 路径**（webhook 要用它）；Windows 上列目录另外传 `--local-root`。
- 想跑"一次性批量作业"而不是长期增量维护，用 `python -m orchestrator.main`
  （`preflight` / `run` / `status` / `prestage`，见 SUMMARY §11.10）。

### A.13.2 多包支持（已接入 3 个包）

| 包 | 单片单位 | 结构 |
|---|---|---|
| `frds-top250-2024` | 一部电影 | ✅ 根目录子目录即发布名 |
| `mbf` | 一季（S01~S04） | ✅ 根目录子目录即发布名 |
| `dc-collection` | 一部电影 / 一季 | ⚠ 根下多一层中文标签，见下 |

> ★ **包名口径（2026-09-20 实测更正）**：上表**以 `state.db` 的 `pack.name` 为准**
> （`SQLite: SELECT name FROM pack`）—— 实测就是 `dc-collection` / `frds-top250-2024` / `mbf`
> 三个。★ 本节**曾写 `my-brilliant-friend-s01-s04`**，那是**描述性名字、不是登记名** ⇒ 已改。
> 与 `drive-loop.py` 的 `PACKS_DEFAULT`（**一处**声明，`= "dc-collection,mbf,frds-top250-2024"`）
> **逐字一致** —— 两者不一致时**以 `PACKS_DEFAULT` 为准**（那是"谁会被驱动"的真源）。

#### ★★★ 「什么算一个大包」—— **判据就一条：单片停在哪一层**（2026-09-21 立）

> ★ 起因：用户问「程序怎么辨别大包？大包下面可以是**系列电影**、也可以是**多季电视剧**，
> 而**一季下的单集不算大包**，能不能先按这个标准实行？」
> ★★ **正面回答**：**你的标准就是系统现在的行为**（下面逐条对上）；
> ★ 但**"程序辨别"这件事不存在** —— 见 `A.15.1` 原理 A：
> **「哪个是大包」这个判断从来不发生在代码里，只发生在人手写下的配置里。**
> ⇒ **不是"先按标准实行"，是"先把这条标准写下来"**（就是本节）。

**★★ 判据两条，缺一条就判错**（2026-09-21 **实测**，不是读代码读出来的）：

| 层 | 是什么 | 会被**枚举**到吗 | 会被当成**单片**吗 |
|---|---|---|---|
| **大包根**（`pack.roots` / `FARM_SOURCES` 指向的那一层） | 大包**本身** | — | ❌ 它是**根**，不是单片 |
| **第 1 层子项** | **发布名那一层** | ✅ 会 | ✅ **是单片** |
| **第 2 层（单集 / 附件）** | `Arrow.S01E01.mkv` | ⚠️ **会**（`depth=2` 枚举到） | ❌ ★ **不是** —— **被"塌回"季层** |

★ **关键：不是靠"不枚举"把单集挡在外面，是靠 `dir_name_of()` 的"最长前缀匹配"把它塌回发布名那一层。**
实测（`find_searchee_paths` 合成目录）：
```
depth=2 -> ['Arrow.S01', 'Arrow.S01\\Arrow.S01E01.mkv', 'SomeMovie.mkv']
```
⇒ ★ **单集**确实**在枚举结果里**；而 `dir_name_of("…/Arrow.S01/Arrow.S01E01.mkv", …)`
命中**已登记的季层** `Arrow.S01`（源码原文：「cross-seed 的 searchee 既可能是目录、
也可能是目录里的 `.mkv` 文件，两者都归到**同一个单片** —— 这正是我们要的粒度」）。
⇒ ★★ **`depth` 管的是"枚举到哪一层"，"什么算一片"由 `dir_name_of()` 的塌回规则定** ——
**两个判据，别混成一个**（`B.10` 第 20 条那个形状：两个判据量的不是同一样东西）。

> ★★ **这就是"一季下的单集不算大包"的机理** —— 单集**归到它所在的那一季**，
> 于是"一片"= **一季**，不是"一集"。★ 从结果看**和你要的标准一致**，
> 但机理和"程序跳过单集"**不是一回事**。
> ★ `pack` 表**每包一行**都带自己的 `max_depth`（`SELECT name, max_depth FROM pack`）——
> ⇒ 个别包要破例时**改那一个包的行**（如 DC 那种"根下多一层标签"的形态），不动全局。

**★ 对上你那三条**（你说 → 现状）：

| 你说的 | 现状 | 为什么 |
|---|---|---|
| 大包下可以是**系列电影** | ✅ **是**（`frds-top250-2024` = 250 部电影） | 每部电影 = 发布名 = **第 1 层子项** |
| 大包下可以是**多季电视剧** | ✅ **是**（`dc-collection` 里电影和整季都有；`mbf` 是 `S01~S04`） | 每一季 = 发布名 = **第 1 层子项** |
| **一季下的单集不算** | ✅ **对**（单集归到季） | ★ 单集虽被枚举到，但 `dir_name_of()` **塌回**季层 |

**★★ 登记一个包时，人实际要回答的只有两个问题**：

1. **哪一层是"发布名那一层"？** ⇒ 写进 `roots`（`init --root <那个路径>`）。
   ★ `dc-collection` 的坑就在这里 —— 根下**多一层中文标签**，
   `roots` 必须指到**标签下面那一层**，否则**拿标签名去搜索**（`A.13.2` 上一小节）。
   ★★ 判据是"**哪一层能被拿去站点搜索**" —— 不是目录深度，是**发布名**。
2. **枚举到第几层？** ⇒ 默认 **2** 通常够；★ **它只管"扫得够不够深"**，
   **不管"什么算一片"**（后者由塌回规则定，见上表下方）。
   ★ 只有"发布名之上还有分层"的形态才要动它。

> ★ **"像大包"不等于"是大包"** —— 判据只有"人把它登记成包"，**结构上认不出来**。
> `find-packs.py` 的数子目录判据**实测会误报**（`The Beatles - Discography +` 音乐、
> `StarRupture-*` 游戏，**都满足判据却都不是 PT 大包**）⇒ **它只报信号、人自己判**（见本节末）。
> ★★ 且 **"找得到" ≠ "搜得到"** —— `mbf` 在 `unclaimed`/`report`/`trend` 上**全绿**，
> 实测 **`Found 0 torrents`** ⇒ **登记一个结构合规的包，也可能一个种子都搜不到。**

#### ★★★ 大包怎么"按季拆" —— **季界判据分档：能自动的自动，判不出的明确拒绝**（2026-09-21 立）

> ★ 起因：用户问「发布者把**整部剧**打成一个发布名（`…Complete…`）、单集平铺在下面，
> 这种大包怎么办？能不能不查 TMDB 自动拆？」
> ★★ **先摆正视角**（这是前面几轮我走偏的根因）：
> **本项目的视角是「我要发什么种」，不是「站点上有什么」** ——
> 用户 2026-09-21 明说「我**会发种的**」⇒ ★ **"站点上没有"不是约束，自己发。**
> ⇒ 所以"拆不拆"由**剧的真实结构**定，**不由站点现状定**。

**★ 判据一：一季 = 不算大包**（用户 2026-09-21 拍）
> 只有一季 ⇒ **整包一片就行**（农场条目名 = 源发布名），**不用拆**。
> ★ 因为"大包"的定义就是「一包多片」—— 一季是一片，不是多片。

**★ 判据二：≥2 季 ⇒ 拆，且名字从源发布名机械派生**
```
源：  [夺爱].Duo.Ai.2011.Complete.WEB-DL.4K.HEVC.AAC-CMCTV
                      ↓ 把「季/完整性」那一格换掉，其余**逐字保留**
S01： [夺爱].Duo.Ai.2011.S01.WEB-DL.4K.HEVC.AAC-CMCTV
S02： [夺爱].Duo.Ai.2011.S02.WEB-DL.4K.HEVC.AAC-CMCTV
```
> ★ 派生的名字**就是"我们要发的那个种的名字"** ⇒ **它自己就是规范**（不另立一套）。
> ★ 找不到可替换的那一格时**报出来、不硬塞**（硬塞会造出一个不存在的发布名）。

**★★★ 判据三：季界从哪来 —— 分三档，第三档**拒绝猜****

| 档 | 依据 | 依赖 | 可靠度 |
|---|---|---|---|
| **1** | **文件名里的 `SxxExx`**（`S01E05`/`s1e5`/`S01.E05`/`S01-E05`） | ★ **零** | ✅ **能自动** |
| **2** | **季层目录名**（`S01/`、`Season 2/`、`第1季/`） | ★ **零** | ✅ **能自动** |
| **3** | ★★ **平铺流水号**（`E01..E24`，无任何季痕迹） | ⚠️ **要外部季界** | 🚫 **本工具拒绝猜** |

★★★ **档 3 为什么必须拒绝猜**：`E01..E24` 里「前 12 集是 S01」这个信息
**不在文件名里、也不在目录结构里**。用文件大小/时长聚类去蒙 = **在编**，
而编错的代价是**发出去一个错种**。
⇒ 档 3 的正路是**外部季界**，两条，★ **都还没实现**：
1. ★★ **Prowlarr / Torznab**（**优先** —— 用已有的 Prowlarr，不引入新凭据）：
   Torznab 的 `tvsearch` **支持 `season=` 参数**（`scripts/diag/torznab-probe.py` 就在用）。
   ★★ 但「**返回的 item 里有没有可解析的季字段**」**尚未实测** ⇒ **不许假装它有**。
   ⇒ 待办：跑一次 `tvsearch` 看返回体（★ 一条只读侦察，见 `§26.38`）。
   ★★ **2026-09-21：判据工具已备好，只剩"真跑一次"这个动作**（那个动作**耗站点额度**）：
   - `scripts/diag/torznab-probe.py` 新增 **`--dump-attrs`**（逐条摆出 item 全文 +
     **列全** `torznab:attr`）与 **`--raw`**（原始响应前 4000 字符，★ **过 `redact()`**）。
   - ★ **修掉的那个真缺口**：该探针原先**只抠 `<title>`**，item 后面的
     `link`/`guid`/`enclosure`/`torznab:attr` **整段被丢** ⇒
     「响应里没有季字段」这句话**当时压根没有判据**（不是"看了说没有"，是"根本没看"）。
   - ★★ **"两个对照"已离线钉住**：`season=1` 与 `season=2` 产出的 query **确实不同** ——
     否则那次"对照"是**两次一样的请求**，返回集当然一样，会得出
     「season 在服务端不生效」的**假结论**。★ 至于**服务端认不认**，只能真跑。
   - ★ **判"没有"之前先列全 attr**：区分「站点不给这个字段」与
     「**有，但名字不叫 `season`**」（`rageid`/`tvdbid` 也是可用的**季界锚**）。
2. **TMDB / 豆瓣**（外部凭据 + 联网）—— 用户 2026-09-21 拍：**接口先留、实现后补**。

**★ 工具：`scripts/diag/split-pack.py`**（2026-09-21 新增，**只读**）
```bash
python scripts/diag/split-pack.py "<源包目录>"              # 档 1（默认）
python scripts/diag/split-pack.py "<源包目录>" --by-dir      # 档 2
python scripts/diag/split-pack.py "<源包目录>" --emit-cmds   # 额外打印拟命令
```
- 退出码：**0** = 出了方案（或"只有一季⇒不用拆"）；**3** = ★ **判不出季界（不猜）**；**2** = 读不到目录。
- ★★ **它只报方案、不动任何文件** —— 建条目、建链接**都由人执行**（`A.10`）。
- ★★ **源绝不能移动/改名** —— 它是硬链接的目标，也是**原本在做种的源文件**（`A.16`）。


#### ⚠ 嵌套结构：dataDir 要指到「发布名的那一层」

cross-seed 用**目录名**去站点搜索。若大包根下还有一层分类/标签目录，那层名字会被当成 searchee 名 ——
**搜不到任何东西，却照样消耗查询额度**。

> 这一段原文还有 `#### 生产 .env 怎么更新` 与 `#### 其它要点` 两小节（**通用做法，未搬**）——
> 它们仍在 `README.md` 的 `## 扩展` 下。

#### ★★ 怎么**再加一个大包**（2026-09-20 立，三步 + 三个必踩的坑）

> ★ 用户问「怎么自动扩展别的大包呢？」⇒ **加包不能全自动**，理由见本节末。
> ★ 先跑 **`python scripts/diag/find-packs.py`** —— 它**只读**扫 NAS、列出
> **像大包但没登记**的目录（★ 只报信号、不替你判）。

```bash
# ① 登记：让状态机认识这个包（--roots-from-env 从 .env 派生根，不手抄路径）
python scripts/reseed-state.py init --pack <包名> --root /volume1/video/download/...

# ② ★★ 排进「哪几个包会被驱动」—— 唯一声明点，在 scripts/drive-loop.py 的 PACKS_DEFAULT
#    改完要重新部署：deploy.sh 白名单里有 drive-loop.py（☆ 但它是容器里的脚本，见坑②）

# ③ 可选：给这个包设农场根（v3；不设则沿用全局）
python scripts/reseed-state.py farm --pack <包名> --root /volume1/video/download/reseed/reseed_farm
```

**★★★ 三个必踩的坑**（每个都有实测依据，别跳）：

| # | 坑 | 依据 / 后果 |
|---|---|---|
| ① | **只做 ① 忘了 ②** ⇒ 包登记了但**永远不被驱动** | 会被 `reconcile_watch` 的「**登记了却没被驱动**」（`packs_undriven`）**喊出来** —— 那是判据在保护你 |
| ② | **加包会稀释其他包的轮换频率** | `drive-loop.py:103-132` 原文：「名单 2→3 个，dc/frds 各自的**轮换频率从 1/2 掉到 1/3**」（`once_round` 取模就是它）|
| ③ | ★★ **`PACKS_DEFAULT` 的顺序有意义，改顺序 = 偷偷改"下一批跑谁"** | `once_round` 存进 `.drive-loop.state` 的是**下标而不是名字** ⇒ **动顺序前先看 `last_pack_idx`** |

**为什么不能全自动加包**（三条，全部引自 `drive-loop.py:103-132`，不是推断）：

1. **那是产品决定，不是机械判断** —— 原文：「它**不是**一行等着被消除的代码债，**是一个产品决定**」
2. **会稀释其他包**（见坑②）
3. ★★ **"找得到" ≠ "搜得到"** —— `mbf` 在 `unclaimed`/`report`/`trend` 上**全绿**，
   而实测 **`Found 0 torrents`** ⇒ 自动加包会把「**一个不产出放量的包伪装成正常**」

> ★ **假阳性警告**（`find-packs.py` 实跑，2026-09-20）：判据是「子目录数 ≥N」，
> 但 `The Beatles - Discography +`（**音乐**）、`StarRupture-*`（**游戏**，含 `_crack`）
> **都满足该判据却都不是 PT 大包**。⇒ **它只报信号，人自己判。**

### A.13.3 预留位（本次不实现）

#### IYUU 扩散（本次不实现）

单种在 `:3060` 做种后，由 IYUU 读其 InfoHash 扩散到更多站点。`hlink/config.yml` 里 `jobs[].iyuu_handoff` 为其预留开关。

#### 别的匹配器（本次不实现）

`orchestrator/matcher.py` 的 `Matcher(ABC)` 是接口落点；`hlink/config.yml` 的 `matcher.engine` / `jobs[].matcher_engine`
预留 `iyuu`/`custom`（当前会明确报"未实现"）。

---

## A.14 存储与农场（真实值）—— 硬链接农场 v3

> ★★ **本节是 2026-09-20 从 `README.md` 逐字节搬来的**（README 瘦身成门面）。正文一字未改。
> ★ 与 **`A.3` 存储布局与容量水位**是**一对**：`A.3` 讲「卷怎么分、容量怎么读」，
> 本节讲「农场怎么建、怎么验、怎么切」—— 判据本体都在这一对里。
> ★ 本节带**真实路径**（`/volume1/video/download/reseed/reseed_farm`）⇒ **别公开贴出**。

### A.14.1 硬链接农场（v3，**农场已建好 / 配置待切换**）—— 把 49 条 `dataDirs` 收成 1 条

**它解决什么**：现在 `DATA_DIRS` 是 **49 条**（3 个大包 + DC 的 47 个标签目录），
每加一个包就要重算一遍路径、同步 `.env`、重建容器。农场把这些归一成**一条**。

**做法**：在源同一个物理卷上建一个**扁平**目录，每个子项 = 一个"单片"，用**硬链接**
指向真实数据（不占数据块，只多 inode + 目录项）：

```text
/volume1/video/download/reseed/reseed_farm/     ← 唯一的 dataDir
├── Arrow.S01-S08.2012-2020.Bluray.1080p.MNHD-FRDS/   ← 硬链接副本
├── 守望者S01.Watchmen…@FRDS/
└── …（实测 475 条）
```

`dataDirs` 从此只有这一条，`maxDataDepth` 保持默认 **2**（不用动全局开关 →
FRDS/MBF 行为完全不变）。

> **为什么"每个 dataDir 的直接子项"就是全部规则**：cross-seed 的枚举是
> `dataDirs → 每个直接子项 → 按 depth 展开`。农场的子项**恰好等于**原来那 49 个
> dataDir 的直接子项之并集，所以「农场 + depth=2」与「49 条 + depth=2」产出的
> searchee **逐条相同**。★ 这是**构造上**的等价 —— 同一个函数作用在同一批首层条目上，
> 不依赖我们对 depth 规则的建模是否精确（实测 475 条、**零重名**）。

#### ★★ 三个口径别混（2026-09-21 实测）—— **475 ≠ 做种数，做种数 = 517**

> ★ 起因：被问「**7 个大包**为什么农场才 475？」—— ★ **问句里两个前提都不对**
> （不是 7 个是 **3** 个；475 不是做种数）。三个数**各量各的**，别互相减：

| 数 | 怎么读 | 真值 |
|---|---:|---:|
| 状态机登记过的片子 | `SELECT COUNT(*) FROM movie` | **605** |
| 状态机认为**做上种**的 | `SELECT COUNT(*) FROM movie WHERE stage='SEEDING'` | **517** |
| 农场**顶层条目数** | `ls -1 /volume1/video/download/reseed/reseed_farm \| wc -l` | **475** |

- **605 的分档**（`SELECT stage, COUNT(*) FROM movie GROUP BY stage`）：
  **SEEDING 517 / UNMATCHED 83 / PENDING 4 / MATCHED 1**。
  ★ **`UNMATCHED` 不进农场是正常的** —— 站上没匹配到，本来就没种可做。
- **按包**（`SELECT pack, stage, COUNT(*) … GROUP BY pack, stage`）：
  `frds-top250-2024` 443 SEEDING / 42 UNMATCHED / 1 PENDING；
  `dc-collection` 74 / 37 / 3 / 1 MATCHED；★ **`mbf` 0 SEEDING / 4 UNMATCHED**。
- **农场里 `ls` 出 475 条，其中目录 474**（`find -maxdepth 1 -type d`）——
  那 1 个非目录是 `0观影清单chrlee整理`。
- ★★ **已证"没丢片"**：把 517 个 `movie.dir_name` 与农场条目名对差集
  （`LC_ALL=C comm -23`，**两边都要 `LC_ALL=C sort`**），**差为 0**。
  ⇒ **517 个片名在农场里全找得到**。
  ★ `comm` 前置条件是"两边同一把尺子"：SQL 的 `ORDER BY` 是**字节序**、`comm` 要的是
  **当前 locale 序**，不一致会报 `file 1 is not in sorted order` 并**输出一条垃圾流**（别信）。
- 🔴 **未验项**：**475 与 517 相差 42，成因未验**（不许写"无"，见 `B.10`）。
  已**证伪**两种解释：**"合季合并"**（`grep -cE 'S[0-9]+-S[0-9]+'` 只 4 条，凑不出 42）、
  **"一片一目录"**（`find -mindepth 2 -type d` = **131**，两边都对不上）。
  ★ 再查此数时**不许复用这两个已证伪的解释** —— 见 `ERR-AI-08`。

**怎么用**。`deploy.sh` 只管容器文件，**不含** `scripts/`，所以先把它拷到 NAS 的
compose 目录（纯 LF，`sh` 可直接跑）：

```bash
# 本地：拷到 NAS 的 compose 目录
cp scripts/build-farm.sh "//iSunker-DS423/docker_ssd/prowlarr_cross-seed_autohardlink/"
```

然后**在 NAS 上跑**（最省事）：

```bash
cd /volume2/docker_ssd/prowlarr_cross-seed_autohardlink
sh build-farm.sh                    # 默认 dry-run：只统计预演，一个文件都不建
sh build-farm.sh --apply            # 真建（增量：已存在的跳过）
sh build-farm.sh --apply --prune    # 顺带删掉"源已经没了"的条目
sh build-farm.sh --verify           # 只校验农场 vs 源
```

> ✅ **`--verify` 在 `DATA_DIRS` 切到农场之后会失效 —— 2026-09-12 已修，不再是问题**。
> 当时的毛病是它取「期望集」的方式**从 `.env` 的 `DATA_DIRS` 派生**（脚本第 108 行），
> 而 v3 的全部意义正是把 `DATA_DIRS` 改成农场这一条 —— 于是它**拿农场校验农场，永远 PASS**。
> 现在期望集改由独立的 `FARM_SOURCES` 给出，并加了一道**自指闸**（期望集里出现农场自身
> 就拒绝执行），`--verify` 也补齐了退出码与漂移明细（0=无漂移 / 1=有漂移 / 其它=没跑成）。
> 详见 **SUMMARY §16.2.1**。
> ✅ **它已经挂成定期巡检了**，且已在生产跑过一次 —— 见 **SUMMARY §16.2.2 / §16.2.2.1**。

> ★ **也能直接从 Windows 跑**（走 SMB）—— 2026-09-11 实测：这个 NAS 上
> `os.link()` / `cp -al` 经 SMB 过来是**服务端真硬链接**（同 inode、`nlink=2`、
> 删掉原文件后另一个还在）。原先"Windows 建不了硬链接"的说法**是错的**。
> 此时 `.env` 里的 `/volume1/...` 在本机不存在，用 `--map` 做前缀翻译即可：
>
> ```bash
> COMPOSE_DIR="//iSunker-DS423/docker_ssd/prowlarr_cross-seed_autohardlink" \
> FARM="//iSunker-DS423/video/download/reseed/reseed_farm" \
> sh scripts/build-farm.sh --map "/volume1=//iSunker-DS423"          # 先 dry-run
> # 确认 → 同一行末尾加 --apply
> ```
>
> 两种跑法结果完全一样（脚本每次都用探针**自证**硬链接真的建成了，见下）。
> Windows 跑的代价是逐文件 RPC，475 条用时数分钟。

确认无误后，切换（**与"移除 BTSCHOOL"合并成一次容器重建更省事**）：

1. 本地 `.env` 把 `DATA_DIRS` 改成农场那一条路径；
2. `python scripts/diag/gen-nas-env-update.py` 生成新的 `nas-update-env.sh`；
3. NAS 上 `sh nas-update-env.sh`（改 `.env` + `--force-recreate` 容器）；
4. 验证：`drive-loop` 启动时的「索引器自检」与 searchee 数应与切换前**一致**。

**安全边界**：脚本**只增不删**（除 `--prune`）；`--prune` 带安全闸 ——
期望集为空时**拒绝**执行（否则 `.env` 一读错就会把整个农场删光）。
★ 脚本**绝不** `chown -R`：硬链接文件与源文件是**同一个 inode**，
对链接文件 chown 会改掉**源文件**的属主 —— 所以只对目录（新造的）改属主。

设计取舍与 A/B 方案对比见 SUMMARY §10.5。

---

## A.15 原理与风险（真实值）—— 声明点 / 搜索压力 / piece 校验

> ★★ **本节是 2026-09-20 从 `README.md` 逐字节搬来的**（README 瘦身成门面）。正文一字未改。
> ★ 本节**各条都含本机实测**（HDtime 224 部一部没重搜、农场 475 条、1011 那个全天数…）
> ⇒ **别公开贴出**。★ 完整的论证与四条对策仍只在 **SUMMARY §19**。

### A.15.0 原理技术和风险须知

> 两条「不知道就会踩」的原理。**完整论证、实测数据与四条对策见 SUMMARY §19。**
> 这里只放**要照做的部分**。

### A.15.1 原理 A：没有「识别」，只有「声明」—— 系统不会告诉你哪个目录是包

系统只有两件**被动**能力，**两件都不判断「这个目录是不是包」**：

| 能力 | 输入 → 输出 |
|---|---|
| **枚举**（`scan_pack()` / `find_searchee_paths()`） | 一个根 + 一个 depth → 根下的单片清单 |
| **归属**（`_resolve_searchee_to_pack()`） | 一条 searchee 路径 → 它属于哪个包的哪个单片 |

> 「哪个是大包」这个判断**从来不发生在代码里，只发生在人手写下的配置里**。

**声明点清单 —— 分两档，因为「漏了会怎样」完全不同：**

**① 静默档：漏了没有任何人知道**（**只有这一档需要检测器**）

| 声明点 | 在哪 | 声明什么 | 漏了会怎样（两边看起来都正常） |
|---|---|---|---|
| **`FARM_SOURCES`** | NAS `.env` | 哪些目录是**源根** | `build-farm.sh --verify` 期望集里没有它 → **连漂移都不报**（两边一致地当它不存在） |
| **`pack` 表一行** | `<compose>/drive-loop/hlink/state.db` | 名字 + `roots` + `farm_root` + `max_depth` | 农场里的片**归不到任何包** → 静默 continue（只记进 `other_pack`） |
| **`--packs`** | `drive-loop.py` 的 **`PACKS_DEFAULT`**（`dc-collection,mbf,frds-top250-2024`；`run.sh` **没传**） | 哪个包**会被驱动** | 状态机有它、农场有它，**就是不排它** —— `mbf` 就是这么被落下很久的（2026-09-13 已排进去，见第 7 条） |
| **`--indexers`** | `run.sh` | 哪些**站**会被搜 | 站没进名单 = 对每部片子来说「那个站从没搜过」**根本不会被表达出来** → HDtime 就是这么卡住的（§18.11） |
| **文档里的 init 配方** | `SUMMARY §10.2` 与本节的示例 | 包名 + `--match` 关键词 | 照旧配方重跑 → 挑 0 条根；或**多建一个包行**。★ **登记名以 `state.db` 为准**：那个包叫 **`mbf`**（`my-brilliant-friend-s01-s04` 只是 `hlink/config.yml` 的 **job 名**，2026-09-12 已按库改正，见 `summary/10`）|

**② 会响档：漏了立刻报错**（不用盯，但**别和上面混成一张表**）

| 声明点 | 在哪 | 漏了会怎样 |
|---|---|---|
| `DATA_DIRS` | NAS `.env` | cross-seed 扫不到东西 —— 农场空转，日志当场不对 |
| `LINK_DIR` | NAS `.env` | 硬链接建不出来，cross-seed 报错 |
| `TORZNAB_URLS` | NAS `.env` | 那个站压根不在容器里，`check_env_applied()` 下一批就喊 |

> ★ **分档本身就是判据。** ② 漏了会自己喊；① 漏了**两边都正常** ——
> 所以「要不要给它配检测器」这个问题，答案完全由落在哪一档决定。
> 把两档并回一张"完整的声明点表"看起来更整齐，但会把这条判据抹掉。
>
> ★ ① 里的**每一处**都是「登记了却不在名单里」和「压根没登记」**长得一模一样**。
> 2026-09-12 的 HDtime 就是活例 —— 它早在 Prowlarr 和 `.env` 的 `TORZNAB_URLS` 里，
> 名单里没有它，**224 部 UNMATCHED 一部都没往新站重搜**，而 Prowlarr / cross-seed
> 那一侧看起来一切正常。

**看起来像识别、其实不是**：

```bash
python scripts/reseed-state.py init --roots-from-env .env --match "DouBan_IMDB"
```

它是「**从人给的清单里，按人给的关键词挑**」，不是「识别出哪些是大包」——
**关键词是人给的**，系统不生成关键词。所以新包没进**清单**，`--match` 也永远挑不到它。

⚠ **v3 之后它一度连"挑"都做不到**：农场切换把 `DATA_DIRS` 从 49 条源目录改成
**1 条**（农场本身），而它**只读 `DATA_DIRS`** → `--match "DouBan_IMDB"` 一条都匹配不上，
直接报「没挑到任何根」。源根清单搬到了 **`FARM_SOURCES`**，**工具没跟着搬**。

✅ **2026-09-12 已修**：`_roots_from_env()` 现在**首选 `FARM_SOURCES`、缺席时退回 `DATA_DIRS`**
—— 照的是 `build-farm.sh:132-155` 里**早就写好**的同一种优先顺序（连退回时的报错文案
`build-farm.sh:178-181` 都有），不是另立一套规矩。验收见 §19.1.3。

**农场之后，「包」只剩两个用途**（搜索那一侧已经不需要它了）：

* **webhook 发源路径** —— 发的是 `movie.path` = **源大包路径**，不是农场路径；
* **按包统计 / 轮流跑** —— `report` / `trend` / `--packs` 的分账。

> ★ 换句话说：**「包」现在是个纯账本概念** —— 决定怎么记账、发什么 webhook，**不决定搜什么**。

**盯着漏声明的两个计数器**（都在日报里，各配一条基线 —— 期望值都指回一条判据之外的真实记录）：

| 计数器 | 覆盖 ① 的哪一处 | 基线 | 涨了意味着 |
|---|---|---|---|
| **全场无人认领**（§18.19.2） | `pack` 表 | **1**（那条 `0观影清单chrlee整理` 的 xlsx） | 有人加了包、忘了登记 `pack` 表 |
| **声明点〔`--packs`〕**（§19.1.6） | `--packs` | **0**（2026-09-13 起 —— `mbf` 已排进 `PACKS_DEFAULT`；此前恒为 1） | 有人又落下一个包 —— 或被驱动名单里的名字写错了 |

⚠ 覆盖面仍要说清：① 里的 **`FARM_SOURCES`** 与 **文档里的 init 配方**今天**仍然没有检测器**
—— 它们没有一条能自动对账的"另一侧"（前者要 NAS 上真去 build 才看得出，后者是文档）。

### A.15.2 原理 B：搜索压力来自「分母」，不是「频率」

* **不是「一直在搜」，是每站 14 天一轮** —— `DEFAULT_CADENCE_DAYS = 14`，
  注释原话就是「**永不停止的机器人流量**」。这是**设计时就知道的代价**（见「为什么是 14 天而不是 7 天」）。
* **当前那个「均摊」是「优先级插队」的副产品** —— 待搜清单按
  `SKIPPED → ERROR → PENDING → UNMATCHED` 排序、每批 `--limit 50`，
  于是 `UNMATCHED` **永远排在队尾**。它今天能被节流，只是因为 `SKIPPED`/`PENDING` 还多；
  **等它们被清空，`UNMATCHED` 的压力会突然全部释放。**
* **压力的真源**：`PENDING`/`SKIPPED` 都是**一次性**的（搜/重搜一轮就没了），
  只有**到期的 `UNMATCHED` 每 14 天重生一次** —— 这才是永久压力。

> **所以杠杆不在「均摊时间」，在「减少分母」** —— 而减少分母**唯一安全**的做法是
> **「按站历史表现降频」**，不是「按片预测会不会命中」。
>
> ★ 四条思路（抖动 / 按站自适应周期 / `EXHAUSTED` / 全局日预算）各自的成本、风险
> 与推荐排序，见 **SUMMARY §19.2.4**。**这四条都还没实现。**

### A.15.3 原理 C：BT 只校验 **Piece**，不校验「整个文件」

`.torrent` 里**没有"文件哈希"这种东西** —— 只有 **`pieces`**：
文件被按**建种人选定的固定大小**（常见 1/2/4/8 MB）切成片，
**每片一个 SHA-1**，按顺序拼成一个长字符串。**校验只发生在这一层**：

| 层 | 校验什么 | 谁校验 |
|---|---|---|
| **Piece** | 每片的 SHA-1 是否与 `.torrent` 里记的一致 | **BT 客户端**（qB / Transmission） |
| **文件** | ★ **没有独立的文件级校验** | —— |

**由此推出的两条**：

* **piece 全过 = 做种完整**；**有一片不过 ⇒ 客户端会去重下那一片**（**不报错、不拒绝**）。
* ★ **「文件看起来一样」≠「piece 全部通过」** —— piece **会跨文件边界**
  （一个片 = 文件 A 的尾部 + 文件 B 的头部），且**片大小是建种人定的**。

★ **对本项目意味着什么**（两条，都很实用）：
1. **判「能不能跨站做种」的唯一判据是「字节是否一致」** —— 字节一样，
   qB 按新种子的 piece 表重算**照样 100%**，**片大小不同不影响命中**。
2. **真正咬人的是 `partial`** —— 它按"名称 + 大小"就放行，**piece 哈希压根没对过** ⇒
   qB 重下那几片 ⇒ **源文件被写穿**。见下节。

> ★ 本项目**只**做「搬运 + 触发 recheck」，**不**给任何"这是真种"的保证 ——
> **那是 qB 的 piece 校验说了算**，不是本项目说了算。责任边界见 **⚠ 免责声明与使用边界**。

### A.15.4 风险清单

| 风险 | 现状 | 谁在看着 |
|---|---|---|
| 漏声明 → 包**静默不存在** | 3 个声明点**全是手工** | 只有第 2 处有（无人认领计数器，基线 1） |
| **搜索总量没有上限** | `delay` 控速、退避检测**被动等** —— 但「一天总共能发多少」**没有闸门** | 无 |
| `UNMATCHED` 的「软性节流」会**突然释放** | 靠 `SKIPPED`/`PENDING` 还多撑着 | 无 —— **不可观测** |
| 站上「后来才有人发种」的机会被漏掉 | 14 天周期是当前兜底 | 无 |
| 「按片预测会不会命中」这条路 | **没采用，也不该采用** —— 判据只有模型自己一个来源 | 见 SUMMARY §19.2.5 |
| **源文件被 qB 就地重下写穿**（partial 注入 × 硬链接） | 2026-09-13 起 `matchMode` ⇄ `linkType` **互锁**（只有 reflink 才许 partial）+ **链接守护**每日差分 | 存量 628 条仍是硬链接（**故意不重建**）—— 靠守护发现、按需重建，见下节 |
| **产物是「不完整做种」**（partial 注入的形状） | 同一轮已关闸；已注入的 **629 条里 551 条（88%）来自非完整匹配** | 链接守护只盯「文件被改没改」—— **不盯「这条种子产不产出」** |
| ★ **有人拿本工具链去做「假种」/ 刷流量 / 冒充** | 本项目**只建立链接 + 注入种子**，无任何生成或掩盖数据的路径 | **无 —— 这是责任边界，见「⚠ 免责声明与使用边界」** |

> ★ **上表的「见下节」指 `A.16`** —— 原文在 README 里指向紧接着的「源文件被写穿」一节；
> 搬迁后紧邻变成了 `A.16`，**指代关系未变**（本节 `A.15` 之后即 `A.16`）。

---

## A.16 写穿事件与链接类型（真实值）—— partial × 硬链接

> ★★ **本节是 2026-09-20 从 `README.md` 逐字节搬来的**（README 瘦身成门面）。正文一字未改。
> ★ 本节是 **2026-09-13 那次事故的完整现场记录**（含 9 个确证文件、分秒吻合的实证表）
> —— 它是本项目**最贵的一条教训**的落点，**别公开贴出**，也别精简。

### A.16.1 源文件被写穿 —— partial 匹配 × 硬链接农场（2026-09-13 发现）

**一句话**：cross-seed 以 `matchMode: partial` 把「名称+大小匹配、但 piece 哈希不一致」的
单种也注入 qB；qB 校验(recheck)不通过时**不报错**，而是**就地重下**那几个 piece；
而 `linkDirs` 是硬链接 —— 于是这次重下**直接写进源文件**，无声改写库里母本，
Farm 侧还显示 100%。

**三个前提各自都"合理"，叠在一起才致命**：
① partial 是官方推荐的高命中模式；
② `skipRecheck: false` **看起来**是安全网（实际上它不通过时 qB 只会重下，不会拒绝）；
③ 硬链接是为了零磁盘开销。
上游文档甚至把这个现象写成正常行为：

> Nearly all partial matches recheck to **99.9%** rather than 100% *(expected due to piece hashing)*

**实证**（两处时间**分秒吻合**）：

| 包 | 注入种 decision | 种子完成时刻 | 对应源文件 mtime |
|---|---|---|---|
| 哥谭.全5季 `04421b52` | `MATCH_PARTIAL` | 09-13 16:35 | 15:57 / 16:05 / 16:35 |
| 教父 1972 `db7be3ac` | `MATCH_PARTIAL` | 09-12 09:49 | 09-12 09:49 |

反证（天然对照）：同一份《致命魔术》被注入 **6 次** —— 5 条 `MATCH`（完成 09-12 19:22）
源**毫发无伤**，第 6 条 `MATCH_PARTIAL` 卡在 `99.9996%` 正在重下。

规模：已注入的 629 条里 **551 条（88%）来自非完整匹配**（`MATCH_PARTIAL` 472 +
`MATCH_SIZE_ONLY` 79 —— 后者只按大小匹配）。

**处置**：分两步走，**第一步关闸门，第二步给存量上锚**。

#### A.16.1.1 第 1 步 · 关闸门：把 `matchMode` 和 `linkType` 做成**结构性互锁**

`cross-seed/config.js` 里 `resolveMatchMode()` 不再单独看 `MATCH_MODE`，而是先看链接类型：

| `linkType` | `matchMode` | 为什么 |
|---|---|---|
| `hardlink` / `symlink` | **强制 `strict`**（`.env` 的值被忽略并告警） | 同 inode / 同文件 ⇒ 重下 = 写穿源文件 |
| `reflink`（COW） | `.env` 的 `MATCH_MODE` 生效，可以 `partial` | 重下只改副本，源不受影响 |

这么耦合是**故意的**：把"放宽匹配"的前提写进代码，以后谁想调 partial，**必须先让
链接真的是 COW 的**，而不是在 `.env` 里改个值就绕过。`reflinkOrCopy` 会被**降级成
`reflink`** —— 它在 reflink 失败时**静默整份拷贝**（上游文档自己标了 Danger），
而本卷已近满，一次静默拷贝就能把注入卡死，且"静默"意味着你只看到跨站做种莫名停了。

`orchestrator/hardlink.py::prestage()` 是**另一条**建链接的路径，也一并接上了
`matcher.link_type`（此前 `config.py` 里那个键只被定义和校验、**全仓库无人引用**，
是个死键 —— 不接上它，config.yml 里写 reflink 也只是好看）。

#### A.16.1.2 第 2 步 · 给存量上锚：链接守护（the 628 条仍是硬链接）

reflink 只保护**新建**的链接。改动前已建的 **628 条仍是硬链接**，且**故意不重建**
（重摆几十 TB 链接的风险大于收益）—— 但必须能发现"它正在被写穿"。

判据：给这 628 条 payload 的文件建一份 `(size, mtime_ns)` 指纹基线，**定期差分**。

- **判据本体**在 `orchestrator/state.py`（`linkguard_snapshot` / `linkguard_diff` /
  `linkguard_owner` / `linkguard_inflight`）—— 本进程跑在 NAS 宿主机上，
  判据只有一份才谈得上两侧一致。
- **基线**落在 `<compose>/drive-loop/scripts/.linkguard.state`（**单独一个文件**：
  几千~几万条快照不该让每个 watch 都搬一次 —— `.reconcile.state` 是几个 watch
  整文件读改写的）。
- **接线**在 `drive-loop.linkguard_watch()`，挂在**每日台账**里，只报数量：
  `被改写 N · 新增 N · 消失 N · 正在动 N`。**首读不响**（现状不是新闻，#47 的规矩）。
- **诊断端** `scripts/diag/crossseed-linkguard.py`（Windows 上跑，**只读**）：告诉你
  具体是**哪几条**（默认只出 hash 12 位，`--show-names` 才出发布名）。
- ★★ **`_linkguard_detail.changed` 在首读那一轮必须是空的**。首读 `base={}` ⇒
  `linkguard_diff` 把**整库**算进 `added`，而 `linkguard_watch` 把 `changed + added`
  合并写进同一个键 ⇒ 诊断端吐出「★ 被改写 / 新增（4 条种子受影响，共 **2816** 个
  文件）」—— **而那天什么写穿都没发生**（2026-09-13 18:00 起锚时真的这样，已修、
  已钉回归）。注意**只看日报是发现不了的**：正文与 metrics 本来就对（`first` ⇒
  三个数全 0），错的恰恰是"出事后去查证时读的那份清单"。

★ **为什么不直接监控"是否被 recheck"**：qB 不提供逐种的 recheck 计数，
`checkingDL`/`checkingUP` 只是**瞬时**状态，事后查不到；而且 recheck 本身无害 ——
有害的是它**失败后那一次就地重下**。所以判据落在**结果**（文件到底变了没）上。
瞬时状态另记一格（`linkguard_inflight`），用来抓"此刻正在发生"。

★ **`uploading` 那个坑**：`uploading` **不以 `UP` 结尾**，只按 `endswith("UP")`
判会被当成"正在下载"⇒ 误暂停健康做种。这个判据写错了**不会报错**，只会把
628 条好种子一条条 pause 掉 —— 而"暂停"看起来完全正常。是 `tests/test_linkguard.py`
当场抓到的（生产上那把临时看门狗用的是同一个写错判据，只是那批种子恰好全是
`stalledUP` 才没误伤）。

**残留风险 / 边界**：
- `/volume1` 确认是 **BTRFS**（`/dev/mapper/cachedev_1 /volume1 btrfs …`）⇒
  **reflink（COW）可用**。★ 这块卷**已用满 100%**（仅剩 ~19 GiB）—— COW
  "不写不占空间"在这里尤其划算，但重下分离出来的块**要占真实空间**，留意容量。
- 这块卷上 **reflink 是真的在发生**：删掉旧根那 9 个文件时，`%h == 1`（不像硬链接）
  却**一分空间都没回来** —— 因为 COW 共享 extent。⇒ **判 inode 数判不出 COW**，
  要看 `btrfs filesystem du -s` 的 Exclusive 列。
- **写入面已实测（2026-09-13）**：qB 全部 931 条的 `save_path` **都**在
  `reseed_singles/<站点>/`（HDFans 488 / 南洋 258 / BTSCHOOL 143 / HDtime 42），
  **没有一条**落在 `reseed_farm` —— 面很窄，只有这一处要改。
- 已被改写的文件**无备份可恢复**：`net view` 没有任何备份共享，4 处 `#snapshot` 均不存在，
  `download/可删` 与 `download/temp` 为空。诊断出"被改写"之后，恢复要**回站点重新下**
  —— 那条数据**和源是同一份**，删种子只会少一份证据（见任务 #74）。
- inode 基线存档在 **`D:\tmp\reseed-inode-baseline\`**（**仓库外** —— 里面有完整文件名）。
  日后谁再被改写，拿这份比即可。

**损失清单与收尾（2026-09-13 定案，按 16:51 那份基线快照判）**

- 全库 **5654** 个 payload 文件里 mtime 异类 **18 条**，去重后是 **9 个文件**：
  **5 个确证被写穿**（源侧同时异常 —— 哥谭S01 ×2、教父1972 ×1、新蝙蝠侠2160p ×1、
  Arrow S01 ×1）；**1 个**哥谭S01 文件被改写但源侧没有对应硬链接（**只伤它自己**）；
  **3 个判不了**（源侧反而更旧 ⇒ 它本来就不是"同一份"）。
  ★ 其中**哥谭、教父**两组另有上文那张**分秒吻合**的实证表 —— 那是当场抓到的 **2 例**；
  这里说的 5 个是事后按基线快照扫出来的，两者口径不同、互不冲突。
- 改写时刻：哥谭 09-13 15:57–16:35、教父 09-12 09:49（**都在本周、当时正在进行**），
  另有 2026-06-13 ×3 与 2025-12-14 ×2 属历史改写。
- ★ **本条到此闭环，没有待办动作。** 当初记录里那句"待盯"——看 17:38 那轮 inject
  会不会把暂停的条目唤醒——**已经盯完**：17:38:48 那轮是 `No torrent files are
  awaiting injection`（**0 个文件**），`c802c614` 在日志里的最后一处提及是 16:39:21，
  qB 里现在 **0 条**，暂停连续 **8 轮**没被唤醒。
- 恢复路径**确认不存在**（见上一节最后一格）⇒ 剩下的只是"**回站点重新下**"这一个人的
  动作，**不是代码动作**，所以不挂在任务列表上。

#### A.16.2 附：硬链接 vs reflink —— 差在哪、**怎么判**、以及本项目**判错过一次**

| 维度 | **hardlink（硬链接）** | **reflink（COW / 写时复制）** |
|---|---|---|
| 本质 | **同一个 inode** | 看起来像副本，实则**共享数据块**，**inode 不同** |
| 创建时占用 | **0 字节** | **0 字节** |
| ★ **有人写时** | ★ **改全部**（含源文件） | ★ **只改自己**（复制被写的那一块） |
| 写后占用 | 0（但**源被改**） | **被写的那一块** |
| 跨卷 | ❌ 不行 | ❌ 不行 |

**三条硬性警告**：

1. ★ **hardlink 下，qB 的任何"补下载"都直接改源文件，且不可逆** —— 没有快照就回不来
   （本项目实测：`net view` 无任何备份共享、4 处 `#snapshot` 全不存在）。
2. ★ **reflink 只保护"新建的"链接** —— 已存在的 hardlink **不会自动变**，要**重建**。
   本项目**故意不重建**那 628 条（重摆几十 TB 链接的风险大于收益），改用**链接守护**发现。
3. ★ **reflink 的代价是"重下的那块占真实空间"** —— 对本项目这块**已满 100%** 的卷是直接副作用。

**怎么判「是不是同一个 inode」—— 本项目在这上面判错过一次，务必照这条来**：

| 路 | 可用否 |
|---|---|
| Windows 经 SMB：`st_nlink` | ❌ **恒读成 0**（`orchestrator/state.py` 里写着这条注释） |
| Windows 经 SMB：`nNumberOfLinks` | ❌ **恒读成 1** —— ★ 拿一对**定义上同 inode** 的硬链接（农场条目 vs 它的源，`cp -al` 建的）做对照，**两边都报 1** |
| **Windows 经 SMB：`fid`** | ✅ **唯一可用** —— `GetFileInformationByHandle` 的 `nFileIndexHigh/Low` **就是服务端 inode**，跨 SMB 准确透传（同一路径连读两次结果一致） |

★★ **关键不是"读不到"，是"读得到一个假的"** —— `nlink = 1` 看起来完全正常，
于是「`nlink = 1` ⇒ 不是硬链接」这条推断**整套是错的**。
这是本项目反复栽的**假阴性**形状：**命令成功了、结论是空的**。判据一律用 **`fid`**。

★ **再进一步：`fid` 两两不同 ≠ 删了能释放空间。** 删旧根那 9 个文件时实测
`%h` **全部 = 1**、`fid` 也**两两不同**，删完**一分空间都没回来** ——
因为 **COW 共享 extent 时 inode 本来就是不同的**。
⇒ 判"删了省不省空间"要看 **`btrfs filesystem du -s` 的 Exclusive 列**（≈0 ⇒ 与别处共享）。
同一实测见本节末尾「残留风险 / 边界」第二条。

---

## A.17 部署、漂移与凭据（真实值）

> ★★ **本节是 2026-09-20 从 `README.md` 逐字节搬来的**（README 瘦身成门面）。正文一字未改。
> ★ 本节讲的两件事**都在 Windows 侧跑**（`deploy.sh` / `check-deploy-drift.py` / `scan-secrets.py`），
> 是「唯一从电脑发起的写操作」及其两道闸 ⇒ **别公开贴出**（含 NAS 路径与杂物计数）。

### A.17.1 漂移哨兵 —— NAS 和仓库到底一不一致

`deploy.sh` 是**白名单式、单向**的：它保证**白名单里**那些文件两边逐字节一致，
**白名单之外的一律不碰、也不报告**。于是有两个方向会悄悄长东西：

| 方向 | 长出什么 | 后果 |
|---|---|---|
| **A（NAS 上冒出来的）** | 手工拷上去的脚本、忘了删的一次性补丁、调试产物 | **不在任何同步机制里** —— 既不会被更新也不会被发现，只会在某天以「NAS 上跑的行为和仓库里这份不一样」的形式咬人 |
| **B（仓库里没登记的）** | 新写了脚本，忘了加进 `deploy.sh` 的 FILES | `git push` 把它带走了，但**它永远上不了 NAS**，而且没有任何东西会提醒你 |

两个方向都有前车之鉴：`build-farm.sh`、`fix-statedb-farm-root.py`、`nas-update-env.sh`
三个文件都曾长期**靠手工拷**，2026-09-12 才逐个收进白名单。

```bash
python scripts/diag/check-deploy-drift.py              # 两个方向都查（DST 取自 --dst / 环境变量 / scripts/.nasrc）
python scripts/diag/check-deploy-drift.py --all        # 连已知生产独有的一并列出
python scripts/diag/check-deploy-drift.py --cleanup    # 额外打一份「整理杂物」的 mv 计划（仍然只读）
python scripts/diag/check-deploy-drift.py --no-nas     # 只查 B 方向（NAS 不通时也有用）
```

**退出码**：`0` 干净 · `1` 有需要人看一眼的 · `2` 环境问题（NAS 不可达 / `.env` 解析失败）。

> ★ **受管集合是从 `deploy.sh` 的 FILES 数组解析出来的，没有第二份拷贝** ——
> 复制一份就是又造一个漂移源。解析对不上（有 `::` 的行没解析出目的地）时**直接退出码 2**：
> 受管集合一旦悄悄变空，所有文件都会被误报成「未知」，那种假警报会把真信号淹掉。

**只有三种东西会出现在 NAS 上**：白名单里的（受管）、`KNOWN_NAS` 里逐条写明了理由的
（`.env`/`prowlarr/`/库/日志/缓存…）、以及**杂物**。第 4 种——没登记过的——才会报警，
提示二选一：**收进白名单**，或**加进 `KNOWN_NAS` 并写清它凭什么在那儿**。

> **杂物**（`.env.bak.*` / `.env_bak` 等任何 `.env` 备份变体、`build-farm.sh.bak.*`、
> `notify/probe-artifacts-*/`、`__pycache__/`）
> 只**报数**，不自动删。`--cleanup` 会打一份**只含 `mv`、不含 `rm`** 的计划 ——
> 搬进 `<compose>/_cleanup-<日期>/`（同文件系统内的 rename，原子且可逆），
> 确认无碍后在 NAS 上（或 DSM File Station，那边有回收站）删掉那一个目录即可。
> ⚠ **对 NAS 的 UNC 路径跑 `rm` 是禁止的** —— SMB 上没有回收站，glob 打错一次不可逆。
>
> ★ 杂物搬走**不等于**消失，哨兵的杂物计数**不会因此下降** —— 要等那一整个目录被删掉。
> 哨兵量的是「NAS 上有什么」，不是「看着乱不乱」。
>
> ★★ 而且 `__pycache__` **会自己长回来** —— NAS 上**真的在跑 Python**（`drive-loop`），
> 它 import 一次就写一次 `.pyc`。2026-09-12 晚实测：整理完计数 23，二十来分钟后复跑
> **变成 26**，多出来的 3 个正是 `drive-loop/…/__pycache__/` 下的。
> **所以杂物的下界不是 0，而是「正在跑的那几个模块」** —— 计数涨回去是**正常现象**，
> 既不是整理失败，也不是哨兵坏了。别把它当成"没清干净"去反复清理。

**★★★ 2026-09-21：「未知 2 个」是怎么定性的 —— 两个都**不是**代码缺陷，且两个都已登记**

> 症状：哨兵连续报 `★ 未知 2 个：_AW7IV~D / drive-loop/state.db`（rc=1）。
> ★★ 按本节自己的规矩，常红就等于**没有哨兵** ⇒ 必须逐个定性，不能放着。

| 文件 | 读数（`stat` 实读，不是推断） | 定性 | 处置 |
|---|---|---|---|
| `_AW7IV~D` | 0 字节；属主 **197609(艾森克)**＝**Windows 侧用户**（NAS 服务一律 root，见 `A.2.1`）；`Birth == Modify`（**从未被写过**）；名字是 8.3 短名 + `~` 的 Win32 保留形状 | **SMB 客户端临时残留** —— 创建时刻本机正在做整目录 SMB 遍历 | ★ 登记进 `KNOWN_NAS`（**锚死这个具体名字**，不写宽规则） |
| `drive-loop/state.db` | 0 字节；真库是 `drive-loop/hlink/state.db`（1.1 MiB，同时刻） | **0 字节孤儿库**（存量） | ★ 登记 + 建议 `--cleanup` 搬走 |

★★ **两条都要说清"为什么必须登记，不能只删"**：它们**没有可复现的来源**
（代码里 zero 处引用，已 grep）⇒ 删了下次扫描照样报「未知」⇒ 哨兵**常红**。
`KNOWN_NAS` 里的每一条本来就是"凭什么该在 NAS 上"的**公开声明**，这两条按同一规矩办。

★ **顺带查清一件本来会误判的事**：`docker-compose.yml:130` 那个
`RESEED_STATE_DB=/state/state.db` **不是**这条孤儿库的成因 —— 它容器内挂的是 `./drive-loop/hlink:/state:ro`，
指的是**真库**。⇒ 不要拿这个赋值去解释那个 0 字节文件（**两个都叫 `state.db`，但不是同一个**）。

★ **生成它的那条路已经堵住了**：`StateStore.__init__` 的 `create=False`（默认）
在文件不在时抛 `FileNotFoundError`，`create=True` 只有 `reseed-state.py init` 与测试夹具显式传。
⇒ 「打错一层路径就在媒体目录里留下一个空 `state.db`」这个现场**不会再发生**（`ERR-SQL-04` 那个形状）。
★★ 而 `orchestrator/main.py:176` 与 `drive-loop.py:1714` 各自那一道 `is_file()` 前置闸**仍然要留着** ——
它们防的是**路径笔误**，而笔误永远会有新花样（`ERR-SQL-04` 的现场就是"看着命令跑成功了"）。

---

### A.17.2 推前凭据扫描 —— 别把密钥推上去

`git push` 一旦把 cookie / passkey / apikey 推上去，**内容就已经在远端了** ——
删掉也只是多一个 commit，历史里还在。所以在 push **之前**拦一道。

```bash
python scripts/diag/scan-secrets.py      # 0 = 可以推；1 = 有新引入的命中，别推
git push origin main                # 绿了再推
```

它**按值的形状**找，不按键名 —— 按键名匹配会漏掉嵌在结构里的凭据
（`TORZNAB_URLS` 里逗号分隔的多条 URL 各带一个 `apikey=`、Prowlarr 的
`indexer.fields` 里塞着 cookie/passkey、`options`/`fields` 里还嵌一层）。两道网：

| 网 | 怎么找 | 覆盖 |
|---|---|---|
| **① 值指纹** | 从本地 `.env` 提出凭据值，**只算 sha256**，再找仓库里哪一行出现了同样的值 | ⚠ **薄**，见下 |
| **② 形状正则** | 完全不依赖 `.env`：URL 里的 `apikey=`、`passkey=`、`KEY=` 赋值、`Bearer`、`Cookie` | ✅ **真防线** |

> ★★ **为什么①的"0 命中"几乎不算保证**：本地 `.env` 是**开发存根** —— 它的
> `TORZNAB_URLS` 用的就是 `.env.example` 里那两个 `xxxx…/yyyy…` 占位符（实测 sha256
> 相同），真实凭据只存在于 **NAS 的生产 `.env` 与 Prowlarr UI** 里，本地从来没见过。
> 所以拿本地 `.env` 去比，等于**拿一份占位符去查泄露**。
> **这道闸门挡住的东西，几乎全部来自②。** 详见 SUMMARY §18.15。

**它只报「命中/未命中」与 `文件:行号`，绝不打印命中到的那个值本身**（只打前 4 位 + 长度）。
所以它读生产 `.env` 也是安全的，输出可以直接贴给人看。

**退出码判的是「这次推新引入了什么」**，不是「仓库里有没有」：

* 命中所在的那一行**在 `HEAD` 里就有** ⇒ 早就推上去了，**不拦本次**（单列一句警告）；
* 只有**本次新引入**的才 `exit 1`。

★ 这条很要紧：本地 `.env` 是存根时，`.env.example` 里那两个占位符会和 `.env` 撞上 ——
按「仓库里有没有」判，**闸门会次次都红**，而次次都红的闸门等于没有闸门。

**三类"看着像、其实不是"的会被规则跳过，并连理由一起列出来**（不静默跳过 ——
静默跳过等于把闸门悄悄钻个洞）：

| 形态 | 例子 | 为什么不是凭据 |
|---|---|---|
| 读变量的代码 | `api_key=cfg.matcher.crossseed_api_key`、`process.env.X` | 是表达式，不是字面量 |
| 英文短语/占位符 | `please-change-me-to-a-long-random-string`、`yyyy…` | 随机凭据不会既无数字又无大写 |
| 同一字符重复 | `xxxxxxxxxxxxxxxx` | 占位符 |

> ⚠ **已知盲区**（写在代码注释里，不装作没有）：一个**真的**由小写字母和连字符组成、
> 每段都是纯字母的长密钥，这道闸门**抓不到**。真实凭据（hex / base64 / 混合字母数字）
> 几乎不长这样，所以这条换来的降噪远大于它带来的盲区 —— 但"几乎不可能"不是"不可能"。

回归在 `tests/test_scan_secrets.py`（**25 条断言**，8 组：阳性两条路 / 阴性 / 三类假阳性 /
形状层**单独**也要拦得住 / 输出接到 `NUL` 也一样 / 只拦本次新引入）。跑：

```bash
python tests/test_scan_secrets.py
```

---

## A.18 通知链路与 DSM 任务（真实值）

> ★★ **本节是 2026-09-20 从 `README.md` 逐字节搬来的**（README 瘦身成门面）。正文一字未改。
> ★ 本节带**真实主机/路径/任务名**（spool 路径、`synoschedtask --get` 的转抄、
> `//iSunker-DS423/...` 的验收命令）⇒ **别公开贴出**。
> ★★ 本节里那两处**勘误**（`21:20` 与「摘要任务曾不存在」）是 `ERR-SCHED-11` 的落点，
> **照旧有效**，见 `#### A.18.1.4`。

### A.18.1 通知 / 告警（NAS 侧发信）

无人值守最怕的不是出错，是**出错了没人知道**。这套东西负责在出问题时主动发邮件。

#### A.18.1.1 分工：跑批只写文件，发信才碰凭据

```
drive-loop.py ──写纯文本事件──▶ <compose>/notify/spool/*.txt ──▶ notify-spool.sh ──▶ 你的邮箱
   (NAS，零凭据)                    (NAS 上的普通目录)            (NAS，读 DSM 自己的 SMTP 配置)
```

**跑批那一侧一行凭据都没有**（不 import smtplib、不读密码）—— 邮件配置只在 DSM 里，
发信由 `notify-spool.sh` 在 NAS 上完成。代价是推送有延迟（NAS 侧定时轮询），
收益是**凭据从没离开过 DSM**。

> ★ 2026-09-12 电脑端退役前，这张图写的是「Windows 只写文件、NAS 才发信」，
> 中间那一跳走 SMB。现在跑批也在 NAS 上，spool 就是本机的一个目录（见上节）。

#### A.18.1.2 两条通道：坏消息立刻发，好消息进日报

| 事件 | 何时发 | 例子 |
|---|---|---|
| `alert` | **立刻发信** | `.env` 未生效（410/401/403）、索引器拉不到名字、连续 3 批失败、整批异常 |
| `batch` / `info` | **只进每日摘要** | 本批新增做种 11 部、全部包已无待搜项 |

同一个告警 key **12 小时冷却**（问题不修每天最多提醒 2 次，不会变成每 15 分钟一封骚扰）；
`batch` **不冷却** —— 每批都是新信息，冷却它只会让摘要数错。

> ⚠ **告警正文里的查证命令必须自带脱敏**（2026-09-13 起）。
> 告警是**给人照着敲的**，而人照着敲的时候正是最急的时候 —— 正文里那条命令
> **等于让程序替我们决定"这时候会把什么打到终端上"**。
> 具体栽过的：`索引器拉不到名字` 那条原本写 `docker inspect reseed-cross-seed | grep TORZNAB_URLS`，
> 而这个键的值是**逗号分隔的一串 URL、每条各带一个 `apikey=`**，那个 key 就是 Prowlarr 的
> 应用级 key —— 它**完全控制 Prowlarr**，Prowlarr 里存着所有 PT 站的 cookie。
> 现在的写法是 `… | sed -E 's/(apikey=)[^,&]+/\1<redacted>/g'`，并在正文里写明
> 「★ 先脱敏再看，**别把原样输出粘进聊天/命令行**」。
> ★ 拿掉那个值**不影响判读**：要判的是「容器里还剩哪个站」，靠 `apikey=` 这个键名和 `/N/api` 就够了。
> 规矩是**写在命令里**，不是写在"记得别看"里 —— 靠人记得的那种防护，这条链上已经漏过一次
> （见「推前凭据扫描」那节对①值指纹的批评：**覆盖薄的那道网，绿了也不代表安全**）。

#### A.18.1.3 每日摘要 = 心跳（死人来信开关）

**该来而没来的日报，本身就是 NAS / 任务计划出事的信号。**
摘要里写明「最近一次批次记录是几小时前」，用来区分「NAS 挂了」和「主机没开机」，
末尾还报告**通知链路自己的健康状况**：

```
── 通知链路 ──
发信方式  : python
spool 积压: 0 条告警
```

积压持续 >0 = 发信链路坏了（而那些告警你根本没收到）—— 这是唯一一个"只能靠摘要告诉你"的故障。

> ★★ **2026-09-13 更正：这句话原先的结论是「所以发不出去的告警**一直留在 spool 里重试、
> 绝不归档**（归档 = 静默丢弃）」—— 那个结论**正是那场邮件风暴的成因**。**
> 「归档 = 丢弃」在后半段是对的，错在把它当成了**绝对律**，于是漏掉了另一半：
> **「重试」本身没有上界**。一条永远发不出去的告警会随每 5 分钟一趟的排空任务
> **无限重发** —— 那天「站点退避中：HDtime」重发了约 6 次。
> 现在的口径是**两条都要**：
> - 发不出去 → 留在 spool 重试，**不丢**（这一半保留）；
> - 但连续失败到 `MAX_SEND_TRIES`（默认 3）→ **归档 + 记一条 `[未确认]` 告警**，
>   于是它会出现在日报的「告警明细」里 —— 「有一条告警你没收到」这件事
>   **既没被丢弃、也没被重发**，而是**变成了一条更响的告警**。
>
> 摘要里 `spool 积压: N 条告警` 旁边还会报「其中 M 条已经在重试」。
> 详见 SUMMARY §21.7 与 `tests/test_notify_drain.py`（28 条断言，**同一份测试
> 跑在修复前的代码上会精确复现「信发了、没记账、下轮再发」**）。

#### A.18.1.4 NAS 侧一次性配置

1. **DSM → 控制面板 → 通知 → 电子邮件**：勾「**启用电子邮件通知**」，用
   「**自定义 SMTP 服务器 + 应用专用密码**」填好并存盘。
   ✅ **2026-09-12 实测已通** —— 配好之后脚本这边不需要再做任何事。
   ★★ **别去看 `/etc/ssmtp/ssmtp.conf`**：DSM **改过** ssmtp 的配置路径，它实际读的是
   `/usr/syno/etc/synosmtp.conf`。前者在本机是个 **0 字节空壳**，
   **邮件天天正常送达时它照样是 0 字节** —— 从它推断"邮件没配"必然出错
   （我这么错过一次，详见 SUMMARY §15）。那个文件里的密码还是**加密**存的
   （`eventpasscrypted`），所以「读 DSM 配置、自己发信」这条路也不存在。
2. 把 NAS 侧脚本同步过去 —— **已在 `deploy.sh` 白名单里**，一条命令：
   ```bash
   DST=//YOUR-NAS/docker_ssd/prowlarr_cross-seed_autohardlink bash deploy.sh          # 先看 diff
   DST=//YOUR-NAS/docker_ssd/prowlarr_cross-seed_autohardlink bash deploy.sh --apply   # 写入
   ```
   > ⚠ **别在批次运行中 `--apply`**（2026-09-12 踩过）：`sh` 是**边读边执行**脚本的，
   > 覆盖正在跑的 `run.sh` 会让它执行到错位的代码 —— `deploy.sh` 现在改成**原子替换**
   > （写 `.new` 再 `mv`，换 inode，老进程继续读旧内容），并会在**检测到心跳新鲜**时给出告警。
   > 但它不会拦你：那批会继续用旧脚本跑完。详见 **SUMMARY §17.2**。
   >
   > ⚠ 想拿临时目录演练（`DST=/tmp/x bash deploy.sh --apply`）之前请确认脚本是新的 ——
   > 旧版本的 `DST=` 会被 `scripts/.nasrc` **静默覆盖**，演练会**打到生产**。已修（§17.4.1）。
   > 现在规则是：**命令行 > `.nasrc`**。
   >
   > ⚠ ⚠ **`deploy.sh` 末尾那句「下一步 `--force-recreate`」不是每次都适用**。
   > 本轮同步的 4 个目的地里，只有 `<compose>/drive-loop/**` 那 3 个是**热路径** ——
   > 它们跑在 NAS 宿主机上（DSM 计划任务），**拷完即生效，下一次唤醒就用新的，不需要动容器**。
   > 第 4 个 `orchestrator/state.py` 是**构建上下文**副本：它进不了正在运行的容器，
   > 因为 `reseed-orchestrator` 是**一次性 CLI**（`ENTRYPOINT python -m orchestrator.main`，
   > 无 daemon），而 `cross-seed` 用的是官方镜像、**完全不吃我们的文件**。
   > **要更新它得 `docker compose build reseed-orchestrator`** —— `up -d --force-recreate`
   > 不带 `--build` 只会拿**旧镜像**重启，对一个一次性 CLI 等于什么也没做，
   > 代价却是白掀一次容器。**别照着那句话无脑跑。**

   然后在 NAS 上填配置：
   ```bash
   cd /volume2/docker_ssd/prowlarr_cross-seed_autohardlink/notify
   cp notify.conf.example notify.conf && vi notify.conf
   #   MAIL_TO   = 你的收件邮箱
   #   MAIL_FROM = ★ 必须与 DSM 里那个**认证账号完全相同**（逐字相同，包括域名）
   #               QQ/163/Gmail 都要求「信封发件人 == 认证账号」，不一致就会得到：
   #                 ssmtp: 501 Mail from address must be same as authorization user.
   #               ★ 这条 501 是**服务器回的**，说明连接和认证都成功了，只差发件人。
   #               留空也行（交给 ssmtp 用它自己配置里的默认发件人）。
   ```
   ★ `notify.conf`（含你的收件人邮箱）**不在白名单里**，`deploy.sh` 永远动不到它 ——
   和白名单"绝不覆盖生产独有内容"的原则一致（`.env` / `prowlarr/` 同理）。
   （不想用 `deploy.sh` 就手工拷这两个文件。）
3. **先验证，再挂任务**：
   ```bash
   sh notify-spool.sh --selftest     # 探测发信方式 / 配置 / 目录（**不判断**邮件配置对不对）
   sh notify-spool.sh --test-mail    # ★ 唯一的判据：真发一封
   ```
4. **DSM 任务计划**建**三个**任务，**用户都选 `root`**（要读 `.env` / spool，还要发信）：

   | 任务 | 计划 | 脚本 | 发送运行详情 | 状态 |
   |---|---|---|---|---|
   | 排空 spool | 每 5 分钟 | `sh <路径>/notify-spool.sh` | **不要勾** | ✅ **保持启用**（`reseed-notify-drain`）|
   | 每日摘要 | 每天 **21:20** | `sh <路径>/notify-spool.sh --digest` | **不要勾** | ✅ **已建回并启用**（`reseed-notify-digest`，2026-09-21）|
   | 驱动跑批 | 每 15 分钟 | `sh <路径>/drive-loop/run.sh` | **不要勾** | ⛔ **已停用**（`reseed-drive-loop`，由容器取代）|

   > ★★ **2026-09-20 勘误（两处，都会让你"看起来修好了"）**：
   > **① 时刻是 `21:20`，不是 `21:00`。** 本表原先写 `21:00`，是**过期读数**
   > （真源：`ENVIRONMENT.md:185` 与 `summary/15` L99 的实测值）。
   > ★★ 照 `21:00` 建回去，会得到一个**跑得起来、但和你以前收信的时刻对不上**的任务。
   > **② 「每日摘要」这条任务曾经不存在** —— 它的**唯一自动调用者**
   > `reseed-notify-digest` 在 2026-09-12 被误判为"多余"删掉了，而那个判断里藏着一步
   > 没验证的跳跃：`batch` 事件**不发信**（`notify-spool.sh` 里 `kind != alert` 只归档）
   > ⇒ **删掉的是出口，"生产"那端毫不知情，继续天天产出**。
   > 症状极具欺骗性：批次照跑、`.tsv` 里天天有「每日台账」、spool 照清空 ——
   > **没有一行日志是红的**，只有邮箱是空的。
   > ⇒ 完整因果见 `ENVIRONMENT.md` 的 **`ERR-SCHED-11`**；本轮实测坐实与处置见 `summary/26` **`§26.29`**。
   > ★ **建完的判据**：手动跑一次 `--digest` 收到信 = 通路通；但**手动成功 ≠ 定时成功**
   > ⇒ 还需**跨一个 21:20** 复读一次"是否自动发出"。
   >
   > ★★★ **2026-09-21 状态更新：② 已处置完毕（用户实答）** —— 用户在 DSM GUI 里把
   > `reseed-notify-digest` **建回并启用**了。⇒ **本表「每日摘要」那一行的状态已改为
   > 「✅ 已建回并启用」**（见上）。
   > ★★ **但「跨一个 21:20 复读」这件事仍未做** —— 上面那条★判据**依然有效**：
   > **手动成功不能推出定时成功**（同族：`§29.8`「`cat` 一次状态文件不是有效取证动作」）。
   > ⇒ 下次开局若已跨过 21:20，**回读一次** `synoschedtask --get \| grep 'Name:'` + 问用户是否收到信。

   > ★★ **2026-09-17 迁移说明（`#58`）**：这条「驱动跑批」任务**在 `drive-loop` 容器启用后应当停掉** ——
   > 两者是**同一个工作的两条路**、且**没有跨进程互斥**：同时发 webhook ⇒ 一次 429 可能废掉几百条（坑 4）。
   > ⇒ **正确顺序：① 先在 DSM 里停掉这条任务；② 再**显式**启用容器：**
   > ```bash
   > docker compose --profile drive-loop up -d drive-loop autoheal
   > ```
   > ★ **启用命令是显式的 —— `docker compose up -d` 不会起它**（那两个服务带 `profiles: ["drive-loop"]`，
   > 这是一把**防呆锁**：代码已就绪、`deploy.sh` 会推，但不会被谁顺手拉起来）。
   > ★ 详细取舍、接管机制（`healthcheck` + `autoheal`）、以及刻意与其余三个服务不同的两处，
   > 见 **`ENVIRONMENT.md` 的 `ERR-SCHED-*` 与 `SUMMARY §26.5`**（原「还没做」第 15 条，2026-09-19 改指）。
   > ⚠ **没停 DSM 任务就起容器 = 两条路同时跑**（容器入口自己有一道兜底闸门会退出码 3，
   > 但它只是兜底，**别当主闸**）。
   > ★★ **实测记录（2026-09-17 晚）**：启用那一刻确实短暂并行过（`--once` 那条 23:45:03 那次
   > 撞上容器），而 `--once` **自己让开了** —— `drive-loop.log` 原话：
   > `上一批（pid 14）仍在运行，跳过本轮`。现已 `disabled`，窗口关上。
   >
   > ★★★ **但「排空 spool」那条（`reseed-notify-drain`）不能跟着停 —— 保持启用。**
   > 它和容器**不是**同一个工作的两条路，而是**分工**：
   > `drive-loop.py` **只往 `notify/spool/` 写事件**（它自己的注释：「发信由 NAS 上的
   > `notify-spool.sh` 读 DSM 自己的 SMTP 配置完成」），**排空/发信只有 `notify-spool.sh` 做**。
   > ⇒ 停了它 = **告警只进不出，全堆死在 `spool/` 里**。
   > ★ 而且它**搬不进容器**：`notify-spool.sh` 读的是 **DSM 自己的 `/etc/ssmtp/`**，
   > **容器里没有那个文件**（在容器里跑 `--test-mail` 会报 `No such file or directory`）。
   > ⇒ 详见 `ENVIRONMENT.md` 的 `ERR-SCHED-08` 与 `ERR-DOCKER-10`。
   > ★ **两条 DSM 任务状态不同是刻意的**：跑批 `disabled`、发信 `enabled`。
   > 复核用 `sudo /usr/syno/bin/synoschedtask --get | grep -A 5 'Name: \[reseed-'`
   > （★ 只能**读**，`synoschedtask` **没有**改状态的子命令 —— `ERR-DSM-10`）。

> ⚠ **三个都不要勾「发送运行详情」**：排空任务每 5 分钟一趟 = **一天 288 封**，
> 驱动任务一天 96 封 —— 那不是告警，是骚扰。结果一定是你去建一条
> 「来自 NAS 的邮件」过滤规则，**连真正的告警一起过滤掉**。
>
> 勾了的唯一好处是"能收到失败邮件"；但失败信号在下面这些文件里**更全**
> （还带退出码和时间线）。装上后想确认 DSM 失败时会不会发信，
> 把任务脚本临时改成 `sh /nonexistent` 手动跑一次就知道了。
>
> ★★ **更正（2026-09-13 晚）：这里原先写「任务脚本别把输出重定向到 `/volume1`」，
>    并说那正是邮件风暴的触发条件 —— 那句话是错的，而且错法和 §21.5 一模一样：
>    拿假设当结论写进了文档。**
>    晚上在 NAS 上回读了任务定义（`sudo /usr/syno/bin/synoschedtask --get`）：
>    排空任务的 `Command` 就是裸的 `sh /volume2/.../notify/notify-spool.sh`，
>    **一个重定向都没有**。所以"重定向到满卷"这件事，这条任务根本没做。
>
>    仍然成立、也仍然要守的是另一半（见 README 上文「磁盘」一节）：
>    `/volume1` **剩余 0 字节**是真的，**别往它写真实文件**。至于任务 stdout
>    落到哪儿 —— 那是 **DSM 自己**决定的，不在我们脚本里，所以这条任务
>    的 stdout 失败了我们也看不见。**别加 `> /volume1/xxx.out` 这类重定向**；
>    要看输出就看下面那些日志文件。
>
>    **还没判明的是**：风暴窗口（12:45–13:10，约 6 趟）里那条任务每趟的退出码。
>    这能把触发条件二选一定下来 ——
>    **退出码非 0** ⇒ 脚本是半途退出的（`say` 写 stdout 失败，`set -e` 带走整趟，
>    设置条件就是那个满卷）；**一路 Success** ⇒ 那就是 `send_mail` 假阴性
>    （脚本正常走完，只是信没出去，`continue` 后退出 0）。
>    取法：DSM 任务计划里那条任务的「运行结果」，或
>    `sudo find /var/log -iname '*sched*'` 后翻它。
>    ★ 两种触发条件的**可观测行为与修法完全相同**（都是"信发了、没记账、
>    下轮再发"），所以这个读数只影响"当年到底怎么坏的"，**不阻塞修复**。
>
> **运行详情不用靠邮件看**（SSH 关着也不再是障碍，这些文件经 SMB 直接可读）：
> - `<路径>/drive-loop/attempts.log` —— 每轮的起跑时间 + 退出码。四个信号分得很清：
>   没有 start 行 = 任务压根没被触发；**有 start 无 exit = 先别下结论**，
>   最常见的是**批次还在跑**（50 部 × `--interval 30s` ≈ 25 分钟起步，
>   再加回灌前的 `--settle 90s`），其次才是进程树被强杀；exit≠0 = 它自己出错了。
>   ★ 判活**别用** `ps | grep drive-loop.py` —— BusyBox 的 `ps` 只显示 `comm`
>   （也就是 `python3`），**看不到命令行参数**，怎么 grep 都是空的。
>   用 `pgrep -f drive-loop.py`，或看 `drive-loop/scripts/.drive-loop.state`
>   里的 `heartbeat_ts` 在不在涨（批次运行期间每 60s 刷一次）。
> - `<路径>/drive-loop/scripts/drive-loop.log` —— 完整日志（自轮转，5MB × 6）。
> - `<路径>/notify/log/` —— 发信记录。
>
> 真正**该**打扰你的东西（连续 3 批失败、`.env` 没生效、索引器拉不到名字）
> 由 notify 走**它自己的 SMTP 通道**发信 —— 那条路才是告警路径，
> 和 DSM 的任务邮件完全是两回事。

> ★ 实测这台 NAS（DSM 7.2 / 423+）发信走的是 **`/usr/bin/ssmtp`** —— 它存在，
> 而且**真的能把信发出去**（2026-09-12 实测收到）。脚本按
> `ssmtp → sendmail → msmtp → mail → python3+smtplib` 的顺序探测，本机命中第一个。
> （早先这里写着"一个发信程序都没有"，那是只查了 sendmail/msmtp 的误判，已纠正。）
>
> ★★ **同一个 `python3` 也是 drive-loop 的解释器** —— 于是它成了整条链路的
> 单点依赖：版本是 **3.8.15**（DSM 自带，Package Center 里没有更新的）。
> ✅ **2026-09-11 23:27 已在 NAS 上实测通过**（`sh drive-loop/run.sh --dry-run`
> → `attempts.log` 记到 `start py=/usr/bin/python3` + `exit=0`；
> 旁边生成了 `__pycache__/*.cpython-38.pyc`，版本由此坐实）。
> 这就是 `scripts/drive-loop.py` 顶部那行 `from __future__ import annotations`
> **不能删**的原因：本文件通篇用 `str | None` / `list[str]`，在 <3.10 上会在
> **函数定义时**当场求值并抛 `TypeError`（不是等到调用），而且崩在 `logging`
> 配置之前 —— **连一行日志都留不下**，表现就是「任务跑了，什么都没发生」。
> `state.py` / `notify.py` / `reseed-state.py` 早就带这行，drive-loop 是补的。
>
> 万一那台 NAS 的 python3 版本对不上（或 DS 升级后换了），症状同样会是「静默」，
> 所以 `run.sh` 在 shell 这一层另记了一份 `attempts.log`（见上）。
> 后备方案：把 `run.sh` 里的 `PY=` 指到一个 Docker 一次性容器
> （`docker run --rm -v ... python:3.12-slim python3 ...`），
> 或换 Entware 的 `python3`（`opkg install python3`，3.11+）。
> 你的 NAS 未必一样：`--selftest` 会打印实际探测结果。

#### A.18.1.5 通知开关（跑批那一侧）

```bash
python scripts/drive-loop.py --once --no-notify                  # 本次不发任何通知
python scripts/drive-loop.py --once --notify-cooldown-hours 1    # 临时缩短冷却（调试用）
python scripts/drive-loop.py --once --notify-spool "D:/tmp/x"    # 换个 spool
```

启动日志第四行会打 `通知: ✓ 启用 → <spool 路径>` / `✗ 已禁用` / `试运行（只打印，不写文件）`
—— 一眼看出通知通没通。环境变量 `NOTIFY_DISABLE=1` 等价于 `--no-notify`。

> ⚠ 电脑端退役后这些开关**主要是手动调试用**（正常路径是 DSM 计划任务跑 `run.sh`，
> 参数在 `run.sh` 里）。在 NAS 上直接跑就用 NAS 原生路径，没有 git-bash 那一层。

> 从 git-bash 传 `--notify-spool` 时**别用 POSIX 路径**（如 `/tmp/x`）：
> MINGW 会把它改写成 `\tmp\x`，Python 按当前盘解析成 `D:\tmp\x`。
> 用 `D:/tmp/x` 或 UNC。（退役前默认值就是 UNC 指向 NAS；现在默认值是 NAS 原生路径。）

> 通知坏了**不会拖垮跑批**：`notify.py` 吞掉所有异常，只是打一行
> `投递通知失败（忽略，不影响跑批）`。NAS 没挂上时批次照跑，只是没有通知。

---

## A.19 观测口径与状态快照（真实值）

> ★★ **本节是 2026-09-20 从 `README.md` 逐字节搬来的**（README 瘦身成门面）。正文一字未改。
> ★★ **本节是「照抄会错」的重灾区** —— 日初期望值、`metrics` 八格基线、
> 「三种读不到」的区分，都是**判据本体**（`B.10` 第 14 条那个形状）。
> ★ 它同时含**本机实测读数与真实路径** ⇒ **别公开贴出**。
> ★ 状态快照会过期 —— **它的当前版本以 `A.19.1.1` 上标的日期为准**。

### A.19.1 当前状态（「下一步」已撤 —— 见 `§26.2`）（2026-09-14 更新）

> 本节是**操作入口**：怎么继续跑 / 卡住了看哪。
> 完整状态数据与逐项快照只维护在 **SUMMARY §13.7 / §15.6 / §17**，避免两处漂移。

#### A.19.1.1 系统现状（一句话）

**调度**：✅ **已完全迁到 NAS** —— DSM 三个任务已建，**2026-09-12 00:17 首次由计划任务自动跑完一批**
（23:51 那批，26 分钟，`成功 50 / 失败 0`），跨进程节流与 60s 心跳都正常。见下「把调度挂到 NAS 上」·
**通知**：✅ **端到端打通并实测收到邮件**（`--test-mail` → QQ 收件箱；批次通知也真的写进 spool 并被取走）·
**生产 NAS 磁盘上的 `.env`**：`DATA_DIRS` ✅ **1 条（已切农场）** · `LINK_DIR` ✅ **1 条** · `TORZNAB_URLS` ✅ **4 条**（HDtime `/1` + HDFans `/2` + BTSCHOOL `/3` + NanyangPT `/4`）·
**跑着的容器**：✅ **已重建过**（2026-09-12 上午实测改判）—— 容器内的 `DATA_DIRS` **就是农场那条**、
★ 但 `.env` 下午又改过（见 §18.9）：`/3`（BTSCHOOL）**又加回来了**，并且新加了 HDtime `/1` —— 所以容器**要再重建一次**才读得到 `/1`。
索引器 HDFans ✅ / NanyangPT ✅ / BTSCHOOL ✅ / **HDtime ⏳（cookie 已换、Prowlarr Test 通过，卡在重建容器）**。
状态机 `hlink/state.db` ✅ 605 部 ·
农场 `/volume1/video/download/reseed/reseed_farm` ✅ **475/475**（2026-09-12 15:50 复核，退出码 0）· qB :3060 的 **509/509 全部落在新根下** ✅（2026-09-12 16:25 复核：旧根 **0** 条、异常状态 **0** 条；新根下 BTSCHOOL 23 / HDFans 318 / NanyangPT 168，与站点侧种子数逐个吻合）。见 SUMMARY §18.9.2 / §18.10。

> ★★ **2026-09-20 加：这三句都有探针了 —— 别照抄上面那几个数，先跑一遍。**
> 上面「605 部 / 475 / 509」是**当时的快照**（注意它们**不是同一天**：
> 605 与农场是 09-12，qB 那句也是 09-12）。**读数会动** ⇒ 现值当场读：
>
> ```bash
> python tools/doc-audit/04_probes.py    # 8 条探针，其中三条专盯本节
> ```
>
> 2026-09-20 实测**现值**（三条里两条对上、一条**本来就该涨**）：
>
> | 本节声称 | 实读 | 判读 |
> |---|---|---|
> | `605 部` | **605 部**（`COUNT(*) FROM movie`）| ✅ 对上 |
> | 农场 `475/475` | **475**（474 目录 + 1 散文件）| ✅ 对上 |
> | qB `509/509` | 本项目 category `reseed-singles` = **930 条** | ⚠️ **不是"漂了"，是"涨了"** |
>
> ★ **qB 那条为什么从 509 变 930**：`509` 是 **2026-09-12 那次「全量搬 509 条」**
> 的**任务快照**（`summary/11` 那张表里 **605 = 单片总数 / 509 = 待搜**，本来就不是一回事），
> 搬迁之后农场**继续在做种**、条数自然增长 ⇒ **930 是"现在"，509 是"当时"**。
> ★★ 而且 `:3060` 是**共享** qB：读它的**总条数**会得到 **2141**（含**别人**的 1211 条）
> —— 那 1211 条**从来不在本项目的范围里** ⇒ **凡读它必须先按 category 收窄**（同 `A.4`）。
> ⇒ 本节那三个数**当"当时的证据"读没问题**，**当"现在是几"读就不对了**。

> ★ **改判（2026-09-12 上午）**：本节此前写的是「容器 `DATA_DIRS` 仍是 49 条、`/3` 还在 ——
> 只差一次 `--force-recreate`」。实际核对下来**那一步已经做过了**，三条独立证据：
> ① `.env` 的 `DATA_DIRS` 已是 **1 条**，而 cross-seed 日志里**实际出现**
>    `/volume1/video/download/reseed/reseed_farm` 路径 → 容器确实在扫农场；
> ② 近 3000 行 cross-seed 日志里 `410/401/403` **0 条**（此前 `/3/api` 会持续报 410）；
> ③ `drive-loop` 的「索引器自检」连续 **31 次通过**（若容器里还留着 `/3`，这里会警告）。
> 所以下面「收尾命令」**无需再执行** —— 保留仅作流程记录。
> 若与你的记忆不符，请优先复核 ①。
>
> ★ **2026-09-12 12:42 补：推断已变成直接证据。** 这一条原本靠日志**推断**（今天重搜日志里没有 410），
> 现在有了一次**主动的** `--force-recreate`，重建后新容器的启动日志里索引器**恰好两个**
> （`HDFans`、`NanyangPT (南洋)`），`/3/api` 在当日日志里 **0 次**，
> 且 `docker inspect` 回读到 `logging` 段生效。**再无疑点。**

> ★ **磁盘 vs 容器**是本项目头号复发坑：`.env` 改了不会自动生效，
> 必须 `up -d --force-recreate`（`restart` **不重新注入环境变量**）。
> `drive-loop.py` 的「索引器自检」（⑧）会在启动时扫日志自动喊出来，见 SUMMARY §13.10。

#### A.19.1.2 🔴 下一步（按优先级）

> ★ **本节已撤 —— 待办与历史表都已搬走。**
> ⇒ **待办真源：`summary/26-跨会话任务盘面与旧会话清场.md` 的 `§26.2`（主表）+ `§26.2.3`（三张总表）。**
> ⇒ 曾在这里的 **17 行历史表（截至 2026-09-14）**：详表早就在各章；原表全文可从
>   git 历史取回（`git show <本提交之前>:README.md`）。

#### A.19.1.3 ★ 09-13 首读实测：`unclaimed=1` 那条告警**不是修复失效**（2026-09-13 复核）

00:38:28 的 TSV 里有两条 alert：`unclaimed=1`（`农场里有 1 条 searchee 谁都不归`）和
`streak=3`。**第一条字面上正是 #47 要消掉的噪音**（每天响两次、永远），所以它很容易被读成
「修复没生效」。**它不是。** 物证来自 `.deploy-backup/` —— 备份存的是**被覆盖前**的内容，
所以每份备份恰好是**那一次部署之前、磁盘上正在跑的那份代码**：

| `.deploy-backup/` | `_unclaimed_baseline` | `_packs_baseline` | `aborted_kind` |
|---|---:|---:|---:|
| `20260912-231726` | 0 | 0 | 0 |
| `20260913-101900` ← **00:38 在跑的就是这份** | **0** | 2 | **0** |
| `20260913-105013` | 2 | 3 | 3 |

⇒ 00:38 那一刻，**packs 基线已在**（09-12 23:17 部署，所以首读静默、没有 `packs-mismatch`），
而 **`unclaimed` 基线还没上线**（09-13 10:19 才部署）⇒ 那条告警是**旧行为**，符合当时的代码。

★ 为什么非要把这段写下来：**这是一份"只凭 TSV 就能得出错误结论"的记录。**
不写的话，下次翻到 00:38 那行会以为 `unclaimed` 的修复白做了 —— 而真相是
**它到那时为止一次都还没在生产上执行过**（10:19 上线，11:30 之前没有任何批次跑完）。

> **另外两条不在这里重复登记**（各自已有唯一出处，免得两处漂移）：
> * 「搬迁后次日 01:45 之后复核 `IYUU自动辅种` 条数是否仍在增长、首读 **09-14 凌晨**」
>   —— 进每日台账，基线 100 条（真源见 `§26.2`）。
> * 「暂存区 / 回收站清理」 —— **2026-09-13 已做完**：`_cleanup-20260912/` 与
>   `#recycle/env-bak-20260912/` 用 `rm-staging.sh` 真删，另手删 3 条回收站残骸。
>   ✅ 已做完（读数与「别放宽闸 ③」的理由见 `summary/14` / `summary/18`）。
>   `__pycache__` **不清**——它会自己长回来。

#### A.19.1.4 🆕 日常看板（2026-09-12 新增，都在 NAS 上跑）

```bash
D="//iSunker-DS423/docker_ssd/prowlarr_cross-seed_autohardlink"

# 新增做种趋势：按 (周, 站) —— 换站决策看这个，别看某个瞬间的快照
python scripts/reseed-state.py --db "$D/drive-loop/hlink/state.db" trend --weeks 8 -v

# 站点额度台账：滚动 24h 的各站搜过多少「部」（★不是查询次数，也★不是站点余额）
python scripts/reseed-state.py --db "$D/drive-loop/hlink/state.db" \
  quota --db-path "$D/cross-seed/cross-seed.db"

# 农场漂移：有漂移会返回退出码 1（以前恒返回 0，等于永远不报警）
# ★ 环境变量必须写在命令**前面** —— 写在后面会被当成脚本的参数
COMPOSE_DIR="$D" FARM="//iSunker-DS423/video/download/reseed/reseed_farm" \
  sh "$D/build-farm.sh" --verify --map "/volume1=//iSunker-DS423"
```

回**电脑上**再跑两条（查的不是农场，是「NAS 与仓库」本身、以及「待推的东西里有没有凭据」）：

```bash
python scripts/diag/check-deploy-drift.py      # 0 干净 / 1 有要看的 / 2 够不着 NAS
python scripts/diag/scan-secrets.py            # 0 可以推 / 1 有新引入的凭据形状命中
```

日报里**自动**带「每日台账」（额度 + 趋势），每 24 小时最多一条；
「某站此刻正在退避」会**立刻**发告警（按站 12 小时冷却）。

> **★ 对账八格怎么读**（每份日报的 `metrics` 里，TSV 流水只记数字不记正文）：
>
> | 格 | 期望值 / 基线 | 它指回的真实记录 |
> |---|---|---|
> | `fa` / `fb` / `fd` | ★ **不变式：`a == b`、`fd = a − b = 0`**（**不是** 1011 —— 见下） | 当日 `info.current.log` 里的 `Found` 行 |
> | `fb_c_all` / `fb_c_farm` | `0` / `0`（全量口径 / 农场行计数） | `hlink/state.db` 的 `pack`+`movie` |
> | `unclaimed` | **基线 1**（`0观影清单chrlee整理`），**只报新增** | 农场里那个只有 `.xlsx` 的目录 |
> | `packs_undriven` / `packs_unreg` | **基线 0 / 0**（2026-09-13 起 —— `mbf` 已排进 `PACKS_DEFAULT`；此前恒为 1 / 0） | `--packs` 的实际取值 vs `pack` 表 |
>
> ★ **为什么 `fa` 不是 1011**：日报在**当天首批**跑，而它读的是**当日日志**
> （`info.current.log` 按天轮转）⇒ 天然是「**今天 00:00 到现在**」，会随一天推进而涨。
> 所以日报里 `fa` 总是个小数字（实测 09-13 首批 = **76**）。
> **1011 是 09-12 的「全天」数**（手工核时读的同一个文件、读得晚）—— 拿它当日报的期望值
> 是**把一条全天记录写在了日初的读法旁边**。要核全量用 `scripts/diag/audit-found-lines.py`。
> ★ `fb_c_farm` 恒为 `0` 是**自检**：它一旦长期为 0，就说明**农场那条防线还没被走到**
> （`_RE_FOUND` 的 `[inject]` 分支 / `_resolve_searchee_to_pack()` 的农场分支流量为 0）
> —— 上面那个 `fb_c_all = 0` 此时**覆盖不到农场**。绿是真的绿，但它管不到那条路。
>
> **三种"读不到"必须分开看**，混起来就会把「判据坏了」念成「今天没事」：
>
> * **`n/a` = 这一路没输入**（没跑到），**不是**"没事"。括号里会跟上
>   `上次成功 X 前`；若**从未**读到过则写 `★ 从未成功读到过` ——
>   连续几天 n/a 是**判据长期失效**，昨天还好今天抖一下是**偶发抖动**，
>   两句话指向完全不同的处置，所以必须指得回时间。
> * **`0` = 跑了、确实没有**（判据是好的，只是今天真没发生）。
> * **`unclaimed` 报 `0`** —— 那是**跑了、且比基线少**（那条 `.xlsx` 被清掉了？）
>   ⇒ 静默采纳新基线，**但正文会把「比基线少 N 条」写出来**，所以不是无痕；
>   将来**再长回来会被抓住**（当新增报）。
> * **`unclaimed` / `packs_*` 都只报「变化」，不报「现状」**（2026-09-12 深夜 / 09-13 改）：
>   现状（农场里那条 `.xlsx` 谁都不归；`mbf` 登记了却一直没被驱动是 **2026-09-12 之前**的现状
>   —— 09-13 已排进 `--packs`）是**已接受**的，
>   非空就每天喊一次只会把告警喊成噪音 —— 而噪音的代价是真的出问题时没人看。规则：
>   **出现基线里没有的 → 发 alert**；与基线一致、或缩回基线之内 → 不发。
>   基线存在 `.reconcile.state` 的 `_packs_baseline` / `_unclaimed_baseline` 里
>   （**不进代码** —— 写进代码就分不清「回到基线」和「判据死了」），
>   缩也采纳新基线，所以它**再长回来会被抓住**。
>   ★ **不告警 ≠ 看不见**：日报正文每天照旧打印完整差集 / 完整清单。
>   ★★ **不告警也 ≠ 跑了** —— alert 沉默有**四个**来源：
>   ① 与基线一致 ② 缩回基线之内 ③ 状态库读不到（正文写「没跑成」）
>   ④ 调用方没给 `--packs`（正文写「跳过」）。**后两种同样不发 alert**。
>   所以「沉默 = 没事」**要多读一步**：看正文那一行**有没有值 / 是不是 `n/a`**。
>   反过来，**响铃不需要附加判断** —— 响 ⟺ 真的算了、且真有新增。
>   （`n/a` 的读法见上面那条：`n/a` = 没跑到，**不是**「差集是空的」。）
>
> 差不为 0 时**立刻发 alert**，`key` 就是排查入口：`log-parse-miss`（正则没吃下形状行）、
> `reconcile-controls`（判据压根没走到）、`reconcile-empty`（真没搜出去）、
> `unclaimed-searchee`（★ 只在**新增**时发）、`packs-mismatch`（★ 同上）。

> **★ 「连续 N 批失败」不一定是失败**（2026-09-13 改，两个 key）：
>
> | key | 什么时候发 | 是什么性质 |
> |---|---|---|
> | `consec-abort` | 连续 3 批**真失败**（webhook 400/401/403、或整批抛异常） | **真故障** —— 去查鉴权/路径/`.env` 是否 force-recreate |
> | `consec-backoff` | 连续 3 批**提前收工**（站点一直在退避，等到 `--max-wait` 就先收工） | **良性** —— 条目没失败，剩下的下轮重排；长期如此才考虑摘站 |
>
> ★ **2026-09-16 起「提前收工」只发生在"全都退避"时**：还剩健康站就照发，不算收工。
> 所以这个计数从此**只在真有事时才涨** —— 09-13~09-15 那种「一个站被禁 24 小时、
> 另外三个干等」的日子不会再刷它。依据与实测见 SUMMARY §11.13。
>
> 原先只有一个计数、只看 `stats.aborted` 的真假 ⇒ 实测 2026-09-13 的 TSV 里
> **同一批**既写 `ok=22 failed=0`、又写「连续 3 批失败（索引器 HDtime 要等到 …）」，
> 而它的正文还把人指去查 `force-recreate` —— **名字和指向都是错的**。
> 现在分开计数（`DriveStats.aborted_kind`），**两个计数互不干扰**，
> 只有**正常跑完**才一起清零 —— 所以「中间夹了几批退避」**掩盖不了**真故障。
> 两种混着出现时标题会补一句「共 N 批没跑成」，免得把 N 念成"连续"。

> **趋势里的两个口径别搞混**：`本周新增做种 N 部` 的 N 是**按片去重**的，
> 而下面按站列的数字之和**会比它大** —— 一部片同时在两个站做种时，两个站各记一次
> （渲染里会自动补一行说明，只在"和 > 总数"时出现）。

#### A.19.1.5 三个包（**数字会变，别信写死的**）

阶段分布**一律以状态库为准** —— 下面这张静态快照在 2026-09-12 已实测**过期**，不再维护：

| 包 | 单片 | 结构 |
|---|---|---|
| `frds-top250-2024` | 486 | ✅ 根目录子目录即发布名 |
| `dc-collection` | 115 | ⚠ 根下多一层中文标签，见「多包支持 → 嵌套结构」 |
| `mbf` | 4 | ⚠ HDFans 上 **0 匹配**（4 个季包全 `Found 0 torrents`）；**2026-09-13 起已排进 `--packs` 被驱动**（#40）—— 若 HDtime 上也 0 匹配就移出名单 |

```bash
DB="//YOUR-NAS/docker_ssd/prowlarr_cross-seed_autohardlink/drive-loop/hlink/state.db"
python -c "
import sqlite3; c=sqlite3.connect(r'$DB')
for r in c.execute('SELECT pack,stage,COUNT(*) FROM movie GROUP BY pack,stage ORDER BY pack,stage'): print(r)"
python scripts/reseed-state.py report --pack frds-top250-2024    # 带按站重搜周期表
```

> **⚠ MBF**：HDFans 上已实测 0 匹配。状态已是 `UNMATCHED`，加新站后会自动解锁、**不会重复浪费额度**。

#### A.19.1.6 怎么继续跑（两种方式）

**方式 A：自动续跑（推荐）**

```bash
# 真跑一轮（当前包下一批，约 24 分钟）
python scripts/drive-loop.py --once --indexers HDFans,NanyangPT --limit 50

# 循环跑：DC↔FRDS 自动轮流，间隔按站点反馈动态调整（45min~4h）
python scripts/drive-loop.py --indexers HDFans,NanyangPT
```

> `drive-loop` 已内置 `--db-path` / `--qbit-url` 的**候选探测**（`CROSSSEED_DIRS`），
> 所以**回灌不会漏参数**。
> ★ 2026-09-12 电脑端退役后 `CROSSSEED_DIRS` 只剩 NAS 原生路径一条 ——
> 原来还有一条 UNC 兜底（Windows 经 SMB 跑时用），现已注释。
>
> **★ 2026-09-11 起，调度跑在 NAS 上**：DSM 任务计划每 15 分钟执行
> `sh /volume2/docker_ssd/prowlarr_cross-seed_autohardlink/drive-loop/run.sh`。
> ★ **2026-09-12 电脑端彻底退役**（包装器与 PC 侧状态已删，见「⛔ 电脑端已不参与」）。
> 当初搬走的原因不是性能，是 Windows 侧**批次会静默消失**，
> 查下去是**两个独立的坑**（完整证据与复现见 SUMMARY §14）：
>
> * **`<StopOnIdleEnd>true`**（当场读任务定义确认）：你一动鼠标/键盘，
>   Windows 就 `TerminateProcess` 整个任务实例 —— 没有 traceback、没有
>   finally 收尾、状态文件里 `running_pid` 永远挂着，之后每轮都空转。
> * **包装器 `.cmd` 是 LF 行尾**（这一条是 2026-09-11 深夜改包装器时引入的）：
>   cmd.exe 按字节块读批处理文件，LF-only 会让它**从某行中间开始执行** ——
>   `REM` 注释里的英文单词被当成命令跑，前面的 `set` 全部丢失，于是留下
>   `exit=9009` + `'""' 不是内部或外部命令`，且**有 `exit=` 没有 `start=`**。
>   已修（转 CRLF）。`deploy.sh` / `run-batch.sh` 的 CRLF 行尾也一并归正成 LF。
>
> `--once` 的自我节流（有人在跑 / 距上批结束不足 30 分钟 → 直接退出）和
> **心跳超时接管**（卡死 10 分钟后下一轮自动接管）两边通用，
> 所以 15 分钟一次**不会**和 24 分钟一批重叠。这也是没有改用
> 「常驻容器跑自带循环」的原因 —— 常驻进程卡死就没人接管了。
> 真实进度一律以 `drive-loop.log` 为准，**别看终端**（输出会被块缓冲吞掉，SUMMARY §13.8）：
> `<compose>/drive-loop/scripts/drive-loop.log`。
> （退役前 Windows 侧那份是 `scripts/drive-loop.log`，**已删**。）
>
> ⚠ 万一要**回退到 Windows 跑** —— 包装器 `scripts/drive-loop-once.cmd` **已删**，
> 但它踩过的两个点必须守住，否则回退会原样再踩一遍（脚本本体在 git 历史里）：
> 1. **别把包装器换成直接指向 `python.exe`** —— python 路径里有中文用户名，
>    经 `schtasks` / MINGW 传递会被搞坏；包装器要用 `%USERPROFILE%` 让 cmd.exe
>    在**运行时**展开，任务定义本身保持纯 ASCII。
> 2. **包装器必须是 CRLF 行尾** —— LF-only 会让 cmd.exe 解析错位、整行命令失效
>    （现象：`exit=9009` 且 `attempts.log` 里没有 `start` 行）。改完包装器务必确认：
>    ```bash
>    python -c "b=open('scripts/drive-loop-once.cmd','rb').read(); print('CRLF',b.count(b'\r\n'),'LF',b.count(b'\n')-b.count(b'\r\n'))"
>    # 要 CRLF 59 / LF 0
>    ```
> 3. ★ 还有 `os.kill(pid, 0)` 那一支 —— 它在 POSIX 上是"探测存活"，
>    在 Windows 上**会真的把进程杀掉**，必须切回 `tasklist` 分支
>    （代码在 `pid_alive()` 里，连 `import subprocess` 一起打开）。

#### A.19.1.7 把调度挂到 NAS 上（✅ 已完成 —— 2026-09-12 凌晨）

代码、状态库、DSM 计划任务**都已就位**并实测跑通：

- `deploy.sh --apply` 已执行（`drive-loop/` 下 6 个文件）；
- `hlink/state.db` 的 605 部已迁到 `<路径>/drive-loop/hlink/state.db`，
  `local_roots` 已从 UNC 归一成 NAS 原生路径；
- DSM 三个任务已建（见「通知」那节的任务表）；Windows 侧任务已 `DISABLE`；
- ✅ **2026-09-11 23:51 / 2026-09-12 00:01 实测**：手动触发的那批正常跑起来
  （`attempts.log` 有 `start py=/usr/bin/python3`），下一次唤醒时正确地打出
  「上一批（pid …）仍在运行，跳过本轮」—— **跨进程节流工作正常**。

**★ 建任务时最容易踩的一脚：「最后运行时间」的小时位存错。**

`synoschedtask --get` 实测到的**错误**状态：

```
reseed-drive-loop    Run time: [2]:[0]    Repeat every [15] min until [2]:[45]
reseed-notify-drain  Run time: [0]:[0]    Repeat every [5]  min until [0]:[55]
```

—— 两条任务分别**只在 02:00–02:45 / 00:00–00:55 之间**才触发，于是表现成
「**手动点『运行』能跑，计划却永远不自动触发**」。

正确值：**小时位明确设成 `23`** —— `drive-loop` → 最后 `23:45`、每 15 分钟；
`drain` → 最后 `23:55`、每 5 分钟（`digest` 每天 21:20、无 repeat，不受影响）。
改完务必复核：

```bash
sudo /usr/syno/bin/synoschedtask --get | grep -A 12 'reseed-drive-loop'
#  期望看到： Repeat every [15] min (s) until [23]:[45]
```

**排错**：`attempts.log` 里 `exit=127` = `run.sh` 找不到 python3 或脚本；
`exit≠0` 且有 traceback = python 层出错（最可能是版本，见「通知」那节关于 3.8 的说明）；
**压根没有 `start` 行** = 任务没被触发（没启用 / **计划窗口不对** / 脚本路径错）；
**有 `start` 没 `exit`** = **先别下结论**，最常见的是**批次还在跑**（见上）。
完整排查表见 SUMMARY §14.5；计划窗口这个坑见 SUMMARY §15.5。

**方式 B：手动单批（原来的做法）**

```bash
URL=http://192.168.0.7:2468
KEY=<CROSSSEED_API_KEY>
DB="//iSunker-DS423/docker_ssd/prowlarr_cross-seed_autohardlink/cross-seed/cross-seed.db"
Q="http://192.168.0.7:3060"          # ★必须带 --qbit-url，否则 SEEDING 被误降级！

python scripts/reseed-state.py drive --pack dc-collection --indexers HDFans,NanyangPT --limit 50 \
  --url $URL --api-key $KEY --db-path "$DB" --qbit-url $Q --apply
```

**一批做完重跑同一条命令就自动推进**，不用记批次号。**先 `--plan` 确认再加 `--apply`**。

#### A.19.1.8 ⛔ 电脑端已不参与（2026-09-12 退役）

**一句话**：跑批、发信、建农场全在 NAS 上；Windows 只剩 **`deploy.sh`**（把代码推上去）、
**`check-deploy-drift.py`**（查两边一不一致）、**`scan-secrets.py`**（推之前扫一遍凭据）、
**`audit-found-*.py`**（对账 `Found` 行：只读 NAS 的日志与库）四个用途。

为什么单独写一节：代码里**到处**都有"Windows 也能跑"的痕迹（UNC 路径兜底、
`tasklist` 判活、计划任务包装器、git-bash 路径转换的告警……），
不写清楚的话，下一个人会以为这条路还活着，然后照着它去配、去调、去踩已经踩过的坑。

**① 退役了什么**

| 东西 | 处置 |
|---|---|
| `scripts/drive-loop-once.cmd` | **已删**（原 Windows 计划任务的入口） |
| Windows 计划任务 `reseed-drive-loop` | 已 `/DISABLE`，**目标文件已删 → 现在是个悬空任务**。彻底删需要管理员权限，见下 |
| `scripts/drive-loop.{log,attempts.log,task.err}`、`.drive-loop.state`、`.notify.state` | **已删**（PC 侧运行时残留） |
| `drive-loop.py` 的 `CROSSSEED_DIRS` UNC 兜底 | **已注释**（标记 `[电脑端已退役 2026-09-12]`） |
| `drive-loop.py` 的 `pid_alive()` tasklist 分支 + `import subprocess` | **已注释**（同上标记） |
| `notify.py` 的 `SPOOL_CANDIDATES` UNC 兜底 | **已注释**（同上标记） |

> 注释保留而不是删除，是因为它们各记着一个**反直觉的坑**，值钱的不是代码是那句话：
> `os.kill(pid, 0)` 在 POSIX 上是"探测存活"，搬到 Windows 上就变成**"探测即击杀"**；
> 以及 UNC 兜底在 NAS 上不报错、只是**悄悄绕一圈 SMB 连自己**（慢，且难查）。
> 要回退就取消注释 —— **`subprocess` 那行要一起打开**，否则 tasklist 分支会 NameError。

**② 还剩两件要你手动做的**

```bash
# 1) 彻底删掉那个悬空计划任务（必须**管理员** PowerShell/cmd，普通权限会「拒绝访问」）
schtasks /Delete /TN "reseed-drive-loop" /F
#    不删也不影响 —— 它是"已禁用"状态，而且目标 .cmd 已经没了，永远不会跑。

# 2) scripts/diag/run-batch.sh 仍然保留（它是一次性的**手动**命中率试跑，不是调度）
#    但它从 Git Bash 跑，照样要读 scripts/.nasrc。
```

**③ 明确保留的东西**

- ✅ **`deploy.sh`** —— Windows Git Bash 跑的，**唯一**还从电脑发起的**写**操作。别删。
- ✅ **`scripts/diag/check-deploy-drift.py`** —— Windows Git Bash 跑的，**只读**。它回答
  「NAS 上有没有我不知道的文件 / 仓库里有没有该部署却没进白名单的文件」，见「漂移哨兵」一节。
- ✅ **`scripts/diag/scan-secrets.py`** —— Windows Git Bash 跑的，**只读**。`git push` 前跑一遍，
  按值的形状找漏进仓库的凭据。★ 它**读**本地 `.env` 但**只算 sha256、从不打印命中到的值**，
  所以输出可以直接贴给人看。见「推前凭据扫描」一节。**只扫不改，也不是调度的一部分。**
- ✅ **`scripts/diag/run-batch.sh`** —— 手动试跑工具，不是调度的一部分。
- ✅ **`scripts/diag/find-packs.py`**（2026-09-20 新增）—— Windows Git Bash 跑的，**只读**。
  回答「**磁盘上有没有像大包、但还没登记的目录**」（扫 `video/download/{movies,TV}` 里
  直接子目录数 ≥N 的）。★★ **它只报信号，绝不自动加包** —— 加包是**产品决定**，
  且会**稀释**其他包的轮换频率；而「注册了」≠「搜得到」（`mbf` 全绿却 `Found 0 torrents`）。
  ★ 所以它**只拟命令，人自己判**；文件头列了两条实测假阳性（音乐包 / 游戏包）。
  ★ 判据：**"读不到 NAS"必须报 `ERR`，不许报成"没有"**（阴性对照已验）。
- ✅ **`scripts/nas-update-env.sh`** —— **2026-09-12 晚已进 `deploy.sh` 白名单**（映射到
  `<compose>/nas-update-env.sh`），跟着 `deploy.sh --apply` 走，**不用再单独拷**。
  它是**生成物**（`scripts/diag/gen-nas-env-update.py` 产出，故本地被 gitignore）——
  之所以能进白名单，是因为它只装载 `DATA_DIRS` + `LINK_DIR` 两个**路径**键、不含任何凭据。
  ⚠ 若哪天它开始携带别的键，**先回看 `deploy.sh` 里那段注释**再同步。

#### A.19.1.9 ⚠ 交接必读的坑

1. **`drive` 忘给 `--qbit-url` → `SEEDING` 被误降级成 `MATCHED`。**
   现象：回灌打出 `已在 qB 里: 0` / `本次新增做种: -51`（**负数**）。
   根因：`stage` 是推导的纯函数，缺 qB 数据就推不出"在做种"。
   补救：补跑一次带 `--qbit-url` 的 `sync` 即完全恢复（幂等）。详见 SUMMARY §13.6 坑 1。
2. **`drive` 忘给 `--db-path` → 回灌被跳过，状态不更新。**
   现象：webhook 全发成功（204），但结尾打
   `[!!] 回灌需要 --db-path（cross-seed.db 路径），已跳过`，退出码 3。
   补救：补上 `--db-path` 重跑，或单独跑 `sync` 回灌（都不发新请求，幂等）。
   ⚠ 附带的**假象**：重跑时终端可能返回**空输出**，看着像"没执行"，其实跑完了 ——
   **判断 drive 是否真跑，查 cross-seed 日志 / `drive-loop.log`，别看终端**（SUMMARY §12.5）。
3. **`.env.new` 存在 ≠ `.env` 已更新。** 判断是否生效一律看
   ① 容器内 `DATA_DIRS` 条数 ② `cross-seed.db` 的 `data` 表，别看 `ls`。
4. **别对大包根打 webhook。** cross-seed 的 webhook 是单线程顺序处理，
   中途撞一次 429 → 后面几百条全部 `Skipped searching (filtered by temporarily
   disabled indexers)`。已实测：一次 429 废掉 295 条。改用 `drive --limit N` 分批。
5. **`--depth` 必须等于 cross-seed 的 `maxDataDepth`**（本项目默认 2），
   对不上就会出现"状态机有、cross-seed 没有"的幽灵条目。

#### A.19.1.10 还没做（**已撤 —— 见状态指针**）

> ★★ **本节原先的 1–15 条（已完成工作的一次性过程记录）已于 2026-09-19 移除。**
> ⇒ **当前待办真源：`summary/26-跨会话任务盘面与旧会话清场.md` 的 `§26.2` + `§26.2.3`。**
> ⇒ **旧会话移交史：同一章的 `§26.1` / `§26.3`。**
> ⇒ 原文全文可从 git 历史取回：`git show <2026-09-19 之前>:README.md`。
> ★ **原先引「第 N 条」的 5 处已一并改指**；本节末尾那条**可复用流程**（改 `DATA_DIRS`/
> `LINK_DIR` 时的重建 + 闭环回读）**仍在下面保留**。

##### A.19.1.10.1 收尾命令（一次重建同时办完两件事）—— ✅ **已执行过，此节仅存流程**

> ★ **2026-09-12 上午改判**：核对发现**这一步已经做完了**（容器内的 `DATA_DIRS` 已是农场那条、
> `/3` 已不在 `TORZNAB_URLS` 里，三条独立证据见上文「系统现状」的改判说明）。
> **不需要再跑下面的命令** —— 保留是因为将来改 `DATA_DIRS`/`LINK_DIR` 还要走同一条路。

本地已把 `.env` 的 `DATA_DIRS` 切成农场那一条，并生成/拷好了更新脚本。
**在 NAS 上**（SSH 或 Container Manager「终端」）：

```bash
cd /volume2/docker_ssd/prowlarr_cross-seed_autohardlink
sh nas-update-env.sh --dry-run     # 先看会改什么：DATA_DIRS 49 条 → 1 条
sh nas-update-env.sh               # 改 + --force-recreate + 闭环回读校验
```

脚本自带：备份 `.env.bak.<时间戳>`、安全闸（**除 `DATA_DIRS`/`LINK_DIR` 外的行必须
逐字节不变** —— 防止本地脱敏占位符污染生产密钥）、改完回读容器内 `DATA_DIRS` 条数
做闭环验证。回滚：`cp -p .env.bak.<时间戳> .env` 后重建。

**重建后请确认**：`drive-loop` 日志里「索引器自检」变绿、`/3/api` 的 410 警告消失、
searchee 数与切换前**一致（1888）**。

> ⚠ **备份文件同样含密钥** —— `.gitignore` 里 `.env` 是**精确匹配**，
> 拦不住 `.env.bak.20260911-204109`。
> ★ **2026-09-13 再修：当时补的 `.env.bak.*` 仍然窄。** `git check-ignore -v` 逐个实测，
> `.env.bak`（无时间戳）/ `.env_bak` / `.env.backup` **全都不被忽略** ⇒ `git add -A`
> 能把一份带全部密钥的副本**直接提交进仓库**。现改成前缀全兜 `.env*` +
> 显式放行 `!.env.example`（纯占位符模板，本来就该提交）。
> 若你在 NAS 上另存备份，别把任何一份拷进仓库。

详细的过程记录、踩坑与决策都在 **SUMMARY.md**
（老会话见 §11.12 / §11.13 / §11.14 / §12；本轮见 **§20**（观测层「沉默 / 响铃」语义收敛）
与 **§22**（`matched_indexers` 恒空 · HDtime 的 429 到底是哪种限流 · `ERR-SVC-17` 改「有据」），
一轮见 **§23**（卷归属双来源复核 · ★★ `stat -c %d` 只在 NAS 侧成立 · 枚举 ≠ 全量 ·
一份「日报加各包进度」方案的 8 条审订），
最近两轮见 **§25**（三轮收口 · `#115` 等触发 · `#116` 措辞 · `#60` 结案 · `#119` 名文件漏扫）
与 ★★ **§26**（**跨会话任务盘面 —— 待办真源** · 旧会话清场 · `#129` 断点 · `#58` 切断点），
观测对账的判据本体见 §18.18 / §18.19）。

# B 部分 · 环境错误条目（事后归因）

> **统一的七格 schema**：**症状** / **环境前提** / **根因** / **触发条件** / **规避做法** / **怎么发现的（证据）** / **边界**。
> 「**环境前提**是灵魂」—— 漏了它，条目的适用范围会被误读成"所有环境"。
> 「**边界**与症状同等重要」—— 知道什么情况下**不**适用，比知道什么情况下适用更值钱。
> ★ 同一条错误可能在多层出现（如 CRLF 既是 OS 层也是 shell 层），**两层都登记、互相指路，不合并** ——
> 合并会丢掉"从哪一层看"的信息。
> ★ 节末的 **◆ 未实例** 列出的，是大纲里有、但本项目**没有实例或未验证**的条目 —— **不编**。
>
> ★★ **可选的第八格：`> **状态**：…`**（2026-09-19 立）—— 紧贴条目标题下方，**只在有状态变化时才加**：
>
> | 标记 | 含义 | 判据 |
> |---|---|---|
> | **✅ 已结案** | 修好了，且**代码/文档能实证** | 规避做法说「已修」**且**能指出落点（文件:行号 或 `§N`） |
> | **⚪ 已废止** | 不是修好，是**这件事不存在了**（环境选型改了、任务撤了） | 症状**不会再发生**，但**不是**被修的 |
> | **⚠️ 已缓解** | **部分**做了，仍有未做项 | 必须写出「① 已做 / ② ⬜ 未做」，**不许自述成"已结案"** |
>
> ★ **为什么是"可选"而不是给 126 条全加**：状态**已经在别处维护**（`§26.2` 表、`§26.2.3`）——
> 给每条都加会**制造第二份真源**，下次必然分家（`A.11`）。**只标"有状态变化"的**，
> 其余默认 = 活跃前提（如 `ERR-SVC-17` 限流三机制、`ERR-FS-04` 写穿 —— 它们**没有"状态"可言**）。
> ★ **判据（可机器检查）**：`grep -c '^> \*\*状态\*\*' ENVIRONMENT.md` ≤ **12**。
> ★★ **反面教训**：`ERR-OS-02` 那条若标成「✅ 已结案」就是**假话** —— 它自己写明"**不是代码 bug，是环境选型错误**"；
> 标「⚪ 已废止」才不丢那层信息。**别把"症状不见了"一律读成"修好了"。**

---

## B.1 操作系统层

### B.1.1 Windows 侧

### ERR-OS-01 `.cmd` 的 LF 行尾让 cmd.exe 从行中间开始执行
- **症状**：`attempts.log` 里出现 `exit=9009` + `'""' 不是内部或外部命令`，且**有 `exit=` 没有 `start=`**；批次静默消失。
- **环境前提**：Windows 计划任务 → 一个 `.cmd` 包装器 → 里面再调 python。**路径里有中文用户名**。
- **根因**：`cmd.exe` **按字节块**读批处理文件。LF-only 会让它从**某行中间**开始执行 ——
  `REM` 注释里的英文单词被当成命令跑，前面的 `set` 全部丢失。
- **触发条件**：任何 `.cmd`/`.bat` 出现 LF 行尾（典型来源：跨平台编辑器保存、Git 的 `core.autocrlf`）。
- **规避做法**：包装器**必须 CRLF**；`.sh` / `.py` **必须 LF**。
  改完务必确认：`python -c "b=open('x.cmd','rb').read(); print('CRLF',b.count(b'\r\n'),'LF',b.count(b'\n')-b.count(b'\r\n'))"`。
  同时**别把包装器换成直接指向 `python.exe`**（路径里的中文用户名经 `schtasks` / MINGW 传递会被搞坏），
  要用 `%USERPROFILE%` 让 `cmd.exe` 在**运行时**展开，任务定义本身保持纯 ASCII。
- **怎么发现的**：2026-09-11 深夜改包装器时引入的，故障现象与"没跑"一模一样；见 README「怎么继续跑」的「包装器 `.cmd` 是 LF 行尾」、SUMMARY §14。
- **边界**：**只对 Windows 侧的 `.cmd`/`.bat` 成立**。`.sh` 在 Git Bash 与 DSM BusyBox 下**必须 LF** —— 同一条"行尾"知识，两个方向相反，别套错。
  弯路 #2（当初的形态：`.cmd` 包装器 LF ⇒ 丢 `set` 变量 + `exit=9009`）。

### ERR-OS-02 `StopOnIdleEnd` 一动键鼠就把整批 `TerminateProcess`
> **状态**：⚪ 已废止（2026-09-19）—— **环境选型已改**（全量迁到 NAS / DSM 计划任务），
> Windows 任务这条路径不再存在。★ **它不是"代码 bug 被修好了"**（本条自己写的），
> 所以标"结案"会丢掉「当初是选型错了」这层信息；回退到 Windows 时必须显式关掉该字段。
- **症状**：批次跑一半静默消失，**没有 traceback、没有 finally 收尾**，状态文件里 `running_pid` 永远挂着，之后每轮都空转。
- **环境前提**：Windows 计划任务 + 一台**被人在用**的桌面机。
- **根因**：任务定义里 `<StopOnIdleEnd>true` —— 系统一旦检测到"用户不再空闲"，就直接杀整个任务实例。
- **触发条件**：任务运行期间碰鼠标/键盘。
- **规避做法**：**已全量迁到 NAS**（DSM 计划任务）。若将来要回退到 Windows，**必须显式关掉这个字段**。
- **怎么发现的**：当场读任务定义确认；见 README「怎么继续跑」的「`<StopOnIdleEnd>true`」、SUMMARY §14。
- **边界**：桌面机特有；服务器上无此问题。这条是"**环境选型错误**"，不是代码 bug —— 弯路 #1。

### ERR-OS-03 `os.kill(pid, 0)` 在 Windows 上是"探测即击杀"
- **症状**：只想判进程活着，结果把它杀了。
- **环境前提**：同一段 Python 代码要同时在 POSIX（NAS）和 Windows 上跑。
- **根因**：`os.kill(pid, 0)` 在 POSIX 语义是"发 0 号信号 = 只探测"，
  在 Windows 上 **`os.kill` 直接映射到 `TerminateProcess`**。
- **触发条件**：跨平台复用同一段判活代码。
- **规避做法**：Windows 分支改走 `tasklist`。★ 回退时要**连 `import subprocess` 一起打开**，
  否则分支会 NameError（那段被注释而非删除，见 README「怎么继续跑」的「回退到 Windows」第 3 条）。
- **怎么发现的**：迁移时踩到；弯路 #3。
- **边界**：**只在"同一段代码跨平台"时成立**。纯 POSIX 环境这条永远不会咬人 —— 这正是它危险的地方。

### ERR-OS-04 ★ 控制台代码页 GBK 编不出 `⑪`，脚本会**在打印到那一节时中断**
- **症状**：脚本跑到某一行**突然中断**，而**已经过的断言看着全是 `ok`** —— 极易误判成"代码坏了"。
- **环境前提**：Windows 控制台 / 管道的默认编码是 **GBK（cp936）**，`sys.stdout` 用的是它。
- **根因**：`①`..`⑩`（U+2460–U+2469）在 GBK 里有，**`⑪`（U+246A）不在**。
  `print` 到它时 `UnicodeEncodeError`，脚本当场停。
- **触发条件**：源码里出现 `⑪` 及以后的圈码；或任何 GBK 表外的字符。
- **规避做法**：每个脚本开头**强制** `sys.stdout.reconfigure(encoding="utf-8")`；
  **加新的节编号时不要越过 `⑩`**。
- **怎么发现的**：写测试时踩到，`tests/README.md`「写测试时踩过的坑」里 `⑪` 那条有原话。
- **边界**：只在 **stdout 是非 UTF-8**（GBK 控制台 / 管道 / 重定向到文件）时触发；
  在 UTF-8 环境里完全正常 —— 所以它**在本机报、在别人那儿不报**。
  ★ 同一现象也在 **编码层**登记（`ERR-ENC-01`），**两处都留**。

### ERR-OS-05 反斜杠 UNC 在这个 Git Bash → Python 环境里失败
- **症状**：`\\iSunker-DS423\...` 传给 Python 后路径不认 / 报错。
- **环境前提**：Windows + Git Bash（MINGW64）+ Python。**多层各有自己的转义规则**。
- **根因**：反斜杠要经过 bash 引号 → Python 字面量 → Win32 API 三层转义，任何一层没对上就废。
- **规避做法**：**一律用正斜杠** `//iSunker-DS423/...`。Python 与 Win32 都接受正斜杠 UNC。
- **怎么发现的**：多次踩到后固化成规矩。
- **边界**：**只在这个 shell → Python 的栈上成立**。在 `cmd.exe` / PowerShell 里反斜杠是正常的。
  ★ 与 `ERR-CMD-01`（MINGW 路径改写）是**两个相反方向**的坑，别混。

### ERR-OS-06 MINGW 会把命令行里的 POSIX 路径参数改写成 Windows 路径
- **症状**：`--notify-spool /tmp/x` 实际落到 `D:\tmp\x`；`curl` 带路径参数**全部 400**。
- **环境前提**：Git Bash（MSYS/MINGW）调用**原生 Windows 程序**（python.exe、curl.exe）。
- **根因**：MSYS 认为你在传 Unix 路径，**自动做前缀翻译**；但接收方是原生 Windows 程序，它拿到的是被改写过的串。
- **触发条件**：参数值以 `/` 开头、且看起来像路径。
- **规避做法**：用 `D:/tmp/x` 或 UNC；curl 的路径参数**走 stdin** 绕开；
  必要时 `MSYS_NO_PATHCONV=1`（★ 有副作用：**所有**参数都不再翻译）。
- **怎么发现的**：`run-batch.sh` 首次跑出「全 400」；SUMMARY §6.3。
- **边界**：只在 **Git Bash → 原生 Windows 程序**这一跳上成立。**在 Linux / NAS 上不存在这个问题** ——
  所以本地测过的命令直接抄到 NAS 上通常没事，反过来会。

### ERR-OS-07 判活别用 `ps | grep`（见 `ERR-DSM-01`）
> 交叉引用：这条本体在 DSM 侧（BusyBox 的 `ps` 只显示 `comm`），登记在 `ERR-DSM-01`，**不在此重复**。

**◆ 本小节未实例 / 未验证**（大纲里有、本项目没踩到，**不编**）：
文件名里的其他特殊字符（除中文用户名与空格已实例）、
防病毒/安全钩子对 `rm`/`del` 的干预（本机未见）。

### B.1.2 Synology DSM 侧

### ERR-DSM-01 BusyBox 的 `ps` **只显示 comm**，看不到命令行参数
- **症状**：`ps | grep drive-loop.py` **怎么 grep 都是空的**，于是误判"任务没在跑"。
- **环境前提**：DSM 用的是 **BusyBox** 的 `ps`（精简版），不是 procps 那个。
- **根因**：BusyBox `ps` 默认只打 `comm`（进程名），对 python 就是 `python3` —— **命令行参数根本不在输出里**。
- **触发条件**：任何"用 ps 找带参数的进程"的写法。
- **规避做法**：用 **`pgrep -f drive-loop.py`**；或看
  `<compose>/drive-loop/scripts/.drive-loop.state` 里的 `heartbeat_ts` 在不在涨（批次运行期间每 60s 刷一次）。
- **怎么发现的**：排查"批次到底在不在跑"时踩到；README「NAS 侧一次性配置」的「判活别用 `ps | grep`」条。
- **边界**：**DSM 专有**。在普通 Linux（procps）上 `ps aux | grep` 是好的 —— 别把这条当成通用规则。

### ERR-DSM-02 DSM 7 **没有 `nc`**、**没有 `jq`**
- **症状**：抄来的"测端口 `nc -vz`"和"解析 JSON `jq`"两条命令**当场 command not found**。
- **环境前提**：DSM 7 的最小化用户态。
- **根因**：这些工具不在 DSM 自带集合里。
- **规避做法**：测端口用 `netstat`；解析 JSON 用 `grep -o` + `cut`（或在 Windows 侧用 Python 解）。
- **怎么发现的**：SUMMARY §9 原话。
- **边界**：DSM 7 特指。装 Entware 后会变，但**本项目没装**。

### ERR-DSM-03 共享文件夹**看不出**在哪块物理盘上
- **症状**：以为 `/volume1/docker` 和 `/volume1/video` 是一回事（都 `/volume1/`），实际归属要靠 `ContainerManager/all_shares` 读。
  ★ 2026-09-17 更正：症状**前半句对、后半句过时** —— `docker` 与 `video` 其实**同属 `cachedev_1`**
  （`docker` 是共享文件夹且**可达**，只是不进"枚举"，见 `ERR-HW-03`）；
  而 `all_shares` **只在 NAS 上读得到**，本机有两条替代通路（见下「规避做法」）。
- **环境前提**：DSM 上 `video` 与 `docker_ssd` 这类共享文件夹**分属两个不同的卷**，路径前缀却都是 `/volumeN/...`。
- **根因**：共享文件夹 → 卷的映射是 DSM 的元数据，**不体现在路径字符串里**。
- **触发条件**：任何"按路径前缀判断是不是同一块盘"的推理。
- **规避做法**：读 `ContainerManager/all_shares`（★ 2026-09-14 补：**它只在 NAS 上读得到** ——
  从 Windows 侧有两条替代通路，见 `A.3` 的「卷归属怎么核」）；硬链接判据一律用 `stat -c %d`
  比设备号（**仅 NAS 侧**成立，Windows/SMB 侧零分辨力，见 `A.3` 末 ★）。
- **怎么发现的**：2026-09-13 复测补上"卷归属表"那一格（SUMMARY §9 原话：当时没记，只写了两个挂载点）。
  ★ 2026-09-14 复核：`df`（Windows 侧）与群晖 Storage Analyzer 的 `share_list.csv`
  **两条独立来源逐格吻合**（`docker_ssd`/`qb_temp` 在 `cachedev_0`；`video` 等 6 个在 `cachedev_1`）。
  ★★ 2026-09-17 实测：`share_list.csv` 是**三条里最强的一条** —— 它**逐共享**给出
  `Volume` 列 + **精确到字节**的两个 size 列，本机经 SMB 直读（见 `A.3`），
  实测 **12 个共享**（`docker` → **Volume 1**、`ActiveBackupforBusiness`/`Download`/`drive`/
  `homes`/`video`/`web`/`web_packages` → Volume 1；`000 临时文件夹nas…`/`docker_ssd`/
  `qb_temp`/`技术文档` → Volume 2）⇒ **一次读全，不依赖枚举**（★★ `ls` / `net view` 只列 5 个，
  正是这里要绕开的东西）。★ 注意文件是 **UTF-16LE**（`utf-8-sig` 解会得到交错的空字节），
  zip 内层名与列名按 `utf-16-le` 解才对（编码坑见 `ERR-ENC-01` 一族）。
- **边界**：DSM 专有。★ 与 `A.3` 的 ★ 是同一条知识的两种表述（一个在前提层、一个在错误层），**都留**。

### ERR-DSM-04 `/etc/ssmtp/ssmtp.conf` 是 **0 字节空壳**，真路径是 `/usr/syno/etc/synosmtp.conf`
- **症状**：探测到 `ssmtp.conf` 为 0 字节 ⇒ 误判"邮件没配" ⇒ 白干半天。
- **环境前提**：DSM **改过** ssmtp 的配置路径。
- **根因**：DSM 实际读 `/usr/syno/etc/synosmtp.conf`；`/etc/ssmtp/ssmtp.conf` 是个空壳，
  而且**邮件天天正常送达时它照样是 0 字节**。
- **触发条件**：拿"配置文件为空"当"功能没配"。
- **规避做法**：判邮件通不通**只有一条路：真发一封**（`sh notify-spool.sh --test-mail`）。
  另外别想"读 DSM 配置自己发信" —— 那个文件里的密码是**加密**存的（`eventpasscrypted`）。
- **怎么发现的**：先误判、后实测；SUMMARY §15。
- **边界**：**DSM 专有**（群晖移植 ssmtp 时改的路径）。弯路 #13。

### ERR-DSM-05 ★ `synoschedtask` 的**小时位存错** ⇒ "手动能跑、计划永远不跑"
- **症状**：在 DSM 里点「运行」正常跑完，**计划时间到了却从不触发**。
- **环境前提**：DSM 任务计划的「最后运行时间」字段，GUI 与 CLI 的**存储语义不一致**。
- **根因**：实测存成了 `Run time: [2]:[0] Repeat every [15] min until [2]:[45]`
  ⇒ 这条任务**只在 02:00–02:45 之间**才触发。
- **触发条件**：在 GUI 里选"重复"时没注意小时位。
- **规避做法**：**小时位明确设成 `23`**。改完**务必复核**：
  ```bash
  sudo /usr/syno/bin/synoschedtask --get | grep -A 12 'reseed-drive-loop'
  # 期望看到： Repeat every [15] min (s) until [23]:[45]
  ```
- **怎么发现的**：`synoschedtask --get` 读出来的实测值；README「把调度挂到 NAS 上」的「小时位存错」那段。
- **边界**：**DSM 专有**。★ 它属于「配置层错误」，但形状和代码 bug 一样（"看着正常"），所以必须列进来。
  **弯路 #14** —— 当初的形态正是"小时位填成 `[2]:[0]`，任务全天大部分时段不触发"。
  ★ 教训不在"填错了"，而在**没有自检机制**：GUI 看着是对的、手动能跑，
  唯一的判据是回读 `synoschedtask --get`。
- **判据**：`attempts.log` 里**压根没有 `start` 行** = 任务没被触发（没启用 / **计划窗口不对** / 路径错）。
  见 `ERR-SCHED-01` / `ERR-SCHED-05`。

### ERR-DSM-06 DSM 任务计划脚本框：多行 `\` 会被折叠；Git Bash 的**括号粘贴模式**破坏多行粘贴
- **症状**：粘一串多行命令进 DSM，只有第一行生效 / 报奇怪的语法错；或粘进 Git Bash 后前面多出 `^[[200~`。
- **环境前提**：给 NAS 的命令**只能靠人粘贴**（没有 SSH）。
- **根因**：① DSM 脚本框会**折叠 `\` 续行**；② MINGW 的 bracket paste mode 会在粘贴内容外面包一层控制序列。
- **触发条件**：任何多行粘贴。
- **规避做法**：**一律写成单行、一次一条**。逻辑超过一屏就**写成脚本文件走 SMB 拷过去**，不要靠粘贴。
- **怎么发现的**：SUMMARY §9 原话。
- **边界**：只在这套"人肉粘贴"的链路上成立。★ 相关：`B.10` 元层第 2 条（代码块里只放能直接粘的东西）。

### ERR-DSM-07 `#recycle` 的语义：**删除 ≠ 真删**；File Station 删除只是挪进回收站
- **症状**：以为删干净了，**空间一分没回来**，凭据副本还在盘上。
- **环境前提**：DSM 的共享文件夹默认启用**回收站** `#recycle`。
- **根因**：`rm` 之外的删除途径（File Station、DSM GUI）是**移动**到 `#recycle`。
- **触发条件**：走 File Station / GUI 删除。
- **规避做法**：**别走 File Station**。用 `rm-staging.sh`（`sh <compose>/rm-staging.sh <目标> --apply`），
  或用户在 NAS 原生路径上 `rm -rf`。★ 删完**回读读数**确认空间真的回来了。
- **怎么发现的**：2026-09-13 实测：`docker_ssd/#recycle/env-bak-20260912/` 里躺着 **8 个 `.env.bak*`**
  （其中 4 个与生产 `.env` 同档大小 7.0–7.3 KB），**全是之前几次"File Station 删除"留下的**。
- **边界**：DSM 专有。★ **但"删了没释放空间"还有第二个原因**（BTRFS COW），见 `ERR-FS-04` ——
  **两条成因不同、表现一样**，都要能想到。

### ERR-DSM-08 DSM 自带的 python3 是 **3.8.15**，且是整条链路的**单点依赖**
- **症状**：见 `ERR-PY-01`（静默崩溃）。以及：DS 升级后可能换版本，症状同样会是"静默"。
- **环境前提**：`drive-loop.py` / `notify.py` 跑在 **NAS 宿主机**上，用 DSM 自带的 `/usr/bin/python3`。
- **根因**：DSM 自带版本老（3.8.15），Package Center 里**没有更新的**。
- **触发条件**：用了 3.8 不支持的语法 / 依赖更高版本的库。
- **规避做法**：文件顶部**必须有** `from __future__ import annotations`；
  `run.sh` 在 shell 这一层另记一份 `attempts.log`（用来区分"python 没起来"与"python 崩了"）。
  后备方案：把 `run.sh` 的 `PY=` 指到一次性容器（`docker run --rm … python:3.12-slim`），或装 Entware 的 python3。
- **怎么发现的**：`attempts.log` 记到 `start py=/usr/bin/python3` + `exit=0`，
  旁边生出 `__pycache__/*.cpython-38.pyc` ⇒ 版本由此**坐实**（不是猜的）。
- **边界**：**这台 NAS 上成立**。别把它当成"python 3.8 通用问题" —— 关键前提是"跑在 NAS 宿主机、用 DSM 的解释器"。

### ERR-DSM-09 DSM 的 SSH 默认关闭 ⇒ **没有远程 shell**
- **症状**：任何"SSH 上去跑一下"的方案直接作废。
- **环境前提**：`22` 端口拒绝连接。
- **根因**：DSM 默认不开 SSH（安全设置）。
- **触发条件**：任何"你 ssh 上 NAS 执行 X"的思路。
- **规避做法**：**只读走 SMB / HTTP 端口；写操作给命令让人在 DSM 里执行**（或用 `deploy.sh`）。
- **怎么发现的**：SUMMARY §9、README `docker-compose.yml` 注释。
- **边界**：**本机设置**。★ 这条是整个协作模式的**前提** —— 见 `A.2` 的 ★。

**◆ 本小节未实例 / 未验证**：`/usr/syno/bin` 下哪些是 setuid、哪些依赖 i18n 资源（本项目未碰）。

### B.1.3 Linux 通用（与 DSM 的差异点）

### ERR-OS-08 交互式 shell 与非交互式 / `sh` 与 `bash` 的差异
- **症状**：登录 shell 里好好的脚本，被计划任务拉起后行为不同；`pipe` 里的 `while` 计数器传不出来。
- **环境前提**：DSM 计划任务用 `/bin/sh`（BusyBox 的 ash）拉脚本，**不是交互式 bash**。
- **根因**：`sh` 与 `bash` 语法/内建不完全一致；管道里的每一段跑在**子 shell** 里，变量改了也传不回父进程。
- **触发条件**：用 bash 特性写 `run.sh`；或在管道里累加计数。
- **规避做法**：面向 NAS 的脚本**按 `sh` 写**（POSIX 子集）；需要跨管道传值就用临时文件或进程替换。
- **怎么发现的**：`attempts.log` 与 `run.sh` 的设计（`run.sh` 是 `sh` 脚本）；README 顶部几处 `sh <路径>/…`。
- **边界**：★ **本项目只在"NAS 侧 `sh`"这条上实例化**。"管道子 shell"那条**本项目无实例**，此处只作提醒。

---

## B.2 Shell / 解释器层

### B.2.1 shell 脚本解析

### ERR-SH-01 ★ `sh` 是**边读边执行**的 —— 脚本运行中被覆盖会执行到错位的代码
> **状态**：⚪ 已废止（2026-09-19）—— 症状不复现，因为覆盖式更新本身没了。
> 依据：规避做法已落地为**原子替换**（写 `.new` 再 `mv`，**换 inode**，老进程继续读旧内容）；
> 但**纪律仍在**：别在批次运行中 `--apply`（原子替换不拦你，只是不再错位）。
- **症状**：`attempts.log` 里出现 `exit=127 (no python: /usr/bin/python3)`，**但同一批的 python 明明跑完了**。
- **环境前提**：`deploy.sh` 会覆盖 NAS 上**正在被 `sh` 读取**的 `run.sh`。
- **根因**：`sh` **不把整个脚本读进内存**，而是从文件描述符**按偏移继续读**。
  文件被覆盖（同 inode，内容变了）后，它会**从旧偏移读到新文件的字节** ⇒ 执行到一半的、错位的代码。
- **触发条件**：批次运行期间 `deploy.sh --apply`。
- **规避做法**：`deploy.sh` 已改成**原子替换**（写 `.new` 再 `mv`，**换 inode**，老进程继续读旧内容），
  并在检测到心跳新鲜时给出告警。**但它不会拦你** —— 那批会继续用旧脚本跑完。
  ⇒ 纪律：**别在批次运行中 `--apply`**；真要动就挑批次间隙（看 `.drive-loop.state` 的 `running_pid`）。
- **怎么发现的**：踩到后定位；README「常见问题」的「假的 `exit=127`」条、SUMMARY §17.2。
- **边界**：**只对"边读边执行"的解释器 + 覆盖式更新**成立；`mv`（换 inode）就不会有这个问题 ——
  **这就是修法的依据**。弯路 #12（当初的形态：直接覆盖 `run.sh` ⇒ `exit=127`）。

### ERR-SH-02 `.env` 是 **CRLF**，锚定 `$` 的 `sed` 会**静默什么都不做**
- **症状**：`sed -i 's|^LINK_TYPE=hardlink$|LINK_TYPE=reflink|' .env` 跑完**毫无反应**，
  而且**不报错、退出码 0** —— 你以为改了，其实没改。
- **环境前提**：本项目的 `.env` **是 CRLF 行尾**（在 Windows 侧维护）。
- **根因**：`$` 锚定的是**行尾**，而 CRLF 的行尾在 `$` 之前还有一个 `\r` ⇒ 模式匹配不上。
- **触发条件**：任何 `sed 's|^KEY=old$|…|'` 的写法。
- **规避做法**：**去掉 `$` 锚定**：`sed -i 's|^LINK_TYPE=hardlink|LINK_TYPE=reflink|' .env`。
  或整体按 `awk` 就地替换（`nas-update-env.sh` 就是**故意不走 `sed`** 的，为的是避开分隔符与 `&` 转义坑）。
- **怎么发现的**：切换 reflink 时踩到；见 `local-env-is-a-stub` 一族的记录。
- **边界**：**只在 CRLF 文件上成立**。同一段 sed 在 LF 的 `.sh` 上完全正常 ——
  ★ 这正是"**同一条命令在两个文件上行为不同**"的典型，也是 `B.10` 元层要防的形状。

### ERR-SH-03 `$()` 只剥尾部的 `\n`，**不剥 `\r`**
- **症状**：`x=$(cat file)` 之后 `"$x"` 里带着不可见的 `\r`，比较/拼接时莫名不等。
- **环境前提**：CRLF 文件 + 命令替换。
- **根因**：命令替换的尾部去除规则是 POSIX 定义的 —— **只去 `\n`**。
- **触发条件**：把 CRLF 文本读进变量再精确比较。
- **规避做法**：读进来先 `tr -d '\r'`，或用 `awk`/参数展开剥掉。
- **怎么发现的**：与 `ERR-SH-02` 同族，`.env` 处理时暴露。
- **边界**：只在"源是 CRLF"时成立。

### ERR-SH-04 ★ `source` 会**覆盖环境变量** ⇒ `DST=/tmp/x bash deploy.sh` 打到了**生产**
- **症状**：想拿临时目录演练，**实际写进了生产 NAS**。
- **环境前提**：`deploy.sh` 会 `source scripts/.nasrc` 来提供默认值。
- **根因**：`.nasrc` **在**命令行赋值**之后**被 source ⇒ 它把 `DST` 覆盖回去了。
- **触发条件**：任何"用环境变量临时覆盖某个默认值"的写法（`X=… cmd`）。
- **规避做法**：**先把外部给的值存下来，`source` 完再恢复**：
  `_DST_FROM_ENV="${DST:-}"; source …; DST="${_DST_FROM_ENV:-${DST:-}}"`。
  ⇒ 规则：**命令行 > 环境变量 > `.nasrc` 默认值**。
- **怎么发现的**：当时正想做沙箱演练，差点打到生产；README「NAS 侧一次性配置」里「旧版本的 `DST=` 会被 `.nasrc` 静默覆盖」那句、SUMMARY §17.4.1。
- **边界**：**这是 shell 通用规律**，任何"配置默认值靠 source"的脚本都有。
  ★ 危险点在于：**想做安全的事（演练）反而造成了危险的事（打生产）**。

### ERR-SH-05 环境变量必须写在命令**前面**
- **症状**：`sh build-farm.sh --verify VAR=val` —— `VAR` 被当成**脚本的参数**，环境变量没生效。
- **环境前提**：Git Bash / `sh` 的调用形式。
- **根因**：`VAR=val cmd` 是"给这条命令临时加环境变量"；写在后面就是普通参数。
- **触发条件**：抄命令时顺序放错。
- **规避做法**：**一律写在前面**：`COMPOSE_DIR="$D" FARM="…" sh build-farm.sh --verify`。
- **怎么发现的**：README「日常看板」的命令块里专门标了这句。
- **边界**：通用 shell 规律。

### ERR-SH-06 ★ `set -e` + **满卷把 stdout 弄坏** ⇒ 整趟被带走（邮件风暴的机械结构）
- **症状**：告警**发了**、**日志没记**、文件**还在 spool** ⇒ 下一趟**再发一遍**。
  实测：同一封「站点退避中：HDtime」重发了约 **6 次**。
- **环境前提**：`/volume1` 剩 0 字节；`notify-spool.sh` 用 `set -e`，且在
  `send_mail` **成功之后**、`log_event` / `mv` **之前**，有一句 `say "  [已发] $ti"` —— 它写 stdout。
- **根因**：stdout 写失败 ⇒ `say` 非零退出 ⇒ `set -e` **带走整趟** ⇒
  恰好卡在"信已发出"与"记账/归档"之间 ⇒ **状态机永久不一致**。
- **触发条件**：满卷导致 stdout 不可写（`sh -c 'exec 1>&-; exec sh …'` 可复现）。
- **规避做法**：**`say` 的失败不许带走整趟**（单独吞掉）；
  并且**重试必须有上界**：连败到 `MAX_SEND_TRIES`（默认 3）就 **归档 + 记一条 `[未确认]` 告警**，
  于是"有条告警你没收到"会进每日摘要 —— **既没被丢弃、也没被重发，而是变成了一条更响的告警**。
- **怎么发现的**：`tests/test_notify_drain.py`（28 条）**用老代码跑会精确红 8 条**，
  红的恰好就是"信发了、没记账、下轮再发"那几格；SUMMARY §21.7。
- **边界**：★ **触发条件至今没 100% 判明**（风暴窗口 12:45–13:10 那条任务每趟的退出码没取到）。
  但**两种候选触发条件的可观测行为与修法完全相同**，所以不阻塞。
  ★ 另：**"归档 = 丢弃"这个绝对律是错的** —— 错在漏掉了"重试没有上界"那一半。

### ERR-SH-07 `cp` vs `mv` 对运行中进程的影响（inode 换不换）
- **症状**：覆盖式更新正在被读取的东西 → 见 `ERR-SH-01`。
- **环境前提**：要更新一个**可能正在被读**的文件。
- **根因**：`cp` 保持 inode（原地改内容）；`mv` 换 inode（改名/换目录项）。
- **触发条件**：更新长跑进程正在读的脚本 / 库 / 证书。
- **规避做法**：**一律原子替换**（写 `.new` + `mv`），这是本项目 `deploy.sh` 的既定做法。
- **怎么发现的**：由 `ERR-SH-01` 反推出来的规矩。
- **边界**：通用。★ **`mv 文件 /dev/null`** 那条（不是丢弃）**本项目没有实例**，此处不展开。

**◆ 本小节未实例 / 未验证**：`mv 文件 /dev/null` 的真实语义。

### B.2.2 命令行为差异

### ERR-CMD-01 MSYS 版 `sed`/`awk` 会剥 `\r`，Linux 版**不会**
- **症状**：同一条 `sed` 在 Git Bash 里"能用"，搬到 NAS 上**不работа**（或反过来）。
- **环境前提**：两套工具链（MSYS vs BusyBox/GNU）处理文本的默认行为不同。
- **根因**：MSYS 的文本模式会做 CRLF 归一化；Linux 侧按字节原样处理。
- **触发条件**：跨平台复用同一条文本处理命令。
- **规避做法**：**别假设**。涉及 `\r` 的地方显式处理（`tr -d '\r'` / 显式匹配 `\r?$`），
  并在**目标平台上**验证一次。
- **怎么发现的**：由 `ERR-SH-02`（`.env` CRLF）与跨平台脚本的经历归纳。
- **边界**：只在**跨平台**时成立。★ 与 `ERR-OS-05`/`ERR-OS-06` 是同族的"MSYS 悄悄改字节"。

### ERR-CMD-02 ★ 在 56 TB 满盘上跑全树 `du` / `btrfs filesystem du` 是**分钟~小时级**
- **症状**：用户敲下去之后**当场卡住**，两条命令都卡。
- **环境前提**：`/volume1` = **56 TB、100% 满、碎片化的 BTRFS**，而且当时 cross-seed 与 qB **还在写**。
- **根因**：`btrfs filesystem du` 是**逐文件查 extent 表**（元数据随机读）；
  `du -shx /volume1/*` 是**全树遍历**。在这种盘上都是分钟到小时级，不是秒级。
- **触发条件**：对**整棵目录 / 通配整卷**跑量空间的命令。
- **规避做法**：**先估代价、并写在命令旁边**。优先顺序：
  ① **走 SMB 从本机递归量**（实测 173,080 个文件 / **128 秒**跑完 72 TiB 表观大小）；
  ② **抽样** —— `stat -c 'nlink=%h'`、**单个文件**的 `btrfs filesystem du` 都是**秒级**；
  ③ 只在**小目录**上跑全量。
  ⇒ **抽样永远优于全量** —— 判据要的是"能不能定性"，不是"精确到 GiB"。
- **怎么发现的**：2026-09-13 把两条直接发给用户，两条都当场卡住；
  用户原话：「卡住了！！！…你能不能靠谱一点！56tb文件啊！」。
- **边界**：**针对这块盘**。同一条命令在小卷 / 空盘上是快的 ——
  ★ 所以这条**不能**记成"du 很慢"这种通用结论，它的前提是"**56 TB 满盘 + 碎片化 + 有写负载**"。

> ★★★ **2026-09-17 补记：这条在"单文件"上也踩到了 —— 而上面 ② 恰恰写着"单文件是秒级"**
>
> **症状**：`scripts/diag/chk-volume1-free.sh` 的 `[5]` 节按 ② 的指引去抽**单个文件**，
> 结果又卡住：抽到 `reseed_singles/BTSCHOOL/Man.of.Steel.2013.2160p.UHD.BluRay.REMUX.HEVC.TrueHD.7.1.Atmos.-PTer.mkv`
> 时**卡死**（用户 `Ctrl-Z` 挂起）。
>
> **为什么 ② 会失效**：② 的"秒级"实测样本是一个 **13.84 GiB** 的农场文件；
> 而 UHD REMUX 单文件可达 **60–80 GB**，`btrfs du` 要**逐 extent 查 backref**，
> ⇒ **文件大到一定程度就不再是秒级**。**② 的"单文件秒级"是有尺寸前提的，别当无条件成立。**
>
> **一条更重要的推论（这才是本次真正的收获）**：
> **"对目录跑会卡"与"知道了规则却没执行"是同一件事** —— 我明知 `ERR-CMD-02` 还是把
> **整个 `reseed_farm` 目录**丢进 `[5]`（第一版），这是**知道规则但没执行**（同 `B.10` 的元层形状）。
> ⇒ **写脚本时也要"先估代价"**，不能只在脑子里估。
>
> **★ `Ctrl-Z` 那条新手坑（务必写清）**：`Ctrl-Z`（SIGTSTP）只是**挂起**，
> 进程**还占着 I/O**、`timeout` **也管不到它**（`timeout` 只对**它自己启动的那个**进程计时）。
> ⇒ **中止用 `Ctrl-C`**（SIGINT）；已挂起的要先 `kill %1` / `kill %2`。
> ★ 由此**脚本设计**上的教训：`timeout -s TERM <秒>` 要**真杀**，
> 且**超时与出错要分开报**（`rc=124` 才是超时）——
> 否则输出为空会被误读成"没问题"（`B.10` 第 14 条：空集也能骗人）。

### ERR-CMD-04 ★ `btrfs subvolume list` 普通用户报 EPERM —— 而 `wc -l` 会把它读成"0 条"
- **症状**：非 root 跑 `btrfs subvolume list /volume1` 得
  `ERROR: can't perform the search - Operation not permitted`；
  **若把它接进 `$() | wc -l`，会得到 `0`** —— 看起来像"**没有子卷/快照**"。
- **环境前提**：DSM 7.2，普通用户 `iSunker`；BTRFS 卷 `/volume1`。
- **根因**：`subvolume list` 要读文件系统树，需特权；**EPERM 走 stderr，stdout 为空**，
  ⇒ `$(... 2>/dev/null | wc -l)` = **0**，**把"查不了"伪装成"没有"**。
- **触发条件**：**任何**"把命令输出接进计数/判据、且没检查 stderr"的写法。
- **规避做法**：**先把 stderr 一起捕获再看内容**：
  ```sh
  OUT=$(btrfs subvolume list /volume1 2>&1)
  echo "$OUT" | grep -qi 'Operation not permitted\|Permission denied' && echo "读数拿不到"
  ```
  ⇒ **"0 条"只有在「没有报权限错」时才算证据**。
- **怎么发现的**：2026-09-17 `chk-volume1-free.sh` 第一版把 EPERM 记成 `(总条数: 0)`；
  第二版显式识别 EPERM 并打印「**读数拿不到，不是『没有快照』**」。
- **边界**：★ 与 **`B.10` 第 14 条**（空集/非空集/措辞都能骗人）**同族**；
  也与 `ERR-SVC-16` 边界 1（"空间账对不齐"未确认）同族 ——
  **区分「真的没有」与「拿不到」是本项目反复踩的一族**。

### ERR-CMD-03 把中文序号 `①` `②` 放进代码块里 ⇒ 用户当成命令敲
- **症状**：`-sh: ①: command not found`。
- **环境前提**：命令是**给人粘贴**的；人会**整块选中复制**。
- **根因**：代码块里混进了说明性的编号，粘贴时一起进去了。
- **触发条件**：代码块里既有命令又有中文序号/说明。
- **规避做法**：**代码块里只放能直接粘的命令**；中文编号与说明放到**代码块外面**。
- **怎么发现的**：2026-09-13 与 `ERR-CMD-02` 同一轮踩到；已写进记忆。
- **边界**：只在"命令要靠人粘贴"的协作模式里成立 —— ★ 但**本项目恰恰全是这个模式**。

---

## B.3 文件系统 / 存储层

### B.3.1 卷与挂载

### ERR-FS-01 **跨卷硬链接禁止** —— 源与 `LINK_DIR` 必须同卷
- **症状**：链接建不出来，直接失败。
- **环境前提**：`video`（`/volume1`，56 T）与 `docker_ssd`（`/volume2`，448 G）**是两个物理卷**。
- **根因**：硬链接**只能在同一个文件系统内**建立（同一个 inode 的两个名字）。
- **触发条件**：把 `LINK_DIR` 指到 SSD 上"图省事"。
- **规避做法**：`LINK_DIR` 必须在 `/volume1/video` 下。
  `build-farm.sh` 用 **`stat -c %d` 比设备号**做闸；compose 里对相关服务都是 `/volume1/video:/volume1/video` **1:1 挂载**。
  ★ 该 `%d` 闸的**适用范围**：只在 NAS 侧成立（本脚本就跑在 NAS 上）——
  Windows/SMB 侧零分辨力，见 `A.3` 末 ★。
- **怎么发现的**：设计期就确认（SUMMARY §2、§10.5.4）。
- **边界**：通用文件系统规律。★ 但**判可行性要看设备号**，不是看路径前缀像不像（`A.3` 的 ★）。

### ERR-FS-02 同一物理卷的不同挂载点 / 路径前缀 ≠ 同一块盘
- **症状**：同样的字符串前缀，判断同卷时判断错。
- **环境前提**：DSM 上多个共享文件夹映射到同一个卷；容器里又是 1:1 挂载。
- **根因**：路径是**视图**，设备号才是**本体**。
- **触发条件**：用路径字符串做同卷判断。
- **规避做法**：`stat -c %d`（**仅 NAS 侧**；Windows/SMB 侧它对任何一对都返回「不同」——
  实测设备号见 `A.3` 末 ★）。
- **怎么发现的**：与 `ERR-DSM-03` 同源。
- **边界**：★ 与 `A.3` 的 ★ 是同一条知识的两种表述，**都留**（一个在前提层、一个在错误层）。

### ERR-FS-03 满卷的 ENOSPC 会**级联**，波及面远超"写不进文件"
- **症状**：不只是注入失败 —— **告警脚本自己也会坏**（见 `ERR-SH-06`），
  连"记日志"都可能失败。
- **环境前提**：`/volume1` 已用 100%。★ **这是 09-13 当时的读数**；**2026-09-17 实测已用 91% / 可用 5.33 TiB**（见 `A.3` 卷与容量表）。
  ★ 本条**记录的是那次事故的前提**，别当成"现在仍差几十 GB"—— 但**级联机制本身与水位无关**，仍要防。
- **根因**：很多看起来无关的操作（写 stdout 到文件、SQLite 建 WAL、加锁文件）都要写盘。
- **触发条件**：卷可用空间为 0（或接近 0）。
- **规避做法**：**别往 `/volume1` 写真实文件**；临时落脚放 `cachedev_0` 那几个共享；
  容器日志加 `max-size`/`max-file`（`compose.yaml` 的 `x-logging` 锚点，10m × 3）。
- **怎么发现的**：09-13 邮件风暴的触发条件之一就是满卷；README「NAS 侧一次性配置」的「更正：别把输出重定向到 `/volume1`」那段。
- **边界**：★ **满卷"不阻塞本项目运行"** —— 农场与单片全是硬链接，不占数据块。
  这条只在**要写真实数据**时成立。

### B.3.2 文件系统特性

### ERR-FS-04 ★★ BTRFS **CoW / reflink**：`%h == 1` 判不出共享，**删了空间不回来**
- **症状**：删掉 9 个文件（`%h` 全 = 1、看起来都不是硬链接），
  `--apply` 报 `OK：已删`、目录确实消失，**但可用空间 31.91 → 31.76 GiB，48.20 GiB 一分没回来**（连量 4 次，值在动 ⇒ 不是 SMB 缓存）。
- **环境前提**：`/volume1` 是 **BTRFS**（`/dev/mapper/cachedev_1 /volume1 btrfs`），**reflink（COW）可用**。
- **根因**：**reflink 副本有自己的 inode、`%h` 也是 1**，行为却与硬链接一样（**共享 extent**）。
  ⇒ **inode 计数判不出 COW**。
- **触发条件**：任何"用 `nlink` 或 inode 判断"的判据，在 BTRFS + reflink 上都会给出**看起来正确**的答案。
- **规避做法**：判据换成能看见 extent 的：**`sudo btrfs filesystem du -s <目录>`，看 `Exclusive` 列**
  （≈ 0 ⇒ 与别处共享、删了不释放）。
  ★ 单文件也适用且是**秒级**：实测某个农场文件 `13.84 GiB / 13.84 GiB / 0.00B` ⇒ 它是自身那 13.84 GiB 的**唯一引用者**。
  ★ `Set shared` 那个数是**集合范围**的（对参数去重），而 `Exclusive` 用的是**整个文件系统**的 extent backref。
- **怎么发现的**：删旧根残留时实测；README「链接守护」的 reflink 实测那段、
  README「🔴 下一步」第 11 条（收拢 `:3060` 上 22 条指向旧根的种子）、SUMMARY §21.5。
- **边界**：**BTRFS/XFS 的 reflink 专有**。NTFS / ext4 上没有 COW，这条不成立。
  ★ **"不是硬链接"与"删了能释放"是两件事** —— 这条的意义就在这。
  ★ 已知警告：intra-file reflink 的情况下 `Exclusive` 可能被**高报**。

### ERR-FS-05 NTRS 与 POSIX 的语义差异（权限、mtime）
- **症状**：经 SMB 看到的 mtime / 权限与 POSIX 侧不一致。
- **环境前提**：NAS 是 Linux（BTRFS），从 Windows 经 SMB 访问。
- **根因**：SMB 协议有自己的属性模型，与 POSIX 不是一一对应。
- **触发条件**：拿 SMB 侧读取的属性去做**精确判据**。
- **规避做法**：mtime 判据要留余量；真要在意 ±1 秒就回 NAS 侧读。
- **怎么发现的**：`linkguard` 的 `(size, mtime_ns)` 指纹设计（同时看 size，不只看 mtime）。
- **边界**：**只在这条 SMB 链路上成立**。

### ERR-FS-06 「SMB 上的硬链接是假的」——★ **本项目实测**：**假的这个假设才是假的**
- **症状（当初的假设）**：以为 Windows 经 SMB 建的"硬链接"只是伪装的，`stat -c %h` 会谎报。
- **环境前提**：Windows 走 SMB 对 NAS 上的文件调 `os.link()` / `cp -al`。
- **根因（实测结论）**：**这个 NAS 上经 SMB 过来是服务端真硬链接** ——
  同 inode、**`nlink=2`**、**删掉原文件后另一个还在**。
  原先"Windows 建不了硬链接"的说法**是错的**。
- **触发条件**：把"Windows 不支持硬链接"这条常识直接套上去。
- **规避做法**：`build-farm.sh` **每次都用探针自证**硬链接真的建成了（inode 自检，不成立就 `die`）。
  Windows 侧要跑就加 `--map` 做前缀翻译，结果与 NAS 本机跑**完全一样**（代价是逐文件 RPC，475 条数分钟）。
- **怎么发现的**：2026-09-11 实测（SUMMARY §10.5.7），推翻了一个写在文档里的假设 —— 弯路 #「推翻假设」。
- **边界**：★ **这张结论只对这一台 NAS + 这个 SMB 实现成立**。
  换一台 NAS / 换一个 SMB 版本，可能就真的建不了 —— **所以判据必须是"每次自证"，不是"记住结论"**。

### B.3.3 SMB / UNC 访问

### ERR-SMB-01 对 UNC 路径跑 `rm` 的后果（**无回收站**）
- **症状**：glob 打错一次**不可逆**。
- **环境前提**：从 Windows 经 SMB 看到的 NAS 路径**没有回收站**这一层。
- **根因**：回收站是 DSM 在共享文件夹层提供的，SMB 直删**绕过**它。
- **触发条件**：任何在 `//iSunker-DS423/...` 上跑的 `rm`。
- **规避做法**：**禁止**。要删就在 **NAS 原生路径**上用 `rm-staging.sh`（有闸），或用户手动 `rm -rf`。
  `rm-staging.sh` **只认两类路径**：`_cleanup-*` 与 `#recycle/` 下的 `env-bak-*`（**都按 basename**），其余一律拒。
- **怎么发现的**：作为硬规矩写进 README「漂移哨兵」（原「还没做」第 14 条，2026-09-19 随该节移除**改指** `summary/18` / `summary/20`）。
- **边界**：★ 注意"按 basename 当通行证"会把**任何叫这名字的目录**一起放行 ——
  一次性口子用**完整路径字面量**，且**删完立刻撤**（本项目就是这么处理旧根残留那第三类的）。

### ERR-SMB-02 UNC 上的 mtime / inode 可信度 —— ★ **判不出 reflink**
- **症状**：见 `ERR-FS-04`。
- **环境前提**：从 SMB 侧只能读到 `nlink` / inode 这类**POSIX 元数据的一个投影**。
- **根因**：COW 共享 extent 这件事**不在 inode 层体现**。
- **触发条件**：用 SMB 侧读数下"删了能释放空间"的结论。
- **规避做法**：**下结论前说明这是 SMB 侧读数**；真要坐实就回 NAS 侧看 `Exclusive`。
- **怎么发现的**：旧根残留那次，"可释放 48.20 GiB"就是 SMB 侧读数得出的**错误结论**。
- **边界**：★ 这条不是说"SMB 读数不可信"，而是**"它证明不了这件事"** —— 两者差别很大。

### ERR-SMB-03 UNC 上 `mv` 与本地 `mv` 的语义差异
- **症状**：以为在做 rename，实际在做跨机拷贝。
- **环境前提**：UNC 的 `mv` 要跨网络。
- **根因**：同文件系统内的 `mv` 是改目录项（原子、瞬时）；跨主机/跨协议的 `mv` 往往是 **copy + delete**。
- **触发条件**：对 UNC 做大目录 `mv`。
- **规避做法**：要原子的目录级搬移，**在 NAS 侧做**（`deploy.sh` 的 `.new` + `mv` 就是在 NAS 侧）。
- **怎么发现的**：搬家（`migrate-reseed-dirs.py`）时的设计与踩坑。
- **边界**：只在跨协议时成立。

### ERR-SMB-04 反斜杠 UNC 会失败 —— 用正斜杠（见 `ERR-OS-05`）
> 交叉引用：登记在 OS 层（`ERR-OS-05`），**不在此重复**。

### B.3.x 一条不属于以上任何格、但必须记的

### ERR-FS-07 ★ 机械盘被大量占用 = **一定有人在拷贝，而不是在链接**
- **症状**：以为"全是硬链接 ⇒ 零开销"，结果盘在满。
- **环境前提**：本项目所有设计都建立在"硬链接不占数据块"之上。
- **根因**：只有**真拷贝**才会吃数据块。
- **触发条件**：有人用 `cp` / 下载 / 还原代替了链接。
- **规避做法**：**把"盘在被吃"当成一条判据**，而不是背景噪声。
- **怎么发现的**：48.20 GiB 旧根残留就是这么**暴露**出来的（SUMMARY §9 原话）。
- **边界**：`/volume1` 上成立。★ 与 `ERR-FS-04` 配套：**一个说"该释放的没释放"，一个说"不该占的占了"**。

---

## B.4 容器 / 网络层

### B.4.1 容器网络

### ERR-NET-01 ★ 群晖上的 Docker **hairpin NAT 不通**：容器 → 宿主机 LAN IP → 另一个容器 = timeout
- **症状**：容器里 `curl http://192.168.0.7:3060` **超时**。
- **环境前提**：群晖 DSM 上的 Docker（bridge 网络）+ 端口映射。
- **根因**：发往宿主机 LAN IP 的包要经 DNAT 回到同一台机器上的另一个容器 —— 这条回环在群晖上走不通。
- **触发条件**：容器内的代码用**宿主机 IP**去访问**同机另一个容器**。
- **规避做法**：容器之间**用服务名**（`http://qbittorrent-reseed:3060`），
  并把对方栈的网络以 `external` 接进来（`qbit-net` = `qbittorrent-reseed_default`）。
- **怎么发现的**：2026-09-11 实测；`docker-compose.yml` 顶部**专门写了"不要改回 `${NAS_IP}`"**。
- **边界**：★ **只对"容器 → 同机另一容器"成立**。
  **宿主机上跑的 `drive-loop.py` 走的是另一条路**（LAN IP + 端口映射），**两边是故意不同的**。
  —— 这就是 `A.1` 那个 ★ 最具体的实例。

### ERR-NET-02 容器名 vs 宿主机 IP：**哪个地址从哪个位置解析得到**
- **症状**：见 `ERR-NET-01` 与 `A.1`。
- **环境前提**：同一个服务在三个位置有**三个不同的地址写法**。
- **根因**：DNS / 服务发现的范围不同：容器名只在容器网络内可解析；宿主机不解析容器名。
- **触发条件**：把在一个位置验证过的地址抄到另一个位置。
- **规避做法**：按 `A.1` 的表选。
- **怎么发现的**：与本项目多处配置注释对照得出。
- **边界**：通用容器知识，但**本项目三个位置同时存在**，所以必须显式。

### B.4.2 容器配置

### ERR-DOCKER-01 ★★ `docker compose restart` **不重新注入环境变量** ⇒ 改了 `.env` 像没改
- **症状**：磁盘上的 `.env` 明明是对的，**跑着的容器用的是旧值**；
  cross-seed 持续对已移除的站发请求、吃 `HTTP 410`。
- **环境前提**：`env_file` / `environment` 是在**创建**容器时注入的。
- **根因**：`restart` 只是停+起**同一个容器**（沿用旧 spec）；`up -d --force-recreate` 才**重建**容器并重新注入。
- **触发条件**：改了 `.env` 里任何键后只 `restart`。
- **规避做法**：**必须** `sudo docker compose up -d --no-deps --force-recreate cross-seed`
  （`--no-deps` 防连带重建 Prowlarr）。★ 重建会打断正在跑的 drive，**等批次跑完**。
  **弯路 #15** —— 当初的形态正是"以为 `restart` 能刷新环境变量，旧配置一直驻留"。
  ★ **代价不对称才是关键**：多重建一次只是中断一批；少重建一次是**磁盘上写着对的、跑着的是错的**，
  之后所有排查都被这个假象带偏（`ERR-AI-05`：绿，但绿是因为规则压根没参与匹配）。
- **怎么发现的**：`/3/api` 的 410 排查 —— 磁盘上 `.env` 早就是对的，**纯粹是容器没重建**（SUMMARY §10.5.10）。
- **边界**：★ 反过来说：**不是每个键都需要重建**。
  `FARM_SOURCES` 只被 `build-farm.sh` 读（compose 只透传 `DATA_DIRS`）⇒ **改它不用重建**。
  ★ **弯路 #4 是这条的近亲**：`DATA_DIRS` 早期是**49 条子目录硬编码**，
  每加一个大包都要重算整张表 + 重建容器 —— 改一个键的成本高到能劝退人，
  于是"该改的没改"变成常态。v3 把它收成**单根硬链接农场**（1 条 `dataDirs`）之后，
  这个键才回到"一条一改"的量级。**配置项的成本也是设计的一部分。**
- **判据**（★ 别看 `ls`，别看 `.env.new` 存不存在）：
  ① `docker inspect reseed-cross-seed` 回读容器内的键；② `cross-seed.db` 的 `data` 表里有没有新路径。

### ERR-DOCKER-02 `--no-deps`：避免连带重建
- **症状**：只想重建 cross-seed，结果 Prowlarr 也被掀了。
- **环境前提**：compose 里 `depends_on: [prowlarr]`。
- **根因**：默认 `up -d` 会连带处理依赖。
- **触发条件**：重建有依赖关系的服务。
- **规避做法**：加 `--no-deps`。
- **怎么发现的**：README「多站点」里 `--force-recreate` 那句原话。
- **边界**：通用。

### ERR-DOCKER-03 ★ Docker 默认的 `json-file` 日志驱动**没有上限**
- **症状**：容器一直往 `/volume2/@docker/containers/*/*-json.log` 写、**永不清理**。
- **环境前提**：四个服务原先**都没有 `logging` 段**；而 `/volume1` 只剩约 20 GB。
- **根因**：除非全局 `daemon.json` 配过，`json-file` 默认**无上限**。
- **触发条件**：长期运行的容器。
- **规避做法**：用 YAML 锚点 `x-logging` 统一给四个服务加 `max-size: 10m` / `max-file: 3`
  （10m × 3 = 每容器 30 MB，四个合计 ~120 MB）。★ **改这里必须 `--force-recreate` 才生效**。
- **怎么发现的**：探"日志轮转"时**推翻了两条前提** ——
  `drive-loop.log`（`RotatingFileHandler(5MB × 5)`）与 `cross-seed/logs/*`（按天轮转、留 14 天）
  **其实都已在自我轮转**；**真正没上限的只有容器日志**。
  ⇒ 照"给日志加轮转"的直觉去动那两处，等于做一个**已经存在的东西**。
- **边界**：★ 判据：`docker inspect --format '{{.HostConfig.LogConfig}}' <容器>` 回读 `{"Type":"json-file","Config":{"max-file":"3","max-size":"10m"}}`。

### ERR-DOCKER-04 ★★ **构建上下文 vs 运行时副本**：同一个文件被 deploy 两次，漏一个只在某条路径上炸
- **症状**：`docker compose run reseed-orchestrator state` 报**模块级 `ImportError`**，
  **连默认的 `preflight` 一起打挂**。
- **环境前提**：`orchestrator/state.py` 在 `<compose>/` 下**有两个不同用途的副本**：
  `<compose>/orchestrator/`（**构建上下文**，进镜像）与 `<compose>/drive-loop/orchestrator/`（**运行时**，宿主机直接 import）。
- **根因**：`deploy.sh` 白名单原先**只同步了后者**；而 Dockerfile 是 `COPY orchestrator/ /app/orchestrator/`
  ⇒ 镜像里**没有** `state.py`。平时看不出来（`main.py` 以前不导入它），加 `state` 子命令就成了硬故障。
- **触发条件**：新增一个被 import 的模块，而白名单只更新了一处。
- **规避做法**：**"这个文件在哪几个地方需要"要按「谁拷它」分别数**。
- **怎么发现的**：加 `state` 子命令时炸出来；`deploy.sh` 里那段注释是原话。
- **边界**：★ 与 `A.12` 的**三处路径**是同一条知识。**三条路径**：构建上下文 / 宿主机 / 容器内。

### ERR-DOCKER-05 `up -d --force-recreate` 对**一次性 CLI** 等于什么也没做
- **症状**：改完 `orchestrator/state.py`，`--force-recreate` 之后**行为没变**。
- **环境前提**：`reseed-orchestrator` 是**一次性 CLI**（`ENTRYPOINT python -m orchestrator.main`，**无 daemon**）。
- **根因**：不带 `--build` 的 `--force-recreate` 只会拿**旧镜像**重启 ⇒ 白掀一次容器。
- **触发条件**：照 `deploy.sh` 末尾那句"下一步 `--force-recreate`"无脑跑。
- **规避做法**：**要 `docker compose build reseed-orchestrator`**。
- **怎么发现的**：README「NAS 侧一次性配置」专门标了「**`deploy.sh` 末尾那句不是每次都适用**」。
- **边界**：★ **只对一次性 CLI 成立**。
  同一轮同步的 4 个目的地里，`<compose>/drive-loop/**` 那 3 个是**热路径**（跑在宿主机上，**拷完即生效，不需要动容器**），
  第 4 个才是构建上下文副本。**别照着那句话无脑跑。**

### ERR-DOCKER-06 容器默认保存路径落在**容器可写层**（宿主机看不见、重建即丢）
- **症状**：任何不带 savepath 的添加会落进可写层。
- **环境前提**：`qbittorrent-reseed` 只挂了 `/downloads/incomplete`，而默认保存路径是 `/downloads/`。
- **根因**：**没挂载的那部分路径落在容器可写层**。
- **触发条件**：有代码/工具往 `:3060` 添加种子而不指定 savepath。
- **规避做法**：走 qB API 改 `save_path`（`app/setPreferences`，**零停机、可逆**，不动 conf 也不动 compose）。
  ★ 验收判据是 qB 自己报的 **`free_space_on_disk`**：改前 **302.14 GiB**（= volume2，可写层所在卷）
  → 改后 **0**（= volume1）⇒ 落点**真的**换了卷。
- **怎么发现的**：2026-09-13 潜伏雷排查；SUMMARY §21.6.1。
- **边界**：★ 落点**只能选在 `/volume1/video` 下**，因为**硬链接不能跨卷**（`ERR-FS-01`）。

### ERR-DOCKER-07 只读语义的子命令，挂载**必须 `ro`**
- **症状**：一个"只读"的子命令**有写坏生产库的能力**。
- **环境前提**：`StateStore` 打开库时会跑**一遍 schema 迁移**（`ALTER TABLE …`）。
- **根因**：`rw` 挂载 + 打开即迁移 ⇒ 语义上的"只读"被实现细节击穿。
- **触发条件**：给只读子命令挂 `rw`。
- **规避做法**：`- ./drive-loop/hlink:/state:ro`。这一行**不用重建容器**，下次跑编排器就生效。
- **怎么发现的**：SUMMARY §11.10 的四个决定之一。
- **边界**：通用。★ 配套：`StateStore(create=…)` 那条根因闸 —— **库不存在时绝不许把文件建出来**
  （连父目录都不能建），否则会留下 0 字节 `state.db`（本项目的 `ERR-SQL-04`）。

### ERR-DOCKER-08 ★★ 容器里**没有 `ps`**（`python:3.12-slim` 极简镜像）—— 判"在不在跑"要用 `/proc`
- **症状**：`sudo docker exec reseed-drive-loop ps -eo pid,args` ⇒
  `OCI runtime exec failed: exec: "ps": executable file not found in $PATH`。
  ★ **这不是"容器有问题"，是"镜像里没这个二进制"**（与 `ERR-DSM-01`"BusyBox 的 `ps` 只显示 comm"是**两条不同的坑**）。
- **环境前提**：`drive-loop` 的镜像基于 **`python:3.12-slim`**（刻意极简 —— 无 pip 依赖，import 闭包纯标准库）。
- **根因**：slim 镜像**不含 `procps`**（也不含 `net-tools`/`curl` 之类）。`docker exec` **不继承宿主工具链**。
- **触发条件**：照宿主机习惯用 `ps` / `netstat` / `dig` 去容器里查。
- **规避做法**：**用 `/proc`（内核接口，不需要任何工具）**：
  ```bash
  sudo docker exec reseed-drive-loop sh -c \
    'for p in /proc/[0-9]*; do echo "$p: $(cat $p/cmdline 2>/dev/null | tr "\0" " ")"; done'
  ```
  ★ 它比 `ps` **信息更多**：`cmdline` 给出**完整命令行**（能直接看出有没有 `--once`）。
  实测输出（`#58` 收口时的原样）：
  ```
  /proc/1:  sh …/drive-loop/run-resident.sh
  /proc/14: python …/drive-loop.py --url http://cross-seed:2468 --qbit-url http://qbittorrent-reseed:3060 …
  ```
  ⇒ `pid 1` 是入口脚本、`pid 14` 是 python ⇒ **两层父子关系**；`--url` 里**没有** `--once` ⇒ **常驻分支**。
- **怎么发现的**：`#58` 迁容器后想确认"容器到底在不在干活"，`ps` 缺席，改用 `/proc`。
- **边界**：**只对极简镜像成立**（`cross-seed` / `prowlarr` 那些镜像里 `ps` 是有的）。
  ★ **`/proc/14` 这个 pid 与状态文件里的 `running_pid` 是同一个数** —— 这不是巧合：
  容器里 `os.getpid()` 在 pidns 内可见，而 `running_pid_pidns: "container"` 就是为它标的。

### ERR-DOCKER-09 ★★ **`docker exec` 的工作目录是容器的 `WORKDIR`，不是你的宿主 cwd**
- **症状**：在 `<compose>/` 下敲
  `sudo docker exec reseed-drive-loop sh notify/notify-spool.sh --test-mail`
  ⇒ `sh: 0: cannot open notify/notify-spool.sh: No such file`（**读着像"容器里没这个文件"，其实有**）。
- **环境前提**：卷是**整目录 1:1 同名同路径**挂载（宿主 `/volume2/…/<compose>` == 容器同路径）。
- **根因**：`exec` 不继承宿主的 `cwd`；镜像 `WORKDIR=/app` ⇒ 相对路径解析到 `/app/notify/…`，**那里没有**。
- **触发条件**：把宿主上"相对某个目录"的路径写法直接搬进 `docker exec`。
- **规避做法**：**一律用绝对路径**（挂载是 1:1，所以宿主路径 == 容器路径）：
  ```bash
  sudo docker exec reseed-drive-loop sh /volume2/docker_ssd/prowlarr_cross-seed_autohardlink/notify/notify-spool.sh --test-mail
  ```
  ★ 自证这条假设的一行（`#58` 实测）：`pwd` 打 `/app`、`ls -d /volume2/…/<compose>` 打得出来
  ⇒ **相对不成立、绝对成立**，一次说清。
- **怎么发现的**：`#58` 想验"容器写的告警真能发出去"时踩到。
- **边界**：通用容器知识，但**本仓的 1:1 挂载让绝对路径恰好等于宿主路径**，容易误以为相对也能用。

### ERR-DOCKER-10 ★★ **发信这半边天生在容器外**：`notify-spool.sh` 读的是 DSM 的 `/etc/ssmtp/`
- **症状**：`sudo docker exec reseed-drive-loop sh …/notify/notify-spool.sh --test-mail` ⇒
  `读不到 /etc/ssmtp/ssmtp.conf: [Errno 2] No such file or directory（任务计划的用户要选 root）`。
  ★ **这不是故障，是"搬进容器必然如此"**。
- **环境前提**：`notify-spool.sh` 靠 **DSM 自己的 SMTP 配置**发信（`ERR-DSM-04`：真路径
  `/usr/syno/etc/synosmtp.conf`，而 `/etc/ssmtp/ssmtp.conf` 是空壳）。
- **根因**：那个配置**只在宿主机上，容器里没有**（镜像里没有、也没挂进去）。
- **触发条件**：把"发信"当成能跟着容器一起搬过去的东西。
- **规避做法**：**让发信留在容器外**（DSM 任务计划里那条 `reseed-notify-drain`，以 root 跑）；
  容器侧只负责**写 spool**。两侧分工见 `ERR-SCHED-08`。
  ★ 若哪天真要搬：得**再挂宿主机的 `/etc/ssmtp`**（或 `/usr/syno/etc/synosmtp.conf`）——
  那等于把**邮箱口令**再多暴露给一个容器，**权衡不划算**。
- **怎么发现的**：`#58` 收口时想验"整条通知链通不通"，在容器里跑 `--test-mail` 撞上；
  ★ **它反而把"notice 不搬"这个决定从"我推的"升级成了"实物证据"**。
- **边界**：**DSM 专有**（换一台普通 Linux 就没有这条）。
  ★ 与 `B.10` 第 1 条（"某台机器上没有" ≠ "环境里都没有"）**同族但方向相反**：
  这里**确实是"容器里没有"**，而**正确的解读是"它本该在容器外"**，不是"要去装一个"。

### ERR-DOCKER-11 ★★★ **`qbittorrent-reseed` 的 RSS 是套件 qB 的 4.5 倍**（同口径、且它种子更少/数据更少）
> ★★★ **2026-09-21 深夜：本节标题所述的「4.5 倍」已被推翻 — 实为 ~1.7 倍。**
> 那个「4.5 倍」的比较用的是 **`VmRSS`**（8.82 GiB），而当晚实测证明 **`VmRSS` 是误导值**
> （见「四次勘误」）。**同口径重比**（都是"实际占用"）：
> | 实例 | 口径 | 值 |
> |---|---|---|
> | 容器 `qbittorrent-reseed` | cgroup `memory.usage_in_bytes` | **3.11 GiB** |
> | 套件 qB（宿主 pid `12823`，`:8080`） | `VmRSS` | **1.83 GiB** |
> ⇒ **约 1.7 倍**，且**可解释**（`4.6.5` vs `5.2.3` 版本差 + 我们挂着 `reseed-singles` 930 条）。
> ★ **标题保留不改**（改标题要动锚点，且"当年错在哪"本身是记录）—— **以本块为准。**
> **状态**：⚠️ **活跃观察**（2026-09-19 起，2026-09-21 修订口径）—— **不是"已结案"**：
> ① ✅ 已执行（`memory_working_set_limit` 512 → 4096，`app/setPreferences`，零停机可逆）：
> +3.5 小时 `3784568 kB` ⇒ **降 59%**；
> ★★ **但 2026-09-21「稳定」被证伪**（`5381424 kB` = 5.13 GiB）—— ★★★ **见下方三次勘误**；
> ★★★ **而当天深夜，"持续增长"这个结论本身又被撤回**：`VmRSS` 与 cgroup **差 5.3 倍**，
> 而**主判据应当是 cgroup**（本容器实测只有 **1.72 GiB**）⇒ ★★ **见下方四次勘误**。
> ⇒ **"到底涨没涨、涨到哪"这件事，截至目前仍未定** —— **读数口径问题，不是内存问题**。
> ② ⬜ **未做**：升镜像到 qB 5.x（残留 ~2.2 GiB 差，属生产变更，性价比待评估）。
> ⇒ 本条**仍在活跃清单**上（见 `§26.2.3`）。

- **症状**：`qbittorrent-reseed`(:3060) 吃 **8.82 GiB RSS**（/ **10.07 GiB cgroup**），
  而**套件** qB(:8085) 只吃 **1.95 GiB** —— 而套件**种子多 36%、数据多 8.8 倍**。
  用户原话：「我的套件 qb 快 3000 个种子，都没这个高啊」——★ **这个反例是对的**。
- **环境前提（全是 2026-09-19 现读，两个来源互证）**：

  | 项 | 容器 :3060 | 套件 :8085 |
  |---|---|---|
  | qB 版本 | **v4.6.5** | **v5.2.3** |
  | 种子数 | 2070 | **2818** |
  | 总数据量 | 22.5 TiB | **198.8 TiB**（8.8×） |
  | **进程 RSS** | **8.82 GiB** | **1.95 GiB** |
  | 每种子 RSS | 4.36 MiB | **0.71 MiB**（**6.2×**） |
  | `memory_working_set_limit` | **512** | **4096** |
  | `disk_cache` | -1 | -1 |
  | `max_connec` | 500 | 800 |

- **根因（★★ 2026-09-19 实测：已从"未定"变成"已定位"）**：
  ★★ **`memory_working_set_limit` 是承重的** —— 把它从 **512 调到 4096**（走 `app/setPreferences`，
  **零停机、可逆**）后，RSS 立刻 **8.82 GiB → 3.33 GiB**，**15 分钟后 4.47**、
  **再 3 分钟 4.21 GiB**（微降 ⇒ **不是"一直爬"，是"回落后稳在 4.2–4.5"**）。
  ⇒ **降幅 52%，稳定**。判据（**必须两次以上**，见下"为什么单点不够"）：

  | 时刻 | RSS | 换算 |
  |---|---|---|
  | 改前 | `9250040 kB` | **8.82 GiB** |
  | 改完立刻 | `3493512 kB` | **3.33 GiB** |
  | +15 分 | `4690040 kB` | 4.47 GiB |
  | +18 分 | `4415848 kB` | **4.21 GiB** |

  ★★ **但**：稳定值 **4.21 GiB > 4096 MiB** ⇒ **这个键在 4.6.5 下不是硬顶**
  （允许超出），与套件 5.2.3 的表现（设 4096、只用 1.95 GiB）**仍不同代**。
  ⇒ **大版本差异(4.6.5 vs 5.2.3)从"唯一强候选"降为"残余差异"**：
  512 确实是**主因**，4.x 对该键的处理是**次要因素**。
- **★★★ 三次勘误（2026-09-21）—— 「稳定」被证伪：它不是稳态，2 天里又涨回去了**
  > ★★★ **2026-09-21 深夜撤回：本条结论作废** —— 它**只用了 `VmRSS`**，
  > 而 cgroup 显示本容器**一直是 1.7–2.2 GiB**（差 5.3 倍）⇒ **见下方「四次勘误」**。
  > ⇒ **下面这段保留为过程记录**（"我拿一个数就下了结论"这件事值得留），**别当现状引用**。
  ★★ **判据（`2ec60e86#2` 的本体，用户代跑）**：同口径复读，两次以上读数。
  实测（2026-09-21，NAS 上 `docker exec` 读 `/proc/143/status` 的 `VmRSS`）：

  | 时刻 | VmRSS | GiB |
  |---|---|---|
  | 09-19 +3.5 小时（**板上"稳定"的依据**） | `3784568 kB` | **3.61** |
  | 09-21 ①  | `5369672 kB` | 5.12 |
  | 09-21 ②  | `5379780 kB` | 5.13 |
  | 09-21 ③  | `5379860 kB` | 5.13 |
  | **09-21 ④（18:18:47 CST）** | **`5381424 kB`** | **5.13** |

  ⇒ ① **今天 4 次读数全在 5.12–5.13 GiB，波动 < 0.03%** ⇒ **"此刻稳定"是铁的**；
  ② **但比 09-19 的"稳态 3.61"高 42.2%** ⇒ 板上「**降 59% 且稳定**」**在两天后没复现**。

  ★★ **同口径对照（★ 这一步是关键，否则两个数不可比）**：
  种子数取自容器内 `ls /config/qBittorrent/BT_backup/ | grep -c '\.fastresume$'`
  （★ 每个种子一个 `.fastresume`；**注意路径是 `/config/qBittorrent/BT_backup/`，
  **不是** `/config/qBittorrent/data/BT_backup/`** —— 我先猜错了一次）：

  | 口径 | 09-19（板上） | 09-21（今天） | 变化 |
  |---|---|---|---|
  | 种子数 | 2070 | **2276** | **+10.0%** |
  | VmRSS | 3.61 GiB | 5.13 GiB | **+42.2%** |
  | **每种子 RSS** | 1.79 MiB | **2.31 MiB** | **+29.3%** |

  ⇒ ★★ **种子只多了 10%，RSS 却涨了 42%** —— 口径**确实变了，但远不足以解释涨幅**；
  按**每种子**算仍涨 **29.3%** ⇒ **不是"种子变多"，是真的涨回去了**。
  ⇒ **结论：「稳定」是错的**（`B.10`：单窗口 + 一次复读都不够；**这次是隔了 2 天才发现**）。

  ★★★ **另一条已独立确认的事实：`memory_working_set_limit` 不是硬顶**（板上 2741 行原先只是**猜**）：
  ```
  docker inspect qbittorrent-reseed --format '{{.HostConfig.Memory}} {{.HostConfig.MemorySwap}}'  →  0 0
  docker exec ... cat /sys/fs/cgroup/memory/memory.usage_in_bytes                            →  5516996608
  ```
  ⇒ **`Memory=0`（容器层完全没限制）** + cgroup 实测 **5.14 GiB**（**超 4096 MiB 达 28%**、无 OOM）
  ⇒ **`memory_working_set_limit` 只是 qB 内部的工作集软目标，压不住 cgroup 用量。**
  ★ 这**支持**板上 2741 行那句"不是硬顶"，但把它从**推测**升级为**实测**。
  ★ 本机是 **cgroup v1** ⇒ 用 `memory/memory.usage_in_bytes`（`memory.current` 不存在，见下）。

  ★ **未做**（仍开着）：① 更长窗口（≥1h）的第二次读数（今天 4 次只跨几分钟）；
  ② 这 42% 是"**回涨**"还是"**换了个稳态**"需要更长观测（今天内 4 次平得几乎没有斜率，
  但 2 天的尺度上是涨的）⇒ **不能只靠一天的数据判方向**。
- **★ 一条方法边界（本轮亲踩，务必记住）：`cat`/`grep` 一次 `/proc/<pid>/status` 不足以定性。**
  改完立刻读到 3.33 GiB 时我以为"解决了"；15 分钟后再读是 **4.47 GiB**（**涨回去了**）。
  ⇒ **"内存有没有降"必须至少两次读数 + 一次等窗口**，单点会给出**方向相反的结论**
  （同 §29.8「`cat` 一次状态文件不是有效的取证动作」**同族**：
  那条是"每拍就翻的字段"，这条是"会按新上限重建的缓存"）。
- **已排除（每条都有读数，不是推理）**：
  - ❌ **cross-seed**：`reseed-cross-seed` 只 **9.2 MiB**（全表第 13 位）；
  - ❌ **坏种子**：「装不出来 3 + 没 peer 4 = 7 条」（`reseed-freeze-report.py` 现读）
    ⇒ 上限 **13 MiB**；★ 且其中 4 条是 IYUU 的（按 §27 只记数不处理）；
  - ❌ **校验堆积**：`MaxActiveCheckingTorrents=3`；
  - ❌ **peer 连接**：`transfer/info` 的 `up_speed=0`，且 **2060/2070 条是 `stalledUP`**（无 peer）；
  - ❌ **位图/种子体积**：套件数据多 8.8 倍却更省 ⇒ 体积不是主因；
  - ❌ **缓存参数设大了**：**容器 512 < 套件 4096**（方向相反）。
- **★★★ 二次勘误（2026-09-19 深夜，同日第二轮）—— 三条命令缺陷 + 我自己一次误判**：
  （一）**`awk` 字段错**：我原先写的判据是 `awk '/VmRSS/{print $2" "$3}'`，
  但 **`grep` 保留行首空白** ⇒ 进 `awk` 的行是 `VmRSS:	  9250040 kB` ⇒
  **`$1="VmRSS:"`、`$2=数值`、`$3="kB"`** ⇒ 原式输出**前导空格 + 数值**，格式是错的。
  （二）**"读容器 pid 1"错**：见下 ★★★（s6-overlay）。**这两条都已在上面定案命令里修掉。**
  （三）★★★ **我最该记的一条：我把"格式对不上"读成了"读数是假的"。**
  看到 `8 kB` 与预期范围差 1000 倍，我先断定它"**是假读数 / 被 `/1024` 骗了**"，
  而**两条独立证据当时就能否掉它**：① 宿主 `awk` 版与容器 `grep` 版**都给 `8 kB`**；
  ② `grep` 版**不做任何字段解析** ⇒ `8 kB` 就是文件里的字面内容。
  ⇒ **`8 kB` 从来就是真值，只是属于 `s6-svscan`。**
  ★★ 这又是 `§28` 发现三的形状：**两个东西都对，只是量的不是同一样东西**
  —— 那次是 `diff`↔`cmp`，这次是 **`grep` 的输出** ↔ **文件本身**。
  ⇒ **判据**：**结论"这是坏数据"之前，先回答"这两个数各自量的是什么"**（`B.10` 第 1 条那族）。
- **★★ 一条必须记住的读数陷阱（2026-09-19 深夜**勘误**：原先记的"低报 2.2 倍"是**口径错**，不是探针缺陷）**：
  ★★★ **"`docker stats` 低报 2.2 倍"与"`9250040 kB` 到底是多少 GiB"是同一个数字问题的两面，而当初当成了两件事。**
  实测算术：`9250040 kB ÷ 3.984 GiB = **2.2142**` —— 那个"2.2 倍"**恰好**是一次 `/1024` 归一化的因子。
  ⇒ 真相是：**两者量的是同一份 RSS，只是单位不同**，不是"一个探针缺了分辨力"。
  ★★ **本仓的换算先例是自洽的**（`tools/doc-audit/04_probes.py`：`u.free / 2**30` 标成 GiB）
  ⇒ **GiB = 字节 / 2³⁰**。而 **Linux `/proc/<pid>/status` 的 `VmRSS` 单位 `kB` 就是 KiB**
  ⇒ `9250040 / 2²⁰ = **8.82 GiB**`，**路径 (A) 成立且正确**。
  ⇒ ★ **本节"3.33 / 4.47 / 4.21 GiB"与"降 52%"这些数：`VmRSS` 路径的换算无误，可继续引用**；
  **但"报 3.984 GiB"那一侧未经同一核对 ⇒ 在能同时复读两条命令之前，该并排对照标记为"未复验"。**
  ⇒ ★★ **判内存仍建议直接用 `/proc/<pid>/status` 的 VmRSS 或
  cgroup v1 的 `memory/memory.usage_in_bytes`**（理由从"`docker stats` 低报"改为
  **"少一层单位换算、少一个出错环节"**）；★ **但"不要用 `docker stats`"这个禁令要撤回** ——
  它**不成立**，且已**误传进 §26 / §30 / SUMMARY** 三处。
  ★★★ **一条更要紧的、我自己亲踩的坑（与上面同族，且是本条勘误的起因）**：
  我给出的一条"读 VmRSS"命令**本身是错的**，而它**看不出错**：
  ```sh
  pid=$(docker inspect -f '{{.State.Pid}}' qbittorrent-reseed)   # 上面那条失败时 pid 为空
  awk '/VmRSS/{print $2" "$3}' /proc/$pid/status                 # 退化成 /proc/status
  ```
  ⇒ 当 `docker inspect` **失败**（本机实测：`docker` 不在 `iSunker` 的 PATH / 权限不够）
  时，`pid` 为空 ⇒ `/proc//status` 被展成 **`/proc/status`** ⇒ 它**读的是内核的 `/proc/status`**，
  实测打出 **`8 kB`** —— ★★ **正是要测的那个字段名（VmRSS），而数值荒谬 1000 倍**。
  ⇒ **症状形态：不报错、不空输出、字段名对、数值离谱** ⇒ **最像"内存真的降了"。**
  ★ 规避：**读之前先证明 pid 是真的**（`test -n "$pid"` + `test -d /proc/$pid`），
  ★ **但**：把它"换成" `/proc/1/status` **也是错的** —— 见下 ★★★（**2026-09-19 深夜二次勘误**）。
  ★ ★ **更根本的规避**：`/proc/status` 这个坑的**真正条件**是「**pid 为空/无效**」，
  与"读 1 号还是读 pid"无关 ⇒ 修法是 **先证明 target 存在**（`test -n` + `test -d /proc/$t`），
  并**让失败显式化**（下面那条命令的 `[ -n "$r" ] &&` 就是为此）。
- **★★★ 正确读法（2026-09-19 深夜**二次勘误**后定案 —— 遍历进程、按 RSS 排序）**：
  ```sh
  sudo docker exec qbittorrent-reseed sh -c '
    for p in /proc/[0-9]*; do
      [ -r "$p/status" ] || continue
      n=$(awk "/^Name:/{print \$2}" "$p/status")
      r=$(awk "/^VmRSS:/{print \$2}" "$p/status")
      [ -n "$r" ] && echo "$r $n $p"
    done | sort -rn | head'
  ```
  ★★★ **为什么不能用"容器 pid 1"—— 它是错的，且这是实测踩出来的**：
  本镜像（`linuxserver/qbittorrent:4.6.5`）用 **s6-overlay** ⇒ **PID 1 是 `s6-svscan`（init）**，
  **真正的 `qbittorrent-nox` 是它的子进程**。实测（2026-09-19）：

  | pid | 进程 | VmRSS |
  |---|---|---|
  | 1 | `s6-svscan` | **8 kB** |
  | **142** | **`qbittorrent-nox`** | **3784568 kB** |
  | 143 | `busybox` | 540 kB |
  | 765 | `sh` | 1000 kB |

  ⇒ ★★ **任何"读容器 pid 1 的 VmRSS"的判据在这个镜像上恒等于 `8 kB`** ——
  而它**不报错、字段名也对**，只是**量错了进程**。
  ★ 与 `B.3`（`stat -c %d` 只在 NAS 侧成立）**同族**：**不是探针没分辨力，是"指向"错了对象**。
  ★ 该命令**不依赖"谁是 pid 1"** ⇒ 换成别的镜像（无 s6、或 pid 1 就是主进程）也照样对。
  ★ 顺带：`/sys/fs/cgroup/memory.current` **不存在**（这是 **cgroup v1**）。
- **触发条件**：把「种子多 ⇒ 内存高」当成必然，**不去做同口径对照**。
- **规避做法**：★ **先建对照组**（同口径、同一台机器上的另一个 qB 实例），
  再判"这个数正常吗"。**跨版本对照要注明版本差**。
  ★ 处置：① ✅ **已执行（2026-09-19）** —— `memory_working_set_limit` 512 → **4096**，
  走 `app/setPreferences`（**零停机、可逆**）：**RSS 降 52% 且稳定**（见上表）。
  ★ 回滚只要把 `4096` 换回 `512` 重发一次（**不需要**停容器）。
  ★★ **长期稳态已复读（2026-09-19 深夜，+3.5 小时）⇒ 当时判「稳定」成立，且原描述偏保守**
  ★★★ **但 2026-09-21 被证伪 —— 那句"稳定"只在这 3.5 小时的窗口里成立，两天后就不成立了**
  （09-21 复读 `5381424 kB` = 5.13 GiB，比这里的 3.61 **高 42%**；见本节顶部"三次勘误"）。
  ★ **教训**：`B.10` 那句"至少两次读数 + 一个等窗口"**仍需再加一条** ——
  **窗口要跨到"业务量不变"的另一个时间尺度上**：+3.5 小时里种子数几乎不动，
  所以它**测不出"随种子缓慢累积"这类趋势**。下面的表保留为**过程记录**（当时的真实读数）：

  | 时刻 | VmRSS | GiB |
  |---|---|---|
  | 改前 | `9250040 kB` | **8.82** |
  | 改完立刻 | `3493512 kB` | 3.33 |
  | +15 分 | `4690040 kB` | 4.47 |
  | +18 分 | `4415848 kB` | 4.21 |
  | **+3.5 小时** | **`3784568 kB`** | **3.61** ✅ |

  ⇒ ① 当时（**仅 3.5 小时窗口内**）**没有涨回去** —— 比 +18 分那个点**还低 14%**，且**接近"改完立刻"的 3.33**；
  ② 对照改前**降 59%**（原记 52%）。
  ⇒ ★ **以上两条在 09-21 都不再成立**（已涨回 5.13 GiB）—— **保留原文以存过程**，但**别当现状引用**。
  ★★ 这条**正好兑现 `B.10`**：当初 +18 分读到 4.21 时若就此定论，
  会以为"稳在 4.2–4.5"，**多等一个窗口才看到它其实是 3.6** ⇒
  **"至少两次读数 + 一个等窗口"这条规矩值回票价**。
  ★★★ **而 09-21 的复读又给这条规矩加了一课**：**3.5 小时也还不够** ——
  判"内存稳不稳"，**窗口必须跨过一个业务量变化的尺度**（这里种子数一天涨 ~10%），
  否则读到的是**同一批种子在短窗口内的抖动**，不是稳态。
  ② ⬜ **未做**：升级镜像到 qB 5.x —— **收益已不如先前估算的大**（512 是主因，
  升级只剩最后 ~2.2 GiB 的差），而它是**生产变更**（4→5 的 conf 键名有迁移风险，
  见 `patches/reseed-qbit.conf.md` 开头的版本免责声明）⇒ **性价比需重新评估**。
- **怎么发现的**：用户报「qb-seed 内存占用太高」，并**拿套件 qB 做反例**；
  排查中**连续推翻了 AI（我）的 6 个假设**（见"已排除"），
  最后靠**同口径 RSS 对照**定案。★ 教训：**"能解释" ≠ "已确认"**（`B.10`），
  而**"正常代价"这个说法本身需要一个对照才能成立**。
- **边界**：**qB 版本相关**（4.6.5 vs 5.2.3）；换同版本实例就没有这条。

- **★★★ 四次勘误（2026-09-21 深夜）—— 「持续增长」被**撤回**：`VmRSS` 与 cgroup 是两个口径，
  差 8 倍，而**主判据应当是 cgroup**。**

  ★★ **本节"三次勘误"里那句「不是稳态，2 天里又涨回去了」—— 结论作废。**
  写它的时候我**只看了 `VmRSS`**，而**同一批读数里就有 cgroup 数**（`5516996608` = 5.14 GiB），
  **我把两个数当成"互相印证"了，其实它们差得很远，而我当时没去解释那个差。**

  ★★★ **决定性实测**（2026-09-21，用户代跑；`pid 143` 是常驻 `qbittorrent-nox`）：

  | 时刻 | `VmRSS`(/proc/143) | cgroup `memory.usage_in_bytes` | 差 |
  |---|---|---|---|
  | 20:11:53 | **`9086844 kB` = 8.67 GiB** | — | — |
  | 20:13:07 | **`9580384 kB` = 9.14 GiB** | **`1849466880` = 1.72 GiB** | **5.3×** |

  ★ 且 `cat /proc/143/cgroup` 实测确认 **pid 143 就在容器自己的 memory cgroup 里**：
  ```
  5:memory:/docker/9ca8c739921c401783dc5752b92d83bc2142272e2a3b2311943fa2a752b63245
  ```
  ⇒ ★ **不是"读错了组"** —— **同一个容器、同一时刻，两个口径差 5.3 倍。**

  ★★ **为什么 cgroup 是主判据**：它是**内核按页记账**（硬账）；`VmRSS` 是**进程自报**，
  在 qB 这种**大量 mmap / 共享内存 / page cache 参与**的场景下会**远高于实际占用**
  （qB 的 libtorrent 会 mmap 缓存区，`VmRSS` 把它全算进去）。

  ★★★ **⇒ 那次"宿主被打爆"的重新判读**（**这才是真结论**）：

  | 读数 | 20:13（危险时） | 重启后 | 说明 |
  |---|---|---|---|
  | 宿主 `available` | **652 MiB** | **4031 MiB** | ≥ 真的紧张过 |
  | 宿主 `Swap used` | **13057964 kB（打满）** | 9299 MiB | ≥ 真的换爆过 |
  | **本容器 cgroup** | **~1.7–2.2 GiB** | **1.72 GiB** | ★ **一直不大** |
  | `VmRSS` | 9.14 GiB | 0.51 GiB | ⚠️ **误导值** |

  ⇒ **qB 从来就没占过 9 GiB。** 打爆宿主的是**整机层的 page cache 撑涨 + swap 抖动**
  （qB 周期性遍历 2276 个种子写 resume 数据 ⇒ 内核把该容器/相关文件页 cache 起来），
  而**这件事在 cgroup 账上不显**（page cache 算在整机，不必然算进该 cgroup 的 `usage_in_bytes`，
  且会被回收）。

  ★★ **已知事实（用户 2026-09-21 现场确认）**：**没有大批种子在重新校验，是正常做种。**
  ⇒ ★ **我之前"最可能是正在重校验"的猜测被否定**（`MaxActiveCheckingTorrents=3` 也在"已排除"里）。
  ⇒ **触发源仍未定**：`1475` 那个短命 `qbittorrent-nox` 进程（20:04 在、20:06 消失）
  **是现象不是原因**，**未验**。

  ★★★ **先前的处置建议"给容器加 `mem_limit`"—— 撤回。**
  理由：cgroup 实测只有 1.7–2.2 GiB ⇒ **加限掐的是它，而爆的是整机 page cache**
  ⇒ **加了没用，还可能把 qB 自己掐死**。★ 这条**同时给 `ERR-DOCKER-11` 那条「
  `memory_working_set_limit` 不是硬顶」补上了更好的解释**：**它压根没超。**

  ★★★ **本节最该记住的一条（读数口径判据）**：
  > **判"容器占多少内存"，`cgroup/memory.usage_in_bytes` 是主判据，`VmRSS` 只能当参考。**
  > **两者差 2 倍以上时，必须先解释差异，再下任何结论。**
  ★ 这与本节既有的两条同族：**"读容器 pid 1"错**（`s6-overlay` ⇒ 量错了进程）、
  **"`/proc/status` 8 kB"错**（pid 为空 ⇒ 量错了对象）—— ★ **三条都是"量错了东西"，而读数本身是真的**。
  ⇒ **判据**：**下结论"这个数太大/太小"之前，先回答"它量的是什么、和哪个数比"**（`B.10` 第 1 条那族）。

  ★★ **顺带定案一条命令坑**（本轮亲踩）：**`A || B` 是短路，不是"两手准备"** ——
  `ps ... || { /proc 遍历 }` 里 `ps` 一成功，**fallback 永不执行** ⇒
  ★ **"备用判据"在最需要它的时候不会跑**（同族：`§26.36`「推完即生效 ≠ 进程读到了」——
  **判据存在 ≠ 判据在跑**）。要两个都跑就写 `A; B`。

  ★★★ **元凶定案：不是我们的容器，是宿主上的套件 qB（`/usr/bin/qbittorrent-nox --webui-port=8080`，pid `12823`）**
  ★ 认领命令（只读）：`sudo cat /proc/12823/cmdline | tr '\0' ' '`
  ★ 它的 **`VmSwap` = `7396744 kB`（7.06 GiB）**，**比全部容器之和还大**；
  而它的 **`VmRSS` = `1916588 kB`（1.83 GiB）** —— ★ ★ **与本节原先记的「套件 1.95 GiB」几乎相同**，
  说明**当年那个数是对的**，错的是**拿它去比容器的 `VmRSS`**。
  ★★ **它不在任何容器的 cgroup 里**（宿主机直跑）⇒ ★ **`docker stats` 永远看不到它**
  —— 这正是"我一直盯着自己的容器找不到元凶"的原因。

  ★★★ **本节最该记住的第二条（范围判据，2026-09-21 立）**：
  > **判"整机资源"问题，先列"宿主上的全集"，再缩小到自己的容器。**
  > ```bash
  > free -m                                   # ① 整机水位（最先读这个）
  > sudo docker stats --no-stream --format '{{.Name}} {{.MemUsage}}' | sort -k2 -h -r | head
  > for f in /proc/*/status; do awk '/^VmSwap/{print $2, FILENAME}' $f; done | sort -rn | head
  > ```
  > ★ **顺序反了（先看自己的容器）会一直盯着一个无辜的对象**：本轮我花了一小时
  > 全在容器内找，而元凶是**宿主机上的一个非容器进程**。
  ★ 根因（我的）：**我把"我们项目的容器"当成了"问题的全集"** ——
  `§26.36` 那条「读数当场读」**管不到"范围"**，这条把它补上。

  ★★ **一条配套的"路径也要读数"教训（同日亲踩，连错三次）**：
  我三次拼**状态库路径**都漏了 `hlink/`（拼成 `drive-loop/state.db`），
  ★ **而真源两行就写着**：`ENVIRONMENT.md:119`（本文件，本节上方）
  与 `drive-loop.py:101` 的 `DEFAULT_DB = ROOT / "hlink" / "state.db"`。
  ⇒ **`CLAUDE.md` 第 1 节明说"运维事实以 `ENVIRONMENT.md` 的 `A.13`–`A.19` 为准"** ——
  **该做的第一件事是 `grep` 路径，不是猜路径。**
  ★ **新落盘的读数**：三包**共用一个 `farm_root`** =
  **`/volume1/video/download/reseed/reseed_farm`**（实测：`SELECT name, farm_root FROM pack`）。

### ERR-DOCKER-12 ★★★ **DSM 的「容器 X 意外停止」邮件里没有病因** —— 它可能正是你自己重建造成的

- **症状**：群里连收几封「Container Manager 容器 `reseed-drive-loop` 意外停止」，
  于是推断「**容器一直在崩** ⇒ 分批加太多 ⇒ 内存爆 ⇒ 索引器挂了」。
- **根因（两条，都要知道）**：
  1. ★★ **那封邮件只报"容器退出了"这一个事实** —— 措辞是 DSM 的固定模板，
     **不含退出码、不含 OOM、不含原因**。⇒ **不能从它读病因**；
  2. ★★★ **`--force-recreate`（部署/换镜像）会让 DSM 报同一句「意外停止」** ——
     因为容器**真的退出了一次**（再以新镜像起来）。
     ⇒ **"我看到的告警"可能是我自己的动作造成的**（同 `§26.15`「自己制造的证据」那族）。
- **判据（三条命令，全只读；**别再从邮件推**）**：

  | 命令 | 读什么 | 判 |
  |---|---|---|
  | `docker inspect <c> --format '{{.RestartCount}}'` | **自动重启次数** | `0` ⇒ **autoheal/restart 策略一次都没动过它** |
  | `docker inspect <c> --format '{{.State.ExitCode}}'` | **上次退出码** | `0`/`143` ⇒ 正常停止或被重建；**`137` 才是被杀/OOM** |
  | `docker ps -a --format '{{.Names}}	{{.Status}}'` | 现状 | `Up N hours (healthy)` ⇒ **现在没崩** |

  ★★ **`RestartCount` 是最硬的那一条** —— 它数的是"**被重启过几次**"，
  与"有过几次退出记录"是**两件事**：前者才是"一直在崩"的判据。
- **实测（2026-09-19，`reseed-drive-loop`）**：`RestartCount=0` /
  `Up 4 hours (healthy)` / `ExitCode=0` / `StartedAt=2026-09-18T21:24:50Z`
  ⇒ ★★ **零自动重启、且那时刻正是 `§26.15` 记的「21:25 我 `--force-recreate`」**
  ⇒ **那封 21:25 的邮件就是那次部署**，**不是崩溃**。
- **★★ 顺带兑现一条修复的正面证据**：`compose.yml` 的 healthcheck 那条注释记着
  「睡眠期心跳冻结 ⇒ **每 41 分钟一封**」（`ERR-SCHED-04` 那族）。
  本轮 `RestartCount=0` + **连续 4 小时 healthy** ⇒ **09-18 那次修复
  （睡眠期也刷心跳 + 阈值 900→1800s）生效了**。
  ★ **但口径要说清**：统计窗口只有 **4 小时**（容器 `Up 4 hours`）
  ⇒ 准确说法是「**近 4 小时内零重启**」，**不是**"永久修好"。
- **★ 一条反向教训（本轮亲生）**：**"137" 出现在别的容器上，才是真 OOM 的指纹**。
  同一屏里 `moviepilot-v2` / `emby` 都是 **`Exited (137) 8 hours ago`** ——
  那 **才是**「机器上真发生过内存压力」的证据，**而它不是本项目造成的**
  （qB 是 4.2 GiB 且已被 `ERR-DOCKER-11` 压住）。
  ⇒ **别拿"我收到的告警"当"我的系统在崩"**：先分辨告警的**主体**与**病因**。
- **边界**：**DSM 专有**（`Container Manager` 的邮件模板）。

### B.4.3 凭据边界

### ERR-SEC-01 哪些端口 / 目录对外可见
- **症状**：以为"内部服务"，其实**已发布到宿主机局域网**。
- **环境前提**：9696 / 2468 / 3060 / 8191 / 8787 都是 `ports:` 发布的。
- **根因**：发布 = 局域网任何机器都能连。
- **触发条件**：把"只在容器网络里"当成"对外不可见"。
- **规避做法**：`:3060` 走**局域网免密白名单**（`172.16.0.0/12` + 局域网段）**或** `.env` 里的密码，**二选一**。
- **怎么发现的**：Phase 0 的配置项；SUMMARY §3.2。
- **边界**：★ 白名单**必须包含 `172.16.0.0/12`**，否则容器过来的请求会被 403。

> ★ **现状走的是哪一支**（2026-09-14 实测，不是推断）：`:3060` 用的是**免密白名单**这一支，
> **不是** `.env` 里的口令。旁证：本机 `.env` 的 `QBIT_PASSWORD` 是**空串存根**（与 `.env.example`
> 同形），而 `scripts/diag/qb-census-savepath.py` 以 `admin` + 空口令**登录成功**。
> ⇒ ★ **Windows 侧那批只读工具（`qb-census-savepath.py` / 各 `probe-*.py`）能跑，靠的是白名单**，
> 不是凭据 —— 所以「本地 `.env` 是存根」这件事一直没被谁发现。
> ⇒ 失效形态是**响亮的**：白名单一收紧，它们当场**登录失败**（rc=1），
> **不是**静默返回空数据。★ 这一点值得记住：它把"收紧"从**悄悄变哑**降级成**当场报错**。
> ⇒ 也顺带解释了 `ERR-SEC-03` 那条「**覆盖薄的那道网，绿了也不代表安全**」为什么长期没暴露。

### ERR-SEC-02 容器内的 env 与磁盘上的 `.env` **不是一回事**
- **症状**：见 `ERR-DOCKER-01`。
- **环境前提**：`.env` 是**创建时**注入的快照。
- **规避做法**：判"生效没生效"一律**回读容器内**的键，不读磁盘文件。
- **怎么发现的**：同 `ERR-DOCKER-01`。
- **边界**：通用。

### ERR-SEC-03 ★★ **告警正文在教人泄露凭据** —— 而且专挑人最着急的时候
- **症状**：告警里的查证命令 `docker inspect reseed-cross-seed | grep TORZNAB_URLS`
  **原样会把凭据打出来**（那行里每条 URL 各带一个 `apikey=`，
  值就是 **Prowlarr 的应用级 key**，而 Prowlarr 里存着**所有 PT 站的 cookie**）。
- **环境前提**：告警是**给人照着敲的**；人照着敲的时候**正是最急的时候**。
- **根因**：**让程序替我们决定了"这时候会把什么打到终端上"**。
- **触发条件**：告警正文里嵌了未脱敏的诊断命令。
- **规避做法**：改成
  `… | sed -E 's/(apikey=)[^,&]+/\1<redacted>/g'`，并**在正文里写明**
  「★ 先脱敏再看，**别把原样输出粘进聊天/命令行**」。
  ★ 拿掉那个值**不影响判读**：要判的是"容器里还剩哪个站"，靠 `apikey=` 这个键名和 `/N/api` 就够了。
- **怎么发现的**：2026-09-13 审计告警正文时发现；README「两条通道」里「告警正文里的查证命令必须自带脱敏」那条、SUMMARY §20.8。
- **边界**：★ **规矩要写在命令里，不是写在"记得别看"里** ——
  靠人记得的那种防护，这条链上**已经漏过一次**（本地 `.env` 是存根 ⇒ 值指纹那道网覆盖很薄）。
  **覆盖薄的那道网，绿了也不代表安全。**

---

## B.5 运行时 / 版本层

### B.5.1 Python

### ERR-PY-01 ★★ `str | None` 在 3.8 上**定义期求值** ⇒ 崩在 logging 配置之前，**连一行日志都留不下**
- **症状**：任务跑了，**什么都没发生**（静默）。
- **环境前提**：NAS 宿主机的 python3 = **3.8.15**（`ERR-DSM-08`）。
- **根因**：`str | None` / `list[str]` 这类注解在 **< 3.10** 上是**函数定义时**当场求值的，
  直接抛 `TypeError` —— **不是等到调用**。而且它崩在 `logging` 配置**之前**。
- **触发条件**：文件里通篇用了 3.10+ 的注解语法，且**没有** `from __future__ import annotations`。
- **规避做法**：文件顶部**必须有** `from __future__ import annotations` —— **不能删**。
- **怎么发现的**：版本坐实见 `ERR-DSM-08`（`attempts.log` + `__pycache__/*.cpython-38.pyc`）；
  README「NAS 侧一次性配置」的「同一个 `python3` 也是 drive-loop 的解释器」那段。
- **边界**：★ 只在 **< 3.10** 成立。但**症状（静默）在任何版本不匹配时都一样** ——
  兜底（`run.sh` 另记 `attempts.log`）见 `ERR-DSM-08`。

### ERR-PY-02 `locale` / `encoding` 默认值：`isatty()` 对 NUL 返回 True
- **症状**：脚本以为自己在跟终端说话，其实输出被重定向 / 接到了 NUL。
- **环境前提**：Windows + GBK 控制台（与 `ERR-OS-04` 同族）。
- **根因**：`isatty()` 的判定不总是符合直觉。
- **触发条件**：靠 `isatty()` 决定输出格式 / 是否加颜色 / 是否重定向。
- **规避做法**：别依赖它做**承载语义**的判断；显式参数开关（如 `--no-color`）。
- **怎么发现的**：`scan-secrets.py` 的输出接到 `NUL` 也要一样（`tests/test_scan_secrets.py` 有一格专门钉这个）。
- **边界**：★ 本条**本项目实例较薄**（主要落在"输出接到 NUL 也一样"那条断言上），**别过度引用**。

### ERR-PY-03 ★ `spec_from_file_location` + `@dataclass`：必须先 `sys.modules[name] = mod`
- **症状**：报一个看着毫不相干的 `AttributeError: 'NoneType' object has no attribute '__dict__'`。
- **环境前提**：测试里用 `importlib` **把被测文件真的那份载进来**（不是切源码再 exec）。
- **根因**：`dataclasses` 要 `sys.modules.get(cls.__module__).__dict__` 去找注解的命名空间，
  而 `spec_from_file_location` **不替你登记**。
- **触发条件**：被测文件里有 `@dataclass`。
- **规避做法**：**先 `sys.modules[name] = mod` 再 `exec_module`**；吞掉 `SystemExit`、重定向 `stdout`。
- **怎么发现的**：`tests/README.md`「写测试时踩过的坑」里 `spec_from_file_location` 那条有原话 ——
  ★ **同一段 `_load` 代码对两个文件行为不同，原因却不在它们身上**：
  `drive-loop.py` 没有 dataclass 所以先跑通了，一度让人以为只有 `state.py` 有问题。
- **边界**：只在"动态加载 + dataclass"这一组合上成立。

### ERR-PY-04 `read_text` / `write_text` 的默认 `newline` 行为会**改行尾**
- **症状**：读进来再写出去，行尾被悄悄改了（CRLF ↔ LF）。
- **环境前提**：Windows 侧维护 `.env`（CRLF）与 `.sh`/`.py`（**必须 LF**）。
- **根因**：文本模式默认做换行转换。
- **触发条件**：用 `Path.read_text()` / `write_text()` 处理**行尾敏感**的文件。
- **规避做法**：**显式传 `newline=""`** 或按二进制读写（`bytes`）保真。
- **怎么发现的**：本项目同时存在"必须 CRLF"和"必须 LF"的文件（`ERR-OS-01`），所以这条是**结构性的**。
- **边界**：★ 只在 **Windows** 上会咬人；在 Linux 上默认不转换。

### B.5.2 各服务版本

### ERR-SVC-01 ★ qB 的 `setLocation` 是**异步**的，且**校验队列是单槽**
- **症状**：`max_active_checking_torrents = 1` ⇒ **145 条 IYUU 种子**挤在 `checkingDL`，**8 小时一步没挪**。
- **环境前提**：qB v4.6.5 / WebAPI 2.9.3。
- **根因**：`setLocation` 会触发**重新校验**，而校验队列**一次只跑一条**。
- **触发条件**：批量 `setLocation`（本项目一次 509 条）。
- **规避做法**：**不是"一条一条地调"** —— 调用按 `save_path` 分组、一批一次（500 条只有 3 个
  `save_path`），规则本体在 `A.2`。**能避开的只有"在途时别碰"**；避不开的是排队 —— 队列单槽，
  一大批下发后照样会出现一波 `checkingUP` ⇒ 搬完等它校完（`wait-for-checks.py`）。
- **怎么发现的**：搬迁后 145 条卡住；README「🔴 下一步」第 12 条（校验队列疑似卡死，卡住 145 条）。
- **边界**：★ **"卡住"的判据要小心**：已排除三种（497 个文件逐个 `stat` 全部大小相符、`save_path` 无映射失败、
  不是缺文件）⇒ 剩下的"**校验队列/校验器卡住**"**仍是"最符合证据的解释"，不是已坐实**。
  坐实要一条写操作：对任意一条卡住的种子手动「强制重新校验」，几秒内跳 100% 即坐实。

### ERR-SVC-02 Prowlarr 的 **`indexerId` 会变**
- **症状**：`TORZNAB_URLS` 里的 `/N/api` 指错了站。
- **环境前提**：索引器详情页 URL 里的数字（如 `/indexer/3` → id=3）。
- **根因**：**删站再重加，ID 往往就换了**。
- **触发条件**：增删索引器之后没重新核对。
- **规避做法**：**每次增删索引器后都要重新核对**：
  ```bash
  python scripts/diag/prowlarr-indexers.py --torznab
  ```
  ★★ **2026-09-17 更正：给药改掉了 —— 原来这里的处方是一条裸 `curl`。**
  这个端点的响应**每一条都带 `fields`**（那里面有 cookie / passkey）。
  早先的补法是「管道是必须的，不是顺手」——**那条补法不够**：
  · 它只挡住「裸跑」，挡不住「为了看一眼结构」而 `| head` / `| jq` / `| less`；
  · 更根本的是**没有替代品** ⇒ 规矩和处方在同一个文件里打架（本文件开头就写着
    「绝不打印 `indexer.fields`」，而处方逐字打出来）。
  ⇒ 现在改成 `scripts/diag/prowlarr-indexers.py`：**同一个端点，但只取
    `id` / `name` / `enable`** 三个白名单字段，`fields` 从不进内存、更不进 stdout。
  它的输出**可以安全粘贴**。★ 这是「**规矩要有工具守着**」那条的又一次落地：
  光有规矩、没有能用的工具，处方就会被抄成违规的样子。
  ★ 同族：**key 不进命令行**（会留在 shell 历史与进程列表里）。新脚本从 `.env`
  读 key、只走 `X-Api-Key` header（同 `scripts/diag/prowlarr-indexerstatus.py`、
  `scripts/diag/check-indexer-timestamps.py`）。
- **怎么发现的**：SUMMARY §6.6。
- **边界**：★ 还要区分：**「只在 Prowlarr 里禁用索引器」≠「cross-seed 不搜它」** ——
  cross-seed 只认自己的 `TORZNAB_URLS`，会继续请求已禁用的站，每搜一次吃一个 `HTTP 410` 并 snooze。
  **彻底停用某站必须把它从 `TORZNAB_URLS` 移除**再重建。
  **弯路 #17** —— 当初的形态：只在 Prowlarr 界面禁用，cross-seed 持续请求，大量 `HTTP 410`。
  ★ 一般化：**"禁用"是个界面动作，"不请求"是个配置事实**，中间没有任何连线。

### ERR-SVC-03 cross-seed 的**静默过滤**与**周期粒度**
- **症状**：以为"搜过了"，实际被静默筛掉 / 跳过。
- **环境前提**：cross-seed v6.13.7。
- **根因**：① `includeSingleEpisodes` 会**静默过滤**；② **索引器退避时待搜项直接跳过**（cross-seed **不排队、不重试**）。
- **触发条件**：一次 429 之后继续跑。
- **规避做法**：判据落在外围（状态机的 `SKIPPED` 阶段）；重搜周期粒度是 **(片 × 站)**，默认 **14 天**，
  来自 cross-seed 自己的 `timestamp(searchee_id, indexer_id, last_searched)` 表。
- **怎么发现的**：2026-09-11 有 **295 条**就这么消失了（SUMMARY §5.3）。
- **边界**：★ `searchCadence` / `excludeRecentSearch` 的粒度**不能互相替代**。

### ERR-SVC-04 ★ IYUU 辅种的目录从哪来：**qB 里同 infohash 那条自己的 `save_path`**，原样回灌
- **症状**：纠结"要不要手动配监控文件夹/资源文件夹/种子文件夹"。
- **环境前提**：IYUU Plus 往 `:3060` 辅种。
- **根因（溯源结论）**：源码三跳 ——
  `Client.php:726` → `ReseedServices.php:379` → `ReseedDownloadServices.php:149`，
  `savePath` = **qB 里同 infohash 那条已有种子自己的 `save_path`**。
  ⇒ 那三个路径框**在这条路上一次都没被读**，`cn_folder` 目录映射表也**零行**。
- **触发条件**：（不触发 —— 这是一条**澄清**）
- **规避做法**：**不需要手动配任何目录**。唯一参与路径决策的是「创建多文件子目录」→ qB 的 `root_folder`
  （已勾是对的）；`autoTMM='false'` 硬关 ⇒ 分类的 savePath 不会覆盖。
- **怎么发现的**：源码三跳溯源，README「🔴 下一步」第 10 条（IYUU 辅种的目录从哪来）/ SUMMARY §21.2–§21.3。
- **边界**：★「容器路径映射不一致」（IYUU 看 `/video`、qB 看 `/volume1/video`）**碰不到辅种目录** ——
  它只对那两个**空转的字段**成立。

### ERR-SVC-05 FlareSolverr：Prowlarr **按索引器挂 tag** 启用，**没有全局开关**
- **症状**：装了、容器在跑、健康 —— **一次都没被用上**。
- **环境前提**：Prowlarr + FlareSolverr v3.5.2。
- **根因**：启用方式是**给那个索引器挂一个名为 `flaresolverr` 的 tag**；
  「Settings → Indexers 里填个 URL」这个说法是**错的**。实测 Prowlarr 的 tag 列表是**空的**，
  四个索引器**一个 tag 都没挂**。
- **触发条件**：以为配了 URL 就等于启用了。
- **规避做法**：**怎么判断某个站要不要它**：在 Prowlarr 里手动搜一下 ——
  看到 `403` + 挑战页 HTML / 日志里出现 `Cloudflare` → 需要；
  `500/502/520/522/timeout`（站点后端挂了）或 `429`（限流）或能正常返回 → **不需要**。
  ★ **现在四个站都不需要它**。
- **怎么发现的**：SUMMARY §18.12；弯路 #7。
- **边界**：★ 「从 GHCR 拉镜像失败」那条**已过时**（2026-09-12 已成功拉过 12 层），
  compose 里的镜像名**不用**换。**别引用旧状态**（`A.8` 的半衰期）。

### ERR-SVC-06 cross-seed 启动报 `unknown option` / 键名错误
- **症状**：容器起不来或配置被忽略。
- **环境前提**：cross-seed 版本升级会**改配置键名**。
- **规避做法**：按 `cross-seed/config.js` 顶部说明与官方文档改键名。
- **怎么发现的**：Phase 2 的验证步骤就是这么设计的（`docker compose logs -f cross-seed`，看有无 `unknown option`）。
- **边界**：★ 本项目用的是 `ghcr.io/cross-seed/cross-seed:latest` —— **版本会自己飘**，这条随时可能复发。

### ERR-SVC-07 各系统自己的 cron 时刻
- **症状**：拿"搬迁当天"的读数去判"搬迁有没有把 IYUU 搞停"。
- **环境前提**：IYUU 的 cron 是 **`45 1 * * *`（01:45）**；日报在**当天第一批（约 `00:0x`）**采样。
- **根因**：采样时刻**早于**被观测事件。
- **触发条件**：日报读得比事件早。
- **规避做法**：**第一个有意义的读数是 09-14 凌晨，不是 09-13**。
- **怎么发现的**：原 README「还没做」第 14 条的「搬迁后次日 01:45 之后复核」专门标了这句（**该节 2026-09-19 已移除**）。
- **边界**：★ 这是**时间窗判据**（`A.7` 的 ★）最典型的一例：**"判据跑了"和"判据能看见"是两件事**。

### ERR-SVC-08 Prowlarr 会**伪造 429**；它的 Query Limit 是**本地保险丝**，不是站点真实余额
- **症状**：把 Prowlarr 的 Query Limit 当节流阀用，结果**没有节流作用**。
- **环境前提**：Prowlarr 作为代理层。
- **根因**：Query Limit 的设计目的是**全局保险丝**（设成明显高于实际用量的值，如 1000/天）；
  真实控速要靠 cross-seed 的 `delay`。
- **触发条件**：拿它当节流阀。
- **规避做法**：`delay` 保持 **30~45**；对站点友好的节流在 cross-seed 侧。
- **怎么发现的**：弯路 #16、README「多站点」的「对站点友好」那段。
- **边界**：★ **站点真实余额 Prowlarr 根本不知道** —— 它能提供的是"代理视角"，见 `A.8`。

### ERR-SVC-09 外部站点的状态**有半衰期**
- **症状**：拿着两天前的故障描述做今天的决策。
- **环境前提**：4 个外部 PT 站。
- **根因**：站点侧故障会自己恢复，而**文档不会自己更新**。
- **触发条件**：引用旧记录。
- **规避做法**：引用前**回读一次**。
- **怎么发现的**：BTSCHOOL —— 当初因为它出 CF 挑战页而要换站，
  后来它**自己恢复了**且是四站里命中率最高的（23/46 = 50%），**只是没人回来销账**。
  ⇒ 结论是「**当初的判据失效了**」，不是「换好了」。
- **边界**：★ 反例也要记：**真要换，按数据该换的是 `NanyangPT`（4/452 = 0.9%）**，
  不过南洋可能只是**片库对不上**这批包。**先看目录，再下结论。**

### ERR-SVC-10 ★ 状态机的 `stage` 是**推导出来的**：少喂一个输入 ⇒ **必然**误降级
- **症状**：回灌打出 `已在 qB 里: 0` / `本次新增做种: -51`（**负数**）；已做种的片子被从 `SEEDING` 打回 `MATCHED`。
- **环境前提**：`stage` 由「状态机库 + cross-seed 库 + **qB 实况**」三者联合推导，qB 那一路靠 `--qbit-url` 进来。
- **根因**：忘传 `--qbit-url` ⇒ 哈希交集恒为空 ⇒ 推不出"在做种" ⇒ **降级**。
  它**不是**"数据暂时缺"的容错问题，是**函数少了入参，结果必然错**。
- **触发条件**：手敲 `reseed-state.py drive` / `sync` 时漏参数（脚本里写死的路径不会漏，手敲会）。
- **规避做法**：★ 补救是**幂等**的 —— **补跑一次带 `--qbit-url` 的 `sync` 即完全恢复**，不发任何站点请求。
  ⇒ 看到负数**别慌、别重建库**，补跑即可。
- **怎么发现的**：README「常见问题」的「回灌打出 `新增做种` 负数」那条、README「交接必读的坑」第 1 条；SUMMARY §13.6 坑 1。**弯路 #8**。
- **边界**：★ 这里有个更深的教训：**"降级"和"真的没做种"在输出上长得一样**。
  ★ **同一节课本项目上了两次**：后来 v3 农场切换又出过一次同形状的
  （cross-seed 报农场路径、`pack.roots` 还是原路径 ⇒ searchee 全被静默跳过 ⇒ 状态机再次误降级）。
  第一次（缺 `--qbit-url`）**补跑 `sync` 就够了**；第二次**补跑治不了** ——
  实测只从 13 回到 21，修掉真根因（`pack.farm_root` 列）才到 **201**。
  ⇒ 判据：**"补跑一下就好了"只对"输入缺失"成立；对"判定依据过时"必须改数据。**
  详见 SUMMARY §17.5.1、§17.3（当时的误判过程）。

### ERR-SVC-11 ★ 两个独立系统各有一个"深度"参数，对不上 ⇒ 幽灵条目 / 漏搜
- **症状**：状态机里有这条、cross-seed 里没有（幽灵条目）；或反过来，真在搜的片子状态机漏掉。
- **环境前提**：`reseed-state.py` 的 `--depth` 与 cross-seed 的 `maxDataDepth` 是**两处独立配置**。
- **根因**：两边枚举出来的 searchee 集合**不同**。含义就是"从 dataDir 往下数几层"，
  第 1..N 层的**目录和视频文件**都算 searchee（不是"几层目录"）。
- **触发条件**：任一侧改了深度，另一侧没跟。
- **规避做法**：**`--depth` 必须与 `maxDataDepth` 完全一致**（本项目未显式配置 ⇒ 默认 **2**）。
  ★ **别用"调大 `maxDataDepth`"去解决某个包的局部问题** —— 它是**全局**开关，
  会连带改变 FRDS / MBF 的枚举行为、产生更多 searchee 和更多 indexer 请求。
- **怎么发现的**：README「单片状态机」的 `--depth` 警告、README「交接必读的坑」第 5 条；SUMMARY §10.6.1。**弯路 #9**。
- **边界**：★ 两套系统的边界校验**在运行时没有任何断言** —— 它们只是各扫各的，
  差异只以"某些条目说不出话"的形式浮现。**跨系统对齐的配置项，必须有一处写死"必须相等"。**

### ERR-SVC-12 ★ 轮询间隔 **长于**被观测的窗口 ⇒ 判据**从来没生效过**
> **状态**：✅ 已结案（2026-09-19）—— 依据「规避做法」自述「已修」，且**代码实证**：
> `--backoff-check-secs`（默认 60）在 `scripts/drive-loop.py:2794` 与 `scripts/reseed-state.py:825`。
- **症状**：`backoff_hits` 一直是 **0**，**但**索引器状态明明是 `RATE_LIMITED`。
- **环境前提**：退避是"按秒"的短事件；检查是"按批"的长周期。
- **根因**：检查间隔 `--check-every × --interval = 10 × 30s = 300 秒`，
  **比实测的退避窗口（30~60 秒）还长** ⇒ 整段窗口落在两次检查之间 ⇒ 既不等待、也不计数。
- **触发条件**：任何"采样周期 > 现象周期"的观测。
- **规避做法**：已修 —— 加 `--backoff-check-secs`（默认 **60**）**按秒**触发检查，
  并按 `retry_after` **变化**认定"新发生了一次退避"（而不是按"当前是否在退避"）。
  现在是**双触发**：每 `--check-every 10` 条 **+** 每 `--backoff-check-secs 60` 秒。
- **怎么发现的**：README「常见问题」的「`backoff_hits` 一直是 0」那条、原 README「还没做」第 8 条（**该节 2026-09-19 已移除**）；SUMMARY §17.5.2。**弯路 #10**。
- **边界**：★★ **这类失效最坏的地方是它长得像"没事"**（0 就是"没发生过"）。
  ⇒ 判据：**任何恒为 0 的计数器，先算一遍"采样周期 vs 现象周期"**，再决定信不信它。

### ERR-SVC-13 ★★ `--once` 模式下的 `next_sleep()`：**降速策略压根没接线**
> **状态**：✅ 已结案（2026-09-19）—— 依据「规避做法」自述「已修（SUMMARY §17.5.4）」，
> 且 `summary/17-首次真跑的探针发现.md:293` 的 §17.5.4 确实存在（`next_sleep()` 已接线）。
- **症状**：日志里**写着**"站点在退避，缓一缓"，实际上**没有任何等待**（`--once` 模式）。
- **环境前提**：同一份 `drive-loop.py` 有两种跑法：常驻循环 / 一次性（DSM 计划任务每 15 分钟拉一次）。
- **根因**：`next_sleep()` 的返回值**只进了日志**，阻塞等待只接在常驻分支上。
  ⇒ 本项目实际用的是 `--once` ⇒ **这条策略从上线起就没生效过**。
- **触发条件**：任何"分支模式"的代码 —— 只测/只接线了其中一条。
- **规避做法**：已修（SUMMARY §17.5.4）；`--once` 模式下的接管机制见 `ERR-SCHED-04`。
- **怎么发现的**：查 `backoff_hits=0` 时**连带**发现 —— 不是主动查出来的（README「常见问题」的「`backoff_hits` 一直是 0」那条、原 README「还没做」第 8 条，**该节 2026-09-19 已移除**）。**弯路 #11**。
- **边界**：★★ 它与 `ERR-SVC-12` 是**同一次排查里挖出的两根钉子**，形状不同：
  - `ERR-SVC-12`：**判据看不见**（采样太快，事件在两次采样之间）。
  - `ERR-SVC-13`：**判据看见了、结论也记了，只是没接动作**（日志有、行为无）。
  ⇒ 「日志说它做了」**从来不等于**「它做了」。判据要落在**可观测的行为**上（心跳、下一批的起始时刻），
  不是落在日志措辞上。（同类：`ERR-AI-05`。）

### ERR-SVC-14 ★ 把**目录**当成搜索名 ⇒ 白烧站点查询额度
- **症状**：搜索请求的名目是 `合集` / `分类标签` 这种**根本不是发布名**的串；额度被无声吃掉。
- **环境前提**：大包（DC 类）解压后是「包根 → 中文分类标签 → 真发布名」的多层结构。
- **根因**：`DATA_DIRS` 直接指向包根 ⇒ 枚举出来的 searchee 包含了**中间层的分类目录**，
  它们被拿去当搜索名。★ 这也是**本项目最高优先的资源是站点额度**的具体体现。
- **触发条件**：接入新的大包时，没先看它的目录形状。
- **规避做法**：**先看目录再定根**。两条正解（按包形择优，都已在用）：
  ① 根指到"标签目录的直接子目录就是真发布名"的那一层，`maxDataDepth` 保持默认 2；
  ② 确需包根作 dataDir 时，才动深度**并**配 `blockList`（如 `nameRegex:^\d{2}\.`）兜底 ——
     属启发式，代价大，见 `ERR-SVC-11` 的"别动全局开关"。
- **怎么发现的**：README「多包支持」整节（尤其「嵌套结构」与「其它要点」两小节）；SUMMARY §10.4 / §10.6。**弯路 #18**。
- **边界**：★ 代价**不在**搜索本身，而在**额度**（本项目最稀缺的资源）；
  而且它**不报错** —— 只是命中率悄悄变低。**先看目录，再下结论**（同 `A.8` / `ERR-SVC-09`）。

### ERR-SVC-15 ★ cross-seed 的 `ENOENT` 是**它自己的并发**，不是丢种
- **症状**：`error.<日期>.log` 里 `[inject] Failed to parse ... ENOENT` —— 看着像注入失败、种子丢了。
- **环境前提**：cross-seed 注入完成后会做**善后清理**（删掉已没用的存档）；清理与其它操作并发。
- **根因**：清理拿到的是一份**文件清单**，然后**逐个**打开 —— 清单里的某个文件若在这期间
  被**另一路**删掉 ⇒ `open()` ⇒ `ENOENT`。**竞态，不是损坏。**
- **触发条件**：自身高并发时（`Failed to parse` 与 `ENOENT` 各 7 条，同一批）。
- **规避做法**：★ 判据：**看时间差与后果** —— ENOENT 落在 `09:40:07.336`，与删档动作**相差 320 ms**，
  且同一文件被删了两次 ⇒ **结论：种早已注入且完整，丢的只是一份已经没用的存档**。
- **怎么发现的**：原 README「还没做」第 12 条（**2026-09-19 迁入 `SUMMARY §16.6.6`**）；SUMMARY §16.6.5（那一节的标题就是
  「**两个「看着像事实、其实是观测工具坏了」的发现**」——"先撞上 ENOENT，再撞上真正的根因"）。**弯路 #20**。
- **边界**：★★ **"先撞上 ENOENT，再撞上真根因"** —— 这条的教训是**顺序**：
  噪声先出现，会**带走**排查的方向。⇒ 见到 `ENOENT` 先问"是谁在删、删的是不是废料"，
  **别**直接跳到"我的数据丢了"。（同一形状见 `B.10` 元层清单。）

### ERR-SVC-16 ★★ qB 检测到**文件不在**时不报错，而是**就地重下** ⇒ 旧路径留下整份残骸
- **症状**：旧路径上冒出一批**完整**的重下数据（本例 **44.6 GiB / 40 分钟**，约 19 MB/s），
  而新路径同名文件的 inode 与它**不同** ⇒ 是真副本，不是硬链接。
  qB 自己的执行日志能对上：`文件错误警报`（那条的**文件路径全是旧根**）→ `Torrent 下载完成`。
- **环境前提**：数据**已经迁到新路径**（或迁移进行中），而 qB 这些种子的 `save_path` **尚未同步**。
  ⇒ 对 qB 来说，这不是「数据搬走了」，而是「**我要的文件不见了**」。
- **根因**：文件缺失时 qB **不报错**，按 `save_path` **就地重下**。
  ★ 与 `ERR-SVC-01`（`setLocation` 是**异步**的）**同族**：都是客户端按自己的时刻表，
  去补偿一个它以为存在的缺口。
- **触发条件**：qB 恢复 / 重启**先于** `setLocation` 完成。
  ★★ **具体触发源未定位** —— 是 qB 重启？是 IYUU 那批外部注入的种子？还是别的？
  **别把它当结论**：实测的只有下面那条时间线。
- **规避做法**：本项目**已有** §18.3「先 `setLocation` 让 qB 自己搬」—— 那条仍然对。
  ★ 但它只覆盖**本流程管的种子**；**外部注入的（IYUU 那批）不在迁移流程里**，不受我们控制
  ⇒ 迁移前后要**单独核对这一批的 `save_path`**，不能假定它们跟着一起动了。
- **怎么发现的**：从 `drive-loop/attempts.log` 里长期的 ENOSPC 噪声起疑，顺着
  「**旧路径为什么会有文件**」查到 qB 执行日志的时间线 ——
  `09:11:48 已恢复 204 条 → 09:12:12 文件错误警报 146 条 → 09:15:09 下载完成 29 条 → 10:13:27 设置路径 22 条`。
  ★ 顺序本身就是判据：**先恢复、再找不到、再重下，最后才 setLocation** —— 补的动作发生在修的动作之前。
- **边界**：★★ 两条，都**别当结论**：
  1. **空间账对不齐** —— 「删掉那 44.6 GiB 到底释放了多少」**未确认**；同一个量在不同时刻的读数本身就
     互相不对齐（见「关键取值速查 → 卷与容量」的 ⚠），而 BTRFS CoW 下「删了不释放」另有一支（`ERR-FS-04`）。
  2. **触发源未定位**（见上「触发条件」）。
  ★ 判「是真副本还是硬链接」用 **`fid`（= 服务端 inode）**，**别用 `nlink`** ——
  实测一对**确凿的硬链接**（农场条目 vs 它的源，`cp -al` 建的）两边 `nlink` **都报 1**。
  与 `ERR-FS-04` 的 `%h == 1` 是同一族「链接数不可信」，但差别在**层**：那条在 NAS 本地，
  这条**跨 SMB 也不透传**。

### ERR-SVC-17 ★★ 「被限流久不恢复」：`retry_after` 一个数盖了**三种**机制
- **症状**：某个站被"限流"后长时间不恢复；且它的 `retry_after` 时长**一次比一次长**
  （2026-09-14 实测：**34 → 839 → 1439 分钟**，1439 ≈ 24 h）。
- **环境前提**：请求经 **Prowlarr** 走 Torznab。Prowlarr **自己**会对失败的 indexer 做**本地禁用**。
- **根因**：★ **同一个 `retry_after` 读数底下至少三种机制**，而它在读数上长得一模一样：
  | # | 机制 | 谁设的 |
  |---|---|---|
  | ① | 站点真发 `Retry-After` | 站点 |
  | ② | **Prowlarr 本地禁用**（`indexerstatus` 的 `disabledTill`）| Prowlarr |
  | ③ | **cross-seed 自己的退避阶梯** | cross-seed |
  ②③ 是 `ERR-SVC-08`（Prowlarr 伪造 429）的**下游**：Prowlarr 若在本地拒，
  cross-seed 收到的就是 429 ⇒ 它按**自己的**阶梯退避。与 `ERR-ENC-07`
  （一个名字盖两种模型）**同族，只是这里是三种**。
- **触发条件**：任一站连续失败（真 429 / CF 挑战 / 本地 Query Limit 都可能起头）。
  ★★ **2026-09-14 实测：HDtime 这一例的"起头"不是限流，是站点 5xx。** 三行，全取
  **Prowlarr 自己的日志**（脱敏后原样）：
  ```
  2026-09-13 11:30:11.3|Warn|Cardigann|Request for HDtime failed with status InternalServerError. Retrying in 0.9856542s.
  2026-09-13 11:31:50.1|Warn|Cardigann|Unable to connect to HDtime at [https://hdtime.org/torrents.php?…]
  2026-09-14 11:36:11.4|Warn|Cardigann|HDtime server is currently unavailable. https://hdtime.org/torrents.php?…
  ```
  同一批日志里 **429 一条都没有**（`grep -i 429` 命中的全是 `2429 bytes` / `0.0042937s`
  这类数字噪音）⇒ cross-seed 收到的那个 429 **是 Prowlarr 发的**，不是它转发站点的。
  ★ **残留推断**：没有响应头的直接证据，依据是"cross-seed 只跟 Prowlarr 说话"
  + Prowlarr 侧无 429 记录。**别把它当站点在限流来处置**（那会去等站点或降速，
  而真正该查的是"HDtime 为什么对 Prowlarr 返 5xx"）。
  ★ 顺带**排除**两项：不是我们搜太勤（09-10 / 09-11 就在失败，那时 HDtime
  **还没进 `run.sh` 的 `--indexers`**）；不是 CF 挑战（Prowlarr 全部日志 0 条
  FlareSolverr 字样）。
- **规避做法 / 判据**：★ **别继续看 `retry_after`** —— 它分不开这三种。**换读数**：
  ```bash
  python scripts/diag/prowlarr-indexerstatus.py          # ② Prowlarr 本地禁用（本条的判据）
  python scripts/diag/check-indexer-timestamps.py        # ③ cross-seed 侧：status + retry_after + 还剩多久
  ```
  · 数组里**有**该站、带 `disabledTill` ⇒ **②**（Prowlarr 本地禁用，与站点无关）
  · 数组为空 / 没有该站 ⇒ **①**（站点真发，Prowlarr 只是转发）
  ★ **这两个脚本替换掉了早先的一条裸 `curl`**（`-H "X-Api-Key: …"`）—— 那条有两个毛病：
  key 摆在**命令行**上（进程表 / shell 历史 / 聊天粘贴都漏），而返回的裸 JSON 里只有
  `indexerId`、**要靠人肉对到站名**。脚本从生产 `.env` 读 key（**绝不打印**）、
  只打白名单字段、站名只从 `cross-seed.db` 取 `id`/`name` 两列。
  两个脚本都在 `deploy.sh` 的白名单**之外**（Windows 侧手动诊断用）。
  ★ **两个时钟不一致时以更长的为准**：`disabledTill` 管 Prowlarr 何时肯转发，
  `retry_after` 管 cross-seed 何时肯再问 —— **后者更长就由后者决定恢复时刻**。
  ★★ **2026-09-17 实测：它按的是自己那个时钟，不是"Prowlarr 放行了就走"。**
  证据是 09-14 那一批的逐秒时间轴（`drive-loop.log`，原文可查）：
  ```
  11:15:01  批开始，读到 cross-seed 侧 retry_after = 11:32:28
            这一批**没有**中止，而是**如实等到 11:32:28**（退了 17.5 分钟）
  11:32:28  ← cross-seed 那个时刻到点
  11:32:31  立刻发第 1 条 `OK 204`  ← **差 3 秒**，一路发到 11:36:31 的第 9 条
  11:36:41  Prowlarr 侧在同一分钟内又记了一次失败（写进 retry_after）
  11:37:01  读到 retry_after=11:36:41，**它已经过去 20 秒** ⇒ 中止（要等到次日 1440 分钟）
  ```
  ⇒ ① **"到点就走"这点坐实了**（11:32:28 → 11:32:31，3 秒）—— 它不是"看到 Prowlarr
  放行了才走"，而是**按 `retry_after` 自己到点就走**，与 Prowlarr 放不放行无关；
  ② 所以本行说的"以更长的为准"**成立**，但机制要说准：**不是两个时钟取 max 那么玄，
  而是 cross-seed 只看自己那一个**；Prowlarr 那个更长的窗口，效果只体现在
  "它拒转发 ⇒ cross-seed 收到 429 ⇒ **被写进** cross-seed 自己的 `retry_after`"。
  ③ ★ **反面同样重要**：`disabledTill` 到点而 `retry_after` 还没到，**没观测到
  "Prowlarr 放行了但 cross-seed 不动"那一格**（09-14 那批它就是在等自己那个时刻）。
  ⇒ 别把"两个时钟取更长的"当成一条独立机制去解释现象；**读数取 max 是结果，不是原因**。
  ★★ **2026-09-14 实测：上一句原来写的是「这是推断，不是实测」，现在它有据了** ——
  两个读数取自同一天，而且 **Prowlarr 自己的日志就写着它打算忽略到什么时候**：
  ```
  Prowlarr   11:36:31.6|Debug|IndexerFactory|Temporarily ignoring indexer HDtime
                                            till 09/15/2026 11:36:11 due to recent failures.
  cross-seed indexer      retry_after    = 2026-09-15 11:36:41     ← 差 30 秒，同一时刻
  ```
  ⇒ ① **cross-seed 那个"递增到 24 h"不是它自己发明的阶梯**：Prowlarr 在**同一时刻**
  报的就是 24 h（上面那行）。② 两者可以只差 30 秒 —— 起算点不同：Prowlarr 按
  「上次失败时刻」算，cross-seed 按「自己收到 429 的时刻」算。
  ★ **仍未实测的只剩一处**：cross-seed 是**原样转抄 `Retry-After`**，还是"读到了、
  但按自己的起算点重算"。差别只在那 30 秒，**不影响「谁更长」**。
  ★ **验法**（Prowlarr 放行那一刻 —— 那一刻本身有两个候选值，见下面边界 ③）：
  Prowlarr 放行而 cross-seed **仍不动** ⇒ 「以更长的为准」坐实；
  cross-seed **动了** ⇒ **错**，回来改本条 —— 别把它当中性事实传下去。
  ★★ **2026-09-17 结论：这个验法问错了问题，别再按它判。**
  实测（见上面那段时间轴）是：cross-seed **按自己 `retry_after` 到点就走**，
  3 秒内就发了。所以「Prowlarr 放行而它不动」这一格**观测不到** ——
  不是"没验到"，是**它本来就不会那样**（它压根不看 `disabledTill`）。
  ⇒ 正确的说法只有一句：**恢复时刻 = `retry_after`（cross-seed 自己的那个）**；
  要问"还要等多久"就查它，不必去对 Prowlarr 的时刻。
  （原任务 `#60` 就是卡在这个待验上；2026-09-17 结案，`ERR-SVC-17` 按本段更正。）
- **怎么发现的**：2026-09-14，HDtime 的 `retry_after` 从「剩 34 分钟」跳成「剩 1439 分钟」
  之后，`tools/doc-audit/04_probes.py` 只报出这个数、报不出是哪种机制 ⇒ 换 `indexerstatus` 才定下来。
  **三个数据点**（09-13 那次空转 + 09-14 这次跳变 + 09-14 冲进 `prowlarr/logs/` 才看到的
  `IndexerFactory` 那行「ignoring … till 09/15」）。
- **边界**：★★ **三条，都是"实测推翻先前读法"，别按老读法处置**（第 3 条 2026-09-14 补，
  同时更正了第 2 条里那半句「Prowlarr 恒为 6 小时」）：
  1. **「滑动窗口」不成立。** 曾怀疑"每失败一次窗口就往后推 6 小时 ⇒ 永不恢复"。
     实测**证伪**：两次采样相隔约 2 分钟（批次正在跑），`mostRecentFailure` 与
     `disabledTill` **一字未动** ⇒ 已禁用的站在 Prowlarr 里**被短路、不再记新失败**，
     窗口是**固定的**，到点自己解开（本例 17:38 本地）。
     ⇒ **"继续敲门会把它推得更长"是错的。**
  2. **时长递增不是站点在升级处罚。** 1439 分钟 ≈ 24 h ≈ 一个**上限**。
     ★★ **2026-09-14 更正**：原来这里写「递增形状来自 ③ cross-seed 自己的阶梯，
     **Prowlarr 那侧的窗口反而恒为 6 小时**」—— **后半句被 Prowlarr 自己的日志推翻**：
     它在 `11:36:31` 明写「ignoring … till **09/15/2026 11:36:11**」（= 失败时刻 + **24 h**）。
     ⇒ 递增的那个数**Prowlarr 自己也报过**；"恒为 6 h" 只是 14:27 那一次 `indexerstatus`
     的读数（见下面 ③）。
     仍**成立**的那半句：不是站点按违规次数加罚 —— 窗口是 **Prowlarr 本地**算的。
     ⇒ 按"递进惩罚"处置（**停手等它消气**）会白白停手：恢复时刻由**本地那个更长的
     窗口**决定，不是由站点决定。
  3. ★★ **同一天里 Prowlarr 的窗口变过，而且"放行时刻"现在有两个候选值。** 三个读数：
     | 时刻 | 来源 | 值 |
     |---|---|---|
     | `11:36:31` | Prowlarr 日志 `IndexerFactory` | 忽略到 **09/15 11:36:11**（= 失败 + 24 h）|
     | `14:27` | `GET /api/v1/indexerstatus` | `disabledTill` = **09-14 17:38:01**（= `mostRecentFailure` 11:38:01 + **6 h**）|
     | `14:27` | `cross-seed.db` 的 `indexer` 表 | `retry_after` = **09-15 11:36:41** |
     ★ **矛盾点，别硬凑原因**：`11:38:01` 又记了一次失败 —— 若已禁用的站真被"短路
     （见边界 1）"，这一次失败就不该发生；而窗口还**从 24 h 缩成了 6 h**。
     两件事都与边界 1 / 2 的旧说法不一致。**Prowlarr 的升级规则未读源码，不做推断。**
     ⇒ 实操上只看一件事：**放行时刻 = 上表里最晚的那个**（这里是 `09-15 11:36:41`）。
  ★ 三条都只**改了理由**，没改"别硬试"这个动作 —— 硬试**不会**让窗口变长，
    但也**换不来**恢复，纯属白跑。
  ★★ **补：「清 snooze」≠ 免费修复。** 本条的目的**不是**"怎么把退避清掉"，而是
  **别把清退避当成修复**。三层叠着：
  1. **清 Prowlarr 的禁用不够** —— 抹掉 `disabledTill` 只让 Prowlarr 立刻肯转发；
     cross-seed 那边仍停在 `retry_after`（实测两个时钟差 ≈ 18 h）⇒ 它照样不问，
     产出照旧是 0。
  2. **清 cross-seed 的 snooze 也不够** —— 上游那个 5xx 还在（HDtime 对 Prowlarr
     返 500/502），清完立刻被 429 打回来，而窗口**又按本地规则重算一遍**。
  3. 所以处置的**唯一**抓手是**上游**：查 HDtime 为什么对 Prowlarr 返 5xx。
     在它恢复之前，任何"清一下"只是把同一段等待重放一遍，产出照旧是 0 ——
     而且**不会**更快（边界 1 那层：继续敲门推不长窗口、也换不来恢复）。
     ★ 好消息：重放**不伤站** —— 被禁用的站在 Prowlarr 里就被短路了，请求没到
     HDtime（这也是边界 1 能成立的原因）。
  ⇒ 一句话：**这条是"停手等它 + 去查上游"的判据，不是"清掉就能好"的判据。**
  ★★ **2026-09-17 补：上面第 2 层在实测里被证实了，而且代价比写着的更大。**
  同一条时间轴（09-14 那批）显示：清/到期之后**立刻花掉一次请求**，
  而那次请求**失败并又写了一个更长的 `retry_after`** ——
  阶梯每一步都是这一格：`20:53` 值 `21:24:13` → `00:11` 值 `03:00:11` → `05:03` 值 `09:46:40`
  → `12:38` 值 **次日 11:32:28** → `11:37` 值 **次日 11:36:41**（+24 h 量级）。
  ⇒ 所以"清了试试"**不是中性的**：它把"明天 11:36 恢复"变成"**后天** 11:36 恢复"
  （实测 09-13→09-14 就是这么过去的）。**这一条的代价是 +24 小时，不是一次请求。**
  ★ 判据换成一句**可执行**的：要问"还要等多久"，只查
  `python scripts/diag/check-indexer-timestamps.py`（`retry_after` 剩多久），
  **别去 `tools/doc-audit/04_probes.py` / indexerstatus 对 Prowlarr 的时刻** —— 那个数不构成"恢复时刻"。

**◆ 本小节未实例 / 未验证**：`searchCadence` / `excludeRecentSearch` 的具体键名与取值（本项目走的是自建状态机的周期，未直接用它们）。

---

### ERR-SVC-18 ★★ qB 缺文件时**不报错**，而是去"补"——**没有 peer ⇒ 永远补不上**
- **症状**：`:3060` 里挂着一批未完成单种，`state=stalledDL`、`progress ≈ 0.999x`、
  **永远不动**。用户报的说法是「连校验过后、**一点都不相符**的文件都开始直接下载了」
  ⇒ 听起来像"校验失败 ⇒ 重下"。
- **环境前提**：cross-seed 只匹配「**名称 + 大小**」；农场（`reseed_farm/`）里
  **只有载荷**（我们自己的建场规则）；qB 只认 **piece**、**没有文件级校验**（README「原理 C」）。
- **根因**：★★ **不是校验失败**。实测（2026-09-18，全只读）是：
  该 release 的**发布组把附件（`.nfo` / `.jpg`）也写进了种子内容**，而农场里没有这些附件
  ⇒ qB 发现「有几个文件我这儿没有」⇒ **去补** ⇒ 那几个文件**没有任何 peer 有**
  ⇒ `num_seeds == 0` / `availability ≈ 0.999` ⇒ **永远补不上**。
  ★ **四个位置的读数**（缺一不可）：
  | 判据 | 读数 |
  |---|---|
  | 逐文件 progress | `.mkv` ≈ **99.98%**，而 `.jpg`/`.nfo` **恰好 `0.00000%`** |
  | 那个 0% 的文件在 `reseed_singles/` | **不存在**（`ls` 只有 `.mkv`） |
  | 同一个文件在农场里 | **存在**（`reseed_farm/<片名>/cover.jpg`） |
  | 载荷（`.mkv`）两侧大小 | 逐字节**相同**（`9658005448`）⇒ 载荷**没被改过** ⇒ 没有校验失败 |
  | `amount_left` vs（农场 size − 物理 size） | `2097152` **≠** `254167` ⇒ ★ **对不上账** |
  ★★ 最后那条是这里唯一"反直觉但有信息量"的：它说明 **qB 的进度是「按 piece 全局
  推导」的** ⇒ **不能跨文件对账** ⇒ 「逐文件 progress == 0」**不是**可靠判据
  （要判就用 `amount_left / size` 这个**相对缺口**）。
- **触发条件**：cross-seed 匹配到"附件型"release（附件在种子内容里、而农场里没有）。
  ★★ **互锁已生效 ⇒ 不再新增**：`cross-seed/config.js` 的 `resolveMatchMode` 在
  `linkType` 为 `hardlink`/`symlink` 时**无论 `MATCH_MODE` 是什么都强制 `strict`**。
  实测：`LINK_TYPE=hardlink` + `MATCH_MODE=partial` ⇒ 生效为 **strict**。
  ⇒ 现存那 3 条是**存量**。★ **绝不能**为让"新建的链接也能 partial"去动
  `LINK_TYPE` / `MATCH_MODE` —— 那会**打开**新的写穿（正是 `ERR-SVC-16` / linkguard 要防的）。
- **规避做法 / 判据**：★★ **qB 只重下它缺的 piece，已经有的那块数据不会重下
  ⇒ 源文件目前还没被写穿**（那是 linkguard 在盯的事）。真危害是两条：
  **①永久占位 ②踩 HnR**。
  ★★ **处置第一原则：别去动那几个数据文件** —— 一旦那个 `.mkv` 被碰过/删过/移动过，
  qB 就会真的去"补"整块载荷，**那时才会真写穿源**（硬链接同一个 inode）。
  ★ **不必限速**：这些种子**没有任何 peer** ⇒ 下载速率**本来就是 0** ——
  限速限的是恒为 0 的量，还会**误伤**同一 qB 里正常下载中的种子。
  判据（只读，不碰 qB）：
  ```bash
  python scripts/diag/reseed-freeze-report.py                 # 默认只打聚合
  python scripts/diag/reseed-freeze-report.py --show-hashes   # 12 位短 hash + 站点目录名
  ```
  ★ **阈值 `1e-3` 的依据是实测断层**（全量 1999 条：`(0,1e-3]` **3** 条、
  `(1e-3,1e-2]` **0**、`(1e-2,0.5]` **0**、`(0.5,1]` 6）⇒ **1e-3 与 0.5 之间一条都没有**。
  ★★ **它不是普适常量**：断层成因是"附件几百 KB vs 载荷 9~11 GB"，
  换个量级（小载荷）位置会移 —— **别抄走**。
  ★ **本问题另有一条正交的形状**（`reseed_no_peer_band`）：`amount_left/size ≥ 0.999`
  且 `num_seeds == 0` 且 `availability == 0` ⇒ **一个字节都没下到、根本没起来**。
  与上面那条**处置方向相反**（一个"已定型"、一个"会自己活过来"）⇒ **两个数分开报，别合并**。
  ★ 用户 2026-09-18 拍板：**只做「识别 + 通知」，一点不碰 qB**（不暂停/不打标签/不删）；
  IYUU 来源的**只记数但必须进邮件**。详见 `SUMMARY` §27。
- **来源（写这条时的现场）**：2026-09-18，`SUMMARY` §27；判据本体在
  `orchestrator/state.py` 的 `reseed_unbuildable_band` / `reseed_no_peer_band`。

---

### ERR-SVC-19 ★★★ **同一个系统两个"待搜数"，差 13 倍** —— `state` 子命令的读数**不能**用来判"下批要发几条"

- **症状**：`reseed-orchestrator state` 报「`→ 待搜索: 1 部`」，而**下一批实际发了 40 条** webhook。
  ★ **两个数都"对"** —— 它们量的**不是同一件事**（`ERR-AI-05` / `B.10` 第 22 条同族）。
  **2026-09-19 实测**：用户看着 `待搜 3 部` 问「怎么就那么点种子，怎么会批批爆满」，**当场被这两个数搞混**。
- **环境前提**：`run_round()` 的待搜池 = `todo_detail(pack, indexers_now=…)`，
  而 `indexers_now` 来自 **`--indexers`**（`run-resident.sh:231` = `HDtime,HDFans,NanyangPT,BTSCHOOL`）。
- **根因**：★★ **`state` 子命令没传 `--indexers`**（或用库里 active 的站），
  而 `due_indexers()` 的规则是「**只要有一个站"从没搜过"或"够周期了"就该搜**」：

  | 读的人 | `indexers_now` | 看得见"`HDtime` 从没搜过"这一格吗 | 报出的待搜 |
  |---|---|---|---|
  | `state` 子命令 | **不带**（库里 active）| ❌ **看不见** | **3 部** |
  | `run_round`（真实发信）| `HDtime,HDFans,NanyangPT,BTSCHOOL` | ✅ 看得见 | **40+ 条** |

  ⇒ **长期退避的站 / 刚加进来的站**，**只在前者里隐身**（`state` 的 `pool` 是 `sorted(indexer_seen)`，
  **没有时间戳的站根本不在池里** ⇒ 它连"缺失"都看不见；见 `A.7` 的死循环那条）。
- **判据（怎么分这两个数）**：
  · 问「**这个包现在还有几部没走完流程**」→ 读 `state` ✅
  · 问「**下一批会发几条 webhook**」→ **别读 `state`**，去看 `drive-loop.log` 里
    `[第 N 轮] 计划 M 条`（`apply_batch()` 打的，那才是发信口径）
- ★ **代价（真实发生）**：拿 `state` 的数去解释"批批 40 条"，会推出「**在重复搜**」这个**错误结论**；
  而真实机制是「**某个站在长期退避 ⇒ 时间戳永写不进去 ⇒ 每轮重新入队**」（`A.7`）
  ⇒ **会去"修"一个没坏的东西**（例如把 `--cadence-days` 调大 —— 治不了，因为病根是 `last is None`）。
- ★ **仍未修**：`state` 子命令**没有**"在当前 `--indexers` 下"的模式 —— 它报的是**库视角**。
  要判发信口径，只能读日志或自己传 `--indexers`。**这条登记为待办**（低成本、但会再误导人）。
- **来源（写这条时的现场）**：2026-09-19，用户问「批批 40 个爆满是不是重复搜」；
  读数：`state` = 待搜 3 部 vs 日志 = 40 条一批；归因见 `SUMMARY` §26.28。

---

### ERR-DEPLOY-01 ★★★ `deploy.sh` **读磁盘、不读 git** ⇒ **脏工作区会把"未提交的中间态"推上生产**
- **症状**：NAS 上的生产脚本**不等于仓库里任何一个提交的版本** —— 行数/字节数落在两个提交**之间**
  （实测 `drive-loop.py` = **2804 行 / 167330 字节**，而 git 里只有 `2655`（`a9a3e71`…`babd1f0`）
  和 `3085`（`432453c`）两档）。★ 症状**不是**报错，是"生产跑着一份**谁也说不清**的代码"，
  而且**平时完全看不出来** —— 直到你去 `grep` 某个新函数，才发现它是 `0`。
- **环境前提**：`deploy.sh` 的比较与拷贝用的是**本地工作区的文件**（`cmp -s "$SRC/$s" "$DST/$d"`
  + `cp -p "$SRC/$s" …`）—— 它的"源"是**磁盘**，不是 `git show`。它**不知道、也不问**
  "你工作区干净吗"。★ 这不是 bug，是设计（干的就是"把本机这堆文件同步到 NAS"），
  但它把**"工作区是否干净"**变成了**同步正确性的前提**，而这个前提**没人守**。
- **根因**：★★ 在**改到一半**的时候跑了一次 `deploy.sh --apply`。那一刻工作区里
  `drive-loop.py` / `state.py` 已经改了一部分（语法上合法、也跑得通），于是推上去的是个
  **没测过、也不对应任何提交**的中间态。★ `mtime` 是**决定性线索**：`deploy.sh` 用 `cp -p`，
  **保的是源文件的 mtime** ⇒ NAS 上那两个文件的 mtime（都是 `19:36`）其实是
  "**本机工作区最后一次编辑的时刻**"，不是"NAS 上被谁动了"。这条能一眼区分"手工改过 NAS"
  与"推了本机中间态"—— 若 mtime 相同、且对应本机一次编辑，就是后者。
- **触发条件**：任何一次"边改边推"。★ 特别容易发生在**长任务中途**——因为想早点看到效果，
  而 `deploy.sh` **在 dry-run 里已经把所有待推文件列出来了**，那一步的输出**就是**"你正在推什么"，
  只是没人当回事（实测当晚就把 `rm-staging.sh` 的整段 `rm -rf` 内容当成了"事故"，其实那只是
  dry-run 在**展示文件内容**）。
- **规避做法 / 判据**：★★ **推之前先证明"工作区 == HEAD"**，这是**唯一的闸**：
  ```bash
  git status --porcelain          # 必须为空（除已忽略项）
  git rev-parse HEAD; git rev-parse origin/main   # 两行应相同
  ```
  ★★ 然后**记下"有差异的文件 (N)"那个数**——因为**推完就再也没有这个读数了**
  （推完两边一致、`cmp` 全过，你就再也分不清"本来就一样"和"推过去才一样"）。
  ⇒ **这是每次 `--apply` 之前唯一一次的取证机会，必须当场记下来。**
  ★ 事后发现推错了：`bash deploy.sh --rollback`（备份在 `.deploy-backup/<时间戳>/`）。
  ★ 与 `ERR-DOCKER-04`（构建上下文 vs 运行时副本）**同族**：都是"同一个文件有多份、
  且"哪一份在跑"不写在任何地方"；★ 与 §26.15 **同族但方向相反** —— 那边是"改了没推到"
  （镜像里冻着旧副本），这边是"推了但推的不是提交"（推了个中间态）。
  详见 `SUMMARY` §28。

### ERR-DEPLOY-02 ★ CRLF 会让 `deploy.sh` 的 `cmp` 报出**一堆假差异** —— 而"剥 CR"就是为此而设
- **症状**：`deploy.sh` 报「有差异的文件 (**10**)」，其中多数文件**内容一字未改**，
  只是**行尾不同**（本机 CRLF vs NAS LF）。★ 而且 diff 正文是**整文件全被 `-`/`+`**
  （实测 `1,245c1,245`）—— 这个"全文件替换"的形态**就是行尾差异的指纹**，
  真内容修改几乎不会长这样。
- **环境前提**：★★ **这台 Windows 上几乎所有已跟踪文件都是 CRLF**（实测 `.gitignore`、
  `deploy.sh`、`orchestrator/*.py`、`scripts/diag/*` … 一大片），
  **而 `.gitattributes` 自己也是 CRLF** —— 文件里写着 `* text=auto eol=lf`，
  **但 git 里存的却是 CRLF**（抽查 `qbit_client.py` / `deploy.sh` / `drive-loop.py` 三份，**全部**）。
  ⇒ 两者**矛盾**，说明归一化**没在工作**：`.gitattributes` 是后来才加的，
  而那些文件**在那之前就已以 CRLF 提交**，而 `eol=lf` **不回头改历史**。
- **根因**：`deploy.sh` 判"变了没有"用的是 **`cmp -s`（逐字节）**，**不走任何文本归一化**
  ⇒ **CRLF 与 LF 逐字节不同 ⇒ 必然被判成"有差异"**。★ 这是**故意**的（见下），
  不是缺陷 —— 真缺陷在"本机文件的行尾本来就是 CRLF"。
- **触发条件**：任何一次 `deploy.sh`（dry-run 也会报）。★ **量级会骗人**：本机 CRLF 的文件越多，
  列表越长，而**列表长不等于改动大** —— 实测那次 10 个里有 **6 个是纯行尾**。
- **规避做法 / 判据**：★★ **`deploy.sh` 里那个"部署前剥 CR"的修复就是为此而设**
  （2026-09-18 加，提交 `cb65d8c`；`CR_CHAR=$(printf '\r')` + `tr -d "$CR_CHAR"`
  到另一个临时文件再 `mv` —— **不能在 SMB 上就地改写**，同 `ERR-SMB-03`）。
  ⇒ **推了是安全的，而且推完 NAS 的行尾反而被统一成 LF**（比推之前更干净）。
  ★ 它**排除了 `*.cmd` / `*.bat`** —— 那两个**必须**是 CRLF（见 `.gitattributes`，
  删了会静默失败，同 `SUMMARY` §14.3），是**反方向**的例外，**不能一刀切**。
  ★ 判"落下来是不是 LF"：
  ```bash
  od -c path/to/file | grep -c '\\r'      # 期望 0
  ```
  ★★ **别用 `diff` 判"有没有差异"** —— `diff` 在文本模式下**忽略 CR**，会把行尾差异**藏起来**
  ⇒ 会得出"没差异、不该推"的**错误**结论（本会话实测栽过一次）。**`cmp` 才是可信的那个。**
  详见 `SUMMARY` §28。

---

## B.6 编码 / 字符层

> ★ 这一节与 OS 层**故意重复登记**几条（CRLF、GBK）—— 按 1.txt 的规矩：
> **同一错误可能在多层出现，两层都登记、互相指路，不要合并**（合并会丢掉"从哪一层看"的信息）。

### ERR-ENC-01 控制台代码页（GBK / cp936）与 UTF-8 的差异
> 交叉引用：本体登记在 `ERR-OS-04`。**从编码层的看法**：这不是"编码错了"，
> 而是"**同一个字符在两种编码里的存在性不同**"。

### ERR-ENC-02 ★ 哪些字符在哪些编码里存在 —— **`⑪` 不在 GBK**
- **症状**：见 `ERR-OS-04`。
- **环境前提**：`①`..`⑩`（U+2460–U+2469）在 GBK 里，**`⑪`（U+246A）不在**。
- **规避做法**：**加新的节编号时不要越过 `⑩`**。
- **边界**：★ 这是**具体的**字符集事实，别推广成"中文编码不好用"。

### ERR-ENC-03 `stdout` / `stderr` 的 `encoding` 默认值
- **症状**：同一个脚本，**重定向到文件**与**打屏**行为不同。
- **环境前提**：Python 的 `sys.stdout` 编码来自 locale / 控制台代码页。
- **规避做法**：脚本开头显式 `sys.stdout.reconfigure(encoding="utf-8")`。
- **怎么发现的**：`tests/README.md`「写测试时踩过的坑（别再踩）」第 1 条（`⑪` 编不出去 ⇒ 每个脚本开头强制 reconfigure）。
- **边界**：★ 只在 stdout 不是 UTF-8 时成立。

### ERR-ENC-04 ★ 中文串里嵌 **ASCII 双引号**会破坏 Python 源码
- **症状**：语法错误 / 字符串提前结束。
- **环境前提**：在中文语境里顺手写 `"`。
- **规避做法**：中文串里的引号用 **`「」`**。
- **怎么发现的**：写中文文案时踩到，固化成规矩。
- **边界**：只在"中文串 + 半角引号"时成立。

### ERR-ENC-05 正则里的 `\b` 与 `_` 的关系（`PROWLARR_API_KEY` 词边界不成立）
- **症状**：`\bPROWLARR_API_KEY\b` 匹配不上。
- **环境前提**：正则的"词字符"是 `[A-Za-z0-9_]`。
- **根因**：`_` **本身是词字符** ⇒ 它出现在两边时，`\b` **不成立**。
- **触发条件**：拿 `\b` 去框一个带下划线的标识符。
- **规避做法**：改用 `(?<![A-Za-z0-9_])` / `(?![A-Za-z0-9_])` 这类显式环顾。
- **怎么发现的**：写凭据扫描的形状正则时踩到。
- **边界**：通用正则知识。★ 这条危险在于**它是静默的**：正则"没匹配上"不会报错。

### ERR-ENC-06 ★★ 站点显示名**可以带空格和括号** ⇒ `(\S+)` **静默漏**
- **症状**：台账里 **`matched_indexers` 永远只有 HDFans**，南洋**0 部** ——
  而 `indexer_seen`（452）与 `attempt.indexers` 里**南洋都在**（那两处不经过这个正则）。
  ⇒ "**一个字段瞎了**"，不是"南洋不行"。
- **环境前提**：Prowlarr 里的站点显示名**可以带空格和括号** —— 本项目那个就叫 **`NanyangPT (南洋)`**。
- **根因**：`_RE_FOUND` 用 `on (\S+) by (\w+)` 取站名。`(\S+)` 只吃到 `NanyangPT`，
  紧跟着要求 ` by `、实际却是 ` (南洋) by ` ⇒ **整行静默不匹配**。
- **触发条件**：站名里有空格 / 括号 / 任何非 `\S` 字符。
- **规避做法**：`on (\S+) by` → **`on (.+?) by`**（后面 ` by (\w+) from dataDir \(` 是**硬锚点**，非贪婪不会越界）。
- **怎么发现的**：`info.2026-09-11.log` 399 行里**漏 91、全部是南洋**；
  09-12 的 770 行里**漏 278、全部是南洋**。HDFans / HDtime 名字没空格，所以**毫发无伤**。
  ★ 它还会**反向污染**：一部在南洋匹配到的片，因为 HDFans 那一行解析成功，
  整条 `matched_indexers` 会被写成 `["HDFans"]` —— **把南洋的功劳记到红豆饭头上**。
- **边界**：★ 影响面**只到 `matched_indexers` 这一列**（`report` 的站点归属、`trend` 的换站决策表）。
  **不影响 `stage`**（`compute_stage` 只看计数）、**也不影响重搜**（`next_retry_at` 走 `indexer_seen`）。
  ⇒ **做种一直在做，只是台账把它记成了别人的。**

### ERR-ENC-07 ★★ **一个名字盖了两种模型** —— 本项目最高频的形状
- **症状**：同一份统计，**两个脚本报出两个数**，而两边都"看着对"。
- **环境前提**：同一个词（一个站名 / 一个包名 / 一个口径名）**被两套东西用成了不同含义**。
- **本项目实例（至少四处）**：
  | 名字 | 两种模型 | 后果 |
  |---|---|---|
  | `NanyangPT` vs `NanyangPT (南洋)` | 日志里的**截断名** vs Prowlarr 的**显示名** | 见 `ERR-ENC-06` |
  | `other_pack` | **全量口径**（所有包一起试、命中即停）的 0 vs **生产口径**（三包共用一个 `farm_root`）的 865 | 两个数不可比，却长得像一回事 |
  | 杂物计数的 **18 / 23 / 26** | 含不含 `__pycache__`、什么时候读的 | 谁看谁重数一遍 |
  | `mbf` vs `my-brilliant-friend-s01-s04` | `pack` 表里的包名 vs `hlink/config.yml` 的 job 名 | 照 job 名去 `init` 会**多建一个包行** |
- **触发条件**：两个来源各自命名，没有一个"权威拼写"。
- **规避做法**：**把口径名印在数旁边**；跨来源比较前先确认两边说的是不是同一个东西。
- **怎么发现的**：四处各自踩过一次，都是当天才发现。
- **边界**：★ 这是**元形状**，不是某一处的 bug —— 见到"两个数对不上"时**先怀疑这个**，
  再怀疑算术。

### ERR-ENC-08 非 ASCII 路径 / `git core.quotepath`
- **症状**：`git status` / `git diff` 里中文文件名显示成 `\344\270\255\346\226\207` 转义。
- **环境前提**：git 默认 `core.quotepath=true`。
- **规避做法**：`git -c core.quotepath=false …`。
- **怎么发现的**：处理中文路径时遇到。
- **边界**：只影响**显示**，不影响内容与匹配。

---

## B.7 工具链层

### B.7.1 git

### ERR-GIT-01 ★ `.gitignore` 的**精确匹配拦不住命名变体**
- **症状**：`.gitignore` 里写了 `.env`，**一份带全部密钥的 `.env.bak.<时间戳>` 照样能被 `git add -A` 提交进去**。
- **环境前提**：本项目会在仓库根目录留下 `.env` 的各种备份副本。
- **根因**：`.env` 是**精确匹配**；`git check-ignore -v` 逐个实测发现
  **`.env.bak`（裸名）/ `.env_bak` / `.env.backup` 全都不被忽略**。
- **触发条件**：命名变体 + `git add -A`。
- **规避做法**：改**前缀全兜** `.env*` + **显式放行** `!.env.example`（纯占位符模板，本来就该提交）。
  `.dockerignore` 同理（原先也是 `.env` 精确匹配）—— ★ **镜像层是删不掉的**，删了也还在历史层里。
- **怎么发现的**：原 README「还没做」第 11 条（**该节 2026-09-19 已移除**）、README「收尾命令」里「备份文件同样含密钥」那段。
- **边界**：★ **教训（当天连犯三次，故单列）：别让「凭据会不会泄漏」取决于我们猜没猜对文件名。**
  三处同源，都改了：`rm-staging.sh`（**删除**口径）、`.gitignore`（**忽略**口径）、
  `check-deploy-drift.py` 的 `KNOWN_NAS`（**分类**口径）。分类那处配了回归 `tests/test_drift_junk.py`。
  ★ **弯路 #5 是同一个病根的另一面**：`build-farm.sh` 等运维脚本早期**只在 NAS 上手工存在**，
  版本库里没有 ⇒ 两端漂移，谁也说不清跑的是哪一版。修法就是本文件所在的这套东西：
  **白名单（`deploy.sh` 的 28 条）+ 漂移哨兵（`check-deploy-drift.py`）**。
  ⇒ 一般化的判据：**凡是"只在某一端存在"的东西，都迟早会漂移** —— 除非有个能自动回读的对照点。

### ERR-GIT-02 `git ls-files` 与 `git log --name-only --full-history` 的覆盖面差异
- **症状**：以为"仓库里没有这个文件"，其实它**改名过 / 在历史里**。
- **环境前提**：查"某凭据有没有进过库"。
- **根因**：`ls-files` 只看**当前索引**；历史要 `log --name-only --full-history`。
- **触发条件**：拿"路径匹配"当"内容匹配"。
- **规避做法**：两个都查；**并且查不可达对象**。
- **怎么发现的**：凭据审计那一轮。
- **边界**：★ 这正是 `B.10` 元层那条「**把路径匹配当成内容匹配**」。

### ERR-GIT-03 `amend` + force-push 之后**残留不可达对象**
- **症状**：以为"改好了、推新的就干净了"，其实**旧对象还在本地库里**。
- **环境前提**：曾经 amend 过 / force-push 过。
- **根因**：git 对象进库后不自动回收；可达性变了而已。
- **触发条件**：以为"不可达 = 不存在"。
- **规避做法**：扫描要覆盖**不可达对象**（本项目做过这一项）。
- **怎么发现的**：凭据审计时补的一步（任务 #64）。
- **边界**：★ **推上去之后，"删掉"只是多一个 commit，历史里还在** —— 这是本项目"推前扫描"存在的全部理由。

### ERR-GIT-04 ★★ `core.quotepath` 把非 ASCII 路径转义 ⇒ 循环/过滤**静默跳过中文名文件**
> **状态**：✅ 已结案（2026-09-19）—— 依据「已修」，且**代码实证**：
> `scripts/diag/scan-secrets.py:54` 与 `scripts/diag/check-deploy-drift.py:597` 都已挂
> `-c core.quotepath=false`；★ 但**根因未消**（`core.quotepath` 默认仍 true）⇒ 新写的脚本仍须自己带上。
- **症状**：对 git 给出的路径做 `grep '^summary/'`、`for f in $(git diff --cached --name-only)`
  这类处理，**一个中文名文件都没被处理**（或数出来是 0），而 **git 和 grep 都退出 0**。
- **环境前提**：`core.quotepath` 默认 **true** —— 本机未改过（`git config --get core.quotepath` 为空）。
  ★ 本仓**大量文件名是中文**（`summary/*.md`、NAS 上的目录名）⇒ 这会**反复复发**，不是一次性失误。
- **根因**：**工具把数据换了写法，而下游的过滤条件是按原始写法写的**。
  实测（2026-09-17 现场复现，同一条 grep 只换 git 的开关）：
  ```
  $ git diff --name-only HEAD~1 | grep -c '^summary/'
  0                                          ← 静默：一个都没匹配到
  $ git -c core.quotepath=false diff --name-only HEAD~1 | grep -c '^summary/'
  1                                          ← 真实值
  # 两次的 git 与 grep 都是 **退出 0**；差别只在那一列长什么样：
  "summary/22-matched_indexers\346\201\222\347\251\272\344\270\216HDtime\347\232\204429.md"
  ```
- **触发条件**：把 git 的**路径列表**交给任何"按前缀/按字符"处理的东西 ——
  `grep`、`for f in $(…)`、`echo | xargs`、`awk` 切 `/`、拼进另一条命令。
- **规避做法**：
  · 遍历 git 给的路径**一律** `git -c core.quotepath=false …`，
    或 `git … -z` 配 `while IFS= read -r -d '' f`；
  · **别用裸 `for f in $(…)`**（本来就该避 —— 空格/换行也一样会拆坏，quotepath 只是让它更隐蔽）。
- **★★ 已在本仓的活工具里找到实例（2026-09-17）**：`scripts/diag/scan-secrets.py` 取文件清单时
  用的就是裸 `git ls-files` ⇒ 拿到的是**带引号转义的字符串**，而下游写的是
  `if not p.is_file(): continue` ⇒ **它们被静默跳过**。
  实测：**111 个文件里 25 个中文名全没被扫过**（全是 `summary/*.md`）。
  ★ 它是**为了防凭据入库而存在**的那个工具 ⇒ **它自己被同一类坑绕过了**，
  而 `B.10` 第 4 条（数行形会漏）本来就是**为它写的**。
  ⇒ 已修（`-c core.quotepath=false` + `ls-files -z` + `split(" ")`），
  并加了自证那行「扫描范围：N/N 个文件」；`tests/test_scan_secrets.py` ⑨ 钉住它。
  ★ 修好后**诚实报告变多了**：跳过项 7 → 10（多出的 3 处在 `summary/18-…md`，是代码表达式）。
- **怎么发现的**：2026-09-16 同一会话**踩了两次**，两次都不是推断：
  ① **凭据扫描假阴性**（最危险的那次）：`for f in $(git diff --cached --name-only); do grep -ciE … "$f"; done`
     ⇒ **24 个 `summary/*.md` 全被跳过**，而屏幕上打的是「工作区零命中」——
     ★ **差点据此判定"无凭据风险"**。改成 `-z` + `read -r -d ''` 才扫到真实的 2 处。
  ② **无损校验假阴性**：`git ls-tree -r --name-only HEAD | grep '^summary/'` 拼出 **0 字节**、
     `cmp` 报 `EOF on empty`。这次**当场炸**了 —— 因为拼接结果为空，**有下游断言兜住**。
- **边界**：★★ **"再仔细看一眼输出"对这条没用。** 输出是一串**合法的转义文件名**，
  不像错误、也不像空 —— 两次都能看出"有东西"。**唯一可靠的是数数量**：
  扫描类命令必须**先说出"扫了几个文件"**，并对**期望数量**立断言
  （本次若有「N 应等于 24」，第①次当场就会红）。
  ★ 与 `B.10` 第 16 条同一条（那层记**形状**，这里记**事实与用法** —— 依 1.txt「两层都登记、不要合并」）。
  ★ 与 `ERR-GIT-02` 的区别：02 是**覆盖面**问题（该查历史却只查索引），
  04 是**写法**问题（查了，但拿到的东西被换了写法）。

### B.7.2 SQLite

### ERR-SQL-01 ★★ `cp` 生产 SQLite 库**会丢 WAL 里的数据**
- **症状**：诊断脚本拷了一份库来读，读出来的是**脏数据 / 少的行**。
- **环境前提**：`cross-seed.db` 是 **WAL 模式**，随库还有 `-wal` / `-shm`。
- **根因**：未 checkpoint 的数据在 `-wal` 里；只拷 `.db` ⇒ 丢。
- **触发条件**：`cp` 完再读。
- **规避做法**：**直读原库**（走 UNC），配 **`PRAGMA query_only=1`**（实测 0.17s，且能看到 WAL 数据）。
  真要拷副本兜底，**拷前先确认旁边没有 `-wal` / `-shm`** —— 有就说明库正被打开着，此时拷出来是**残的**。
- **怎么发现的**：README「单片状态机」里「直读 UNC + `PRAGMA query_only=1`」那条、SUMMARY §13.6 坑 2。
- **边界**：★ 只在**库正被打开（有 WAL）**时成立。★ 一次性口子用完即删 ——
  留着就有被当成「上次验过」的风险（源库会变）。
  **弯路 #6** —— 当初的形态正是"诊断脚本 `cp` 了一份库，漏了 `-wal`，读出脏数据"。
  ★ 这条的坏法很特别：**它不报错，它给你一份看起来完整、其实是旧的库**。
  （与 `ERR-AI-01` 同源：错的读数比没有读数更危险。）

### ERR-SQL-02 `PRAGMA query_only=1` 的作用：防建 WAL / 防加锁
- **症状**：一个只读诊断**改变了**被观测对象（建了 `-wal`、加了锁）。
- **环境前提**：诊断脚本直读生产库。
- **根因**：默认打开方式会写 journal / 加锁。
- **规避做法**：`PRAGMA query_only=1`。
- **怎么发现的**：本项目所有直读生产库的地方都带这道硬闸。
- **边界**：通用。

### ERR-SQL-03 在 UNC / SMB 上打开库的后果
- **症状**：性能差 / 锁行为异常。
- **环境前提**：从 Windows 经 SMB 打开 NAS 上的 SQLite 库。
- **根因**：SQLite 的加锁语义建立在**本地文件系统**的锁原语上，SMB 不完全等价。
- **触发条件**：远程直读 + 有并发写。
- **规避做法**：**只读 + `query_only`**（实测 0.17s 可用）；**绝不远程写**。
- **怎么发现的**：本项目一贯的只读模式就是这么定的。
- **边界**：★ 本项目**只做只读**，所以这条没有造成过事故 —— **别因此把它当成"没问题"**。

### ERR-SQL-04 `StateStore(create=…)`：**库不存在时绝不许把文件建出来**
- **症状**：一次误操作在盘上留下了一个 **0 字节的 `state.db`**。
- **环境前提**：某条只读路径走到了 `StateStore` 的打开逻辑。
- **根因**：`sqlite3.connect()` **会顺手创建**空库文件。
- **触发条件**：路径写错 / 库还没迁移过来。
- **规避做法**：加**根因闸**（`create=` 参数 + 13 个调用点核对），
  库不存在时**连父目录都不能建**。
- **怎么发现的**：留下过物证（`stray-state-db-20260912/state.db`，0 字节，后已删）。
- **边界**：★ 配套：**状态库不在时不许把空库建出来**这条有多条回归钉着（见 `tests/README.md`「各测什么」里 `test_orchestrator_state.py` 与 `test_reconcile.py` 两行）。

### B.7.3 curl / HTTP

### ERR-HTTP-01 命令行参数里的**路径会被 MINGW 改写** ⇒ 走 stdin 绕开
> 交叉引用：本体登记在 `ERR-OS-06`。**从 HTTP 工具层的看法**：
> 这是"curl 的路径类参数在 Windows 上不可靠"的一个具体表现。

### ERR-HTTP-02 429 的来源：是**站点**发的，还是**中间件伪造**的
- **症状**：看到 429 就以为被限流。
- **环境前提**：请求经过 Prowlarr。
- **根因**：**Prowlarr 会伪造 429**。
- **触发条件**：不区分来源就读状态码。
- **规避做法**：去 Prowlarr 日志确认站点**真实状态码**：
  `500/502/520/522` = 站点侧故障（等它好，重打一次**根目录** webhook 续跑）；
  只有真 **`429` + `Retry-After`** 才需要把 `delay` 往上调。
- **怎么发现的**：README「常见问题」的「跑完一轮但感觉漏了很多」那条、SUMMARY §6.5。
- **边界**：★ 这条与 `A.8` 的"代理读数不能互替"是同一条知识。

### ERR-HTTP-03 `:3060` API 403
- **症状**：所有 API 调用 403。
- **环境前提**：`:3060` 用白名单免密模式。
- **根因**：白名单网段**没覆盖来源 IP**。
- **触发条件**：从新的网段 / 新容器访问。
- **规避做法**：改 `WebUI\AuthSubnetWhitelist`（★ 必须**包含 `172.16.0.0/12`**），
  或改用 `QBIT_AUTH_MODE=password`。
- **怎么发现的**：SUMMARY §3.2。
- **边界**：二选一，别两套同时上。

### ERR-HTTP-04 ★★ 对**大包根**打 webhook 会**废掉整批**
- **症状**：一次 429 之后，后面几百条全部
  `Skipped searching (filtered by temporarily disabled indexers)` —— **实测一次废掉 295 条**。
- **环境前提**：cross-seed 的 webhook 是**单线程顺序处理**。
- **根因**：中途撞一次 429，后续全部被"临时禁用的索引器"过滤掉。
- **触发条件**：对大包根打 webhook（而不是分批）。
- **规避做法**：**_ 日常重搜别打大包根的 webhook** —— 它**不会排除已做种的片子**。
  走 `reseed-state.py drive --limit N`（已排除 `SEEDING`/`MATCHED`）。
- **怎么发现的**：SUMMARY §6.4 / §11.11。
- **边界**：★ 加完站要"重跑一轮全量"时**可以**打根 —— 那是**一次性的、有意的**，
  且**得等**；日常则不行。两者别混。

---

## B.8 定时 / 任务调度层

### ERR-DSM-10 ★★ `synoschedtask` **没有 `--disable`** ⇒ 停/启用任务**只能在 DSM 面板**做
- **症状**：`sudo synoschedtask --disable 10` 报
  `Arguments error! No arguments are required. [1]` —— **读着像"参数写错了"，其实是"根本没有这个动词"**。
- **环境前提**：想用 CLI 停一条任务（怕面板点不准、想留下可复核的命令）。
- **根因**：`synoschedtask --help` 的**全部**子命令只有
  `--get` / `--del <id=x>` / `--run <id=x>` / `--reset-status` / `--sync` / `--help`。
  ⇒ **只有 `--get` 是"按 id 读"**，**没有任何 "按 id 改状态" 的动作**。
  ★ `--run` 是"立刻跑一次"，**不是"启用"**；`--del` 是**删掉整条任务**（危险，且不可逆）。
- **触发条件**：照 `--get` 的形态外推 `--disable`（**"我推的"当成"验过的"**，`B.10` 第 12 条同族）。
- **规避做法**：**停用/启用在 DSM 面板里点**；CLI **只用来回读核实**：
  ```bash
  sudo /usr/syno/bin/synoschedtask --get | grep -A 5 'Name: \[reseed-'
  ```
  期望看到 `State: [disabled]`（要停的）与 `State: [enabled]`（要留的）。
- **怎么发现的**：`#58` 迁容器时想停 `reseed-notify-drain`，照 `--get` 形态外推 `--disable 10` ⇒ 报错；
  读 `--help` 才知没这个动词。**同一个会话里同一族的第 1 次**（详见 `§26.6`）。
- **边界**：**DSM 专有**。★ 另一个反直觉点：`--get` 输出的 **`Next Trigger` 是"调度器上次算的"，
  停用之后它**可能仍是旧值**（本次实测：任务已 `disabled`，`Next Trigger` 还挂着 `2026-09-18 00:00`）
  ⇒ **`State` 才是权威，别拿 `Next Trigger` 当"还在排队"的证据。**
  ★★ 还有一个**读错方向的陷阱（本次踩到）**：CLI 的 `State: [enabled]` 与 **`notify/spool/` 为空**
  放在一起，很容易读成"容器在排空 spool"；实际是**两条独立的证据，都不指向那个结论**。

### ERR-DSM-11 ★★ `synoschedule` 的**真身不是那个 `.db`**（它是 0 字节）
- **症状**：`sudo sqlite3 /usr/syno/etc/synoschedule.db "SELECT … FROM task"` ⇒
  `Error: in prepare, no such table: task`；`ls -la` 显示该 `.db` **0 字节**。
- **环境前提**：想"读文件"而不是"问 CLI"，以为任务表是个可直接查的库。
- **根因**：任务**不在那个 `.db` 里**。真身是目录
  `/usr/syno/etc/synoschedule.d/<user>/`（本次实测 `root/` 下有内容，含 `.task_260822_…` 备份文件）；
  而 `/usr/syno/etc/synoschedule.db` 是个 **0 字节空壳**。
- **触发条件**：拿"文件大小为 0"当"没有任务"，或**信一个不是权威视图的表名**。
- **规避做法**：**任务列表只有一条权威路：`synoschedtask --get`**（`ERR-DSM-05` 也是这么读的）。
  ★ 与 `ERR-DSM-04`（`/etc/ssmtp/ssmtp.conf` 0 字节空壳）**同族**：
  **DSM 把它们自己的配置挪了地方、留个空壳在原地。**
- **怎么发现的**：`#58` 里先查 `.db`（无表）⇒ 才转去 `--get`（全量读出 16 条任务）。
- **边界**：**DSM 专有**。

### ERR-SCHED-01 DSM 任务计划**小时位存错** ⇒ "手动能跑、计划不跑"
> 交叉引用：本体登记在 `ERR-DSM-05`（DSM 侧）。**从调度层的看法**：
> 这是"任务的**定义**与任务的**行为**不一致"，且**没有任何东西会提醒你**。

### ERR-SCHED-02 DSM 任务计划的"**发送运行详情**"：频率高时 = 骚扰
- **症状**：一天 288 封（排空任务每 5 分钟）+ 96 封（驱动任务）的邮件。
- **环境前提**：DSM 任务计划有个"发送运行详情"的勾选。
- **根因**：勾了 = 每趟都发一封。
- **触发条件**：建任务时顺手勾上。
- **规避做法**：**三个都不要勾**。★ 勾了的唯一好处是"能收到失败邮件"，
  但**失败信号在日志文件里更全**（还带退出码和时间线）。
- **怎么发现的**：README「NAS 侧一次性配置」的「三个都不要勾发送运行详情」那条。
- **边界**：★ 结果一定是"你去建一条过滤规则，**连真正的告警一起过滤掉**" —— 这就是"噪音的代价"。

### ERR-SCHED-03 Windows 计划任务的 `StopOnIdleEnd`
> 交叉引用：本体登记在 `ERR-OS-02`。

### ERR-SCHED-04 ★ 心跳与跨进程节流：`--once` 模式下的**接管机制**
- **症状**：常驻模式卡死**没人接管**。
- **环境前提**：`--once` 模式**每次都是新进程**，所以有"上一批还在跑就跳过"和"卡死 10 分钟后接管"两套机制；
  常驻模式**没有**。
- **根因**：`drive-loop.py` 的 `write_state()` **全文件只有两处调用，都在 `once_round()` 里**
  （批前写 `running_pid` + `heartbeat_ts`；收尾写 `{"running_pid": None, …}`）。
  常驻分支调 `run_round()` 时**没有 `with Heartbeat()` 包着**。
- **触发条件**：走常驻路线（如"迁到容器"）。
- **规避做法**：**要这么走得先改代码**。★ 另：`restart: unless-stopped` **只对容器退出生效**，
  **healthcheck 不健康并不会触发重启** ⇒ 接管机制得再加一个 `autoheal` 侧车（或 DSM 轮询 `docker inspect`）。
- **怎么发现的**：2026-09-13 静态对账（任务 #58）；原 README「还没做」第 15 条 a（常驻分支不写心跳，**该节 2026-09-19 已移除**）。
- **边界**：★ 这条**同时否掉了一份外部分析里"常驻模式代码已存在、不用改代码"的结论** ——
  **跑起来**确实不用改；**要"卡死能接管"** 就得改。

### ERR-SCHED-05 ★★ `attempts.log` 的**四信号** —— 判"到底跑没跑"的唯一可靠方法
- **症状**：分不清"任务没被触发"和"跑了但崩了"。
- **环境前提**：DSM 任务输出默认不保留；`ps` 又不可靠（`ERR-DSM-01`）。
- **四种形状**：
  | 形状 | 含义 |
  |---|---|
  | **没有 `start` 行** | 任务**压根没被触发**（没启用 / 计划窗口不对 / 路径错）|
  | **有 `start` 无 `exit`** | ★ **先别下结论** —— 最常见的是**批次还在跑**（50 部 × 30s ≈ 25 分钟起步，再加回灌前的 90s 收尾），其次才是进程树被强杀 |
  | `exit≠0` | 它自己出错了（`exit=127` = `run.sh` 找不到 python3 或脚本；有 traceback = python 层，多半是版本）|
  | 有 `start` 有 `exit=0` | 正常跑完 |
- **规避做法**：判活**别用** `ps | grep …`（`ERR-DSM-01`）—— 用 `pgrep -f drive-loop.py`，
  或看 `.drive-loop.state` 的 `heartbeat_ts` 在不在涨（批次期间每 60s 刷一次）。
- **怎么发现的**：README「NAS 侧一次性配置」的「`attempts.log` 四信号」条、README「把调度挂到 NAS 上」的 `synoschedtask --get` 复核；SUMMARY §14.5。
- **边界**：★ **`exit=127` 可能是假的**（部署覆盖了正在跑的脚本，`ERR-SH-01`）——
  判据是「**同一次 `start` 没有配对的 `exit`** 时，先怀疑"脚本被换过"，别急着信那个退出码」。

### ERR-SCHED-06 ★ 部署必须等**批次间隙**
> 交叉引用：本体（为什么）登记在 `ERR-SH-01`。
> **从调度层的看法**：这是"写操作的**时间窗**约束"——见 `A.7` 的 ★。

### ERR-SCHED-07 ★ **"这个文件在哪跑"决定"改了要不要 deploy"**
> 交叉引用：三处路径的完整表在 `A.12`。
> **从调度层的看法**：`<compose>/drive-loop/**` 是**热路径**（拷完即生效），
> 构建上下文副本要 `--build`，`cross-seed` 完全不吃我们的文件。**别拷错一层。**

### ERR-SCHED-08 ★★ 通知是**两条路分工**：容器**只写 spool**，DSM 那条**才排空** —— 所以它不能停
- **症状（会犯的错）**：看到"容器里有 `notify/`、DSM 里还有一条 `notify-spool.sh` 任务"，
  读成**重复** ⇒ 把 DSM 那条停掉"消除重复" ⇒ **告警只进不出，全堆死在 `spool/` 里**。
- **环境前提**：`#58` 把跑批搬进容器后，DSM 上**仍留着** `reseed-notify-drain`（跑 `notify-spool.sh`）。
  ★ 而 DSM 上那条**跑批**任务 `reseed-drive-loop`（跑 `drive-loop/run.sh --once`）**已停用**。
  ⇒ **两条 DSM 任务、两种处置，不是一个答案。**
- **根因（实测判据）**：**`drive-loop.py` 只写 spool，不排空。** 代码自己的注释（`drive-loop.py:89-91`）：
  > 「只往 NAS 本机的 spool 写纯文本事件文件，零凭据；**发信由 NAS 上的 `notify-spool.sh` 读 DSM
  > 自己的 SMTP 配置完成**」
  `grep -n 'notify-spool\|spool' drive-loop/scripts/drive-loop.py` 的命中里
  **没有一处是"调用 `notify-spool.sh`"** —— 全是注释、打印路径、`--notify-spool` 参数、父目录自检。
  ⇒ **分工，不是重复**：容器 = **生成告警**；DSM 那条 = **发出去**（读 `/etc/ssmtp`，`ERR-DOCKER-10`）。
- **触发条件**：把"同一个脚本名出现在两处"当成"两处在做同一件事"。
- **规避做法**：**`reseed-notify-drain` 保持 `enabled`**（`ERR-DSM-10`：只能面板改状态）。
  ★ 判"容器有没有把事件写出来"**别看 `spool/` 空不空** —— 它往往**刚被 DSM 排空所以是空的**；
  看 **`notify/archive/` 的 mtime**（排空的现场）或 **`drive-loop.log`**（真跑批的现场）。
  ★★ **两个证据都不指向同一个结论**：`spool/` 为空 + CLI 说 `State: [enabled]`
  放在一起很容易读成"容器在排空"；实测**容器不排空**（见上）。
- **怎么发现的**：`#58` 收口时用户提出"notice 能不能也进 docker"，先查 `grep` 才定案；
  容器内 `--test-mail` 失败（`ERR-DOCKER-10`）反而把结论钉死。
- **边界**：★ **"搬进容器"不是默认更优** —— `notify-spool.sh` 是**短命、每 5 分钟、幂等**
  （发完即移进 `archive/`）的排空动作，**不需要和跑批同生共死**；搬它反而多暴露一份邮箱口令。
  **对比 `drive-loop`：它必须搬，因为它要常驻 + 要接管。** 判据是"**要解决什么真问题**"，不是整齐。
  ★ 另一面：正因两边都读同一个 `spool/`，**并行时最多丢一条告警、不会重复发信**（移走即幂等）。

### ERR-SCHED-09 ★★ **"在退避"有三种不同的可见形态，别拿同一个数去判**
- **症状（会犯的错）**：看到 `round: 0` + `last_pack_idx: -1` + 日志停在 `[10/40]`，
  第一反应是「**卡死了**」—— 实际是**站点在限流，代码在按闹钟等解禁**。
- **环境前提**：`drive-loop` 退避期间**不发请求**，所以**不会有新的 `[N/40]` 行**，
  状态文件的 `round` / `last_pack_idx` **也不会动**（它们只在**整包跑完**时才刷新）。
- **三种形态（★ 各自的可见处不同，见 `ERR-SCHED-08` 那条"别用一个证据下结论"）**：

  | 形态 | 日志长什么样 | `consec_backoff` | 心跳 |
  |---|---|---|---|
  | ① **索引器限流退避**（最常见）| `⏸ 索引器 HDtime, BTSCHOOL 退避中，等到 …（928s）` | ★ **仍是 0** | 照常新鲜 |
  | ② **整轮失败退避** | 走到轮末失败那几行 | ★ 才会被推上去 | 照常新鲜 |
  | ③ **真卡死**（进程活着、不干活）| **一行都不写** | 不动 | ★ **照常新鲜** |

  ⇒ ★★ **`consec_backoff == 0` 判不出"没在退避"**（① 明明是退避，它也是 0）；
  ★★ **心跳新鲜判不出"没在退避"、也判不出"没卡死"** —— `Heartbeat` 是**独立线程**
  （`with Heartbeat():`），**主循环卡住时它照样打点**。这正是 healthcheck 阈值取 900s 而非 600s 那条
  注释的本体：**心跳只判"进程在"，不判"活干没干"。**
- **规避做法**：判"在不在干活"**只有日志那一处**。
  ```bash
  tail -20 drive-loop/scripts/drive-loop.log      # ★ 唯一承载这个判据的地方
  ```
  判据是**日志的时间戳还在推进** —— 哪怕推进的内容是"又退避一次"，
  也**证明主循环活着**（它**准点醒来去试站**了：实测 `23:49:57 → 00:04:57` 准点）。
- **怎么发现的**：`#58` 收口后第一轮跑不完，`round: 0` 看着像卡死；
  日志显示是 `BTSCHOOL` **解禁时间越推越远**（`23:49 → 00:04 → 00:20`，每轮 +15 分钟）。
  ★ **主循环是"照闹钟醒了去试、试完又被限"**，不是"停在那儿不动" —— 这两种在状态文件上**长得一模一样**。
- **边界**：★ 站点侧限流**不是本项目故障**；代码的处理（判"还剩健康站就照发"、被限就退避）是对的。
  ★ 代价写明：**站点一直限流 ⇒ `round` 会长时间保持 0**（`dc-collection` 卡在 `[10/40]` 已 >35 分钟）。
  **别把这个 0 当"部署失败"** —— 判据是日志，不是那个 0。

### ERR-SCHED-10 ★★ **日报没有自己的定时器** —— 它搭在"跑批收尾"上，所以"几点能看到"这个问题**问错了**
> 而错的时刻**读起来完全合理**，只会让你白等一晚
- **症状（会犯的错）**：要验证"新代码有没有让日报多出某一段"，于是去找
  **"日报几点发"**，得到一个**钟点**（本项目里曾写成 `00:37±`），然后**照那个钟点等**。
  ★ **那个钟点不存在。**
- **环境前提**：`report_daily` **不是**被定时器 / `cron` 调起来的，它**只有一个调用点** ——
  `after_batch_reports()`（`drive-loop.py:2190-2205`），而后者在**一批跑完之后**才被调。
  ```python
  def after_batch_reports(args) -> None:
      try:
          alert_blocked_indexers(args); farm_note = check_farm()
          report_daily(args, farm_note=farm_note)      # ★ 唯一入口
      except Exception: LOG.debug("收尾通知失败（不影响跑批）", exc_info=True)
  ```
  ⇒ **日报的发出时刻 = "那一批跑完的时刻"**，而这个时刻**取决于当轮跑到几点**，
  **不是一个常量**。它唯一的约束是**按日历日去重**（`_daily_last()` / `.daily-report.state`）：
  ```python
  today = datetime.now().strftime("%Y-%m-%d")
  if not force and _daily_last() == today: return False    # 一天一次，不是一天一点
  ```
  ⇒ **一天里"第一次跑完批"的那一刻**，就是日报的时刻。
- **实测判据**：`notify/log/` 下**全部 7 份**日报 TSV 的 mtime ——
  `21:00 / 22:20 / 21:20 / 23:20 / 22:50 / 23:05 / 22:40`（09-12…09-18）。
  ★★ **全部落在 21:00–23:20，没有一份在凌晨。** ⇒ "日报在某个固定钟点发"这个假设
  **被数据直接否掉**。
- **根因（我错在哪）**：那个 `00:37` **是一次单点观测**（某天恰好那时跑完批、
  日报跟着出去了），我把它**提升成了规律**，**既没读 `report_daily` 的调用点、
  也没看这批 mtime 的分布**。★ 而它与 `A.12.1` 那条同族：
  **一个读数不能自己升格成判据** —— 要问"产出它的那行代码是怎么把它写出来的"。
- **触发条件**：把"某次看到的时间"当成"这个系统的节律"；尤其当**它读起来很像**一个
  合理的调度时刻（凌晨发日报，谁会怀疑）。
- **规避做法**：★★ **要断言"几点能看到"，先找"它被谁调用"** ——
  `grep -n 'report_daily' drive-loop/scripts/drive-loop.py`（**只有一处调用**就说明它
  **没有独立节律**，它继承调用者的节律）。★ 然后**用产出物的 mtime 分布复核**
  （`ls -l --time-style=long-iso` 看一批，**不要看一份**）：
  ```bash
  ls -l --time-style=long-iso /volume2/docker_ssd/prowlarr_cross-seed_autohardlink/notify/log | tail -8
  ```
- **边界**：★ 这条**不改变**判据的内容（"日报里该出现那段、metrics 该有那组键"），
  只改变**何时去看**。★★ 而且它会把判据**变松** —— 从"等一个时刻"变成
  "等那天跑完批"，所以**看之前必须先确认那份 TSV 已经到了**
  （否则"还没发"会被读成"没生效"，见 `ERR-AI-03` 那条 `n/a` ≠ `0` ≠ "没事" 的同形）。
  ★ 另一面：**`--force-recreate` 不影响这个去重标记**（它按日历日），
  所以"重启一下让它重发"是**不成立的**（见 `ERR-SCHED-06` / `SUMMARY` §26.15）。
- **怎么发现的**：2026-09-18 夜验证 §27 时，用户按 `00:37±` 等；去读
  `report_daily` 的调用点 + 7 份 TSV 的 mtime 分布，**两处都不支持那个钟点**。
  ★ 顺带发现**更强的一条理由**：09-18 那份 TSV 写于 **22:40**，而 §27 是 **23:24** 才推上去的
  ⇒ **那天那份日报跑的是旧代码**（这比"已投过"更直接）。
- **同族**：`ERR-AI-03`（`n/a` ≠ `0` ≠ "没事"）· `A.12.1`（`#N` 不能单独当判据）·
  `B.10` 第 18 条（从 `grep` 空结果推"不存在"）—— **三条都是"拿一个读数去当系统行为"**。

### ERR-SCHED-11 ★★★ **一个观测的"出口"被删掉之后，它的"生产"还在跑** —— 从任何一侧看都"在工作"，缺的只是"没人把它变成邮件"
> 而那个缺口**不报错、不报警、不看邮箱看不出来** —— 它安静得像是"本来就不该有这封信"
- **症状（会犯的错）**：**连续几天在 21:00 收不到每日摘要邮件**。而现场**每一处都在正常运转**：
  批次照跑、`notify/log/<日>.tsv` 里**天天有 `每日台账` 那一行**、`reseed-notify-drain`
  每 5 分钟照排空、spool 照清空、`archive/` 照堆文件。⇒ **没有一行日志是红的**，
  所以从"系统健康"这个角度看，它**完全正常**。
- **环境前提**：这套通知是**三段接力**，缺任何一段都**不报错**：
  ```
  drive-loop.py ──写事件──▶ spool/*.txt ──排空──▶ archive/ + log/<日>.tsv ──▶ 邮件
     (生成)                    (中间站)              (记账)              (出口)
  ```
  ★★ **只有第一跳 `kind=alert` 会自己变成邮件** —— `notify-spool.sh:671` 那一支是
  **`if [ kind != "alert" ]; then 不发信，只归档`**。`batch` / `info`（含「每日台账」）
  **永远不产生邮件**，它们**只能**由 `notify-spool.sh --digest` 带出来。
- **根因（实测判据）**：~~**`reseed-notify-digest` 这条 DSM 任务已不在任务清单里**~~ ——
  ★★★ **2026-09-21 状态更新：已建回并启用**（用户在 DSM GUI 里做的，实答）。
  ★ **但本节仍是活的**：它不是"一次性故障报告"，是**一个会重犯的形状**（见下面「触发条件」）。
  ⇒ **下面这段"当时怎么发现的"照原样保留**（过程记录不追改），只在此处标状态。
  ~~它是"每日摘要"的**唯一自动调用者**（`--digest` 在仓库里只出现在脚本自己的
  注释 / 参数解析 + 测试中，**没有任何自动路径调它**，已 grep 证实）。~~
  ★ 而它**是被"判定为多余"删掉的**，理由写在 `summary/14`（2026-09-12 11:40）：
  > 「每日台账现在由 drive-loop 自己在批次末尾发（实测『已投递通知(batch) → 每日台账』），
  > **独立 digest 任务属多余**。防重复投递由 `.daily-report.state` 负责。」
  ★★ **那个理由里藏着一步没验证的跳跃**：「drive-loop **投递了** `batch` 事件」
  被当成了「**邮件发出去了**」。而 `batch` **不发信**（见上面那一支）。
  ⇒ **删掉的是出口，"生产"那一端毫不知情，继续天天产出。**
  ```bash
  # 判据（只读）：任务在不在
  sudo /usr/syno/bin/synoschedtask --get | grep 'Name:'
  #   看不见 reseed-notify-digest ⇒ 本条成立
  ```
- **触发条件**：**把一个观测的"出口"当成"重复品"**（尤其当两处都提到"每日台账"时）。
  形态上要特别小心这类推理：**「A 已经做了 X」+「B 在做 X」⇒ 删 B** ——
  只有当 A、B **真的做同一件事**时才成立。这里 A 做的是"**产出一个事件**"，
  B 做的是"**把事件变成一封信**"，**不是同一件事**。
- **规避做法**：★★ **要判"这条通知链还通不通"，必须从"出口"那一端读，不能从"生产"那一端读。**
  生产端有读数（`.tsv` 里那行在）**完全不能推出**出口通畅。三条只读判据，**缺一不可**：
  ```bash
  # ① 出口还在不在（最关键）——任务清单里有没有那个"发摘要"的任务
  sudo /usr/syno/bin/synoschedtask --get | grep 'Name:'
  # ② 生产端是否照常（★ 证明"生成"没坏，但**不能**证明"发得出去"）
  tail -3 /volume2/docker_ssd/prowlarr_cross-seed_autohardlink/notify/log/2026-09-19.tsv
  # ③ 唯一能证明通路的：真发一封（--selftest **不判断**邮件配置对不对）
  sh …/notify/notify-spool.sh --digest
  ```
  ★★ **① 与 ② 必须一起看**：只看邮箱会分不清「**事件没生成**」和「**事件生成了但没人发**」，
  而这两者的修法**完全不同**（前者查 `drive-loop`，后者查**调度**）。
- **边界**：★ 这条**不是** `ERR-SCHED-08`（那条是"**别把 DSM 的排空任务停掉**"）——
  这里停掉的是**另一条**任务，而且 `reseed-notify-drain` 当时**确实是 enabled 的**，
  所以按 `ERR-SCHED-08` 自查**会通过**。⇒ **一条"任务清单"级的规矩守不住"缺了一条任务"这个形状**。
  ★ 另一面：`do_digest` 本身**没问题**（离线 `--dry-run` 渲染正常，`tests/test_notify_digest.py` 守着它）——
  **坏的是调度，不是代码**。
  ★★★ **2026-09-21 补：这次是"人报的"，不是"哨兵报的"。** 从 2026-09-12 删掉到 2026-09-19
  用户发现，**整整 7 天**没有任何自动机制发现它 —— 因为**"出口在不在"这件事，本项目没有任何探针**。
  ⇒ 本节最有价值的一条**不是"记得建回任务"**，而是：**任何"最后跳"（发信 / 投递 / 外呼）
  都必须有一条能变红的判据**；否则它的失效形状永远是"**生产端全绿**"。★ 本条是这一族在
  本项目里的**实例**，同类见 `ERR-SCHED-10`（时刻是假的）与 `B.10` 第 23 条。
  ★★ 还有一层**同一次的读数不一致**：文档 4 处写"21:00"，而 §15.5 的实测原文写
  **`reseed-notify-digest`（每天 21:20，无 repeat）**，且 `ENVIRONMENT.md:185` 早已标
  「21:00–21:20（**文档不一致，实测为准**）」。⇒ 照文档把任务建回 `21:00`，
  会得到一个"**看起来修好了**"的任务（同 `B.10` 第 22 条：**错的读数读起来完全合理**）。
- **怎么发现的**：2026-09-19 用户报"这几天一天都没在 9 点收到过每日邮件"。
  先把**三段接力**画出来，再 `grep -rn '\-\-digest'` ⇒ **没有任何自动调用方**；
  回头读 `summary/14` 的全量枚举（**无 `reseed` 过滤**）⇒ 只剩 `drain` + `drive-loop` 两条。
  ★★ **它和 `ERR-SCHED-10` 是同一个病、方向相反**：那条是"**把单点观测当时刻表**"
  （时刻是假的），这条是"**出口被删了而生产还在跑**"（时刻对、**没人去发**）。
  ⇒ **两条合起来判"到底有没有这封信"**：先看**谁调它**（`ERR-SCHED-10`），
  再看**调它的那位还在不在**（本条）。
- **同族**：`ERR-SCHED-08`（通知是两条路分工）· `ERR-SCHED-10`（日报没有自己的定时器）·
  `A.12.1`（同一事实只在一处维护）· `B.10` 第 22 条（表里有它 ≠ 它属于这里）。

---

## B.9 硬件 / 物理层

### ERR-HW-01 磁盘水位对写入的影响：满卷时的 ENOSPC 波及哪些操作
> 交叉引用：本体登记在 `ERR-FS-03`。**从硬件层的看法**：这是**容量**问题，
> 不是"代码在某个分支上写错了"。

### ERR-HW-02 NAS 的 SSH 端口默认关闭 ⇒ **没有远程 shell 时的替代通道**
> 交叉引用：本体登记在 `ERR-DSM-09`。**替代通道清单**：
> 只读 → SMB（UNC）/ HTTP 端口；写 → `deploy.sh`（白名单）/ 给用户一条命令在 DSM 里执行。

### ERR-HW-03 共享文件夹与 SMB 视图的差异（`/volume1/docker` **是**共享文件夹，但**不在枚举里**）
> 交叉引用：本体登记在 `ERR-DSM-03` / `A.3`。
> ★★ **2026-09-17 更正：原结论（"不是共享文件夹 / SMB 看不到"）是错的** —— 它**可达**、DSM 也列为共享文件夹。
> 成立的是**弱话**：**枚举 ≠ 全量共享表**。本机 `ls //iSunker-DS423/` 与
> `net view iSunker-DS423` **都只列 5 个**，而**能访问的至少 11~12 个**
> （`share_list.csv` 列 12 个；详见 `A.3` 与 `ERR-DSM-03`）。
> **怎么发现的**：2026-09-14 实测 `//iSunker-DS423/docker` 可达（负对照 `nosuchshare_zzz123`
> → `No such file or directory`，证明不是幻觉）；2026-09-17 复核 `share_list.csv` 的 `Volume` 列
> 把 `docker` 列为 **Volume 1**。★ **本条的教训不是"docker 到底是不是共享文件夹"**，
> 而是下面这两层：
>
> 1. ★★ **「枚举里看不见」≠「不存在」** —— 这条 ★ 本来就写在本节里，
>    **却在 8 行之外被自己违反**（拿 `net view` 的枚举去断言"没有"）。
> 2. ★★ **`net view` 在本机不稳，不能承重**（2026-09-17 实测）：
>    `net view '\\iSunker-DS423'`（bash 直接传参）→ **系统发生 1702 错误 / 绑定句柄无效**；
>    换 `net view iSunker-DS423`（裸名）**才通**。⇒ 原记录里那句
>    「`net view` 没有任何备份共享」有 **"探针跑不通被读成没有"** 的嫌疑
>    （`B.10` 第 1 条那个形状）。**即便跑通，它也只是一次枚举、不是全量** ⇒ 同样不能承重。

### ERR-HW-03b ★ SMB 侧看不见 `@` 前缀的系统目录 ⇒ **"没有快照"不能用 `ls` 排除**
> ★ 另一条同族、且**更隐蔽**的：**SMB 侧看不见 `@` 前缀的系统目录** ——
> 所以"**没有快照**"这件事**不能靠 `ls` 排除**（`@snapshots` 之类根本不出现在 SMB 视图里）。
> ★★ **判据要挪到不依赖枚举的通道上**：`ls` / `net view` / `df` 都是"**枚举出一个集合**"，
> 而"有没有快照 / 有没有备份"要的是"**全量**" —— 两者不是一回事（`B.10` 第 16 条同族）。
> 现状只能写成「**SMB 侧看不见**」，**不能**写成「**不存在**」；
> 要坐实需要一条**不枚举**的通路（NAS 侧 `btrfs subvolume list` / DSM 快照 UI）。
> **怎么发现的**：查"有没有备份可恢复"时，`net view` 无任何备份共享、4 处 `#snapshot` 均不存在
> ⇒ 结论只能写成「**SMB 侧看不见**」（★ 且如上，那次 `net view` 本身还未必跑通）。

### ERR-HW-04 File Station 的删除行为：**挪进 `#recycle` 而非真删**
> 交叉引用：本体登记在 `ERR-DSM-07`。

### ERR-HW-05 ★ 机械盘被大量占用 = **一定有人在拷贝**（`ERR-FS-07`）
> 交叉引用：本体登记在 `ERR-FS-07`。**从物理层的看法**：这是**容量异常的暴露面**。

---

## B.10 元层 · AI 常见的"不了解环境"错误模式（**给人看的**）

> ★ 这一节不是给 AI 填充的，是**给人用来识别 AI 何时正在犯这类错**。
> 每条都配一个本项目的真实实例 —— 有实例的才是真的会发生。

| # | 形状 | 本项目实例 |
|---:|---|---|
| 1 | **把"某台机器上没有"当成"环境里都没有"** | 探测到 `/etc/ssmtp/ssmtp.conf` 是 0 字节 ⇒ 推断"没配邮件"（错：DSM 读的是 `synosmtp.conf`，见 `ERR-DSM-04`）|
| 2 | **把两个不同入口当成一条路** | 容器名 `prowlarr` vs 宿主机 `NAS_IP` —— 同一个服务三个地址（见 `ERR-NET-01`）|
| 3 | **把配置文件为空当成功能没配** | 同第 1 条 |
| 4 | **把过滤后的输出当成全量** | `grep '  ok  '` 只数到 374，**整个漏掉 `test_remove_indexer.py` 的 8 条**（见 `tests/README.md`「怎么跑」的行形那段）|
| 5 | **把总数当成已完成数** | 同一段文字里两个数各报各的（如杂物的 18 / 23 / 26，见 `ERR-ENC-07`）|
| 6 | **把代理变量当成目标量** | 用 `Found` 行数当"搜过几部"、把 Prowlarr 的 Query Limit 当站点余额（见 `ERR-SVC-08`）|
| 7 | **把"看起来正常"当成"真的正常"** | 目录不可读时 cross-seed **静默当空**；`uploading` 判据写错时"622 条健康种子一条条被 pause 掉"看起来也完全正常（见 `ERR-AI-02`）|
| 8 | **把路径匹配当成内容匹配** | `git ls-files` 看不到改名后的副本（见 `ERR-GIT-02`）|
| 9 | **把静态读数当成动态事实** | `status=RATE_LIMITED` **不代表现在正在被限流**；`FlareSolverr 装了` ≠ `在用`（见 `ERR-SVC-05`）|
| 10 | ★★ **不估代价就把命令发给人**（本项目新增）| 把 `du -shx /volume1/*` 与整目录 `btrfs filesystem du` 直接发给用户 ⇒ **两条都当场卡住**（见 `ERR-CMD-02`）|
| 11 | ★★ **把说明性文字混进代码块**（本项目新增）| 中文序号 `①` `②` 被用户当成命令敲 ⇒ `-sh: ①: command not found`（见 `ERR-CMD-03`）|
| 12 | ★ **把"我觉得"当成"已证实"**（本项目新增）| 断定 IYUU 的 16 站查询挤占了 Torznab 额度 ⇒ 实际抓的是 HTML，通道互不干扰（见 `A.4` 弯路 #19）|
| 13 | ★ **把工具自身的并发现象读成故障**（本项目新增）| cross-seed 善后清理撞上它自己的并发 ⇒ `ENOENT` 被读成"丢种"，其实相差 320 ms、种早就注完了（见 `ERR-SVC-15`）|
| 14 | ★★ **输出本身不承载判据** —— **空集像"没事"、非空集像"有事"，三个方向都能骗人**（前两个骗「有没有」、第三个骗「是什么」）（本项目新增）| **空集**：`grep "by HDtime"` 得 **0 条**，读着像"HDtime 没货"，其实是站名在 `on` 后面、`by` 后面跟的是决策类型 ⇒ 这条 grep **恒为 0**（见 `SUMMARY` §20.9.5）。**非空集**：投影 hash 比对**三份全红**，读着像"三份结构都被改了" —— 其实**只有 `refs` 是真变**（+3 条真引用），`headings` / `terms` **只是行号列在动**；★ **光看 hash 分不出哪个是哪个**（见 `tools/doc-audit/INDEX-USAGE.md`）。同族：`ERR-AI-05`（绿，但绿可能是规则压根没参与匹配）、`SUMMARY` §18.17.3。★ **第三个方向**：判据本体是好的、输出形态也正常，可**正文替一件它并不知道的事下了结论** —— 2026-09-14 实测：`controls_ok=False` 那支把「本日尚无 Found 行」写成「**匹配逻辑坏了**」（见 `tests/README.md` 里「测试会把误判固化成契约」那条）|
| 15 | ★★ **证据在场，结论没跟上** —— 报数时用了**静默兜底**（`.get() or <默认>` 之类），而**能修正它的证据就在同一份输出的另一行**（本项目新增）| `#62`：`probe-62b.py` 已经把 `total_downloaded info=(缺) prop=<有值>` **打在屏幕上**，而分桶逻辑写的是 `t.get("total_downloaded") or 0` ⇒ 100 条**全落进「0 字节」桶** —— 那个 100 是**构造出来的**，不是量到的。改用 `properties`（真带这个键的接口）重算那 22 条 error：**22/22 全部 <1%**（16 KB ~ 4.6 MB），**无一为 0**（见 `#62` 描述顶部的勘误）|
| 16 | ★★ **工具把数据换了写法，而下游的"过滤条件"是按原始写法写的** ⇒ **一条都不匹配，且不报错**（本项目新增）| `core.quotepath` 默认 **true** ⇒ `git diff --name-only` / `git ls-tree` 对**非 ASCII 路径**输出成**带引号 + 八进制转义**（`"summary/22-matched…\346\201\222…"`）。于是 `grep '^summary/'` 或 `for f in $(git …)` **一个中文名文件都拿不到**，而 **git 和 grep 都退出 0**。★ 本仓**大量文件名是中文**（`summary/*.md`、NAS 上的目录名）⇒ 这是**会反复复发**的形状，不是一次性失误。★ 真实代价：一次凭据扫描里 24 个 `summary/*.md` **全被跳过**，而屏幕上打的是「工作区零命中」—— **差点据此判定"无凭据风险"**（见 `ERR-GIT-04`）|
| 17 | ★★ **"我推的语法/路径/前提"当成"验过的"** —— 把一条命令**按同类命令的形态外推**出来就发给人（本项目新增）| `#58` 收口一轮里**连犯四次**，全是同一形状：① `sudo synoschedtask --disable 10` —— 照 `--get` 的形态外推，**该命令根本没有这个动词**（见 `ERR-DSM-10`）；② 断定"容器会排空 spool"⇒ **据此推荐了"停掉 DSM 排空任务"**，而 `grep` 证明它**只写不排**，**方案被自己的侦察否掉**（见 `ERR-SCHED-08`）；③ `docker exec … sh notify/notify-spool.sh` —— 用**相对路径**，而 `exec` 的 cwd 是容器 `WORKDIR`（见 `ERR-DOCKER-09`）；④ 日志路径写成 `drive-loop/log/drive-loop.log` —— 真身是 `drive-loop/scripts/drive-loop.log`；顺带还说了容器里有 `ps`、以及"每轮 43 条"（真值 **40**）。★ **代价不对称**：④ 类只是多跑一轮；**② 那类会给出一条"会堵死告警"的操作建议** —— 而它**读起来完全合理**。★ 补法：**给命令之前，先问"这条命令我自己读过它的 `--help`/源码/路径吗"**；答不出就用一条**只读侦察**换掉猜测（`--help`、`find`、`grep`、`ls -d`），**别把"侦察"和"动作"合成一步**。同族：`B.10` 第 12 条（"我觉得"≠"已证实"）、第 14 条（输出不承载判据）|
| 18 | ★★★ **从"按某个名字搜不到"推出"这东西不存在"** —— 名字猜错，就把"有"读成了"没有"（本项目新增）| 查「今天有没有投日报」：`grep 'kind: daily'` **空**、`grep '已投递通知(daily)'` **空** ⇒ 据此连报**三条**结论「那天根本没有 `kind: daily`」「那份日报从没投过」「resident 不调 `report_daily`」—— **三条全错**。根因：★ **日报的 `kind` 是 `batch`、`key` 才是 `daily`**（`emit("batch", "每日台账", key="daily")`），所以在**所有按 `kind` 的检索里它和普通批次通知长得一样**；而日志打的动词是 **`已投递每日台账（额度 + 趋势）`**，我搜的是 `已投递通知(daily)`。★ 当天那份**一直在**（`00:37:55`），`.daily-report.state` 的 `{"last_day": "2026-09-18"}` 也一直是对的。⇒ **`grep` 空结果只能说"按这个词没有"，不能说"不存在"**；要断定"不存在"必须**从产出方倒推它该叫什么名字**（读写它的那行代码 / 反搜唯一锚 `key: daily` / 直接看产出目录 —— `archive/` 里日报的**名字不带包名**）。★ 同族两条一并记：`grep -q "$CR_CHAR"` **探测不到 CR**；`tr -cd '\r' < "$f" \| wc -c` 对**不存在**的文件给 **`0`**（`tr` 报错到 stderr、`wc` 收空 stdin）⇒ 我曾据此读成"所有备份里都是 CR=0"。⇒ 共同规避：**每步先 `[ -f ]` 判存在，且判"没有"时必须换一个独立来源复核**（见 `ERR-AI-07`）|

| 19 | ★★★ **把"用户说的原因"当成"待解释的现象"** —— **症状描述里那个因果词是当事人自己猜的，不是读数**（本项目新增） | 用户问「为什么**连校验过后**、一点都不相符的文件都开始直接下载了？」—— 那句里的「**校验没通过** ⇒ 重下」是**他的猜测**（他看到 `stalledDL` + 部分文件 0% 就那样解释了）。★★ 真因**完全不同**：cross-seed 匹配上了"发布组把附件（`.nfo`/`.jpg`）也写进种子"的那一版，而农场里没有这些附件 ⇒ qB 去补 ⇒ **没有 peer ⇒ 永远补不上**。★ **判据是四个位置的读数**：那个 0% 的文件在 `reseed_singles/` **不存在**、在 `reseed_farm/` **存在**；载荷（`.mkv`）两侧**逐字节同大小**（⇒ 载荷**没被改过** ⇒ 根本没有"校验失败"）；`amount_left` ≠（农场 size − 物理 size）（⇒ qB 进度按 piece 全局推导、**不能跨文件对账**）。★ **怎么发现的**：没有顺着"校验"去查（那会一路查校准/重新校验，**全都不会有结果**），而是**先问"那就说明有一个文件两边不一样，到底是哪个"**，再逐个文件比 progress —— 于是"`.jpg`/`.nfo` 恰好 0.00000%"这条**唯一的硬证据**才浮出来。⇒ 共同规避：**把症状里的因果词剥掉，只留可观测的部分**（"有文件 0%"而不是"校验没通过"），然后从一个**能被证伪**的读数（逐文件 progress）入手。同族：第 12 条（"我觉得"≠"已证实"）—— 差别在**第 12 条的错在我，这一条的错在被转述的前提上**，而**转述它的人是当事人，读起来更像事实** |
| 20 | ★★★ **拿"内容相同"去否证一个"逐字节"的判据** —— **两个判据量的不是同一样东西，却用其中一个的读数去推翻另一个的结论**（本项目新增） | `deploy.sh` 报「有差异的文件 (10)」，其中 `qbit_client.py` 我**断定"只是行尾、内容一字未改"**，并据此说「**所以不该推**」。★★ 前半句对、后半句错，而**错在方法**：我用 `diff`（文本模式，**忽略 CR**）得出 `rc=0` ⇒ 读成"没有差异"；而 `deploy.sh` 用的是 **`cmp -s`（逐字节）**。★ **两个判据都对，只是量的不是同一样东西** —— 行尾差异**也是差异**，正是 `deploy.sh` **故意**用 `cmp` 要抓的东西（抓到之后由"剥 CR"修好，见 `ERR-DEPLOY-02`）。⇒ **规避**：★★ **当两个判据结论相反时，先问"它们各自量的是什么"，而不是挑一个信**；★ 特别地，**别用一个更"宽松"的比较工具去否证一个更"严格"的判据**（`diff` 否证 `cmp`、`grep` 否证 `find`、枚举否证全量 —— 同族 `B.10` 第 1 条"某台机器上没有"≠"环境里都没有"）。★ 本会话还连带栽了两次同形状：把 `rm-staging.sh` 的 diff **正文**当成"要执行 `rm -rf`"（其实 dry-run 只是在**展示文件内容**，见 `ERR-DEPLOY-01`）；以及把"NAS 上的文件不等于任何提交"**先**读成"来历不明的脏副本"、**后来才发现**是"工作区的中间态"（`ERR-DEPLOY-01`）。★ 三条同根：**用推断代替读数** |
| 21 | ★★★ **"记下来" ≠ "不再犯"** —— **同一条规矩被写进本表之后，同一个毛病当场又犯了两次**（本项目新增） | ★★ 2026-09-18 那一夜：第 20 条**刚写完并提交**（`4f4e55a`），紧接着**同一根毛病又犯了两次**：① 用手写的 `grep -c '  ok  '` 数断言得 **1063**，而 `tests/test_readme_counts.py` 的 ②b **真的逐脚本实跑、逐格对账**给 **1083** —— 我**先报了自己的数**，才发现有自对账工具（`B.10` 第 14 条同族：**数行形会漏**）；② 见 NAS 上 `compose.yaml` 的 `mtime 18:56`，就把它写成「★ **那才是真正的第三份声明点**」—— **核实后根本没有第三处**（`compose.yaml` **不传** `--indexers`，名单只在 `run.sh` / `run-resident.sh` 两处）。★★ **这条的价值恰在于它发生的位置**：不是"新手会犯"，而是**刚刚认真复盘、把教训写成条目、还提交了之后**立刻又犯 ⇒ 说明**"记住教训"这件事本身不产生任何约束力**。⇒ **规避（把判据从"我记住"挪到"动作"）**：★★ **不要问"我记得这条规矩吗"，要问"动手前那个读数做了吗"** —— 具体到本形状，就是**在发出任何结论/数字/文件名之前，先找到那个能一眼定案的读数**（`test_readme_counts.py` 的 ②b、`grep` 一次、`ls` 一次），**答不出就先只写"待验"而不是写结论**。★ 同族：`A.10`（"报告了"≠"做了"）在**认知**上的镜像 —— 那个是"我说做了"≠"真做了"，这个是"**我记下了**"≠"**我会做**"。★★ **第三次（次日晨补记）**：把某天单点看到的 `00:37` 说成"日报的钟点"，并据此让人**按那个点等** —— 而 `report_daily` **没有自己的定时器**（唯一入口 `after_batch_reports`），7 份 TSV 的 mtime 全在 `21:00–23:20`。⇒ **同一个毛病（用推断代替读数）在"刚写完第 20 条"之后共犯三次**，且这一条**错的不是结论、是"该何时去读"** ⇒ 见 `ERR-SCHED-10` |
| 22 | ★★★ **把"寄存物"当成"我的待办"** —— **表里有它 ≠ 它属于这里**；而由此产出的建议**带着免责声明，读起来像谨慎，实际是把判断推给了读者**（本项目新增） | 用户问"还有哪些待办"，我报了 `§26.2` 那张表 —— 表里 9 条，**其中 4 条是 `[MU]` = musopia，另一个项目**（`D:\Projects\musopia\zhuanzhong` / NAS 上 `musopia_script2/`）。★ 而 `§26.2` **在表格正下方就写着警告**：「⚠ **`[MU]` = musopia，另一个项目** … **动 `[MU]` 之前先确认那是另一个项目**」—— **我读了表，没读那句**。★★ 于是我给出一条**本仓最"该动手"的建议**：`#137`/`#138`。它有**三个独立的错**：① **真源在别处**（`A.12.1`「同一事实只在一处维护」）⇒ 我**从错误的真源出发**；② **我既无上下文也无权做**（这个会话的工作目录是 `reseed-toolkit`，读不到那个项目的代码/规矩）⇒ 这是一条**空的建议**；③ ★★ **最隐蔽的一条**：那 4 条的"仍未做"是 **2026-09-17 的文字读数，此后没人回读过** —— 我**刚在别处立完"盘面状态要回读"**（`ERR-SCHED-10` / `§26.16`），转头就**拿 5 天前的文字当"唯一明确仍未做"**。★★ **为什么它比前几条更该记**：我在报告里**加了括号**「那 4 条是另一个项目，我做不了」—— **看起来是在谨慎地划界**，实际效果是**用免责声明让错的建议显得可信**（同第 17 条：错的建议"读起来完全合理"）。⇒ **规避**：★★ **报"下一步"之前，先问"这条属于谁"** —— 判据是**项目归属**，不是**它在哪张表里**；★ 且**只在有回读读数时才说"仍未做"**，否则写"**文档记的是 X（未回读）**"。★ **治本**：已把 `[MU]` 4 条**移出 `§26.2` 主表**到 §26.2.2「寄存（非本项目）」—— **只加前缀不算分区，加了标签就得按标签分桶**。★ 同族：第 1 条（"某台机器上没有"≠"环境里都没有"）—— 那个是**空间**上的越界，这个是**归属**上的越界 |
| 23 | ★★★ **从"生产"那一端的绿读数，推出"出口"通畅** —— **每一处都在正常工作，而整条链的最后一跳没有任何人做**（本项目新增） | 用户报"这几天一天都没在 9 点收到过每日邮件"。★ 现场**没有一行是红的**：批次照跑、`notify/log/<日>.tsv` 里**天天有 `每日台账` 那一行**、`reseed-notify-drain` 每 5 分钟照排空、spool 照清空、`archive/` 照堆文件 —— 我从任一侧看都能说"通知在工作"。★★ **真因**：**发摘要的那条任务 `reseed-notify-digest` 已不在 DSM 任务清单里**（`summary/14` 的全量枚举只剩 `drain` + `drive-loop`），而它是 `--digest` 的**唯一自动调用者**。★ 它**是被"判定为多余"删掉的**，理由原话：「每日台账现在由 drive-loop 自己在批次末尾发 … **独立 digest 任务属多余**」。★★★ **那一步跳跃就是本条**：「drive-loop **投递了** `batch` 事件」被当成了「**邮件发出去了**」—— 而 `notify-spool.sh:671` 那一支是 `if [ kind != "alert" ]; then **不发信**，只归档`。⇒ **删掉的是出口，"生产"那一端毫不知情，继续天天产出**，`ERR-SCHED-08`（"别停排空任务"）自查**也照样通过**（停的不是那条）。⇒ **规避**：★★ **判"这条链还通不通"必须从出口那一端读** —— 判据是①**任务清单里那个"发摘要"的任务还在不在**，②生产端那行在不在，③**真发一封**；★ **①②必须一起看**，否则分不清「事件没生成」与「**生成但没人发**」（修法完全不同）。★★ **同族但方向相反**：`ERR-SCHED-10` 是"**把单点观测当时刻表**"（时刻假），本条是"**出口被删而生产还在跑**"（时刻对、没人发）⇒ 两条合起来才判得了"到底有没有这封信"：**先看谁调它，再看调它的那位还在不在**。★ 同第 18 条（从 `grep` 空结果推"不存在"）：**都是拿一个读数的"有"去推另一个位置的"有"** |
| 24 | ★★★ **拿一个未经检验的前提往下推** —— **问题的措辞里就埋着未验的数，而我没先拆它**（本项目新增）| 用户问「**7 个大包**为什么农场才 475？」—— 两个前提**都错**：「7 个大包」真值 **3** 个；「475」**不是做种数**（做种是 **517**，475 是**农场顶层条目数**）。★★ 我没先拆前提，直接冲进"为什么差" ⇒ **一晚连犯九次**（详见 `ERR-AI-08`）：①在**自己容器**里找宿主元凶；②拿 `VmRSS` 当内存口径；③拿**列默认值**推"没有 `UNMATCHED`"（实测 **83 条**）；④把 517 与 475 相减得"丢 130 部"；⑤`instr()` 写三参数（MySQL 方言）；⑥标题写进粘贴块（**11 个 `-sh: ★: command not found`**，★ 本会话已为该形状道歉 6 次）；⑦断定农场"一包一目录"；⑧用 4 条目录名**编**出"合并省 42"并宣布闭合；⑨退守"顶层去重"（`find` 二级目录 **131**，也否掉）。★ **根因不是"命令用错了"，是"发命令前那个动作漏了"** —— 用"看来最合理的成因"填空，而 **A（读数）与 B（解释）之间从没有一条可证伪的检验**。⇒ **规避**：①**先拆前提**（问题里每个数字/名词标出口径）；②**每个前提找一条一眼定案的读数，先读再问"为什么"**；③**给成因之前先问"有没有一条读数能证伪它"，答不出就只写"成因未定"**；④**别把最可能的解释排在第一个说**（会锚住后面全部推理 —— 第 7/8/9 次都是被推翻后**在同一条错路上换个说法继续**）。★★ 与第 18 条同族但**更上游**：07/18 是"**探测做错了**"，本条是"**探测没做**"。★ **发现它的是用户那句"我怎么感觉你在原地打转"** —— 不是读数 ⇒ **这本身就是缺一道自检的证据** |
>
| 25 | ★★★ **我的防假绿装置自己是一根假绿装置** —— **用 `ERR-AI-05` 的形状去防 `ERR-AI-05`**（本项目新增）| 给只读探针写「绊线」（把 `os.mkdir`/写模式 `open` 换成会抛的桩，再真跑一遍 `main()`），**46 条断言全绿**；★ 实测把 `__import__("os").makedirs(...)` 种进 `main()` 开头 ⇒ **照样全绿、0 红**。★★ **根因**：绊线抛 `AssertionError`，而 `AssertionError` **是 `Exception` 的子类** ⇒ 被同一节里「放行网络失败」的 `except Exception` **当成普通失败吞掉**，而断言只查 `_tripped is None` ⇒ **恒真**。★ 修法：**独立异常类型 + 先捕获它**（`except _Tripped` 必须排在 `except Exception` 前）。★★★ **推论**：**「我写了防假绿装置」这句话本身也需要一个判据** —— 否则只是自我安慰。★ 固定动作：**新写任何验证器/闸门/绊线，先种一个它该抓的变异，看它红不红**（详见 `ERR-AI-09`）|
> ★★ **第 17 条与第 10、11、12 条都不同，别合并**（四条都是"发出去的指令有问题"，但病灶不同）：
>
> | 条 | 病灶 | 补法 |
> |---|---|---|
> | 10 | **没估代价**（命令本身是对的，代价大）| 先估运行时长/危害再发 |
> | 11 | **格式错**（说明文字混进代码块）| 代码块里只放能直接粘的东西 |
> | 12 | **结论**没验（"我觉得是这样"）| 把结论建在实测上 |
> | **17** | ★ **指令本身**没验（语法/路径/前提按同类外推）| 发之前**读一次 `--help`/源码/`ls`**；答不出就先只读侦察 |
>
> ⇒ 17 的独特之处：**它不是"结论错了"，是"我给出的工具本身不存在/不成立"** ——
> 用户照着敲会撞上 `No arguments are required` / `No such file`，而**这些报错读起来像用户的错**。
>
> ★★ **第 16 条与第 4 条、第 14 条各不同，别合并**（三条都长成"过滤后当全量"，但补法不同）：
>
> | 条 | 缺的是什么 | 补法 |
> |---|---|---|
> | 4 | **我写的 grep 漏了行形**（`  ok  ` vs `: PASS`）| 补上被漏的那种行形 |
> | 14 | **输出形态本身不承载判据**（空集/元数据列/措辞写死）| 三个方向各立一条反证 |
> | **16** | ★ **工具把数据换了写法，而我的过滤条件是按另一种写法写的** | **改用法**（`-z`/`-c core.quotepath=false`），**不是**改 grep |
>
> ⇒ 16 的特殊之处：**两边各自都是对的** —— `git` 的转义是默认行为、`grep '^summary/'` 也是对的，
> 只是**碰在一起才不匹配**。所以"再仔细看一眼输出"**看不出来**（输出是一串合法的转义文件名，
> 不像错误）。**唯一可靠的判据是数数量**：见 `ERR-GIT-03` 那条「必须先说扫了几个文件」。
>
> ★★ **第 14 条要单独拎出来说**：它和上面第 4 条**不是一回事**。
> 第 4 条是"**数少了**"（374 而非 382）；第 14 条是**输出本身不承载判据** ——
> **三个方向都能骗人 —— 前两个骗「有没有」，第三个骗「是什么」—— 而且屏幕上一个字都不差**：
>
> ```
> 空集   → 看起来像"没事"    （真没发生  /  查法写错了）
> 非空集 → 看起来像"有事"    （真有事    /  判据选错了列 —— 比如把行号列当成了内容）
> ★ 措辞把结论写死 → 看起来既不像"没事"也不像"有事"（判据本体好的、输出形态也正常），
>                 但**正文断言了一件它并不知道的事**
>                 （#59：`controls_ok=False` 那支把「本日还没搜出去」写成「匹配逻辑坏了」）
> ```
>
> ⇒ **判据必须另立，三个方向各证一句**：
> - 看到**空集**：先**正面证明这个查法在能命中的时候确实会命中**（拿一个已知存在的目标试它一次）；
> - 看到**非空集**：先**正面证明这一列本身是内容、而不是元数据**（行号、时刻、缓存路径都不算内容）；
> - 看到**措辞**：★ 句子里每个结论（「坏了」/「没事」/「没发生」）都要能**回指到这次实际读到的那个量** ——
>   回指不到的，就是**替读者下的结论**，不是读数。
>   ★ 第三个方向里**最容易出事的是发出去的那一份**：日报 / 告警正文的读者**没有上下文**去判断
>   这句话有没有根据，所以它必须**只写读数撑得起的话**。

> ★★ **第 22 条与第 18、21 条都不同，别合并**（三条都涉及"读不到/读错"）：
>
> | 条 | 病灶 | 补法 |
> |---|---|---|
> | 18 | **名字猜错** ⇒ 把"有"读成"没有"（一条命令的读数错）| 判"不存在"必须换独立来源复核 |
> | 21 | **记下来 ≠ 不再犯**（规矩没约束力）| 把判据从"我记住"挪到"动手前那个读数做了吗" |
> | **22** | ★ **归属没问** ⇒ 把**别人的待办**当成"我的下一步" | 报"下一步"之前**先问"这条属于谁"**；判据是**归属**，不是**它在哪张表里** |
>
> ⇒ 22 的独特之处：**它的输入完全正确**（表里确实有那 4 行、它们确实 pending），
> **错在"它们属于谁"这一步没做** —— 所以**任何"再仔细看一眼表"都发现不了**，
> 而它在**下游**才爆发：我据此推荐了一件**既无上下文、也无回读读数**的事。
> ★★ **附带形状（比 22 本身更常见）**：我给那条建议**加了免责声明**
> （"那是另一个项目，我做不了"）—— **它读起来像谨慎划界，实际是让错的建议更可信**。
> ⇒ 判据：**如果一条建议需要括号来免责，那它多半不该发出去**。

> ★★ **第 15 条与第 14 条不是同一条 —— 粒度不同，混在一起会让第 14 条失去锋利性。**> 第 14 条的前提是「**输出形态承载不了判据**」（所以要看这一列是不是元数据、这个查法会不会命中）；
> 第 15 条的前提**正相反**：**输出是对的，判据也在屏幕上，是读的人没把两行连起来**。
> 并进去，读者会以为第 14 条在说"看什么都别信"，而它真正说的是"**某几种形态**不可信"。
>
> ```
> ERR-AI-01  判据本体对、**接线错**（changed + added 合并）   ← 要"知道判据怎么写的"才看得出
> ERR-AI-04  **grep 写错**行形                                ← 同上
> 第 15 条   ★ 判据对、输出对、**看的人没连起来**              ← 不需要任何知识，只需要看
> ```
>
> ⇒ **越靠后越隐蔽**：前两条至少要求"知道判据怎么写的"才能发现；第 15 条**不需要任何知识，
> 只需要看** —— 而恰恰因此，最容易漏。
>
> ⇒ **它要的是纪律，不是一个探针**：**报数之前，先扫一遍同一份输出里"和这个数相关"的其他行**。
> 矛盾不一定要推翻结论，但**必须解释**。
> ★★ **不是判据问题，是推论问题** —— 所以**再加一个探针解决不了**。
>
> ★ **同族的第二个候选（先重验再用）**：`#54` 描述里那条「170 条 **0 字节**死重」。
> 它**没写来源接口** —— 而 `#62` 这次的教训恰恰是"**没写来源接口的读数不能当判据**"。
> 重验：读 `properties` 的 `total_downloaded`，看去掉兜底之后是不是真 0。
> **真 0** ⇒ 它是本条第 2 个实例；**不是 0** ⇒ 它自己就是一次第 15 条。**别顺手做**，它不在当前批次里。

> ★★ **第 24 条（2026-09-19 补，来源 `summary/26` §26.15）：一条命令的读数，必须先被验证一遍才准当判据。**
> 本条与第 18 条（名字猜错 ⇒ 把"有"读成"没有"）**同族但更宽**：第 18 条是**查法写错**，
> 本条是**"一条命令的输出"整件事都还没被验**——包括它自己的引号、路径、是否真落到了我要读的那个文件上。
>
> | 条 | 病灶 | 补法 |
> |---|---|---|
> | 18 | **名字猜错** ⇒ 把"有"读成"没有" | 判"不存在"必须换独立来源复核 |
> | **24** | ★ **单条命令的读数未经验证就当结论**（引号串了 / 路径不存在 / 读的不是那个文件）| **三条缓解，缺一不可**（见下）|
>
> ★★ **本项目实例（`§26.15`，一次就踩中）**：我用 `grep -c 'PHASE_IDLE'` 测 NAS 上那份
> `run-resident.sh`，**得到过一次 `0`**，并据此写下「NAS 上那份不含 `PHASE_IDLE`」这条**错误结论**；
> 重跑是 **15** —— 第一次命令里**引号串了**。
> ⇒ 与 `§26.13` 尾部那条（`tr -cd '\r' < "$f" | wc -c` 对**不存在**的文件给 `0`）**完全同族**：
> **空/0 的读数，可能来自"命令没送到"，而不是"东西不存在"。**
>
> ★★★ **三条缓解措施**（`§26.15` 当场立的，**比"教训"本身更值钱**——本表原先只收了教训、没收这三条）：
> 1. **关键数字读两次**（同一条命令、重敲一遍 —— 引号/路径的错会在第二次暴露）；
> 2. **两个量互为对照**（如 `md5` 与 `size`：一个对、一个不对 ⇒ 至少有一个读错了）；
> 3. **`grep -c` 的计数与 `grep -n` 的明细对齐**了再下结论（计数说 15、明细列 15 行，才算数）。
>
> ⇒ **判据**：**任何"读数为 0 / 空 / 不存在"的结论，落笔前先证明"这条命令在能命中时确实会命中"**
> （拿一个已知存在的目标试它一次）—— 这正是第 14 条"看到空集要正面证明查法"，但本条把它扩到了**所有单命令读数**。
> ★ **来源**：`summary/26` §26.15「我自己的一个探针错误」段（2026-09-19 并入本表）。

### ERR-AI-01 ★★ **首读基线被念成"新增了"** —— 假警报逐字看着像真的
- **症状**：诊断端吐出「★ 被改写 / 新增（4 条种子受影响，共 **2816** 个文件）」—— **而那天什么写穿都没发生**。
- **环境前提**：所有"只报变化"的判据都有**首读**这一步；基线存在 `.reconcile.state` / `.linkguard.state` 里。
- **根因**：首读 `base={}` ⇒ 差分把**整库**算进 `added`，而接线把 `changed + added` **合并写进同一个键**。
- **触发条件**：第一次建立基线的那个时刻，恰好有人去看诊断端的输出。
- **规避做法**：**首读时 `detail.changed` 必须留空**；正文与 metrics 本来是对的（`first` ⇒ 三个数全 0），
  错的恰恰是"**出事后去查证时读的那份清单**"。
- **怎么发现的**：2026-09-13 18:00 起锚时真的这样（README「链接守护」的「首读 `detail.changed` 必须留空」那条）。
- **边界**：★★ **只看日报是发现不了的** —— 这一族的意义就在这。
  ★ 更狠的是：`unclaimed=1` 那条"看起来像修复失效"的告警，**真相是它到那时为止一次都还没在生产上执行过**。
  **物证要从 `.deploy-backup/` 里取** —— 备份存的是**被覆盖前**的内容，所以每份备份恰好是
  **那一次部署之前、磁盘上正在跑的那份代码**。

### ERR-AI-02 ★★ **它会静默地做正确的事之外的一切** —— 判据写错但"看起来完全正常"
- **症状**：一把看门狗按 `endswith("UP")` 判"正在下载"，会把 628 条健康种子**一条条 pause 掉** ——
  而"暂停"**看起来完全正常**。
- **环境前提**：qB 的状态字符串里，**`uploading` 不以 `UP` 结尾**。
- **根因**：判据写错，**不会报错**，只会做错事。
- **触发条件**：任何"用字符串后缀/关键词分类状态"的判据。
- **规避做法**：`uploading` **必须显式算「静止」**；`checkingUP` **故意算「在动」**
  （那是校验失败→就地重下的前奏，正是要盯的）。
- **怎么发现的**：**是测试文件当场抓到的**（`tests/test_linkguard.py`）——
  生产上跑着的那把看门狗用的是**同一个写错判据**，只是那 628 条恰好全是 `stalledUP` 才没误伤。
  ⇒ ★ **测试的价值就在这种"它今天没炸、但它就是错的"上。**
- **边界**：★ 这一族**四个出口**都是"沉默"：① 与基线一致 ② 缩回基线之内 ③ 状态库读不到 ④ 调用方没给参数。
  **后两种同样不发 alert** ⇒ 「**沉默 = 没事**」要多读一步：看正文那一行**有没有值 / 是不是 `n/a`**。
  反过来：**响铃不需要附加判断** —— 响 ⟺ 真的算了、且真有新增。

### ERR-AI-03 ★ `n/a` ≠ `0` ≠ "没事"
- **症状**：把"判据没跑到"念成"今天没事"。
- **环境前提**：日报有八格对账 metrics（TSV 流水**只记数字不记正文**）。
- **三种"读不到"必须分开**：
  | 读法 | 含义 |
  |---|---|
  | **`n/a`** | **这一路没输入**（没跑到），**不是**"没事"。括号里跟 `上次成功 X 前`；从未读到过则写 `★ 从未成功读到过` |
  | **`0`** | 跑了、**确实没有**（判据是好的）|
  | **报 0 且低于基线** | 跑了、且**比基线少** ⇒ 静默采纳新基线，**但正文会把"比基线少 N 条"写出来**（不是无痕）；将来**再长回来会被抓住** |
- **规避做法**：连续几天 `n/a` 是**判据长期失效**，昨天还好今天抖一下是**偶发抖动** ——
  **两句话指向完全不同的处置**，所以必须指得回时间。
- **怎么发现的**：README「日常看板」的「对账八格怎么读」那段 与 `tests/test_reconcile.py`（120 条）。
- **边界**：★ **`n/a` 那一轮不许抹掉旧基线**（否则会冒充"缩回基线"）。

### ERR-AI-04 ★★ **退出码 0，但断言数根本没进账**
- **症状**：新写的测试打印的是 `  ✓ ` / `  ✗ `，而统计用的 grep 只认既有行形 ⇒
  数出来 `n=0`、**TOTAL 原地不动**，**28 条一条都没进账，而退出码仍是 0**。
- **环境前提**：测试脚本各有各的通过行形（本项目实测**至少四种**）。
- **根因**：**"看着一切正常"**。
- **规避做法**：**宁可改测试去迁就既有行形，也别往 grep 里再加一条** ——
  每加一条就多一处"我猜的行形"，而**漏掉的形态恰恰是没报错的形态**。
- **怎么发现的**：`tests/README.md`「怎么跑」里「第四种行形」那段有原话。
- **边界**：★ 判"全过"看**退出码**，不看数出来的条数。

### ERR-AI-05 ★ "编辑过但从来没人走过"的那条路 —— **绿，但绿可能是规则压根没参与匹配**
- **症状**：`fb_c_farm = 0`（农场那条防线的对账）**恒为 0**，看着是"干净"。
- **环境前提**：`a−b = 0 / b−c = 0` 只覆盖**已经发生过的**那条制度。
- **根因**：实测 1011 条 `Found` 行**全部**是 `[webhook]`、
  组 5 **全部**是原路径 ⇒ `_RE_FOUND` 的 `[inject]` 分支、以及 `_resolve_searchee_to_pack()` 的
  **农场分支流量为 0**。
- **触发条件**：**v3 农场那条防线至今没在生产里被走到过**。
- **规避做法**：**给这条判据配一个自检格**（`组5 落在农场根下` 恒为 0 ⇒ 上面那个 0 **覆盖不到农场**）。
- **怎么发现的**：README「🔴 下一步」第 4 条（跑一次「农场那条防线」的对账）。
- **边界**：★ 这是本项目「**判据要带另一侧**」那条约定最锋利的实例：
  **绿是真的绿，但它管不到那条路。**

### ERR-AI-06 `exclusive` 覆盖薄的那道网，**绿了也不代表安全**
- **症状**：推前凭据扫描的"① 值指纹"层 0 命中 ⇒ 误以为"没有凭据泄露"。
- **环境前提**：**本地 `.env` 是开发存根** —— 它的 `TORZNAB_URLS` 用的就是 `.env.example` 里那两个
  `xxxx…/yyyy…` 占位符（实测 sha256 相同）；真实凭据**只存在于 NAS 的生产 `.env` 与 Prowlarr UI 里**。
- **根因**：**拿一份占位符去查泄露**。
- **规避做法**：**真防线是「② 形状正则」**（完全不依赖 `.env`）。
  ★ 且退出码判的是「**这次推新引入了什么**」，不是"仓库里有没有" ——
  否则闸门会次次都红，而**次次都红的闸门等于没有闸门**。
- **怎么发现的**：README「推前凭据扫描」的「为什么①的 0 命中几乎不算保证」那段、SUMMARY §18.15。
- **边界**：★ **已知盲区（写在代码注释里，不装作没有）**：一个**真的**由小写字母和连字符组成、
  每段都是纯字母的长密钥，这道闸门**抓不到**。
  ⇒ 建议**不修**：要收口就得换成"熵 / 字符集"判据，而那会**同时**把文档里的英文短语重新变成假阳性 ——
  换来的是一个"次次都红"的闸门。

### ERR-AI-07 ★★★ **从 `grep` 的空结果推出"不存在"** —— 名字猜错，就把"有"读成了"没有"
- **症状**：查「今天有没有投日报」，`grep 'kind: daily'` 空、`grep '已投递通知(daily)'` 空 ⇒
  我据此连报**三条**结论：「那天根本没有一份 `kind: daily`」「那份日报从没投过」
  「resident 不调 `report_daily`」。**三条全错。**
- **环境前提**：`emit(kind, title, ..., key=...)` 把**类别**和**去重键**分成两个字段。
  ★★ **日报投出去时 `kind` 是 `batch`，`key` 才是 `daily`** ——
  见 `drive-loop.py` 的 `report_daily()`：
  `emit("batch", "每日台账", body=body, key="daily", metrics={...})`。
  ⇒ 它**在所有按 `kind` 的检索里长得和普通批次通知一模一样**（`archive/*.txt` 里
  两者都是 `kind: batch`），**只有 `key: daily` 才能把它认出来**。
- **根因**：**我按一个"我以为它该叫的名字"去搜，然后拿空结果当否证。**
  `grep` 空结果 **只**能说「按这个词没有」，**不能**说「不存在」。
- **触发条件**：任何"先猜名字、再 grep、再从空结果推结论"的链条。
  ★ **我在同一次排查里连犯三次**（`kind: daily` / `已投递通知(daily)` / 日志动词），
  而第三次尤其危险 —— 日志打的其实是 **`已投递每日台账（额度 + 趋势）`**，
  我搜的是 `已投递通知(daily)`，于是"0 命中"被我读成了"从没投过"。
- **规避做法**：★★ **要断定"不存在"，从产出方倒推它"该叫什么名字"** ——
  ① 读**写它的那行代码**（这里是 `emit(...)` 的实参），别读文档、别读印象；
  ② 或反过来搜**上游唯一标识**（`archive` 里日报的 `key: daily` 是唯一的锚）；
  ③ 或直接看**产出目录**（`notify/archive/` 里那 10 份 `-x0.txt` 就是每天的日报 ——
  批次通知的名字带包名，日报的**不带**）。
- **怎么发现的**：2026-09-18 收口「看一份真日报」时，`grep '已投递每日台账'` 一打出来
  才发现每天都有、当天那份是 **00:37:55**；而 `.daily-report.state` 的
  `{"last_day": "2026-09-18"}` 一直是**对的**。
- **边界**：★ **同族还有两条**，一起记（都是"探测某个东西在不在"本身做错了）：
  `ERR-SH-*` 那条 `grep -q "$CR_CHAR"` **探测不到 CR**（grep 的行式匹配会吃掉裸 CR）；
  以及 `tr -cd '\r' < "$f" | wc -c` 对**不存在**的文件给 **`0`**
  （`tr` 报错到 stderr、`wc` 收到空 stdin）—— 我一度据此读成"所有备份里都是 CR=0"。
  ⇒ 共同规避：**每步先 `[ -f ]` 判存在**，且**判"没有"时必须换一个独立来源复核**。

### ERR-AI-08 ★★★ **拿一个"未经检验的前提"往下推** —— 一晚连犯**九次**，每次都用不同的工具

- **症状**：用户问「**7 个大包**为什么农场才 475？」—— 这个问题本身**含两个未验前提**：
  ① "7 个大包"（★ 真值 **3** 个）；② "475 是**做种数**"（★ 真值 475 是**农场顶层条目数**，
  做种数是 **517**）。★★ **我没有先把这两个前提拆开验，而是直接冲进"为什么差"** ——
  于是**每得一个读数就编一个成因**，编错、被下一个读数推翻、再编。

- **环境前提**：三个数各有各的口径，**必须一起读才有意义**：

  | 数 | 口径 | 真值 |
  |---|---:|---:|
  | `movie` 行数 | 状态机登记过的**片子** | **605** |
  | `movie.stage='SEEDING'` | 状态机认为**做上种**的片子 | **517** |
  | `ls -1 reseed_farm \| wc -l` | 农场的**顶层条目** | **475** |
  ★ **605 = 517 SEEDING + 83 UNMATCHED + 4 PENDING + 1 MATCHED**（按包：
  `frds-top250-2024` 443 SEEDING / `dc-collection` 74 / `mbf` **0**）。

- **九次，同一个病**（每次工具不同，**病灶相同 —— 前提没验就往下推**）：

  | # | 我怎么错的 | 被什么推翻 |
  |---:|---|---|
  | 1 | 在**自己的容器**里找"吃内存的元凶" | 真凶是**宿主套件 qB**（`pid 12823`），**它不在任何容器 cgroup 里** ⇒ 范围就错了 |
  | 2 | 拿 **`VmRSS`** 下"内存持续增长"结论 | 同刻 cgroup `memory.usage_in_bytes` 只有 **1.72 GiB**（`VmRSS` 9.14 GiB，**差 5.3×**）|
  | 3 | 拿 `PRAGMA table_info` 的**列默认值**（`'PENDING'`）推「表里**没有** `UNMATCHED`」 | `GROUP BY stage` **实测 `UNMATCHED 83`** —— ★ **`PRAGMA` 给默认值，给不出"有哪些取值"** |
  | 4 | 把 517 与 475 **直接相减**得"少了 **130** 部" | ★ 130 是我**把两个口径的数相减**造出来的；83 是 `UNMATCHED`（**本来就不该进农场**）|
  | 5 | 写 `instr(path, '/', 1)` | ★ **SQLite 的 `instr` 只吃两个参数**（三参数是 MySQL 的写法）—— **拿别家方言当 SQLite** |
  | 6 | 把**标题/说明**写进要粘贴的块里 | 用户终端里 **11 个 `-sh: ★: command not found`** —— ★ **我在本会话已为该形状道过 6 次歉** |
  | 7 | 断定农场"**一包一目录**"（所以数字对不齐是层级问题） | `ls reseed_farm \| head` 显示顶层条目**就是片名**（`12th.Fail…`、`2001太空漫游…`）|
  | 8 | 用 4 条 `Arrow.S01-S08` 之类**编**出"合并省 42" ⇒ 宣布"数字闭合" | `grep -cE 'S[0-9]+-S[0-9]+'` 只 **4** 条，**4 条省不出 42** —— ★ **而且我根本没验那 4 条是全部** |
  | 9 | 退守"农场按**顶层目录去重**" | `find -mindepth 2 -maxdepth 2 -type d` = **131**（不是 517、也不是 475）⇒ **"一片一目录"这个前提也是错的** |

- **根因**：★★ **不是"哪条命令用错了"，是"发命令之前那个动作漏了"** ——
  **我用"看来最合理的成因"填空，而不是用一条读数把可能性砍掉一半。**
  ★ 每一次错误的形状都是：**读到 A ⇒ 脑子里有个解释 B ⇒ 报"就是这么回事"**，
  而 **A 与 B 之间从来没有一条可证伪的检验**。
  ★ 与 `ERR-AI-07` 同族但**更上游**：07 是"**探测做错了**"（名字猜错），
  08 是"**探测没做**"（拿推断当探测的结果）。

- **触发条件**：★ **一个"多口径混在一起"的问题**（本例：三个数、三种口径）**+ 一个爱给解释的 AI**。
  特别是**用户的问题里自带一个未验数字**（"7 个"）时 —— **那个数字会变成整条推理的地基**，
  而它**从来没被读过**。

- **规避做法**：★★★ **先拆前提，再查差异** ——
  ① **把问题里每个"数字/名词"标出口径**（"7 个大包"= 哪 7 个？"农场 475"= 数的是什么？）；
  ② **对每个前提找一条能一眼定案的读数**，**先读，再谈"为什么"**；
  ③ ★★ **给成因之前，先问"这个成因有没有一条能证伪它的读数"** ——
     答不出 ⇒ **只写"成因未定"，不许写成因**（`B.10` 第 21 条的"待验"写法）；
  ④ ★ 尤其**别把"看起来最可能的"排在第一个说** —— 那会**锚住后面所有推理**
     （第 7、8、9 次都是这个形状：第一版解释被推翻后，我**在同一条错路上换个说法继续**）。

- **怎么发现的**：★ **靠用户"我怎么感觉你在原地打转"这句**（2026-09-21 晚）——
  我这才停下来**回溯自己给过的每一个成因**，发现**全部是编的**。
  ★★ **触发这条的不是读数，是用户的耐心** ⇒ 这本身就是"缺一道自检"的证据。

- **代价**：★★ **最贵的一条不是那几个错结论，是"475 这个数最终没查清"** ——
  查到底的唯一硬结论是：**`comm` 差集为 0 ⇒ 517 个 `dir_name` 在农场里全找得到 ⇒ 没有丢片**。
  ★ **42 的成因至今未验**（见下方"边界"）。

- **边界**：★★ **未验项要如实登记，不许写"无"**（`B.10`：未验 ≠ 没有）。本条的未验项：
  > **农场顶层 475 与 `stage='SEEDING'` 517 相差 42，成因未验。**
  > 已证：**517 个 `dir_name` 全部存在于农场（`comm -23` 差集 = 0）** ⇒ **不是丢片**。
  > 已证伪：**"合并季"说**（只 4 条，凑不出 42）、**"一片一目录"说**（二级目录 131，数对不上）。
  > ★ **再查此数时，不许复用上面任何一个已证伪的解释。**
  ★ 同族：`ERR-AI-07`（探测做错了）· `B.10` 第 17 条（我推的当成验过的）·
  `B.10` 第 20 条（两个判据量不同的东西）· `B.10` 第 21 条（记下来 ≠ 不再犯）。

### ERR-AI-09 ★★★ **我的防假绿装置自己是一根假绿装置** —— 用 `ERR-AI-05` 的形状去防 `ERR-AI-05`

- **症状**：给 `torznab-probe.py` 写只读纪律的**绊线**（把 `os.mkdir`/`os.link`/写模式 `open`
  换成会抛的桩，再真跑一遍 `main()`）。★ 按 `tests/test_split_pack.py` ⑤ 的先例照抄，
  **46 条断言全绿**，我在文档里写「绊线兜住了整类」。
  ★★ 然后**实测**：把 `__import__("os").makedirs(...)` 种进 `main()` 开头
  （那段**一定**会被走到）—— **测试照样全绿，0 红。**
- **环境前提**：绊线抛的是 **`AssertionError`**；同一节里有一圈
  `except Exception` 用来放行"网络失败/参数错"（那是**故意**的：离线测试不该真联网）。
- **根因**：★★★ **`AssertionError` 是 `Exception` 的子类** ⇒
  "绊线响了"被那圈 `except Exception` **当成"普通失败"接走**，
  记进 `_rc = f"{type(e).__name__}"`，而断言只查 `_tripped is None` ⇒ **恒真**。
  ⇒ ★ **这不是"绊线写错了"，是"绊线的信号被自己的兜底吞了"** ——
  与 `ERR-AI-05`（绿，但绿是因为规则压根没参与匹配）**同形**，
  只不过这次的"规则"是**我为了防它而新写的**。
- **触发条件**：**任何"绊线 + 宽 except"的组合**。★ 绊线越需要"放行无关异常"，
  就越容易把自己的信号一起放行。
- **规避做法**（两条都要）：
  1. ★★ **绊线用独立异常类型**（本项目用 `class _Tripped(Exception)`），
     且在 `except Exception` **之前**先 `except _Tripped` —— **顺序是判据的一部分**；
  2. ★★★ **反向自证必须真做**，且★ **要走与主线相同的调用路径** ——
     本条里 ① 的反向自证**是通过的**（直接调 `tp.os.makedirs` 确实抛），
     而**经过 `main()` 的那条路**被吞了；两条路的异常一样，
     区别只在"有没有被那圈 `except Exception` 接住" ⇒ 只在旁边戳一下函数**证不到**。
- **怎么发现的**：写完之后**做变异检验**（把写盘种进去看红不红）——
  ★ 这是本仓 `B.10` 第 21 条「记下来 ≠ 不再犯」唯一管用的地方：
  **不靠回忆、靠当场跑一次变异。**
  ★ 同一轮**第二个**同形问题：「`--dump-attrs` 与 `--raw` 都生效」我第一版写成
  **源码文本匹配**（`'dump_attrs = raw = False' in src`）；实测把两个开关改成**共用变量**后
  那条断言**照样绿** —— 它量的是"某几串字出现过"，与"两个都生效"**不是一回事**。
  改成行为判据后才抓住。
- **边界**：★★ **推论（比本条更重要）**：
  **"我写了防假绿装置"这句话本身也需要一个判据** —— 否则它只是一句自我安慰。
  ★ 本项目已有三个落点：`test_scan_secrets.py`（两头都测）、
  `test_split_pack.py` ⑤（绊线 + 反向自证）、`tests/test_torznab_probe.py`（拦绊线被吞）。
  ★★ **新写任何"验证器/闸门/绊线"时的固定动作**：**种一个它该抓的变异，看它红不红**；
  红了才算写完，不红就是**还没写完**（而不是"这段代码没问题"）。
  ★ 同族：`ERR-AI-05`（绿因为没参与匹配）· `ERR-AI-06`（覆盖薄的一道网）·
  `B.10` 第 21 条（记下来 ≠ 不再犯）· `ERR-AI-07`（探测做错了 ⇒ 空结果当否证）。

---

## 附录 1 · 文档自身的边界（**别把这份读成"全知"**）

- 本文是**检索层**，事实的权威版本在 README（怎么做）与 SUMMARY（为什么）。
  **发现冲突时改 README/SUMMARY，再回来更新这里。**
- 本文**不含任何凭据值**。它只说明凭据"住在哪"（`A.5`）。
- 本文里的**地址、端口、路径、版本**都是**2026-09-13 的读数**。
  凡能一条命令读出来的（`docker inspect` / `synoschedtask --get` / `df -h` / `stat`），**用之前回读一次**。
- 已知的**读数冲突**（不是本文的错，是上游文档内部的不一致，已如实标出）：
  每日摘要时间（21:00 vs 21:20）、`/volume1` 可用空间（0 / ~19 / ~30 GiB）。
- 本文**故意不重复登记**的：测试层行形陷阱的完整版（`tests/README.md`）、逐条的调试过程（SUMMARY 各节）。
  —— 依据是本项目的「**同一事实只在一处维护**」。
- ★ **20 项技术弯路**（原 `走过的弯路.md`）**已并入本文**（2026-09-13）：
  它们**唯一**的落点就在 B 部分的 `ERR-*` 条目里，用 `弯路 #N` 标出。原文件已删除，
  **不再有外部出处** —— 上面这条「不重复登记」现在不适用于它们。

## 附录 2 · 这份文档是从哪长出来的

| 来源 | 取了什么 |
|---|---|
| `D:\Projects\1.txt` | 错误大纲的**层与条目骨架**（十节 → B 部分）|
| `D:\Projects\2.txt` | 前提信息大纲的**十二节骨架** + 「硬前提 / 易忽略前提 / 元前提」三档划分（→ A 部分）|
| `README.md` | 环境事实、命令、症状→解法、六条原理与风险清单 |
| `SUMMARY.md` | 卷归属表（§9）、占位符约定（§0）、拓扑（§2）、各处根因（按 §N 交叉引用）|
| `走过的弯路.md` | 20 项技术弯路的归因 —— ★ **已全部并入本文**（2026-09-13，原文件已删）。见下方说明 |
| `tests/README.md` | 测试层踩过的坑 + 行形陷阱（`ERR-AI-04`）|
| `.env.example` / `docker-compose.yml` / `deploy.sh` | 配置键、服务与网络、白名单与三处路径 |
| 会话记忆（`nas-commands-cost-first` 等）| `ERR-CMD-02` / `ERR-CMD-03` 两条最新教训 |

> ★ **维护方式**：新踩一个坑 → 在 B 部分对应层加一条 `ERR-XX-nn`（填满七格）；
> 前提变了 → 改 A 部分对应节。
> **别在这里记"过程"** —— 过程归 SUMMARY。

### 附：20 项技术弯路的落点对照（原 `走过的弯路.md`）

> 2026-09-13 拆解并入。**「弯路 #N」是历史编号**（来自那份已删的文档），
> 只用来跟旧记录对账；**日常检索请用 `ERR-*` 编号**。
> ★ 有 5 条**不落在 ERR 条目上**：它们要么本来就写在 A 部分（前提），要么只进 `B.10` 表（人看的元层）。

| 弯路 | 落点 | 并入方式 |
|---:|---|---|
| #1 Windows 休眠强杀 | `ERR-OS-02` | 该条**本就是为它写的**，边界里已标 |
| #2 `.cmd` LF 截断 | `ERR-OS-01` | 边界补记当初形态 |
| #3 `os.kill` 跨平台 | `ERR-OS-03` | 已有标记 |
| #4 49 条 `DATA_DIRS` | `ERR-DOCKER-01` 边界 | 作为"改配置成本"实例并入 |
| #5 脚本脱离版本控制 | `ERR-GIT-01` 边界 | 作为"只在单端存在⇒必漂移"实例并入 |
| #6 `cp` SQLite 丢 WAL | `ERR-SQL-01` 边界 | 补记"不报错，只给你旧数据" |
| #7 FlareSolverr tag | `ERR-SVC-05` | 已有标记 |
| #8 忘 `--qbit-url` 降级 | **`ERR-SVC-10`**（新）| 新建条目 |
| #9 `--depth` vs `maxDataDepth` | **`ERR-SVC-11`**（新）| 新建条目 |
| #10 退避窗口 300s 失效 | **`ERR-SVC-12`**（新）| 新建条目 |
| #11 `--once` 下 `next_sleep` 没接线 | **`ERR-SVC-13`**（新）| 新建条目 |
| #12 覆盖运行中脚本 | `ERR-SH-01` 边界 | 补记当初形态（`exit=127`）|
| #13 DSM 邮件路径误判 | `ERR-DSM-04` | 已有标记 |
| #14 计划任务小时位填错 | `ERR-DSM-05` | 补记"教训在缺自检" |
| #15 `restart` 刷不掉环境变量 | `ERR-DOCKER-01` 规避做法 | 补记"代价不对称" |
| #16 Query Limit 当节流阀 | `ERR-SVC-08` | 已有标记 |
| #17 禁用站未剥离 env | `ERR-SVC-02` 边界 | 补记"禁用≠不请求" |
| #18 嵌套目录当搜索名 | **`ERR-SVC-14`**（新）| 新建条目 |
| #19 IYUU 流量定性错误 | **`A.4`** ★ 注 + `B.10` 表第 12 条 | **不建 ERR 条目**（它是前提层的一个假设订正 + 一个元层形状）|
| #20 `ENOENT` 误判为丢种 | **`ERR-SVC-15`**（新）+ `B.10` 表第 13 条 | 新建条目 + 元层登记（两层都留）|
| #21 数 `grep` 空结果推断「不存在」 | **`ERR-AI-07`**（新）+ `B.10` 表第 18 条 | 新建条目 + 元层登记（两层都留）；★ 同族两条：`grep -q` 探不到裸 CR、``tr`` 数 CR 对不存在的文件给 ``0`` |
' \| wc -c` 对不存在的文件给 `0` |
| #22 拿未验前提往下推（一晚九次） | **`ERR-AI-08`**（新）+ `B.10` 表第 24 条 | 新建条目 + 元层登记（两层都留）；★ 配套：**农场 475 vs 517 差 42 的成因未验**（已证"没丢片"：`comm` 差集 0；已证伪"合季"与"一片一目录"两说）|
| #23 ★ **防假绿装置自己假绿**（绊线的信号被宽 `except` 吞掉） | **`ERR-AI-09`**（新）+ `B.10` 表第 25 条 | 新建条目 + 元层登记（两层都留）；★ 固定动作：**新写验证器/绊线，先种一个它该抓的变异看它红不红**（红了才算写完）|

> ★ **为什么有的条目进两张表**：1.txt 的成文规则 ——「同一条错误可能在多层出现，
> **两层都登记、互相指路，不要合并**」。#20 就是实例：`B.5.2` 记**事实**（那是竞态不是损坏），
> `B.10` 记**形状**（把噪声读成故障）。合并会丢掉"从哪一层看"的信息。
