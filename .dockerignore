# 编排器镜像构建上下文 = 本目录；下列内容与构建无关或含密钥，排除掉。
# ★ `.env` 必须写成 `.env*`：`.env` 是**精确匹配**，拦不住 `.env.bak.20260911-204109`
#   这类完整副本（同样含 NAS IP / apikey / PT passkey）。
#   实测仓库根目录**确实躺着**这样一份备份 —— 只写 `.env` 的话，
#   `docker build` 会把它原样打进镜像层，而镜像层**删不掉**（删了也还在历史层里）。
#   .gitignore 里 `.env` 与 `.env.bak.*` 是分开两条，这里一条通配更省事。
.env*
prowlarr/
cross-seed/
hlink/
data/
patches/
README.md
**/__pycache__/
**/*.pyc
.git/
.gitignore
