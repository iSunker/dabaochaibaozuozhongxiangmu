"""reseed-orchestrator：大包拆包 · 单种保种编排器。

职责（v1）：
  1. 读取 config.yml 里的"大包任务"（可扩展，含预留接口字段）。
  2. 安全预检：源目录与 linkDir 同卷校验、路径存在性、硬链接计数核对。
  3. 驱动匹配器（当前实现 = cross-seed）按内容匹配单种并注入 :3060。
  4. 轮询 qBittorrent(:3060) 汇报：哪些单片已做种 / 未匹配。

设计原则：匹配与硬链接的重活交给成熟的 cross-seed；本编排器只做
"任务注册 + 安全预检 + 调度 + 汇报"，并通过 matcher.Matcher 抽象类
为未来的 IYUU / 自研匹配器预留接口。
"""

__version__ = "0.1.0"
